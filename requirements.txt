# Dependencies are declared in pyproject.toml — that is the single source of truth.
#
# Install the package with the extras you need instead of using this file:
#
#   pip install -e ".[all,dev]"      # everything (non-GPU) + dev tooling
#   pip install -e ".[boosting]"     # a focused subset
#
# See pyproject.toml [project.optional-dependencies] for the full list of extras.
# pyutilz is a sibling first-party lib; until it is on PyPI, install it from source:
#   pip install -e ../pyutilz
-e .[all,dev]
