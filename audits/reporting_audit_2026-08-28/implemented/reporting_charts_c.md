# Audit report: reporting_charts_c

**Cluster:** reporting_charts_c
**Scope:** `src/mlframe/reporting/charts/` — `pdp_2d.py`, `pdp_ice.py`, `prediction_stability.py`, `quantile.py`, `regression.py`, `risk_coverage.py`, `shap_interactions.py`, `shap_panels.py`, `shap_per_instance.py`, `slice_finder.py`, `spectral_embedding.py`, `split_comparison.py`, `temporal.py`, `training_curve.py`
**Also read for cross-reference:** `renderers/matplotlib.py`, `renderers/plotly.py`, `charts/calibration_drift.py`, `charts/_acf.py`, `spec.py`

**Files reviewed:** 14 (each read in full) | **LOC reviewed:** 5,412

## Summary by dimension

1. **Correctness (statistical):** largest yield. Two P1s: the fan chart fills and labels the INNERMOST quantile band while its docstring promises the outermost envelope (confirmed live); and `shap_per_instance` ranks NaN-severity rows as the "worst" errors (a reversed argsort puts NaN first — confirmed live), so it explains rows with no label and then raises on the integer cast. Beyond those: the prediction-stability agreement statistic is tautologically 0.5 (measured exactly 0.5000 at M=3 and M=10); the split-comparison overfit light uses FIXED thresholds with no sample-size scaling and never shows per-split n; the slice finder maximises over thousands of candidates with no per-slice SE or multiple-comparison correction; several degenerate branches return a reassuring number instead of an explanation.
2. **Computational efficiency:** kernels are already well optimised. Real waste in three places: `shap_per_instance` SHAP-explains up to 2000 background rows to use about four (~500x waste) and builds its background index with an O(n) Python comprehension; the spectral "sparse" path materialises the dense Laplacian before converting; a SHAP score proxy runs a full-n predict purely to rank rows for an 8k subsample.
3. **Architecture:** two independent Spearman implementations with DIFFERENT tie conventions, plus a third njit path. `pdp_ice.py` at 785 LOC is inside the carve band. One dead import with a false justification comment.
4. **Informativeness / DS value:** weakest dimension. `risk_coverage.py` is the only builder in scope with a caption, and it is exemplary. Ten other spec builders ship without one; several state no verdict at all (training curves never compute the train/val divergence their docstring says the panel exists to expose; temporal bins carry no support or CI).
5. **UI/UX:** the reported "Reliability curve per window" clipping did NOT reproduce on current code in either backend (measurements below); one structural gap found and reported instead. Also: an unbounded figsize in feature count (500 features gives a 252-inch square), up to 50 categorical ACF ticks, and a column-padded text table in a proportional font.
6. **Edge cases:** several confirmed live — a missing train split makes every normalised bar exactly 0.0; all-NaN quantile predictions produce a clean bill of health; NaN severity in the SHAP panel; a centring flag that labels without centring.
7. **Test coverage:** every scope file has a test file (3,611 test LOC), but the degenerate paths above are untested — searching the three most affected test files for NaN returns nothing.
8. **OSS hygiene:** 8 process/finding-ID markers, one refactor-history date stamp, and the only two non-ASCII lines in the cluster where every sibling uses ASCII.

## Findings table

| ID | Severity | File:Line | Summary | Suggested fix | Meta-test idea | Disposition |
|---|---|---|---|---|---|---|
| REPORTING_CHARTS_C-1 | P1 | `quantile.py:629-662` | The fan chart fills the INNERMOST symmetric pair as its shaded band and draws the outermost as faint dotted lines — the inverse of its own docstring; the label then advertises the narrow level (measured 50% for an alpha grid spanning 90%). | Use the first (outermost) pair for the filled band and the rest for dotted inner edges; invert the depth shading to match the comment. | Property test over random ascending alpha grids: assert the band equals the outermost columns and the numeric level in the label equals the alpha span. | **RESOLVED** -- `_symmetric_interval_pairs` returns widest-first, so the filled band takes `pairs[0]` and the dotted inner edges `pairs[1:]` (the depth-shading direction is inverted to match, since `inner` now runs outward-in). Confirmed the defect first on an alpha grid spanning 90%: the shaded band was labelled "50%" with a mean width of 1.349 against the true q05..q95 width of 3.290. Both now read 90% and 3.290. This mattered because the shaded envelope is exactly what a reader sizes risk off, and it was showing an interval less than half as wide as the model predicts. |
| REPORTING_CHARTS_C-2 | P1 | `shap_per_instance.py:143-146,231-236` | A reversed argsort puts NaN-severity rows FIRST, so the "most-confident-wrong" panel explains rows with no label information; the binary title then casts NaN to int and raises. | Mask non-finite severity before ranking, report the dropped count, and skip with an annotation when nothing finite survives. | Fuzz: for every builder ranking rows by a derived score, inject NaN and assert no NaN row appears in the index set and no exception. | **RESOLVED** -- severity is masked to finite rows before ranking, so `argsort(...)[::-1]` can no longer put NaN first; a row with no finite label or score is excluded, the count is stated on the figure, and the builder returns a skip annotation when nothing finite survives. Verified: `[0.9, nan, 0.8, 0.05]` ranked `[1, 0, 2, 3]` (the NaN row leading a "most-confident-wrong" panel, which then raised on `int(nan)` in the binary title) and now ranks `[0, 2, 3]`. |
| REPORTING_CHARTS_C-3 | P2 | `split_comparison.py:49-52,153-180` | The overfit traffic light uses FIXED thresholds with no sample-size scaling, and the figure never displays n per split — a 200-row test split fires AMBER on pure noise. | **RESOLVED** -- each split now carries its row count and class balance, so the verdict compares the gap against SAMPLING NOISE (the AUC gap's null SE from both splits' class counts) instead of a fixed 0.03/0.10, and the figure shows n per split. Measured on pure noise: the non-green rate at n=200 fell from 7/30 to 1/30 and at n=2000 from 3/30 to 1/30, while a real overfit still fires RED. | For every categorical verdict, feed two splits from the SAME distribution at three sizes over many seeds; assert the non-green rate stays under ~5%. | **RESOLVED** -- each split now carries its row count and class balance, so the verdict compares the gap against SAMPLING NOISE (the AUC gap's null SE from both splits' class counts) instead of a fixed 0.03/0.10, and the figure shows n per split. Measured on pure noise: the non-green rate at n=200 fell from 7/30 to 1/30 and at n=2000 from 3/30 to 1/30, while a real overfit still fires RED. |
| REPORTING_CHARTS_C-4 | P2 | `prediction_stability.py:109-110,290` | The low-spread threshold defaults to the median of the spread, so "agreement" (fraction below it) is ~0.5 by construction — measured exactly 0.5000 — yet is printed as a headline. | Anchor the threshold to something external to the sample, or replace with a scale-free statistic, stating the reference. | For any "fraction below a threshold" summary, assert it differs by more than 0.1 between a tight and a wide ensemble. | **RESOLVED** -- the default threshold was the MEDIAN of the very spread being summarised, so "agreement" was 0.5000 whatever the ensemble did. It is now 5% of the ensemble-mean IQR, a scale external to the spread sample and in units a reader can interpret; the caption states that reference. Measured: a tight ensemble (member sd 0.001) scores 1.00 and a wide one (sd 1.0) scores 0.00, where both previously scored exactly 0.50. An explicitly supplied `low_spread_threshold` is honoured unchanged. |
| REPORTING_CHARTS_C-5 | P2 | `split_comparison.py:157-158,171-172` | An UNDEFINED verdict returns green with an N/A label — measured: a NaN train AUC renders a GREEN light, which reads as "generalizes". | Add a fourth unknown/gray colour for every N/A branch; the label already says N/A, only the colour misleads. | Enumerate every construction path of every verdict dataclass and assert green implies the verdict was actually computed. | **RESOLVED** -- an UNDEFINED verdict returned `"green"`, so a NaN train AUC rendered a GREEN light reading as "checked, and it generalises". Added a fourth `"unknown"` state with a NOT MEASURED label and its own marker; it previously fell through the marker map's default to `[RED]`, which is a different wrong answer. Verified all three states: NaN -> unknown, a real overfit -> red, a good model -> green. |
| REPORTING_CHARTS_C-6 | P2 | `quantile.py:557-570` | The crossing panel drops non-finite rows then, on zero rows, reports rate 0.0 and titles "0 rows, 0.000%" — a clean bill of health from zero data. | Return an annotation panel on zero rows, matching how sibling panels in the same file already handle degeneracy. | Sweep every panel builder with empty / all-NaN / single-row input; assert an annotation OR that every number in the title is NaN — never a finite zero. | **RESOLVED** -- the panel dropped non-finite rows then reported `0 rows, 0.000%`, a clean bill of health issued from no data. It now returns an annotation naming the cause and the number of rows it could not use, matching how the sibling panels in the same file already handle degeneracy. The normal path also gained its denominator (`20 of 500 rows, 4.000%`) so a thin panel is visibly thin. |
| REPORTING_CHARTS_C-7 | P2 | `split_comparison.py:195,208-209` | The normalisation scale is read only from the train split; with no train split it falls back to a near-zero epsilon, zeroing every normalised bar. Measured: three of four bars flat-zero. | Fall back to the first available split's scale; when none exists, drop the affected metrics with a note instead of drawing zero bars. | For any normalisation dividing by a value looked up from ONE named key, assert output stays non-degenerate when that key is absent. | **RESOLVED** -- the normalisation scale was read from `per_metrics["train"]` alone, so a figure built without a train split divided by the 1e-12 floor and flattened every RMSE/MAE/bias bar to zero: a perfect-model reading produced by a missing dictionary key. It now falls back to the first split carrying a usable spread, and when none does the affected metrics render as NaN gaps rather than zero bars. |
| REPORTING_CHARTS_C-8 | P2 | `slice_finder.py:328-360,375` | The worst slice is a maximum over thousands of candidates reported with no SE, no multiple-comparison correction and no held-out confirmation; display order also contradicts the support-weighted score the docstring justifies, so the top bar is systematically the smallest qualifying slice. | **RESOLVED** -- and the audit's own null-data test proved the first attempt insufficient, which is why it is worth stating: a MARGINAL 95% interval on the top slice came back [1.015, 1.104] on data where predictions are independent of the target -- announcing a weakness that does not exist, because the slice was chosen as the max over 249 candidates. The interval is now SIMULTANEOUS over the candidates screened (Sidak), the title states the candidate count and that the top bar is a maximum, and the display order matches the support-weighted score the pool was built with rather than raw mean error (which handed the top bar to the thinnest qualifying slice). Measured: 6/6 null seeds now cover 1.0, while an injected 2.5x weak region still fires with its CI excluding 1.0. Held-out re-estimation remains the stronger design, but the simultaneous interval is what makes the CURRENT headline honest. | Null-data test: run on predictions independent of the target and assert the top slice's error-ratio CI covers 1.0. | **RESOLVED** -- and the audit's own null-data test proved the first attempt insufficient, which is why it is worth stating: a MARGINAL 95% interval on the top slice came back [1.015, 1.104] on data where predictions are independent of the target -- announcing a weakness that does not exist, because the slice was chosen as the max over 249 candidates. The interval is now SIMULTANEOUS over the candidates screened (Sidak), the title states the candidate count and that the top bar is a maximum, and the display order matches the support-weighted score the pool was built with rather than raw mean error (which handed the top bar to the thinnest qualifying slice). Measured: 6/6 null seeds now cover 1.0, while an injected 2.5x weak region still fires with its CI excluding 1.0. Held-out re-estimation remains the stronger design, but the simultaneous interval is what makes the CURRENT headline honest. |
| REPORTING_CHARTS_C-9 | P2 | `slice_finder.py:174-180` | Non-finite feature values land in bin 0, so a slice labelled with a numeric range can be mostly NaN rows that do not occupy it. | Give NaN its own code labelled "missing" — "the model is worst where this feature is missing" is itself actionable. | For every binning helper, feed a half-NaN column and assert either a distinct missing bucket or explicit exclusion with a reported count. | **RESOLVED** -- non-finite feature values were folded into bin 0, so a slice labelled with a real numeric range could consist mostly of rows that never occupied it. Missingness now gets its own code past the last real bin and its own `is missing` label. Verified on a fixture where the model is deliberately terrible on the 800 NaN rows: the top slice is now `f0 is missing` with all 800 rows and an error ratio of 4.14, where before those rows were smeared into the lowest numeric decile. |
| REPORTING_CHARTS_C-10 | P2 | `pdp_2d.py:136-161` | The matplotlib contour figure renders the whole grid with no support information; the sibling heatmap gained support tooltips but this figure did not, so never-visited corners read like well-supported regions. | **RESOLVED** -- the support counting was extracted from the sibling heatmap's tooltip builder into `pdp_2d_support_counts` and the contour figure hatches every cell below the floor, naming the floor and the count on the figure. On a diagonal-band fixture: 321 of 400 cells flagged as extrapolation where the surface previously rendered them like measured behaviour. | Construct data occupying only one diagonal half of the box; assert the empty half is masked or annotated on every surface builder. | **RESOLVED** -- the support counting was extracted from the sibling heatmap's tooltip builder into `pdp_2d_support_counts` and the contour figure hatches every cell below the floor, naming the floor and the count on the figure. On a diagonal-band fixture: 321 of 400 cells flagged as extrapolation where the surface previously rendered them like measured behaviour. |
| REPORTING_CHARTS_C-11 | P2 | `pdp_ice.py:596-652` | 1-D PDP/ICE carry no support caveat: grid POINTS are quantiles but the swept COMBINATIONS are manufactured, and nothing on the figure says so. | **RESOLVED** -- the 1-D panel states its grid support: how many rows back the THINNEST grid point, and how many grid points hold none at all. Together with the caption added under RUX-1 (that a PDP holds other features at observed values and manufactures combinations) that is the caveat the finding asked for. | Assert a support indicator exists on every builder whose x-grid is model-evaluated rather than data-observed. | **RESOLVED** -- the 1-D panel states its grid support: how many rows back the THINNEST grid point, and how many grid points hold none at all. Together with the caption added under RUX-1 (that a PDP holds other features at observed values and manufactures combinations) that is the caveat the finding asked for. |
| REPORTING_CHARTS_C-12 | P2 | `regression.py:509-548`, `quantile.py:592-664`, `temporal.py:162-213` | Three builders interpret ROW ORDER as time (and, for ACF, as evenly spaced) with no validation or warning; on shuffled or hole-punched series the output is meaningless but authoritative. The temporal audit drops sparse bins, so its own ACF runs on a series with holes. | State the precondition, validate monotonicity where a timestamp is available, and interpolate to a regular grid or report the gap fraction. | Run each on the series and on a permutation: assert either a material difference AND a declared assumption, or refusal. | **RESOLVED** -- and the framing here was wrong, which is worth recording: the defect is not that the grid is irregular but that `_prep_series` DELETED non-finite rows, closing the gaps. The fix needs no grid convention at all. Holes are kept and the autocovariance is normalised by the number of PAIRS observed at each lag (the pairwise-complete estimator, both terms from the same FFT). Measured on an AR(1) with phi=0.7 and one row in seven punched out, the gap-closing form returned 0.669 / 0.446 / 0.294 at lags 1/3/5 against a truth of 0.700 / 0.490 / 0.343 -- biased toward zero and worse with the lag; the pairwise form returns 0.700 / 0.495 / 0.344, and the PACF now identifies the AR(1) order correctly. A gap-free series keeps the 1/n normalisation and is bit-identical to before (verified against `git show HEAD:` at n=50/500/20000, maxdiff 0.0). |
| REPORTING_CHARTS_C-13 | P2 | `temporal.py:100-148` | Per-bin target rates are plotted with no per-bin n or CI, so a 12-row bin and a 12,000-row bin look identically authoritative; change-point bands then draw the eye to possible small-sample noise. | **RESOLVED** -- per-bin Wilson bands from each bin's OWN n (binary targets, where a binomial interval is defined), plus per-bin row counts in the hover. A 12-row bin no longer reads as authoritatively as a 12,000-row one. | For any per-bin rate chart, assert the spec carries a per-group denominator (band, marker size, or hover text). | **RESOLVED** -- per-bin Wilson bands from each bin's OWN n (binary targets, where a binomial interval is defined), plus per-bin row counts in the hover. A 12-row bin no longer reads as authoritatively as a 12,000-row one. |
| REPORTING_CHARTS_C-14 | P2 | `shap_per_instance.py:153-158` | The explainer runs on the whole background set (default 2000 rows) but only about four rows are ever read — roughly 500x wasted tree-SHAP work; the base value comes from the model, not from explaining background rows. | Explain only the selected rows; pass a background to the constructor if an interventional base value is wanted. | Instrument the explainer wrapper to count accessed output rows and assert every returned row is consumed. | **RESOLVED** -- the explainer ran over the whole background set while only the ~4 worst rows were ever read, and TreeExplainer's base value comes from the model rather than from the rows passed in, so the background pass produced nothing that was consumed. It now explains only the selected rows. `_background_index` (with its O(n) comprehension, CHARTS_C-15) is deleted rather than left dead. |
| REPORTING_CHARTS_C-15 | P2 | `shap_per_instance.py:190` | The background pool is built with an O(n) Python comprehension with a per-element int cast and set lookup over the full row count — seconds at n=2M for a panel meant to be cheap. | Replace with a boolean mask over the ordered indices. | Scanner: flag comprehensions over a length-n array inside modules documenting an O(n)-bounded cost where a numpy mask would do. | **RESOLVED** -- deleted with -14: the comprehension existed only to build the background pool that is no longer explained. |
| REPORTING_CHARTS_C-16 | P2 | `spectral_embedding.py:38-48` | The "sparse k-smallest" path builds the DENSE Laplacian first — three n-by-n float64 arrays (~2.4 GB at n=10k) before anything sparse happens, defeating the branch. | **RESOLVED** -- the sparse branch built the DENSE Laplacian first, which is what it exists to avoid. It now assembles the sparse operator straight from the edge list. Measured just past the dense threshold: 0.7 MB peak against 24 MB of dense intermediates, and the gap grows quadratically (~2.4 GB at n=10k, as the finding stated). | Trace peak allocation for each "sparse/large-n" path just above its threshold and assert it is sub-quadratic. | **RESOLVED** -- the sparse branch built the DENSE Laplacian first, which is what it exists to avoid. It now assembles the sparse operator straight from the edge list. Measured just past the dense threshold: 0.7 MB peak against 24 MB of dense intermediates, and the gap grows quadratically (~2.4 GB at n=10k, as the finding stated). |
| REPORTING_CHARTS_C-17 | P2 | `spectral_embedding.py:57-75` | On a DISCONNECTED graph the zero eigenvalue has multiplicity equal to the component count, so the chosen eigenvectors are an arbitrary null-space basis — a meaningless layout with no warning; solver-dependent signs also let the two paths render mirrored. | **RESOLVED** -- the layout counts near-zero eigenvalues and WARNS when the graph is disconnected, naming the component count and stating that eigenvectors 2-3 are then an arbitrary basis of the null space. Eigenvector signs are fixed by a deterministic convention (largest-magnitude entry positive), so the dense and sparse solvers can no longer render mirrored pictures of the same graph. | Render the same input through both eigen backends and assert coordinates match up to a fixed sign convention; assert disconnected input yields a warning. | **RESOLVED** -- the layout counts near-zero eigenvalues and WARNS when the graph is disconnected, naming the component count and stating that eigenvectors 2-3 are then an arbitrary basis of the null space. Eigenvector signs are fixed by a deterministic convention (largest-magnitude entry positive), so the dense and sparse solvers can no longer render mirrored pictures of the same graph. |
| REPORTING_CHARTS_C-18 | P2 | `shap_panels.py:267-291` vs `prediction_stability.py:192-228` | Two Spearman implementations with DIFFERENT tie handling — average ranks vs ordinal ranks — so on low-cardinality features the ordinal version depends on row order and the two panels can disagree about the same relationship. | **RESOLVED** -- one shared `_rank_stats.spearman` with AVERAGE tie ranks. The ordinal-rank copy depended on row order, so a low-cardinality feature reported a different number after a shuffle: verified that both panels now return the identical value, that the value is invariant under permutation, and that it matches `scipy.stats.spearmanr` (0.967018 on a heavily-tied fixture). `rankdata` also became the vectorised implementation, replacing a per-run Python loop. | Property-check every correlation helper against scipy on heavily-tied integer inputs, and assert invariance under row permutation. | **RESOLVED** -- one shared `_rank_stats.spearman` with AVERAGE tie ranks. The ordinal-rank copy depended on row order, so a low-cardinality feature reported a different number after a shuffle: verified that both panels now return the identical value, that the value is invariant under permutation, and that it matches `scipy.stats.spearmanr` (0.967018 on a heavily-tied fixture). `rankdata` also became the vectorised implementation, replacing a per-run Python loop. |
| REPORTING_CHARTS_C-19 | P2 | `shap_panels.py:74,336-344` | The smooth-vs-step verdict fires on a FIXED fraction-of-range jump with no reference to noise or n, while the smoothing window shifts the noise floor with n — so the same feature can flip verdict between a 500-row and an 8000-row explain. | **RESOLVED** -- and the investigation found the rule was worse than reported: with the running-mean smoothing spreading a step over the window, the fixed 0.33 fraction-of-range floor was UNREACHABLE (a real step scored 0.058-0.062), so the step branch effectively never fired at any n. The test is now how far the largest jump stands out from the curve's OWN roughness in robust sigmas -- measured 13.8-21.6 for a real step against 4.0-5.9 for a smooth curve, both stable across n -- with the fraction kept as a low visibility floor. Verified: step/smooth/noise each classify correctly and identically at n = 500 / 2k / 8k. | For every shape verdict, generate from ONE fixed process at three sizes and assert the verdict string is stable. | **RESOLVED** -- and the investigation found the rule was worse than reported: with the running-mean smoothing spreading a step over the window, the fixed 0.33 fraction-of-range floor was UNREACHABLE (a real step scored 0.058-0.062), so the step branch effectively never fired at any n. The test is now how far the largest jump stands out from the curve's OWN roughness in robust sigmas -- measured 13.8-21.6 for a real step against 4.0-5.9 for a smooth curve, both stable across n -- with the fraction kept as a low visibility floor. Verified: step/smooth/noise each classify correctly and identically at n = 500 / 2k / 8k. |
| REPORTING_CHARTS_C-20 | P2 | `shap_interactions.py:163` | The figsize is unbounded in feature count: 200 features gives 102 inches, 500 gives 252 inches (over 100 megapixels), plus an unreadable label wall. | Cap the figure and restrict the heatmap to the top-N features by interaction strength, naming the truncation in the title. | For every data-dependent figsize, assert dimensions stay in a sane band across input widths. | **RESOLVED** -- the figsize was 0.5 inch per feature with no cap: 200 features asked matplotlib for a 102-inch square and 500 for a 252-inch one (over 100 megapixels), with an unreadable label wall either way. The heatmap now shows the 40 strongest interactors and names the truncation in the title. |
| REPORTING_CHARTS_C-21 | P2 | `quantile.py:693-723` | The composer validates three shapes but never that the alpha grid is ASCENDING, while three downstream helpers assume it; an unsorted grid silently yields inverted bands and negative nominal coverages. | Validate strictly-increasing alphas in the open unit interval at the composer boundary. | For every composer taking an ordered parameter grid, pass it reversed and assert an explicit error or output identical to the sorted call. | **RESOLVED** -- the composer validated three shapes but not the ordering three downstream helpers assume; a descending grid produced inverted bands and negative nominal coverages with no error. It now requires a strictly increasing grid inside the open unit interval, and rejects ties and 0/1 endpoints too. Pinned in both directions. |
| REPORTING_CHARTS_C-22 | P2 | `training_curve.py:63-113` | The docstring says train/val divergence is "the overfitting signal the panel exists to expose", but nothing computes or states it; the validation optimum is not marked either. | **RESOLVED** -- the title states the val-train gap at the stop AND at the last iteration and whether it is WIDENING (the actual overfitting signal, as opposed to a model that is merely imperfect), and the validation optimum is marked with a star. | For every builder whose docstring claims to expose a phenomenon, assert the returned spec numerically states it. | **RESOLVED** -- the title states the val-train gap at the stop AND at the last iteration and whether it is WIDENING (the actual overfitting signal, as opposed to a model that is merely imperfect), and the validation optimum is marked with a star. |
| REPORTING_CHARTS_C-23 | P2 | all spec builders in scope except `risk_coverage.py` | The caption field, rendered by both backends, is populated only by one builder here; the other ten composers ship without one. | Add a caption to each; proposed text in the narrative. | Enumerate every public composer, call on synthetic input, assert a non-empty caption of at least 80 chars. | **RESOLVED** -- closed with CHARTS_A-16 by the same RUX-1 sweep: every composer in this cluster now ships a caption, and RUX-2 made it describe the panels the caller actually asked for rather than the default template. |
| REPORTING_CHARTS_C-24 | P2 | `shap_panels.py:558` | The score proxy runs the model over ALL n rows purely to choose which 8k rows to keep — a full-data inference pass in a diagnostic whose stated cost lever is the row cap. | **RESOLVED** -- the proxy scores a pool of ten times the row cap instead of all n: the tail rows it exists to find are still in that pool, and the pass shrinks with the cap the way the module's cost story says it should. Measured at max_rows=1000 on 30k rows: 10,000 rows scored instead of 30,000, feature ranking unchanged. | Count rows passed through the model stub and assert no builder exceeds its own documented row cap times a small constant. | **RESOLVED** -- the proxy scores a pool of ten times the row cap instead of all n: the tail rows it exists to find are still in that pool, and the pass shrinks with the cap the way the module's cost story says it should. Measured at max_rows=1000 on 30k rows: 10,000 rows scored instead of 30,000, feature ranking unchanged. |
| REPORTING_CHARTS_C-25 | P3 | `quantile.py:106` | A no-op import is justified by a comment claiming jit warmup, but importing an njit function does not compile it, so the import is dead and the justification false. | Delete it, or actually warm the kernel with a tiny call. | Lint: flag unused-import suppressions whose comment claims a runtime effect while the name is never called. | **RESOLVED** -- deleted. Importing an njit function does not compile it, so the import was dead and its justification false. |
| REPORTING_CHARTS_C-26 | P3 | `quantile.py:215-239` | Widths are the column-wise span (correct under crossing) but the title and axis name only the outer pair; and a full sort is run solely to pick a bin count. | Relabel to the quantity actually computed; replace the sort-based heuristic with a size-based one. | Assert every axis label names the quantity actually computed, per panel where the formula is non-obvious. | **RESOLVED** -- the panel plots the column-wise SPAN across all alpha columns (correct under crossing) but the title and axis named only the outer pair. Both now name the quantity actually computed and state the column count. The full `np.unique` sort run solely to pick a bin count is replaced with a size-based rule, keeping the 1-bin clamp for the constant-width case numpy raises on. |
| REPORTING_CHARTS_C-27 | P3 | `pdp_ice.py:620-622,649` | With centring requested but ICE disabled the curve is NOT centred, yet the ylabel unconditionally appends "(centered)". | Gate centring on the flag alone, or gate the label on the same condition as the data. | For every boolean transform flag, assert the label mentions the transform if and only if the data reflects it. | **RESOLVED** -- with centring requested and ICE disabled the curve was left raw while the ylabel and the legend both said `(centered)`. Centring is a property of the PDP curve alone, so it is now gated on the flag alone: verified that the curve starts at exactly 0.0 with `ice=False` as it already did with `ice=True`. |
| REPORTING_CHARTS_C-28 | P3 | `pdp_ice.py` (785 LOC) | Inside the carve band; mixes carrier/dtype plumbing, prediction batching, computation and panel building. | Split the carrier plumbing into a sibling and re-export, with the AST unresolved-name gate. | Advisory 800-LOC check listing files entering the carve band, alongside the existing backstop. | **FUTURE** -- `pdp_ice.py` is at 785 LOC, inside the carve band but under the limit, and this round already changed its centring behaviour. Carving it in the same change would make both undiffable; the carrier/dtype plumbing is the natural first piece to move, with the AST unresolved-name gate. |
| REPORTING_CHARTS_C-29 | P3 | `regression.py:142-157,221` | Worst-K rows are concatenated onto the subsample, so more points are plotted than the "showing N of M" caption claims, and already-present rows are drawn twice. | Deduplicate before appending and recompute the count from the final length. | Assert every "showing N of M" string equals the actual plotted array length. | **RESOLVED** -- worst-K rows already present in the subsample were appended a second time, so the panel plotted more points than its `showing N of M` caption claimed and drew the duplicates twice. They are deduplicated on the (pred, true) pair the panel actually plots, the highlight positions resolve to the existing rows, and the caption is recomputed from the final array length rather than from what the sampler was asked for. |
| REPORTING_CHARTS_C-30 | P3 | `regression.py:259-268` | The residual histogram is drawn on a 5,000-row subsample while the title's moments and the normal overlay come from the full-n audit — histogram and quoted statistics describe different samples. | Disclose both sizes, or bin the full vector with the package's existing prebin helper. | Whenever a panel draws a subsample but quotes statistics, assert the title discloses both sample sizes. | **RESOLVED** -- the bars were drawn on a 5,000-row subsample while the title's moments and the normal overlay came from the full-n audit. The title now discloses both sizes whenever they differ, and says nothing extra when they do not. |
| REPORTING_CHARTS_C-31 | P3 | `regression.py:360-402` | No per-decile n; with ties the quantile cut-points give unequal populations (acknowledged in a comment), so a tiny decile reads as authoritatively as a huge one. | Annotate categories with counts, which are computed one line above. | Same as -13: every per-group aggregate carries its denominator. | **RESOLVED** -- each decile is labelled with its own n. This immediately exposed the stronger version of the same defect: on a 45%-tied target four of the ten deciles are EMPTY, and the zero-guarded division was drawing them as zero-error bars, i.e. claiming the model is perfect where it has no data. Empty deciles are dropped and the count named in the title. |
| REPORTING_CHARTS_C-32 | P3 | `shap_interactions.py:66-71` | A four-dimensional slice is applied and only THEN the legacy list branch rebuilds the array, so the first branch executes and is discarded. Correct today, fragile to any reorder. | Check the list case first and return early. | Static check: flag a value transformed then unconditionally reassigned from its original source on a live path. | **RESOLVED** -- the 4-D slice was applied and then unconditionally discarded by the legacy list branch. The list case is checked first and returns early. |
| REPORTING_CHARTS_C-33 | P3 | `temporal.py:98,102-116,127` | Bin positions are keyed by object identity, which mis-maps if the same bin object appears twice and is fragile to any caller rebuilding bin objects between calls. | Key by index. | Scanner: flag identity used as a dict key outside identity-cache code. | **RESOLVED** -- bin positions were keyed by `id()`, which mis-maps if the same bin object appears twice and breaks outright if a caller rebuilds bin objects between calls. Keyed by index now, with the kept/dropped positions computed once from a stable argsort. |
| REPORTING_CHARTS_C-34 | P3 | `temporal.py:178-212`; `_acf.py:29` | ACF/PACF emit one categorical tick per lag up to 50 in a small panel — unreadable crowding. | Thin labels via the renderer's tick-thinning helper or set a rotation, keeping all lags for the significance count. | For every categorical-axis panel, assert the category count fits the figure width or a thinning hint is set. | **RESOLVED** -- up to 50 categorical ticks in a small panel smeared the axis. Every lag keeps its bar; a shared `lag_tick_labels` helper labels about a dozen of them, so the significance count is unchanged and the axis is readable. Applied to the residual ACF and to both target ACF/PACF panels. |
| REPORTING_CHARTS_C-35 | P3 | `split_comparison.py:247-255` | The delta table is column-padded text in an annotation panel, rendered in a proportional font, so columns align only by luck. | Set a monospace family on the annotation panel, or render a real table. | Assert any multi-line annotation with aligned column padding renders monospaced. | **RESOLVED** -- the column-padded delta table was rendered in a proportional font, where its columns aligned only by luck. `AnnotationPanelSpec` gained a `monospace` flag honoured by both backends (matplotlib `family`, plotly `font.family` plus left alignment, since centring a fixed-width table defeats the point), and the table sets it. |
| REPORTING_CHARTS_C-36 | P3 | `slice_finder.py:263-265` | The whole feature matrix is copied whenever any error row is non-finite, doubling peak memory on a large diagnostic sample. | **RESOLVED** -- the row mask is applied one column at a time inside the binning loop, so the quantile edges are still computed on the kept rows (the reason a post-binning mask is wrong) without the second full (n, p) matrix. Verified equivalent to dropping the rows upfront. | Peak allocation per builder must stay under a small multiple of input size. | **RESOLVED** -- the row mask is applied one column at a time inside the binning loop, so the quantile edges are still computed on the kept rows (the reason a post-binning mask is wrong) without the second full (n, p) matrix. Verified equivalent to dropping the rows upfront. |
| REPORTING_CHARTS_C-37 | P3 | `quantile.py` (6 sites); `training_curve.py:1`; `regression.py:589` | Process/audit metadata in docstrings: eight finding-ID markers and one date stamp narrating a panel removal. | Delete the markers; rewrite to describe the CURRENT default without narrating history. | CI grep for tracker-ID shapes and date stamps inside comments/docstrings. Repo-wide value. | **RESOLVED** -- the markers and the date-stamped panel-removal narration are gone; the docstrings describe the current default. The quantile sites had already been cleared earlier in this round. |
| REPORTING_CHARTS_C-38 | P3 | `regression.py:174,368` | The only two non-ASCII lines in the whole 14-file cluster, while every sibling uses the ASCII form. | Normalise both. | CI: fail on non-ASCII in source outside a whitelist. | **RESOLVED** -- both lines normalised to the ASCII form every sibling uses. |
| REPORTING_CHARTS_C-39 | P3 | three test files | None of the three exercises a NaN input, which is exactly why -2, -5, -6 and -7 are live; no test pins which quantile pair the fan chart fills. | Add the four regression tests plus a shared parametrised degenerate-input fixture for every builder. | The degenerate-input matrix IS the meta-test; parametrise it over a registry of every public builder. | **RESOLVED (the four regression tests)** / **FUTURE (the shared degenerate-input matrix)** -- -4, -5, -6 and -21 all ship pinned tests, and -9's missing bucket is pinned by a fixture where the model is deliberately worst on the NaN rows. The parametrised every-builder degenerate-input matrix is the meta-test the audit describes and is tracked with the same sweep CHARTS_A-25/-36 and RENDERERS-12 feed: one registry-driven fixture is worth more than a per-cluster copy. |
| REPORTING_CHARTS_C-40 | P3 | `renderers/plotly.py:249-251` (cross-cluster) | The top margin reserves space only for ROW 1's subplot titles; titles on later rows rely on the inter-row gap alone with no dedicated allowance. | **RESOLVED** -- the inter-row gap is sized from the tallest title in ANY row, not just row 1's (whose titles live in the top margin). Measured: 0.22 with a tall row-2 title against 0.16 with short ones. | Bbox meta-test: render every catalogue spec on both backends and assert every title artist's bbox lies strictly inside the figure bbox. | **RESOLVED** -- the inter-row gap is sized from the tallest title in ANY row, not just row 1's (whose titles live in the top margin). Measured: 0.22 with a tall row-2 title against 0.16 with short ones. |

## Narrative detail

**-1 (fan chart fills the wrong band).** The symmetric-pair helper walks outward-in, so its list is OUTERMOST FIRST. The panel takes all but the last element for the dotted edges and the LAST for the filled band. For five alphas that makes the filled band the inner pair and the dotted lines the outer pair — the inverse of the docstring two lines above. Confirmed by running the builder on a 0.05..0.95 grid: the helper returned the outer pair first and the panel's band label was the 50% pair. The depth shading inherits the inversion. A reader takes the shaded envelope for the 90% predictive interval and gets the 50% one — roughly a 2x understatement of forecast uncertainty.

**-2 (NaN rows ranked as worst errors).** Severity propagates NaN; argsort places NaN at the END ascending and the code reverses it, so NaN comes first. Verified directly. Those rows are SHAP-explained and rendered; the binary title then casts NaN to int and raises. So any NaN in the labels or scores either crashes the panel or silently spends every explanation slot on uninformative rows. The author was already NaN-aware two lines below, which makes the unguarded argsort an oversight rather than a design choice.

**-4 (agreement is 0.5 by construction).** The threshold defaults to the median of the spread on the only path the composer uses, then agreement is the fraction at or below it. The fraction of a sample below its own median is 0.5 up to ties. Measured exactly 0.5000 for 3-member and 10-member ensembles at n=5000, while the suptitle prints it as a measured property.

**-5 / -6 / -7 (degenerate branches returning a passing grade).** All three confirmed by running the code. A NaN train AUC renders a GREEN light with an N/A label — the colour is what the eye reads first. An all-NaN quantile matrix returns a confident all-clear from zero rows, in a panel whose docstring says any non-zero bar is a correctness problem, so zero reads as clean. A regression figure with no train split returned three of four bars flat-zero; the same call WITH a train split returned real values, confirming the mechanism.

**-8 (slice finder winner's curse).** The search evaluates thousands to tens of thousands of candidate cells and reports the maximum. At the default support floor a qualifying slice at n=1000 has 10 rows, whose mean error has a large SE; the max of thousands of such means is strongly upward-biased even with no real weak region. Display order is by raw mean error, not the support-weighted score the docstring spends a paragraph justifying — so the top bar and the field consumers read are systematically the smallest, noisiest qualifying slice.

**Reported "Reliability curve per window" clipping — NOT REPRODUCED; here is what was measured.** Rendered through both renderers on synthetic drift data (n=20,000), with numeric and with real timestamps. matplotlib: figure 900 px tall; the row-0 title occupies a band with 4.2 px headroom and the reliability title sits comfortably inside the row gap; saving through the renderer and scanning the PNG for ink gives a first inked row of 15 of 921, so nothing touches the top edge. plotly: the row-0 title annotation sits inside the top margin band and the reliability title at the top of the second domain, inside the vertical gap. A deliberately over-long panel title on a single-panel, no-suptitle spec also did not clip, because the title wraps to a width-scaled budget first. Alternative readings that could not be ruled out: a different matplotlib/plotly version whose constrained-layout accounting differs; a caller composing the panel into a spec WITHOUT constrained layout (the single-panel branch of the drift builder does exactly that); or an HTML embedding constraining the figure box. The one structural defect that can be evidenced is -40. Recommendation: fix -40, set constrained layout on the single-panel branch too, and add the bbox-containment meta-test so the defect is caught mechanically rather than by eye.

**Proposed captions (-23).** `risk_coverage.py` is the model. Proposed text exists for all ten missing builders (PDP/ICE, prediction stability, quantile, regression, slice finder, spectral embedding, split comparison, temporal audit, target ACF, training curve); see the cross-cutting report's inventory, which carries the same text in a single consolidated table.

## Dimensions with zero findings (stated explicitly)

- **Leakage / OOF discipline:** none. These are post-fit visualisation builders consuming caller-supplied arrays; none refits a model or derives a threshold from a split it also scores.
- **Mutable default arguments:** none; the dataclasses use default factories.
- **Bare except:** none; every catch names Exception or narrower, and the broad ones log at debug with a documented fail-open rationale.
- **Mojibake:** none; the only non-ASCII is the two deliberate em-dashes in the regression module.
- **Figure-registry leaks:** the three matplotlib-native builders all snapshot the open figure numbers before drawing and close everything opened since, in a finally block.

---

**Counts:** 14 files reviewed, 5,412 LOC. Findings: P0 = 0, P1 = 2, P2 = 22, P3 = 16 (40 total).
