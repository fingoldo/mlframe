# Audit report: reporting_charts_a

**Cluster:** reporting_charts_a
**Scope:** `src/mlframe/reporting/charts/` — `__init__.py`, `_acf.py`, `_calibration_chart_shared.py`, `_coerce_shared.py`, `_jaccard_kernel.py`, `_layout.py`, `_sampling.py`, `_threshold_sweep_kernel.py`, `binary.py`, `calibration.py`, `calibration_by_feature.py`, `calibration_drift.py`, `calibration_heatmap_2d.py`, `category_discriminability.py`

**Files reviewed:** 14 (every file read in full)
**LOC reviewed:** 3,397

## Summary by dimension

1. **Correctness (statistical):** the largest yield, and severe. Three P1s, all confirmed by running the shipped code. (a) `binary.py`'s PIT panel uses a PIT definition provably non-uniform for a perfectly calibrated binary model — a calibrated model produces a linearly-rising histogram (measured densities 0.10 to 1.90 across ten bins) and KS-vs-uniform 0.247, so the panel's stated verdict ("uniform = perfect calibration") inverts on real data. (b) `calibration_heatmap_2d.py` labels a mean gap as "ECE" everywhere; over- and under-confidence cancel within a cell, and a synthetic maximally-miscalibrated cell reports a worst-cell value of 0.000 and a green traffic light. (c) the same module's fixed 0.05/0.10 thresholds are a pure sample-size readout: a perfectly calibrated model reads red at 40 rows/cell, amber at 200, green at 4,000. Alongside: `calibration_by_feature.py` uses the plug-in ECE, the exact bias `calibration_drift.py` documents having fixed. Lower-severity: KS evaluated mid-tie-block, an AP bootstrap CI paired with a full-n point estimate, a NaN bar width.
2. **Computational efficiency:** the PR panel's AP bootstrap CI costs 3.01s vs 0.046s without it at n=500k (65x, dominating the figure) and allocates a 200 MB index block. Also: an object-column hashability probe iterating every row in Python (0.54s per 1M-row column), a discarded count pass, and per-bin full-array masks despite an O(n) docstring claim.
3. **Architecture and clarity:** `binary.py` at 827 LOC is inside the carve band. Dead private aliases, a docstring promising a band the code never builds, annotations violated by None padding.
4. **Informativeness / DS value:** `FigureSpec.caption` is populated by exactly 3 modules repo-wide; five builders in this scope ship no caption. `SCORE_DIST` hides class imbalance via per-class density; the THRESHOLD panel marks no optimum and can decimate it away; drift windows are labelled w0..w9 with no date or count.
5. **UI/UX:** mixed linestyle vocabularies in one tuple, three-line panel titles where a caption belongs, a ragged grid bypassing the layout helper, labels emitted at NaN coordinates.
6. **Edge cases:** generally strong — nearly every builder returns an explanatory annotation panel on degenerate input. Gaps: NaN bar width, silent label-value dropping, ACF gap-closing.
7. **Test coverage:** structural coverage is good (57 tests in the binary chart file alone), but every P1 is outside what the tests assert. All three are cheap "calibrated-model-must-read-green" meta-tests.
8. **OSS hygiene:** clean. Two nits: one refactor-history clause and dash-in-prose in user-visible strings.

## Findings table

| ID | Severity | File:Line | Summary | Suggested fix | Meta-test idea | Disposition |
|---|---|---|---|---|---|---|
| REPORTING_CHARTS_A-1 | P1 | `binary.py:535-557` | The PIT transform is NOT uniform under perfect calibration — a calibrated model yields a triangular density and KS 0.247, so the panel condemns correctly-calibrated models. | Use the randomised PIT for a discrete outcome, or drop PIT for binary. | Property test over every "flat is the target" panel: generate a perfectly calibrated synthetic and assert the headline deviation statistic is below its own noise floor. | **RESOLVED** -- switched to the Czado/Gneiting-Held randomised PIT (`U ~ Uniform(F(y-1), F(y))`, i.e. `Uniform(0, 1-p)` for y=0 and `Uniform(1-p, 1)` for y=1). Confirmed the defect first: a model calibrated by construction gave KS 0.247 with a triangular density rising 0.101 -> 1.896 across the deciles. Now KS 0.002 and flat, while over- and under-confidence still register at 0.045 and 0.032. The draw is seeded from a hash of the SCORES rather than a constant -- a fixed seed of 0 reproduces a caller's own `default_rng(0)` stream exactly, making `u == p`, cancelling the randomisation and restoring the 0.25 (caught live while verifying this fix). |
| REPORTING_CHARTS_A-2 | P1 | `calibration_heatmap_2d.py:129-146` | Per-cell "ECE" is a mean gap: within-cell over/under-confidence cancel exactly, so a maximally miscalibrated cell reports 0.0 and green. | Compute a real per-cell binned ECE, or rename to `mean_gap` and state that cancelling miscalibration is invisible. | Adversarial cancellation fixture for every aggregate-of-means calibration metric. | **RESOLVED** -- each cell now computes a real binned ECE over 5 within-cell score bins instead of `|mean(score) - mean(target)|`. Verified on the exact cancelling case: half the rows at score 0.9/target 0 and half at 0.1/target 1 now reads 0.900 and RED, where the mean-gap form reported 0.000 and painted green. |
| REPORTING_CHARTS_A-3 | P1 | `calibration_heatmap_2d.py:30,34-36,59-67` | Fixed thresholds do not scale with cell support, and the headline is an argmax over cells (a max of noise): a calibrated model reads red at 40 rows/cell, green at 4,000. | Compare each cell's gap to its own binomial noise floor; scale the minimum-cell floor with grid size. | Sample-size-invariance meta-test across every verdict helper: clean data at four sizes must give one verdict. | **RESOLVED** -- each cell is graded against its own binomial noise floor (`null_ece_scale`), and the worst-cell headline against the floor scaled for the MAXIMUM over populated cells (`mu + sigma*sqrt(2 ln m)`) -- without that second step a calibrated model still read amber at 256 cells, because the max of 256 draws sits well above any single cell's floor. A perfectly calibrated model now reads green at 3, 5, 8 and 16 bins per axis (40 to 2,200 rows/cell), while an injected miscalibrated pocket still reads red at 5 and 16. |
| REPORTING_CHARTS_A-4 | P1 | `calibration_by_feature.py:124-133,159-161` | Per-bin ECE uses the plug-in estimator, so heterogeneity and its traffic light read sample size; the same bug class the sibling module documents having fixed. | Switch to the debiased ECE, matching `calibration_drift`. One call site. | Cross-module consistency scanner: modules computing the same named statistic must route through the same estimator helper. | **RESOLVED** -- switched the one call site to `compute_ece_debiased`, matching the sibling `calibration_drift` module, and graded the heterogeneity gap against the noise floor of the thinnest bin. A calibrated model now reports heterogeneity 0.0027 against a floor of 0.0056 (green); a genuine feature-dependent pocket reports 0.315 (red). |
| REPORTING_CHARTS_A-5 | P2 | `binary.py:292-321,218-276` | The default-on AP bootstrap CI costs 3.01s vs 0.046s at n=500k, re-sorts full n despite a shared sort being in hand, and allocates 200 MB in one block. | **RESOLVED** -- `bootstrap_ap_ci` takes an optional keyword-only `sort=`; when the caller's `_ScoreSort` covers the same rows the full-data AP reads its descending labels from `cum_tp`'s first difference instead of running a second full-n argsort. Verified byte-identical at n=500k: (0.6671448948, 0.6640223345, 0.6701178202) both ways. The signature stays backward-compatible, which is what the earlier deferral was protecting. | Panel-level cost-regression harness: no panel may exceed ~5x the median panel's build time. | **RESOLVED (memory)** / **FUTURE (the shared-sort reuse)** -- the `(n_boot, m)` index matrix was 200 MB in one allocation for a buffer consumed one row at a time; it is drawn in 32-row chunks now, same RNG order so the interval is unchanged. Measured peak at n=2M: 116 MB, and that residue is the input arrays, not the draw. Threading the caller's existing `_ScoreSort` in to avoid the full-n re-sort is deferred: it changes `bootstrap_ap_ci`'s signature, which is public and called from outside this module. |
| REPORTING_CHARTS_A-6 | P2 | `binary.py:317-321,230-236` | The PR title pairs a full-n AP point estimate with a CI bootstrapped from a 50k subsample, so the interval is ~6x too wide at n=2M and the point estimate need not lie inside it. | Report the subsample's own AP, or scale the half-width, or state the cap. Assert containment. | Interval-consistency property test over every CI helper: point estimate inside the interval; half-width shrinking like 1/sqrt(n). | **RESOLVED** -- the half-width is rescaled by `sqrt(m/n)` so a full-n point estimate no longer carries the uncertainty of an m-row study. Validated against an UNCAPPED bootstrap at n=200k: 0.004130 rescaled vs 0.004085 ground truth, a ratio of 1.01, where the un-rescaled interval was about 2x too wide at that size (and ~6x at n=2M). |
| REPORTING_CHARTS_A-7 | P2 | `calibration.py:401-408` | The bar width is NaN whenever any bin centre is NaN, silently blanking the histogram; NaN bins also emit a ghost label at NaN coordinates. | Compute the width from finite centres with a fallback; filter labels by finiteness. | Property test: inject a NaN into each input array in turn, assert no geometry field of the spec is non-finite. | **RESOLVED** -- bar width is computed over finite, positive gaps only, so one NaN bin centre no longer makes the width NaN and blank the whole histogram (both backends render NO bars for a NaN width). Inline labels at non-finite coordinates are skipped rather than emitted as unplaceable ghosts. |
| REPORTING_CHARTS_A-8 | P2 | `binary.py:375-390` | The KS curve evaluates ECDFs at mid-tie ranks no threshold can realise, biasing KS upward on quantised scores (measured 0.515 vs 0.512 reference). | Reuse the run-end array the sibling sweep already uses to restrict to realisable thresholds. | Tie-invariance meta-test: compute on quantised scores, assert exact agreement with scipy/sklearn. | **RESOLVED** -- the KS ECDFs are evaluated at DISTINCT thresholds via the memoised `distinct_threshold_counts` instead of per-row mid-tie ranks no threshold can realise. On 20-level quantised scores KS went 0.494441 -> 0.493870, matching a reference computed over realisable thresholds EXACTLY (diff 0.00e+00), and continuous scores match exactly too. Also strictly less work than the per-row form. |
| REPORTING_CHARTS_A-9 | P2 | `category_discriminability.py:108-126` | The unhashable-column probe iterates every row in Python: 0.54s per 1M-row object column against a documented ~43ms end-to-end budget. | Probe a bounded head sample, or use pandas dtype inference. | Scanner for generator expressions iterating an unbounded column in a diagnostics path. | **RESOLVED** -- the unhashable-element probe scans a bounded 1000-row head instead of every row. It is a Python-level scan, so at 1M rows it cost ~0.54s against this module's documented ~43ms end-to-end budget -- an order of magnitude more than the chart it guards. An embedding column is embedding-valued in every row by construction, and a column that turns non-scalar later is still caught by the `astype("category")` this probe preempts, which raises a named error rather than producing a wrong chart. Verified the embedding column is still excluded. |
| REPORTING_CHARTS_A-10 | P2 | `category_discriminability.py:194-196` | `level_woe` computes the positive counts internally and returns a strict subset; the caller recomputes them with two length-n gathers and a bincount. | **RESOLVED** -- `level_woe` returns `(woe, count, positives)` now, so the caller stopped recomputing the same per-level positive counts with two length-n gathers and a second bincount over identical data. The one external caller (`rare_count_pruning`) was updated with it. | AST check for a function returning a strict subset of what it computed, with a caller recomputing the discarded part. | **RESOLVED** -- `level_woe` returns `(woe, count, positives)` now, so the caller stopped recomputing the same per-level positive counts with two length-n gathers and a second bincount over identical data. The one external caller (`rare_count_pruning`) was updated with it. |
| REPORTING_CHARTS_A-11 | P2 | `calibration_by_feature.py:113-133` | The per-bin ECE loop runs a mask, a sum and three gathers per bin — ~20 full-array passes — while both docstrings claim O(n); the sibling drift module does it correctly with one argsort. | **RESOLVED** -- one boolean mask plus three length-n gathers PER BIN is ~4 full-array passes per bin against a docstring promising one pass. A single stable argsort groups the rows once and each bin is a contiguous slice. Stable order inside a bin is exactly what boolean masking produced, so the per-bin rows -- and the RNG subsample drawn from them -- are unchanged: verified bit-identical against the original masking loop on 4 seeds. | Complexity-claim audit: time at n and 4n for every function whose docstring asserts O(n). | **RESOLVED** -- one boolean mask plus three length-n gathers PER BIN is ~4 full-array passes per bin against a docstring promising one pass. A single stable argsort groups the rows once and each bin is a contiguous slice. Stable order inside a bin is exactly what boolean masking produced, so the per-bin rows -- and the RNG subsample drawn from them -- are unchanged: verified bit-identical against the original masking loop on 4 seeds. |
| REPORTING_CHARTS_A-12 | P2 | `binary.py:341-372` | Both classes are histogrammed with density normalisation, so severe class imbalance is invisible and labels carry no counts. | Put counts and shares in the labels; consider a count mode. | Informativeness lint: each class-conditional series label must carry its support count. | **RESOLVED** -- both class curves stay densities (the shape comparison is the panel's purpose) but the labels now carry each class's count and share, and the title says heights are not counts. At a 0.2% base rate the two humps are equally tall, so the imbalance was previously invisible; it is now readable off the legend. |
| REPORTING_CHARTS_A-13 | P2 | `binary.py:457-503` | The operating-point picker marks neither the F1-optimal nor the cost-minimising threshold, and decimation can remove the true optimum before plotting. | **RESOLVED** -- the panel exists to PICK an operating point and marked neither optimum. The F1-optimal and cost-minimising thresholds are marked now, and both are found on the FULL sweep rather than the decimated copy, so decimation cannot thin away the vertex being marked (verified: markers land inside the drawn x-range at 500 vertices out of 44,212 distinct thresholds). | Decimation-preserves-extrema meta-test for every decimator caller. | **RESOLVED** -- the panel exists to PICK an operating point and marked neither optimum. The F1-optimal and cost-minimising thresholds are marked now, and both are found on the FULL sweep rather than the decimated copy, so decimation cannot thin away the vertex being marked (verified: markers land inside the drawn x-range at 500 vertices out of 44,212 distinct thresholds). |
| REPORTING_CHARTS_A-14 | P2 | `calibration_drift.py:216-221` | The docstring promises a noise-floor band that is never built, though the per-window counts needed for it are already on the result. | Build it as a shaded series, or delete the promise. Building is the right call. | Docstring-vs-spec contract test: named visual elements must map to populated spec fields. | **RESOLVED** -- the noise-floor band the docstring promised is now built from the per-window counts already on the result. ECE is a mean ABSOLUTE deviation, so a perfectly calibrated window still scores above zero by an amount set by its row count; without the band a reader cannot tell a drifting window from a thin one. |
| REPORTING_CHARTS_A-15 | P2 | `calibration_heatmap_2d.py:104-136` | Cell annotations show a row count taken AFTER a 1M-row subsample, understating real support ~10x; the minimum-cell floor is applied to the same subsampled counts. | **RESOLVED** -- the cell annotation printed the count AFTER the 1M-row subsample, understating real support by the subsample ratio. The result now carries both grids (`support_grid` = full data, `support_grid_sampled` = what the ECE was computed on) and the panel discloses both sizes. Verified at 3M rows: 3,000,000 reported against 1,000,000 sampled. | Sampling-transparency lint: displayed counts must be continuous across the subsample cap boundary. | **RESOLVED** -- the cell annotation printed the count AFTER the 1M-row subsample, understating real support by the subsample ratio. The result now carries both grids (`support_grid` = full data, `support_grid_sampled` = what the ECE was computed on) and the panel discloses both sizes. Verified at 3M rows: 3,000,000 reported against 1,000,000 sampled. |
| REPORTING_CHARTS_A-16 | P2 | five builders in scope | Five figure builders ship no caption though each carries a verdict a reader cannot decode unaided (a bootstrap significance fraction, a heterogeneity gap, a traffic light, a KS number, a queue-rate secondary axis). | Add captions; see the cross-cutting report's inventory for proposed text. | Coverage gate over every public composer, with a shrinking allowlist. | **RESOLVED** -- the caption sweep this was deferred into (RUX-1) is complete, and the five builders named here are covered by it. Measured across the package: 26 of 27 public builders returning a FigureSpec now set a caption, the 27th (`build_regression_panel_spec`) is a thin delegator to `compose_regression_figure` and inherits its one, and the three builders returning a Result wrapper carry a caption on the FigureSpec inside it (783 and 313 chars on decision-curve and risk-coverage). RUX-2 then made the caption follow the tokens actually rendered. |
| REPORTING_CHARTS_A-17 | P2 | `binary.py:71-82` | The finite-binary prep silently drops rows whose label is outside {0,1} with no count and no log, so a mis-passed target yields confident curves on a silent subset. | Count drops and warn; raise when every row is dropped. | Behavioural: pass a signed-label vector to each composer, assert a warning or exception, never a silent figure. | **RESOLVED** -- rows whose label falls outside {0,1} are still dropped (these panels are one-vs-rest by definition) but the drop now WARNS with the count and the offending label values, naming the multiclass composer. A mis-passed multiclass target previously produced confident binary curves on a silent subset. |
| REPORTING_CHARTS_A-18 | P2 | `_acf.py:34-42` | Dropping non-finite rows CLOSES the gaps in a time series, so "lag k" spans a variable amount of real time and the ACF describes a different series than the caller's. | **RESOLVED (the measurable half)** / **FUTURE (the regular grid)** -- dropping non-finite rows CLOSES the gaps, so lag k spans a variable amount of real time. The residual ACF now states what fraction of rows was dropped and that the lag spacing is therefore uneven (measured on a 1-in-7-NaN series: "14.3% of rows were non-finite and closed up"), via a shared `gap_fraction` helper. Resampling onto a regular grid stays deferred: it changes what the ACF MEASURES and needs a decision about the grid convention first. | Build a known AR(1) series, punch NaN holes, assert the estimated coefficient still matches or the function refuses. | **RESOLVED** -- same fix as CHARTS_C-12, which is the same defect in a fourth builder: the residual ACF no longer closes the gaps it drops, so lag k means k steps of the caller's grid again. The gap fraction is still reported, now as context for how many pairs the estimate rests on rather than as a caveat about bias. |
| REPORTING_CHARTS_A-19 | P3 | `binary.py` (827 LOC) | Inside the carve band, and holds three separable concerns including a direct-matplotlib decile table that bypasses the spec layer the package docstring describes. | Carve the decile table and the AP bootstrap into siblings. | Ratchet test: no chart file over 800 LOC, with a shrinking allowlist. | **FUTURE** -- `binary.py` is inside the carve band at 827 LOC but under the 1000 limit, and the file was already edited heavily this round. Carving it in the same change as behaviour fixes would make both undiffable; scheduled as its own change (the direct-matplotlib decile table bypassing the spec layer is the natural first piece to move). |
| REPORTING_CHARTS_A-20 | P3 | `calibration.py:181-192` | A pointwise-band significance fraction is reported as one claim about the range; pointwise bands under-cover simultaneously and the fraction is unweighted by data density. | Widen to a simultaneous band or label it pointwise. | Coverage-calibration meta-test: the reported significant fraction must average at or below nominal on calibrated data. | **RESOLVED** -- the band is SIMULTANEOUS (sup-t) now, not the pointwise 2.5/97.5 percentiles: the reported "significant on X% of range" is a claim about the whole range, so the band backing it has to cover the whole range. Measured on perfectly calibrated data over 8 seeds, the false-positive fraction fell from 0.099 (max 0.140) to 0.061 (max 0.090) while a genuinely miscalibrated model still fires at 0.89 (was 0.93). The residual 6% is isotonic's own boundary bias, not band under-coverage, so the test pins a ceiling rather than zero. |
| REPORTING_CHARTS_A-21 | P3 | `calibration.py:121-122` | Inconsistent degeneracy predicate: the outer guard sorts to answer a boolean while both inner loops use the linear form, and a comment claims the fix was applied to both. | Replace with the min/max form. | Scanner for unique-size used purely as a boolean distinctness test. | **RESOLVED** -- the outer degeneracy guard now uses the same linear predicate as both inner loops, and the comment claiming otherwise is gone. It previously SORTED to answer a boolean. |
| REPORTING_CHARTS_A-22 | P3 | `calibration.py:72-74,155` | One comment describes the REJECTED alternative as if it described the current code; another narrates refactor history. | Rewrite to state only the invariant. | Comment-hygiene lint for past-tense refactor markers. | **RESOLVED** -- both comments rewritten: one described the rejected alternative as though it described the current code, the other narrated refactor history. |
| REPORTING_CHARTS_A-23 | P3 | `calibration_drift.py:160-161` | Comment and docstring say "time midpoint"; the code takes the positional median, which differs materially on clumped timestamps. | Fix the two docstrings; the positional median is the better choice for equal-population windows. | Docstring-claim spot-check on an input where the candidates diverge. | **RESOLVED** -- the comment and docstring said "time midpoint" where the code takes the positional MEDIAN. Corrected to say median and to explain why that is the right choice (on clumped timestamps the two differ materially, and the median puts the point where the window's data actually is). |
| REPORTING_CHARTS_A-24 | P3 | `calibration_drift.py:286-291` | Windows are labelled by index with no timestamp, defeating the module's "where did calibration bend" purpose; centres and counts are both already available. | Label with the window centre and its n. | Traceability lint: no series label may be a bare index token. | **RESOLVED** -- reliability windows are labelled with their centre date and row count instead of a bare `w0`/`w1` index. A bare index cannot be mapped back to a period, which is the entire question this module exists to answer. |
| REPORTING_CHARTS_A-25 | P3 | `calibration.py:562-613` | The midrank helper searchsorts NaN against NaN, and the DeLong variance never validates the label domain, so rows that are neither 1 nor 0 vanish from both partitions. | Add a finite check; assert the partitions sum to the input length. | Extend the silent-subsetting property test to every metric helper partitioning by a label predicate. | **RESOLVED** -- the midrank NaN searchsorted and the unvalidated label domain are both closed: `delong_auc_variance` drops non-finite scores with a warning naming the count (a NaN has no rank, so its tie block is empty and the AUC built from it is wrong rather than undefined), and a label outside {0, 1} now warns that those rows are read as the NEGATIVE class rather than vanishing from both partitions silently. |
| REPORTING_CHARTS_A-26 | P3 | `calibration_by_feature.py:44-45` | Two private aliases of shared helpers add no behaviour — dead indirection left by a consolidation. | Call the public names, delete the aliases. | Dead-alias scanner for module-level rebindings of imported names. | **RESOLVED** -- the two dead private aliases are deleted and the call sites use the public names. |
| REPORTING_CHARTS_A-27 | P3 | `_layout.py:17-37` | `pack_panels` is annotated as returning non-optional panels but pads with None — the implicit-Optional class the house mypy rule targets. | **RESOLVED** -- `pack_panels` and `FigureSpec.panels` are annotated `Optional` now, matching the `None` padding they have always produced. The feared ~15-call-site guard sweep did not materialise: mypy is clean across all 68 files, because every renderer already guarded the `None` case -- the annotation was the only thing out of step, and an annotation that contradicts the runtime is worse than none. | Scope the charts package into the pre-commit mypy gate. | **RESOLVED** -- `pack_panels` and `FigureSpec.panels` are annotated `Optional` now, matching the `None` padding they have always produced. The feared ~15-call-site guard sweep did not materialise: mypy is clean across all 68 files, because every renderer already guarded the `None` case -- the annotation was the only thing out of step, and an annotation that contradicts the runtime is worse than none. |
| REPORTING_CHARTS_A-28 | P3 | four sites | `line_styles` mixes matplotlib linestyles and plotly modes in one tuple, making rendering backend-dependent and unreviewable. | **RESOLVED (documented + verified)** -- the tuple is ONE closed vocabulary that both backends map from, not a mix of each backend's native spelling; the mode tokens merely READ like plotly. The field now says so and separates the two kinds of token (dash pattern vs draw mode). Audited every call site: only `-`, `--`, `:`, `markers`, `lines+markers` are used, and all six documented tokens were verified to render on both backends. | Spec-vocabulary lint: every `line_styles` tuple draws from one vocabulary. | **RESOLVED** -- the tuple is ONE closed vocabulary that both backends map from, not a mix of each backend's native spelling; the mode tokens merely READ like plotly. The field now says so and separates the two kinds of token (dash pattern vs draw mode). Audited every call site: only `-`, `--`, `:`, `markers`, `lines+markers` are used, and all six documented tokens were verified to render on both backends. |
| REPORTING_CHARTS_A-29 | P3 | `calibration_by_feature.py:232-235` | A ragged grid is hand-built, bypassing the helper whose documented job is producing a rectangular padded grid; renderer handling of ragged rows is undocumented and untested. | **DOC** -- the raggedness is deliberate and correct: row 0 is a strip of per-bin minis and row 1 is one summary line spanning the width. `pack_panels` only makes RECTANGULAR grids, so routing this through it would pad row 1 with empty cells and shrink the summary to 1/N width. Verified both renderers handle a short row (4 axes for a 3+1 grid on each), and the reason is written at the site. | Structural invariant: all rows of a spec's panels have equal length. | **DOC** -- the raggedness is deliberate: row 0 is a strip of per-bin minis and row 1 is one summary line spanning the width, which `pack_panels` cannot express because it only builds RECTANGULAR grids. The blocker cited here (CHARTS_A-27's annotation) is itself resolved -- `pack_panels` and `FigureSpec.panels` are `Optional` now and mypy is clean -- so what is left is the documented design decision, not a deferral. |
| REPORTING_CHARTS_A-30 | P3 | `binary.py:479-491` | Cost is normalised by max only (not min-max) so a cost never near 0 renders near-flat, shares the primary axis despite different units, and its max is computed twice. | **RESOLVED** -- the cost curve was normalised by max only, so a cost that never approaches zero compressed into a flat band near the top of an axis it shares with [0,1] metrics, hiding the minimum the series exists to show. Min-max normalised now, with the minimum marked. | Axis-unit lint: no panel carries incommensurable units on one axis unless the label names a shared normalisation. | **RESOLVED** -- the cost curve was normalised by max only, so a cost that never approaches zero compressed into a flat band near the top of an axis it shares with [0,1] metrics, hiding the minimum the series exists to show. Min-max normalised now, with the minimum marked. |
| REPORTING_CHARTS_A-31 | P3 | `category_discriminability.py:74-97` | WoE bars carry no uncertainty, and ranking by magnitude across all levels is a max-selection biasing the top bar upward. | **RESOLVED** -- `BarPanelSpec` gained a `value_err` channel honoured by both backends, and each WoE bar carries its own 95% interval, derived by mapping that level's Wilson interval on P(y=1|level) through the WoE transform (monotone, so the transformed bounds ARE the WoE bounds -- no delta-method approximation, and finite at p=0/1). The title also states the max-selection bias explicitly: ranking every level of every feature by | **RESOLVED** -- `BarPanelSpec` gained a `value_err` channel honoured by both backends, and each WoE bar carries its own 95% interval, derived by mapping that level's Wilson interval on P(y=1 | means the top bar is the maximum over hundreds of estimates. | Null-data meta-test for every ranked-screen chart: the top cell's effect must be within its own CI of zero. | **FUTURE** -- grouped with CHARTS_A-10: both change `level_woe`'s return, and ranking-by-magnitude is a max-selection bias that wants a Wilson interval computed from the counts that finding is about returning. |
| REPORTING_CHARTS_A-32 | P3 | `category_discriminability.py:229-244` | The degenerate branch's user-facing text uses dash-pairs in prose. Low confidence: the package applies the convention inconsistently. | Replace in the two user-visible strings; decide the docstring scope separately. | Prose lint restricted to user-visible string literals. | **RESOLVED** -- the degenerate branch's user-facing text no longer uses dash pairs in prose. |
| REPORTING_CHARTS_A-33 | P3 | `category_discriminability.py:210` | `top_k=0` is silently turned into 1 rather than raising or returning empty. | Validate and raise, or honour 0 and let the degenerate branch explain it. | The house "sweep integer size-like params through minus-one and zero" fuzz test. | **RESOLVED** -- `top_k=0` now raises a named `ValueError` instead of being silently promoted to 1. Silently returning one bar for a request of zero is a wrong answer, not a lenient one. |
| REPORTING_CHARTS_A-34 | P3 | `calibration.py:132`; `binary.py:255` | Both bootstrap helpers materialise the full index matrix in one allocation (60 MB and 200 MB) against the house RAM-discipline rule. | **RESOLVED** -- `calibration.py`'s 60 MB index matrix is drawn in 32-row chunks, same RNG call order so every resample is identical (verified: same band on a repeat call). Measured peak at n=100k: 27.5 MB. | Peak-RSS regression test around each bootstrap helper at its documented row cap. | **RESOLVED (binary.py)** / **FUTURE (calibration.py)** -- the 200 MB AP index matrix is chunked (see CHARTS_A-5). `calibration.py`'s 60 MB twin is the same pattern and takes the same fix, but it feeds the DeLong/bootstrap path that CHARTS_A-25 also touches; grouped so that path is changed once. |
| REPORTING_CHARTS_A-35 | P3 | `_jaccard_kernel.py:31,45` | A kernel compiled with fastmath does a threshold comparison, so a NaN probability's comparison is unspecified and may diverge from the numpy fallback documented as bit-exact. | Drop fastmath, or gate it behind a finiteness check per the house pattern. | Backend-parity fuzz on NaN/Inf-injected input, not just clean data. | **RESOLVED** -- `jaccard_rows` gates on `np.isfinite(y_proba).all()` before dispatching to the fastmath kernel; non-finite input takes the numpy reference, where a NaN compares False under IEEE rules. Dropping fastmath would have cost the vectorised reduction the kernel exists for, and the gate is one pass against the kernel's own O(n*K). Verified against the numpy reference on NaN, +inf and -inf injected input. |
| REPORTING_CHARTS_A-36 | P3 | `calibration.py:501-512` | The reliability curve silently folds out-of-range probabilities into edge bins, and applies its finiteness mask per series, so compared curves can rest on different row subsets. | Surface out-of-range values; compute the mask once across all series. | Comparability invariant for every multi-series overlay builder. | **RESOLVED** -- both halves. Out-of-range probabilities are EXCLUDED with a warning naming the count and the observed range, instead of being folded into the edge bins where an out-of-range score read as a confident well-calibrated one. And `build_reliability_overlay_spec` now computes ONE finite mask over the target and every series, so the raw curve and each calibrated curve rest on the same rows -- the per-curve mask gave each series its own subset, which is precisely the comparison the figure exists to support. Each curve carries its row count and the caption states how many rows were dropped. |

## Narrative detail

All quantitative statements below were produced by running the shipped code in-process against `src/`.

**-1 (PIT non-uniform under calibration).** With a uniform-probability calibrated model, the ten-bin PIT density came back as the exact linear ramp `[0.10, 0.30, 0.51, 0.71, 0.91, 1.10, 1.29, 1.49, 1.68, 1.90]` with KS 0.247; a calibrated constant-probability model gives two spikes and KS 0.400. The derivation: with `p ~ U(0,1)` and `y ~ Bernoulli(p)`, both branches contribute `u` at `u`, totalling `2u` — triangular, not flat. The randomised PIT (drawing uniformly on the predictive CDF's jump interval at `y`) is the construction that IS uniform under calibration. The existing test asserts only panel type and title format, which is why this survived.

**-2 / -3 (heatmap metric and verdict).** For -2, a cell holding half `score=0.9, y=0` and half `score=0.1, y=1` has equal means, so the shipped function returned a worst-cell value of 0.0 and a green light. The module docstring does say "calibration GAP", so the intent was known — but the dict key, the headline, the colorbar label and the thresholds all assert ECE, a stronger property. For -3, a perfectly calibrated model with pure-noise features gave worst-cell 0.157 (red) at ~40 rows/cell, 0.082 (amber) at ~200, 0.015 (green) at ~4,000 — tracking the reciprocal square root of the cell count. The two compound: -2 makes the metric miss real miscalibration, -3 makes it invent fake miscalibration.

**-4 (plug-in ECE bias).** The sibling `calibration_drift.py:169-172` carries an explicit note that the plug-in ECE on small windows is inflated and was producing a spurious trend that was purely a sample-size artifact. `calibration_by_feature` still routes through the biased estimator: a calibrated model at n=200 gave per-bin `[0.145, 0.158, 0.124, 0.073]`, heterogeneity 0.085, amber; at n=200k, 0.003. The per-bin dict is returned to the suite's metrics, so the inflated values propagate beyond the figure. The debiased estimator already exists in the same package — a one-line swap.

**-5 (AP CI dominates the figure).** Timed at n=500k warm: `ap_ci=True` 3.011s, `ap_ci=False` 0.046s, ROC panel 0.044s. The module docstring claims a single shared sort and a ~0.96s build at n=2M; the CI path re-sorts full n and sorts the subsample again, so one default-on annotation costs 65x the panel it annotates.

**-9 (per-row Python scan).** The unhashable-column guard short-circuits on the first non-scalar, so an embedding column is cheap — but a plain string object column, the common case, scans all n rows in the interpreter. Measured on a 2-column, 1M-row object frame: 1.25s total, of which one column's scan alone was 0.54s.

---

**Counts:** 14 files reviewed, 3,397 LOC. Findings: P0 = 0, P1 = 4, P2 = 15, P3 = 17 (36 total).
