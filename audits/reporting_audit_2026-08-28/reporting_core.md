# Audit report: reporting_core

**Cluster:** reporting_core
**Scope:** `src/mlframe/reporting/spec.py`, `output.py`, `colors.py`, `catalog.py`, `__init__.py`, `auto_dispatch.py`, `diagnostics_dispatch.py`, `_diagnostics_dispatch_extra.py`, `report_html.py`, `_risk_coverage_diagnostic.py`, `_benchmarks/**`
**Out of scope:** `reporting/charts/**` and `reporting/renderers/**`; `src/mlframe/training/reporting/**` was read only where needed to confirm how this cluster's outputs are consumed.

**Files reviewed:** 14 (10 in-scope modules + 4 files under `_benchmarks/`, all read in full)
**LOC reviewed:** 3,398 (`wc -l`: 3,197 for the ten modules + 201 for `_benchmarks/`)

## Summary by dimension

1. **Correctness bugs:** 3 confirmed silent-wrong-output bugs, all verified by running the real code: the `DIAG_MAX_FEATURES` column cap silently no-ops on polars frames (`hasattr(frame, "loc")` gate); `_flat_scalar_metrics` silently drops `np.float32` / `np.int64` metrics from the model-comparison leaderboard (those numpy scalars are not `float`/`int` subclasses); `_save_figure` reports "chart failed" for the merely-not-requested-format case, poisoning the `charts["failed"]` accounting a run can assert on. No P0/P1 — nothing corrupts a persisted numeric artifact or ships a wrong metric value; the drops are omissions and the accounting bug is observability.
2. **Computational efficiency:** one material finding — the error-analysis and slice-finder entry points row-subset the frame BEFORE capping columns, so a wide frame materialises up to `DIAG_ROW_CAP` x ALL columns instead of x 200. No O(n^2) patterns; the heavy numeric work lives in the out-of-scope builders and this layer is genuinely O(builders) as its docstring claims. Two micro-redundancies (a duplicate `_select_feature_columns` call, a discarded probe predict) noted as P3.
3. **Architecture and clarity:** module boundaries are sound and the mutual-import cycle between `diagnostics_dispatch` and `_diagnostics_dispatch_extra` is handled correctly (lazy delegates + module `__getattr__`, reasoning documented). `diagnostics_dispatch.py` at 892 LOC and `_diagnostics_dispatch_extra.py` at 711 LOC are both past the 800-900 "carve now" line in CLAUDE.md. `auto_dispatch.py` repeats the same 12-line render/dpi/save block five times. `catalog.py`'s standalone-diagnostics list has drifted badly from what the dispatchers ship.
4. **Informativeness / consumer value:** the largest yield. Eight rendered diagnostics are recorded as saved but never get a `_record_path`, so they are invisible to the combined HTML report a DS actually opens; the combined report itself flattens every chart into one section named `charts` with raw filename labels and no captions, discarding `report_html`'s own section/description/caption machinery; the catalogue that is supposed to be the discoverability surface omits roughly sixteen shipped diagnostics.
5. **UI/UX:** `report_html` emits `alt=""` on every chart image, collapses all but the first section (sidebar anchors land on a closed `<details>`), applies a per-file base64 threshold with no cumulative page-size budget, and embeds plotly fragments verbatim so N fragments can each carry their own plotly.js. Backend parity: `_save_figure`-based diagnostics (decile table, 2-D PDP) are matplotlib-only and the combined report is matplotlib-PNG-only, so a `plotly[html]`-only operator loses them with no clear signal. `LINE_PALETTE` cycles at 20 with no linestyle variation.
6. **Edge cases and robustness:** the no-crash contract is respected everywhere; degenerate inputs are guarded consistently (`m == 0`, single-class, fewer than 2 members/splits/models). Gaps: the emphasis selector derives its base rate from `count_nonzero` (silent no-op for non-0/1 encodings); `render_target_acf_diagnostic` silently skips when timestamps are shorter than the target; the frozen spec dataclasses document field co-requirements that nothing enforces.
7. **Test coverage:** `tests/reporting/` is broad and healthy (22 tests on `report_html`, dedicated files for colors / catalog / output DSL / auto-dispatch / dispatch helpers). Real gaps: no polars case for the column cap, no numpy-scalar case for the metric flattener, no test that every chart recorded as saved is reachable from the combined report, no completeness test for the standalone-diagnostics catalogue, no enforcement test for the spec dataclass invariants.
8. **OSS hygiene:** a date stamp in `output.py`, finding IDs (`INV-58`, `R-3 / INV-11`) in docstrings, `iter71`/`iter83` wave markers in benchmark filenames and docstrings, refactor-history narration in `_diagnostics_dispatch_extra.py`. On `--`: it is used pervasively and deliberately as the ASCII em-dash substitute here (the catalogue even has ASCII-only tests), so it is treated as house convention rather than flagged per occurrence; `output.py`'s real em dashes and set-membership symbol are the inconsistency worth reporting.

## Findings table

| ID | Severity | File:Line | Summary | Suggested fix | Meta-test idea | Disposition |
|---|---|---|---|---|---|---|
| REPORTING_CORE-1 | P2 | `diagnostics_dispatch.py:127` | `_select_feature_columns` gates its column cap on `hasattr(frame, "loc")`, which polars DataFrames lack, so `DIAG_MAX_FEATURES` silently no-ops for polars and the dense-matrix builders receive every column. | Gate on the ability to select columns, not a pandas-only attribute: try `frame[keep]` for anything with `.columns` (works for pandas and polars), fall back only on exception. | Cross-format property test: run the same assertion (capped column count) against pandas and polars frames built from identical data — catches every "pandas-only attribute as capability probe" bug. | TODO |
| REPORTING_CORE-2 | P2 | `_diagnostics_dispatch_extra.py:145,153` | `_flat_scalar_metrics` accepts only `isinstance(v, (int, float))`; `np.float32`/`np.int64` are not subclasses, so those metrics are silently dropped from the leaderboard. | Test with `isinstance(v, (numbers.Real, np.number)) and not isinstance(v, (bool, np.bool_))`. | Property test: feed each dict-consuming helper metrics whose values are `np.float32/np.float64/np.int32/np.int64/int/float` and assert an identical output key set. | TODO |
| REPORTING_CORE-3 | P2 | `diagnostics_dispatch.py:70-77`; `_diagnostics_dispatch_extra.py:293` | `_save_figure` returns `False` when `png` is absent from the DSL (a "not requested" condition) and callers feed that into `_record`, so a `plotly[html]`-only run reports `decile_table`/`pdp_2d` as FAILED. | Return a tristate distinguishing "not requested" from "save failed"; skip `_record` in the not-requested case. | Accounting invariant test: run every `render_*_diagnostic` under a DSL requesting no format it can emit and assert `charts["failed"]` stays empty. | TODO |
| REPORTING_CORE-4 | P2 | `diagnostics_dispatch.py:229-266,318-389`; `_diagnostics_dispatch_extra.py:485` | Eight diagnostics call `_record` but never `_record_path` (`weak_segments`, `error_bias`, `segments`, `psi_heatmap`, `residual_vs_time`, `metric_over_time`, `adversarial`, `target_dist`), so they render to disk yet never appear in the combined HTML report. | Add `if ok: _record_path(charts, base)` at each of the eight sites. | Completeness test: assert `set(charts["saved"]) - {"combined_html"}` maps 1:1 onto `charts["paths"]` basenames. | TODO |
| REPORTING_CORE-5 | P2 | `diagnostics_dispatch.py:199,617` | `_select_feature_columns(_subset_rows(df, idx), ...)` row-subsets first and caps columns second, materialising `DIAG_ROW_CAP` rows x ALL columns (~25x overhead on a 5,000-column frame). | Swap the order: cap columns on the full frame (a view), then row-subset the narrow view. | Shape regression test: 5,000-column frame + spy on `_subset_rows`; assert the frame it receives already has <= `DIAG_MAX_FEATURES` columns. | TODO |
| REPORTING_CORE-6 | P2 | `_diagnostics_dispatch_extra.py:214` | `build_combined_html_report` returns `None` unless `png` is in the DSL, so a `plotly[html]`-only run gets no combined report — even though `report_html` supports plotly fragments and `ChartEntry.plotly_html_fragment` exists for it. | Also assemble when the DSL requests plotly html: locate `<base>.html`/`<base>.plotly.html` and pass them as fragments. | Backend-parity test: for `{matplotlib[png], plotly[html], both}` assert a combined report is produced with one entry per recorded path. | TODO |
| REPORTING_CORE-7 | P2 | `catalog.py:93-108` | `_STANDALONE_DIAGNOSTICS` has drifted: `risk_coverage`, `model_card`, `decile_table`, `prediction_stability`, `split_comparison`, `shap_interactions`, `shap_per_instance`, `pdp_2d`, `cusum_drift`, `target_dist`, `psi_heatmap`, `adversarial`, `weak_segments`, `error_bias`, `worst_k_table`, `training_curve` all ship but are absent. | Derive the list from the `render_*_diagnostic` functions actually exported by the two dispatch modules, keeping hand-written blurbs as an override map (mirroring how panel tokens are derived from the frozensets). | Completeness test mirroring `test_every_token_has_a_real_description`: every `render_*_diagnostic` must have a `standalone_diagnostics()` entry and vice versa. | TODO |
| REPORTING_CORE-8 | P2 | `_diagnostics_dispatch_extra.py:252-256` | Every entry is appended as `("charts", basename(p), png)`, so the combined report has ONE section named "charts", filename labels, no captions and no `section_descriptions` — discarding features `build_combined_report` provides and tests. | Map each chart-base suffix to a (section, human label, caption) triple, reusing `catalog.standalone_diagnostics()` descriptions; pass `section_descriptions`. | Report-quality test: assert >1 `<details>` section, no panel heading is a bare filename, every panel has a non-empty `class="cap"` caption. | TODO |
| REPORTING_CORE-9 | P3 | `diagnostics_dispatch.py:210-222` | `worst_k_table` records failure but never success, so it can never appear as saved — an asymmetry unique to this call site. | Record the success side too. | AST meta-test: every `_record(charts, NAME, False)` in an `except` must have a matching success-path `_record` in the same function. | TODO |
| REPORTING_CORE-10 | P3 | `diagnostics_dispatch.py:356-357` | `higher_is_better = True if _dir is None else _dir` — an unknown/custom metric silently defaults to higher-is-better, inverting the `metric_over_time` trend annotation for a loss-like metric. | On `None`, log a warning and skip or label the trend "direction unknown". | Property test: for every registry metric plus an unknown one, assert the trend verdict is correct or explicitly marked unknown — never silently asserted. | TODO |
| REPORTING_CORE-11 | P3 | `auto_dispatch.py:69` | `n_pos = count_nonzero(finite)` treats any nonzero label as positive, so `{-1,+1}`/`{1,2}` encodings give `n_pos == n` and data-aware emphasis silently no-ops. | Derive the positive count from actual class values (`np.unique`); debug-log when the encoding is not binary 0/1. | Encoding-invariance test: same underlying imbalance must yield the same panel order for `{0,1}`, `{-1,1}`, `{1,2}`, and bool labels. | TODO |
| REPORTING_CORE-12 | P3 | `auto_dispatch.py:114-118` | Docstring lists returns as `"multiclass"/"multilabel"/"ltr"/"quantile"`/None, but the function also returns `"binary"` (line 358). | Add `"binary"` to the documented return set. | Consistency test: the set of returned string literals must be a subset of the tokens named in the docstring. | TODO |
| REPORTING_CORE-13 | P3 | `auto_dispatch.py:180-363` | The same 12-line block (lazy imports, composer call, dpi `replace`, `render_and_save`, `logger.exception` + `panel_failures.append`) is duplicated five times. | Extract one `_compose_and_render(compose_fn, args, branch_name, suffix, ...)` helper. | Duplication tripwire: assert patching `render_and_save` once affects every branch, or an AST cap on structurally identical blocks per module. | TODO |
| REPORTING_CORE-14 | P3 | `spec.py:112-113,141-142,383` | Documented field co-requirements (`bin_width` with `bin_centers`, `trend_xy` with heatmap `trend_line`, `row_height_ratios` length) are unenforced, so a violation surfaces as a backend-specific misrender. | Add `__post_init__` validation raising `ValueError` for each documented co-requirement. | Docstring-derived contract test: for every field comment containing "required when"/"must equal", build a violating spec and assert `ValueError`. | TODO |
| REPORTING_CORE-15 | P3 | `report_html.py:86,89` | Every chart `<img>` carries `alt=""` — unreadable to a screen reader and blank when the image fails to load. | Pass the entry label (and caption) into `_img_tag` as `alt`. | Accessibility lint on generated HTML: no `<img>` may have empty `alt`, and each must match its panel heading. | TODO |
| REPORTING_CORE-16 | P3 | `report_html.py:199` | Only the first section gets `open`, so sidebar anchors target collapsed `<details>`; printing/PDF export yields one section of content. | Open all sections by default, or add a print stylesheet plus a `hashchange` handler. | Rendered-HTML assertion: every `id` referenced by a nav `href="#..."` must resolve to an element visible without script. | TODO |
| REPORTING_CORE-17 | P3 | `report_html.py:83-86` | Base64 inlining is decided per file (256 KB) with no cumulative budget; a 200-chart report can inline ~50 MB into one document. | Track bytes inlined and fall back to relative references past a page budget. | Size-budget test: assemble many just-under-threshold PNGs and assert the output stays under a stated page budget. | TODO |
| REPORTING_CORE-18 | P3 | `report_html.py:232` | Plotly fragments are embedded verbatim; if each baked in `plotly.js`, an N-chart report carries N copies and re-runs the loader N times. | Strip repeated payloads and emit one shared copy in `<head>`, or require `include_plotlyjs=False` fragments. | Assembly test: several fragments each containing a plotly.js marker must yield exactly one marker in the output. | TODO |
| REPORTING_CORE-19 | P3 | `colors.py:169-174` | `__all__` omits `auto_text_colors_batch`, so a star-import consumer silently gets only the per-cell version despite the batch one being the documented fast path. | Add it to `__all__`. | Meta-test: every public module-level function in the reporting package must appear in its module's `__all__` when one is defined. | TODO |
| REPORTING_CORE-20 | P3 | `colors.py:163,166` | Success path returns a `'<U5'` array; the exception fallback returns `object` dtype — divergent behaviour depending on whether matplotlib loaded. | Return the same dtype from both paths. | Dual-path dtype test: for any function with a try/except array fallback, assert `dtype`/`shape` match between success and forced-failure paths. | TODO |
| REPORTING_CORE-21 | P3 | `colors.py:83-85` | `resolve_heatmap_cmap` maps the sentinel `HEATMAP_GENERIC` ("Blues") to viridis, so a builder that genuinely wants Blues cannot request it — sentinel and real colormap share a namespace. | Use a non-colormap sentinel (`"__default__"`/`None`). | Round-trip test: `resolve_heatmap_cmap(x) == x` for every colormap name a builder may legitimately pass. | TODO |
| REPORTING_CORE-22 | P3 | `colors.py:72-74` | `line_color` cycles at 20 with no linestyle/marker variation, so a 25-class overlay reuses exact colors. | Vary linestyle on each palette wrap; have composers annotate when K exceeds the palette. | Distinguishability test: for `K = len(LINE_PALETTE)+5`, every (color, linestyle) pair must be unique. | TODO |
| REPORTING_CORE-23 | P3 | `diagnostics_dispatch.py:726` | `if yt.size < 8 or len(ts) < yt.size: return False` silently drops the target ACF on shorter timestamps instead of aligning to the common prefix like every other entry point. | Use `m = min(yt.size, len(ts))` and proceed when `m >= 8`. | Consistency test: mismatched parallel-array lengths must render on the common prefix or record an explicit skip reason, never a silent `False`. | TODO |
| REPORTING_CORE-24 | P3 | `_diagnostics_dispatch_extra.py:259` | `charts.setdefault("combined_report", out_path)` keeps the FIRST path, so a rebuild leaves the metrics dict pointing at a stale report. | Assign directly. | Idempotency test: call twice with different `base_path`s and assert the recorded path is the second. | TODO |
| REPORTING_CORE-25 | P3 | `output.py:9-14` | Module docstring carries a date stamp ("back-compat with pre-2026-05-08 default") and non-ASCII em dashes plus a set-membership symbol while the rest of the cluster is deliberately ASCII-only. | Drop the date; normalise to ASCII. | Hygiene scanner: flag date stamps or non-ASCII characters in `src/` comments/docstrings. | TODO |
| REPORTING_CORE-26 | P3 | `catalog.py:1`; `_diagnostics_dispatch_extra.py:476` | Finding IDs leak into shipped docstrings: `(INV-58)` and `(R-3 / INV-11)`. | Delete the identifiers. | Hygiene scanner: grep `src/` for tracker-ID shapes in comments/docstrings. | TODO |
| REPORTING_CORE-27 | P3 | `_diagnostics_dispatch_extra.py:136-139,372-375,505-509,543-548` | Four comment blocks narrate refactor history ("was previously ambiguous...", "the old one-sided ...", "the prior reversal...", "that was the bug just fixed above"). | Rewrite as the WHY that still matters; drop the before/after story. | Hygiene scanner: flag comments containing "was previously", "the old", "the prior", "used to", "just fixed". | TODO |
| REPORTING_CORE-28 | P3 | `_benchmarks/bench_slice_finder_codes_layout_iter71.py:1`, `bench_prediction_stability_spearman_iter83.py:1`, `_e2e_slice_finder_ab_iter71.py:1` | Iteration markers (`iter71`, `iter83`) are baked into filenames and docstring first lines — process metadata in shipped code. | Rename to what each measures; drop the marker from docstrings. | Hygiene scanner over `src/`: flag filenames/docstrings matching iteration/wave/phase markers. | TODO |
| REPORTING_CORE-29 | P3 | `_benchmarks/_e2e_slice_finder_ab_iter71.py:27` | The "OLD" baseline loads `git show HEAD:.../slice_finder.py`; once the optimisation is committed HEAD IS the new code, so the bench silently becomes a self-comparison reporting ~1.00x while still passing its identity assertions. | Pin the baseline to the pre-optimisation SHA (or vendor a copy) and assert the baseline source differs from the in-tree module before timing. | Bench-validity test: any A/B bench loading a baseline from git must assert the baseline text differs from current in-tree source. | TODO |
| REPORTING_CORE-30 | P3 | `diagnostics_dispatch.py:515-516` | `_interaction_cost_within_budget` runs a full `predict` purely to time it and discards the result; the composer immediately re-predicts the same rows. The probe also fails open, so a model whose predict raises is still charged the full budget. | Reuse the probe's predictions (or shrink the probe); treat a probe exception as "skip" when the model could not predict at all. | Wasted-work test: spy on `model.predict` and assert the probe's row batch is not predicted twice. | TODO |

## Narrative detail

**REPORTING_CORE-1 (polars column cap no-op).** `_select_feature_columns` reads `if hasattr(frame, "loc") and not isinstance(frame, np.ndarray): try: return frame[keep], keep`. Polars DataFrames expose neither `.loc` nor `.iloc` — confirmed directly (`hasattr(pl.DataFrame({'a':[1]}), 'loc')` prints `False`) — so the capping branch is skipped and the function returns the FULL name list. The agent then ran the real helper on both formats: a 5-column polars frame with `cap=2` returned shape `(3, 5)` and all five names, while the identical pandas frame returned `(3, 2)` and two names. The consequence matches exactly what the constant exists to prevent ("a pathological thousands-of-columns engineered frame would blow the dense matrix up"). Ironically the row-subset helper directly above (`_subset_rows`) DOES handle polars via its `frame[idx]` fallback, so the polars path reaches the builders — just uncapped.

**REPORTING_CORE-2 (numpy scalar metrics dropped).** The filter is `isinstance(v, (int, float)) and not isinstance(v, bool)`. On this platform `isinstance(np.float32(1), float)` is `False` and `isinstance(np.int64(1), int)` is `False` (only `np.float64` inherits from `float`) — verified, printing `False True False False` for float32/float64/int64/bool_. Running the real function on `{'roc_auc': np.float32(0.9), 'n': np.int64(5), 'brier': np.float64(0.1), 'ok': 0.5}` returned `{'brier': 0.1, 'ok': 0.5}`: the float32 AUC and int64 count vanished with no warning. This feeds the multi-model leaderboard, so a metric bar is simply missing for some models depending on which library produced the number (LightGBM/XGBoost commonly emit float32). P2 rather than P1 because no value is shown WRONG — it is omitted — but the omission is invisible to the reader.

**REPORTING_CORE-3 (not-requested recorded as failed).** `_save_figure` opens with `if "png" not in (plot_outputs or "").lower(): return False`, and both call sites do `ok = _save_figure(...); _record(charts, name, ok)`. Under `plotly[html]`, `charts["failed"]` therefore gains `decile_table` (and `pdp_2d`) on every report though nothing failed. The module docstring advertises this accounting as the thing "a run can assert chart presence" with, so a suite-level alert on non-empty `failed` fires spuriously for every plotly-only user. The same code path is why those two diagnostics have no plotly equivalent at all — they are matplotlib `Figure` builders, not `FigureSpec` builders — which is the backend-parity half of finding 6.

**REPORTING_CORE-4 (eight charts missing from the combined report).** The consumer was traced first: `training/reporting/_reporting_diagnostics.py:462-466` reads `paths = metrics.get("charts", {}).get("paths", [])` and passes it as `chart_paths` to `build_combined_html_report`. The only writer is `_record_path` (`diagnostics_dispatch.py:392-395`) plus two training-side appends. Grepping the dispatch modules shows `_record_path` paired with `_record` for pdp_ice, pdp_2d, interaction_strength, weak_slices, decision_curve, calibration_drift, target_acf, cusum_drift, all three shap variants, model_comparison, decile_table, model_card, prediction_stability, split_comparison, separability, category_discriminability, class_structure and risk_coverage — but NOT for `weak_segments`, `error_bias`, `segments`, `psi_heatmap`, `residual_vs_time`, `metric_over_time`, `adversarial`, or `target_dist`. The training tree was also grepped for those base suffixes with no compensating append found. For a data scientist this is the difference between "the drift and weak-segment analysis was run" and "the drift and weak-segment analysis is visible".

**REPORTING_CORE-5 (subset-then-cap ordering).** Both `render_split_error_diagnostics:199` and `render_slice_finder_diagnostic:617` read `_select_feature_columns(_subset_rows(df, idx), feature_names, DIAG_MAX_FEATURES)`. `_subset_rows` fancy-indexes the frame, which for pandas `.iloc[idx]` and polars `frame[idx]` alike materialises a NEW frame containing every column; only afterwards are columns narrowed to 200 — at the documented cap that is 100,000 rows x all columns where 100,000 x 200 would do, in the module whose docstring says "Every entry point pulls column views (never a whole-frame copy)". Column selection is the cheap operation for both formats, so swapping the calls preserves behaviour exactly. Related: line 211 calls `_select_feature_columns(df, feature_names, DIAG_MAX_FEATURES)[0]` a second time for the `n <= DIAG_ROW_CAP` worst-K branch, recomputing what line 199 already computed.

**REPORTING_CORE-7 (catalogue drift).** `catalog.py`'s docstring explains the anti-drift design for panel TOKENS: they come from each chart module's `ALLOWED_*_PANEL_TOKENS` frozenset "so a token added to a chart without a description here surfaces immediately". `_STANDALONE_DIAGNOSTICS` is by contrast a hand-written list of 13 tuples with no such backstop; cross-checking it against the `render_*_diagnostic` functions defined in the two dispatch modules leaves roughly sixteen shipped diagnostics unlisted. `tests/reporting/test_catalog.py::test_standalone_diagnostics_listed_and_described` only checks that listed entries have descriptions — there is no check in the other direction, which is exactly why the drift went unnoticed.

**REPORTING_CORE-8 (combined report loses its own structure).** `report_html.build_combined_report` supports sections, per-section descriptions and per-chart captions, and `tests/reporting/test_report_html.py` covers all three. The only production caller passes `("charts", os.path.basename(p), png)` for every entry and no `section_descriptions`, so the page has a single section named "charts" and headings that are raw artifact filenames. The capability is built, tested, then not used by the one path that matters. The fix is cheap because `catalog.standalone_diagnostics()` already holds one-line descriptions keyed by exactly the suffixes these paths end with — a second reason to fix finding 7.

**REPORTING_CORE-11 (emphasis base rate from count_nonzero).** `select_binary_emphasis_panels` computes `n_pos = int(np.count_nonzero(finite))` then bails when `n_pos == 0 or n_pos == n`. For `{-1,+1}` or `{1,2}` encodings every value is nonzero, so `n_pos == n` and the function returns `requested_panels` unchanged — the data-aware emphasis silently does nothing, with no log line, on precisely the encodings where a reader would still want PR-over-ROC under skew. A quiet no-op rather than a wrong chart (hence P3), but the caller cannot distinguish "balanced, no reorder" from "encoding not understood".

**REPORTING_CORE-16 (collapsed sections vs anchor navigation).** `is_open = " open" if idx == 0 else ""` renders sections 2..N closed, while the sidebar emits `href="#{anchor}"` links into panels inside them. Flagging the alternative reading explicitly: recent Chrome and Firefox auto-expand a `<details>` on fragment navigation, so on an up-to-date browser the links work. What fails regardless of browser is printing and PDF export (collapsed content does not print) and older browsers — hence P3 with a print-stylesheet fix rather than a functional-bug claim.

**How the OSS-hygiene findings were confirmed.** Each was read in the source rather than inferred: `output.py:11` literally contains the pre-2026-05-08 back-compat note; `catalog.py:1` contains `(INV-58)`; `_diagnostics_dispatch_extra.py:476` contains `(R-3 / INV-11)`; the four narration blocks are quoted verbatim in their rows. On `--` in prose: it appears throughout this cluster as the ASCII stand-in for an em dash, consistent with `test_catalog.py::test_descriptions_are_ascii` enforcing ASCII output, so it was treated as intentional house convention — flagging it would mean rewriting most comments in the package and would contradict its own ASCII-only tests.

## Dimensions with zero findings (stated explicitly)

- **Import-cycle / monolith-split hazards:** none. The `diagnostics_dispatch` / `_diagnostics_dispatch_extra` mutual import is handled with lazy function delegates plus a module-level `__getattr__` for the two shared constants, documented at both ends. Every parent-owned name the carved sibling references (`_record`, `_record_path`, `_save_spec`, `_save_figure`, `_column_names`, `DIAG_ROW_CAP`, `DIAG_MAX_FEATURES`) resolves through a delegate or `__getattr__`, so the "moved function NameErrors at first call" class is closed here.
- **Bare `except:` / mutable default arguments / `== None`:** none in this cluster; every catch names `Exception` or narrower and is paired with `logger.exception` or an explicitly justified `logger.debug`.
- **DSL parsing correctness (`output.py`):** validates emptiness, malformed clauses, unknown backends, duplicate backends, empty format lists, duplicate formats and per-backend format compatibility with a clear `ValueError` each; no input was found that parses to a wrong `PlotOutputSpec` rather than raising.
- **`_risk_coverage_diagnostic.py`:** no findings — same guard/record/except contract as its peers, correctly clamps to `m = min(len(yt), ys.shape[0])`, records both metric scalars, calls `_record_path` on success.

---

**Counts:** 14 files reviewed, 3,398 LOC. Findings: P0 = 0, P1 = 0, P2 = 8, P3 = 22 (30 total).
