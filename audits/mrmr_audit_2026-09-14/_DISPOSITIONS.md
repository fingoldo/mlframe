# mrmr_audit_2026-09-14 — implementation dispositions

Every one of the 201 findings gets an explicit disposition. No finding is dropped, deprioritised away, or
silently omitted — including P3/Low, per the owner's instruction to implement all of them.

**Disposition vocabulary**
- **RESOLVED** — fixed in source, covered by a regression test, verified.
- **DOC** — the code is correct; the defect was in a comment/docstring/name, now corrected.
- **FUTURE** — genuinely deferred, with the reason and the concrete next action recorded.
- **REJECTED** — examined and found not to be a defect, with the reason recorded.

Batches are committed as they close (the repo's convention: ~20 fixes per commit, each with its test).

---

## Wave 1 — P0 (5/5 closed)

| ID | Summary | Disposition | Evidence |
|----|---------|-------------|----------|
| NUM-1 | FE collinearity-dedup variance from raw power sums (k=2) in all three backends; large-offset column → `nan` → "not a duplicate" → collinear columns survive | **RESOLVED** | `_row_center_finite` added; centring applied once in the dispatcher so all three backends inherit it. Live repro before/after: at offset 1e6 the uncentred form returned \|r\|=2.12 (impossible for a correlation), at 1e8+ it returned `nan`; centred returns 1.0 in every regime. `test_orth_dedup_large_offset_stability.py` (12 tests, incl. a seed-parametrised teeth test in the arithmetically-guaranteed regime and a negative control) |
| NUM-12 | maxT floor substituted with `0.0` on failure — the literal "gate off" sentinel, indistinguishable from the deliberate narrow-pool no-op | **RESOLVED** | Permissive degradation kept (a failed floor must not kill the fit) but recorded as `_pair_maxt_floor_failed_` state and the log now names what is disabled. Flag reset per call so a stale `True` cannot leak across FE steps. `test_pair_maxt_floor_failure_is_distinguishable.py` |
| NUM-13 | the circuit breaker making NUM-12 recoverable had its own trip failure swallowed to `debug` + bare `pass` | **RESOLVED** | Escalated to `logger.warning` naming the exception type and the consequence; dead `pass` removed. Test drives the real branch (resident path forced on → faults → breaker trip raises) rather than asserting on source text |
| PERIPHERY-1 | `transform()` identity fast-path compared only a column COUNT and returned before the column-name check, so a reordered/different same-width named frame came back unchanged, every column mis-mapped | **RESOLVED** | `_identity_fastpath_is_safe` requires exact name-and-order equality for named frames (pandas + polars); ndarrays stay eligible since their width is validated upstream. `test_mrmr_transform_identity_fastpath_identity.py` (6 tests) |
| RO-1 | RelaxMRMR-3D's two conditional-MI terms had transposed argument slots: computed `I(X;Y\|Z)` where the formula needs `I(X;Z\|Y)`; the joint helper structurally could not express the intended quantity | **RESOLVED** | `_joint_cmi_xy_given_zw_njit` replaced by correctly-oriented `_joint_mi_x_zw_given_y_njit`; both call sites fixed. Verified against analytically-known cases (XOR: pair determines X → ln2; X==Y → 0; single member alone → 0). The 7 pre-existing tests pass on BOTH orientations, which is why the new test asserts the quantity, not the sign. `test_relaxmrmr_3d_cmi_orientation.py` (5 tests, incl. an explicit teeth test that the two orientations differ) |

### Notes carried from the verification pass

- **NUM-12 severity nuance (recorded, not dropped):** the outer handler already logged at `warning` with
  `exc_info=True`, so the failure was never invisible in a log — only in the data. The finding stands on the
  data-indistinguishability, and that is what the fix addresses.
- **Test-construction lesson worth keeping:** `pooled_pair_permutation_null_joint_mi_floor` takes DISCRETISED
  codes, a per-column `nbins` vector, and a per-ROW `classes_y`. Passing the natural-looking
  `classes_y=[0,1]` (distinct classes) plus a scalar `nbins` indexes the njit kernel out of bounds and
  **segfaults rather than raising**. Filed as a follow-up candidate below.

---

## New findings raised during implementation

| ID | Summary | Disposition |
|----|---------|-------------|
| IMPL-1 | `pooled_pair_permutation_null_joint_mi_floor` segfaults (no bounds validation) when `classes_y` is passed as the distinct-class list instead of per-row codes, or `nbins` as a scalar | **RESOLVED** — `_validate_pair_floor_inputs` runs at the Python boundary before any kernel call and raises a `ValueError` naming the expected shape: `classes_y` one code per row, `nbins` one entry per column, same-length 1-D pair vectors with indices in range, target codes inside `freqs_y`'s class count. Measured pre-fix, each case in a child process: the distinct-classes `classes_y` crashed the interpreter (exit 0xC0000005, access violation); the other four (scalar `nbins`, short `nbins`, a pair index past the last column, an out-of-range target code) did not crash but returned a floor with NO error, i.e. silently computed from out-of-bounds reads - worse than the crash. Post-fix all five raise; a well-formed control still returns a floor, and the maxT floor suites pass (25). `test_pair_maxt_floor_rejects_malformed_inputs.py` |
| IMPL-3 | Same closure as NUM-14 (`_pairs_setup._prewarp_generalises`) also returns `True` ("accept") when an operand's length differs from the validation frame's `_pw_n`, with the comment "subsample edge -> don't block" | **RESOLVED** — the question the FUTURE entry left open was answered from the code: under subsampling the pair search narrows `X` itself to the sample rows (`_pairs_core.py:622`), `_extval_raw_col` reads operands from that `X`, and the prewarp target is cut to the same rows, so operand and target lengths agree on every correct path; the "subsample edge" does not exist. A mismatch can only be misalignment, so the closure now checks lengths FIRST and rejects with a throttled warning, before the held-out CV switch (which previously accepted without comparing). `test_prewarp_misaligned_operands_rejected.py`: pre-fix both the CV-on and CV-off cases registered warps for operands 10 rows short of the target; post-fix neither does, and an aligned control still registers. The NUM-14 and other prewarp tests pass (10) |
| IMPL-4 | RO-4's truncation trap is not confined to the ten hybrid-orth copies the RO-4 agent counted in its own cluster. A tree-wide sweep for an int64 cast inside a `<= 32` distinct-count window found **19 live sites**: the 10 hybrid-orth copies, plus `_fe_stage_cascade_early_a.py` 145 / 266 / 547 / 621, `_fe_stage_cascade_mid_a.py` 76 / 143, `_fe_stage_temporal_agg.py` 69, `_fit_impl_core.py` 691, and — outside the MRMR cluster entirely — `_grouped_quantile_fe.py:346` (`generate_target_aware_group_bins`). All carry both halves of the defect: `<= 32` float levels cast with `astype(int64)`, and a `qcut`-failure fallback that is the same truncating cast, logged at debug | **RESOLVED** — all 19 sites route through `_y_encoding.encode_y_for_classif_mi`, whose module docstring describes this exact trap and which uses the same 32-level / 10-bin thresholds; every site feeds the discretised target straight into an MI scorer (traced at the call boundary). `test_no_truncating_y_discretisation.py` is an AST gate over the tree: it found all 19 before the edit and finds 0 after. Selection measured against pre-fix baselines, 5 target shapes, 2 fits each, all deterministic: integral-float and continuous targets unchanged; half-step and unit-interval targets change, as intended, since they were the truncated ones. An integer target with non-dense labels also moved by one engineered column, but that is not this change: legacy casts at all 19 sites together still select it, and a clean copy of HEAD without these edits selects the identical set. See the int_nondense note below |
| IMPL-5 | `_y_encoding.encode_y_for_classif_mi` — the shared primitive the RO-4 fix routes through — handles a `pd.qcut` failure by falling back to plain densify at **debug**. For a genuinely continuous target that densifies into roughly n singleton classes, which its own module docstring names as signal-destroying | **RESOLVED** — throttled WARNING (key `encode_y_qcut_failed`) naming the exception type; throttled because the helper runs per pair x modulus x permutation inside the FE scans. The fallback itself is kept so the fit still runs. `test_y_encoding_qcut_failure_is_warned.py`: failed pre-fix (debug only), passes post-fix, with a no-warning control on a healthy qcut |
| IMPL-6 | FEC-2 has a sibling instance the finding text does not name. `_fe_stage_cascade_early_b.py` snapshots `_engineered_seen_l34` once, BEFORE the count-encoding block; count encoding then appends its outputs to `hybrid_orth_features_` (`:245`). Frequency encoding's auto-detect therefore still sees count-encoded columns — integer counts, very plausibly inside its 5-500 distinct-value window — and can build a frequency recipe on one, which is a nested recipe that raises `KeyError` at `transform()` replay | **RESOLVED** — frequency encoding now excludes `_engineered_seen_l34 | count_encoding_features_` on both branches. Applied as a separate stage after FEC-2 so it has its own failing run: with FEC-2 alone, frequency still auto-scoped `k__count`, `x0__count`, `x1__count`, `x2__count`, `x3__count`; with both, `test_fe_auto_scope_excludes_engineered_columns.py` passes |
| IMPL-2 | Composite-target discovery diagnostics were written FLAT into the run root as `<data_dir>/composite_<target>_<suffix>.png`, so a multi-target run dumped every composite chart beside the data artifacts instead of grouping them per target under `charts/`, unlike every other chart type | **RESOLVED** — owner-reported, outside the 9-agent scope. Now `<data_dir>/charts/<slugify(target)>/composite_discovery/{mi_gain,tdist_<spec>}.png`, using the same `slugify(target_name)` `_setup_model_directories` uses, so the composite dir is a sibling of that target's per-model chart dirs. Filenames drop the now-redundant target prefix. The existing test pinned the OLD flat layout and was re-framed to the new one (per the repo's "re-frame the stale test" rule), plus two new assertions: nothing composite-shaped may remain loose in the run root, and every path recorded in `metadata["composite_target_diagnostic_charts"]` must actually exist |
| IMPL-12 | (found while benchmarking FE_STEP-2) The strict-GPU single-column quantile binner `_sync_free_qbin_codes` built each edge as `xs[lo]*(1-frac) + xs[hi]*frac`. When the two order statistics tie that rounds one ULP off the value (-0.9 -> -0.9000000000000001), and `searchsorted(side="right")` then moved the tied value's whole mass into the neighbouring bin. On correlated blocks with rounded columns, 2 to 5 of every 32 to 128 columns binned differently from the host `_quantile_bin`, by 62 to 6695 rows. That binner feeds the strict-GPU CMI redundancy gate and the escalation pool, so those candidates were scored on a partition the host path never produces. The same form was in the device group-MI edges of `_binned_numeric_agg_fe` and the permutation-null floor quantile of `_fe_cmi_perm_null_gpu` | **RESOLVED** — all three use numpy's `lo + (hi - lo) * frac`, exact on ties. A probe over 192 bench-shaped columns: 10 mismatching before, 0 after; the batched `batched_quantile_bin_gpu` already matched the host on every column | `test_sync_free_qbin_matches_host_on_tied_columns.py`: the three tied-block cases fail pre-fix (62-6695 rows per bad column, run on a HEAD worktree with CUDA visible) and pass post-fix; the five degenerate-shape cases pass both ways. 278 GPU-parity / CMI-gate / perm-null / binned-agg neighbours pass |
| IMPL-13 | (found while verifying batch 36-40, outside the MRMR cluster) `test_biz_val_real_data_noise_injection_value_and_honesty[default-ShapProxiedFS-breast_cancer]` fails deterministically: real-column recall per seed [0.73, 0.40] against a 0.42 floor. Identical numbers at 14 commits spanning months of ShapProxiedFS changes (a1c5f515a-era through 59faed526), locally (sklearn 1.8.0, shap 0.52.0, numpy 2.3.5, Python 3.14) and on Linux CI (deep-nightly run 34815664135 on d028142f2). Seed 1 is not a subset swap: it selects 14 columns (12 real, 2 dup, 0 noise) against seed 0's 35, and every real column it keeps seed 0 keeps too | **FUTURE** — not caused by code in the commit range, and not by the two library versions most likely to move it: an isolated venv sharing every other package gives the identical selection under scikit-learn 1.7.2 and under shap 0.50.0 (both seeds, same columns). pyutilz is imported by ShapProxiedFS only for `tqdmu`. Next action: trace ShapProxiedFS's size-selection stage on seed 1 (where 14 columns are cut from the candidate pool) against seed 0, then decide between a code fix and a floor re-measured on a multi-seed run. The same deep-nightly run fails about ten more `shap_proxied/` biz_val tests, which belong to the same investigation |
| IMPL-14 | (process) Recent commits have no completed CI result. Every per-push `CI` run on master since 2026-09-15 03:31Z ended `cancelled`, each superseded by the next push under `concurrency: cancel-in-progress: true`; the last runs that finished (2026-09-09..11) failed in setup on all ten macOS shards (`tbb>=2021` has no `macosx_26_0_arm64` wheel, introduced by 7ebde971c and already reverted by 1220dbe91). `slow`-marked tests are deselected per push and run only in `deep-nightly`, whose 2026-09-14 run completed about 40 failing tests, including four MRMR ones: `test_fe_path_perf_identity` (non-FE path lost all eight `gate_mask__*` columns), `order2_maxt_floor` (fixture covers 2 genuine pairs, needs 3, floor on and off), `gate_vs_elementary` (1800s timeout) and `fast_search` (fast path not 20% faster) | **FUTURE** — the four MRMR failures are being reproduced at the current HEAD with targeted runs before any attribution; each gets its own disposition once reproduced. The push cadence is the owner's instruction, so the CI gap is reported rather than changed |

---

## Wave 2 — P1 (8 of 25 closed)

| ID | Summary | Disposition | Evidence |
|----|---------|-------------|----------|
| CORE-1 | `sample_weight` / `groups` forwarded into `_stability_outer_fit`, which never referenced `fit_kwargs`; the wrapper's weight resampling runs AFTER that branch returns, so every non-classic `stability_selection_method` selected on unweighted data | **RESOLVED** | The classic path converts weights to a row resample via `_maybe_resample_for_sample_weight` (`_mrmr_class.py:3662`), but `_stability_outer_fit` returns at `:3534` — before it. Fix applies the same resampling inside the method, so it cannot be bypassed by another caller and each replicate inherits the weighting with no inner plumbing. Uniform/absent weights return inputs unchanged, keeping the unweighted path byte-identical. Measured: a split-signal fixture where half A makes `a` predictive and half B makes `b` predictive returned `['a','b']` for BOTH opposite weightings pre-fix; post-fix returns `['a']` and `['b']` respectively |
| FIT_IMPL-1 | stability-replay mask built by comparing `feature_names_in_` indices against cols-space indices | **RESOLVED** | Translated through NAMES, the only space both sides agree on; engineered selections (absent from `feature_names_in_` entirely) now included via `_engineered_features_`. Measured pre-fix on a leading-categorical fixture: report claimed `['n2','n3']` for a support of `['n1','n2']`. `test_stability_replay_mask_index_space.py` (3 tests, incl. one asserting the injected target column is never marked selected) |
| FIT_IMPL-3 | raw-signal-retention re-add did not filter on `_allowed_raw_idx`, re-admitting a column the caller excluded via `factors_names_to_use` | **RESOLVED** | One-line guard matching every sibling re-add pass; `_allowed_raw_idx` was already a parameter of the function and used 233 lines earlier |
| NUM-4 | Surrogate seeder: when every permuted-y run failed it set `perm_std = 0.0` and divided by `perm_std + 1e-9`, so any positive gap became z ~ 1e9 and the PAIR self-gate passed unconditionally -- the opposite of the "nominal spread so the z-gate still applies" its comment claimed | **RESOLVED** | No null, or a degenerate one (std <= 1e-12), now yields an undefined z (`-inf`) that fails the gate, recorded as `info["self_gate_null_available"]`, with a warning naming which case. The analytic baseline is kept for REPORTING only. Verified pre-fix by temp-revert: no-null gave **z = 424999999.99**, degenerate-null **z = 449999999.99**, both gates passed; 4 of 6 tests failed while both healthy-null controls still passed (so the controls are not vacuous). `test_surrogate_seeder_self_gate_needs_a_null.py` (6 tests); the pre-existing `test_biz_value_gbm_seeder_order3_floor.py` (9) still passes |
| NUM-14 | Prewarp held-out validation returned `True` ("accept the warp") on ANY exception, at debug -- promoting an unvalidated operand exactly when the check could not run | **RESOLVED** | Now rejects (`False` is already an ordinary outcome of this closure) with a throttled warning. Verified pre-fix by temp-revert: an ALS fit raising only on the train slice left `spec_by_var = {0: {...}, 1: {...}}` registered; post-fix it is empty. A healthy-validation control proves the setup still admits genuine synergy, so the rejection test cannot pass by the setup simply registering nothing. `test_prewarp_validation_failure_rejects.py` (2 tests). The sibling length-mismatch accept in the same closure is tracked separately as IMPL-3 |
| PERIPHERY-2 | `transform()` deep-copied the WHOLE caller frame on every call (`_X_for_recipes.copy()`) purely to own an append surface, though the replay loop only reads single named columns -- 100+ GB frames | **RESOLVED** | The audit's proposed `copy(deep=False)` was deliberately NOT used: this repo already shipped a bug where a shallow copy shared the source BlockManager and a later setitem leaked the new column back onto the caller's frame (`_build_disc_df_for_target`'s note). Instead the deep copy is kept but narrowed to `_recipe_reachable_columns`: the transitive closure of every recipe's `src_names` (following `nested_parent_a/b`) plus any `extra` string that names a real column, as a deliberate superset so a kind reaching outside `src_names` stays covered. Falls back to the full frame on an empty match or any error. `test_transform_recipe_frame_is_narrowed.py` (8 tests, incl. an explicit caller-frame-not-mutated guarantee and a cyclic-chain termination guard); the existing replay-parity suites (float32 provenance, GPU-fallback dtype, orth-cluster basis — 20 tests) pass unchanged |
| FEC-4 | `scan_engineered_duplicates` allocated its rank buffer as a K x n float64 `np.empty` at function entry, sized by EVERY appended column before any was inspected, though rows are only ever written for fully-finite admitted columns | **RESOLVED** | Lazy allocation, geometric growth, capped by `_RANK_BUF_MAX_BYTES` (= the FE seam's existing 2 GB `FE_EAGER_MATERIALIZE_MAX_BYTES`, no new constant). A column that cannot fit gets no buffer row and falls to the per-pair path, which already compares every kept column without one, so keep/drop is unchanged. Pre-fix proven WITHOUT touching the working tree (the HEAD source was exec'd as a standalone module under the real package): a `(29, 3000)` allocation up front for 29 appended columns -- 3.2 GB at the module's cited ~200 columns / n=2M. Keep/drop verified identical to the unbounded run across 2 preference policies x 4 budgets (0, 1, 2, 5 rows) in a scratch prototype before the edit, then pinned by `test_eng_dedup_rank_buffer_byte_budget.py` (13 tests; allocations observed through a numpy proxy installed only as the module's own `np`, so pandas/numpy scratch arrays inside `rank`/`corrcoef` are not misread as buffer rows). The existing `test_eng_dedup_batch_corr_masked_kernel.py` algorithm-vs-reference parity suite passes unchanged |
| PERF-3 | Three FE permutation-null loops (`_integer_lattice_fe`, `_conditional_gate_fe`, `_pairwise_modular_resident`) inverted each `rng.permutation(n)` with `np.argsort` -- O(n log n), 12 times per candidate -- where an O(n) scatter is exactly equal | **RESOLVED** | Bit-identity proven before editing (805 (n, seed) cases, 0 mismatches, matching int64 dtype). Measured warm, paired and interleaved on a quiet machine: the op is **16.5-18.6x** faster; the full 12-permutation build, which also pays the unchanged `rng.permutation`, is **1.89x at n=10k rising to 2.91x at n=5M**, with the built matrices asserted bit-identical at every n. RNG draw order untouched. The integer-lattice site also now takes a positional float64 view (`ascontiguousarray(...).ravel()`), matching its conditional-gate sibling; its caller passes `np.asarray` columns, so this is bit-identical there. `test_perm_inverse_scatter_is_bit_identical.py` pins the invariant plus BOTH host call sites end to end with `==` against an argsort reference (the resident modular path needs a GPU, so its exact 2-D inverse-index form is pinned directly). Bench kept in-package at `_benchmarks/bench_perm_inverse_scatter.py` |

## Waves 3+ — remaining P1 / P2 / P3

In progress; each finding is appended here as it closes.

| ID | Summary | Disposition | Evidence |
|----|---------|-------------|----------|
| PERIPHERY-5 | `_MRMR_LAST_X_HASH_CACHE` single-slot memo: the write took `_MRMR_IDENTITY_FP_LOCK` but the read (compare key, then fetch value) was unlocked, relying on value-before-key write order to make a torn read a miss | **RESOLVED** — read and write moved under one dedicated `RLock` (not the identity-cache lock, so no caller already holding that lock can deadlock) | The write-order argument is wrong: a reader that has ALREADY matched its key can have a writer publish another frame's digest before it fetches, whatever order the writer uses. Forced deterministically with real threads (reader paused between compare and fetch while a writer hashes frame B): pre-fix returns B's digest for A. `test_single_slot_memo_reads_are_atomic.py` |
| IMPL-7 | The same torn read in the two sibling memos PERIPHERY-5 did not name: `_pipeline_cache._LAST_KEY_CACHE` (no lock at all on either side) and `training/utils._PD_VIEW_LAST_CACHE`, which holds the pandas VIEW itself, so a wrong hit returns another frame's DATA, not just a wrong hash | **RESOLVED** — each gets its own `RLock` around every read, write, the `weakref.finalize` invalidator and `clear_pandas_view_cache`. Re-entrant because that finalizer can be run by the GC on a thread already inside a critical section | Same forced interleave, pre-fix: pipeline memo returned B's cache key for A; pandas-view memo returned frame B's values for frame A. Post-fix all 3 pass, together with the existing memo suites (cache-safety global state x2, pandas-view clear, recycled-id X hash, write order): 29 passed. The existing `test_single_slot_memo_write_order.py` asserted the order as the safety mechanism; its docstring now states the real mechanism is the lock (DOC) and keeps the ordering as defence in depth |
| PERIPHERY-3 | Cross-target identity cache keyed on X (+ optional y) but no constructor param, so a permissive config's identity result let a different config skip its fit and select every column | **RESOLVED** — the key now also folds `_hashable_params_signature`, as the in-object signature and `_FIT_CACHE` layers already did. Built in ONE place, `_mrmr_fingerprints._mrmr_identity_cache_key`, used by the read, the store and the self-refit check | `test_identity_cache_does_not_cross_configs.py`: the entry is stored by a real fit, then a fit with a different `quantization_nbins` took the shortcut pre-fix and runs for real post-fix; a same-config control still hits. Four existing tests had seeded the cache with a hand-built X-only key; three failed on the new key and one (`test_identity_shortcut_ndarray`) had silently stopped reaching the hit path it is named for, since default `include_y=True` already made that key miss. All now seed through the helper: 32 identity-cache tests pass |
| FE_STEP-1 | After a loky pool failure, the batched-CPU pair-MI retry prefilled `cached_MIs` with keys in dispatcher position order instead of the canonical `(low, high)` the primary prefill uses, so one pair could be cached under both orientations | **RESOLVED** — both prefill sites now go through one `_prefill_cached_pair_mis`, which canonicalises every key | The existing loky-failure test used `set(range(10))`, whose iteration is ascending, so it could never see this. The new fixture forces a non-ascending set (id 40 inserted before the ids it collides with) and asserts that precondition. Pre-fix: `retry wrote non-canonical pair keys: [(40, 8), (40, 9)]`. Post-fix it passes with the three loky neighbours: 7 passed. `test_pair_mi_retry_prefills_canonical_keys.py` |
| FEC-1 | Wavelet and rankgauss auto-scoped their sources by excluding only the `hybrid_orth_features_` ledger; MI-greedy runs earlier and never merges into it, so an MI-greedy column could become a recipe source that transform() cannot replay | **RESOLVED** — both scope to `_raw_input_cols_pre_fe`, as the nine sibling stages in the same file already do | `test_fe_auto_scope_excludes_engineered_columns.py`: an MI-greedy stub guarantees an engineered column; spies record the scope each family is handed. Pre-fix: `wavelet was handed the MI-greedy column 'mig_fake_sq'`. Post-fix both scopes are raw-only |
| FEC-2 | Count and frequency encoding dropped engineered columns on the explicit-config branch only; the auto-detect branch got the whole augmented frame | **RESOLVED** — both auto branches filter `_engineered_seen_l34`, matching the explicit branch and `_resolve_missing_cols` | Same test file, with `auto_detect_te_cols` stubbed to admit every column. Pre-fix: count and frequency both auto-scoped `mig_fake_sq`. The local MI gate is off in the fixture because on this small frame it drops every count column, and frequency can only leak count outputs that exist |
| FIT_IMPL-2 | The SIS front gate narrowed X before the fit, so `n_features_in_` / `feature_names_in_` / `support_` described the survivor subset; `fit(X_ndarray).transform(X_ndarray)` raised on the caller's own matrix, and ndarray names were subset positions, so `support_` pointed at the wrong input columns. The gate is on by default | **RESOLVED** — the gate captures the input's names and width; ndarray survivors enter the fit named by their INPUT position (`feature_<orig>`), and right after `_fit_impl` `_remap_sis_fit_to_input_space` maps support, names and width back to input space through names, before the identity-cache store or provenance read them. Per fit, so a cache replay of survivor-space state is remapped too | `test_sis_screen_fit_transform_input_space.py` (the survivor count is patched to 12; the real floor is at least 1000 columns). Pre-fix: `n_features_in_ is 12 ... not 60`, and `support_ [8, 10] misses the signal columns (41, 53)`; post-fix transform round-trips, support holds 41 and 53, and transform output equals `X[:, support_]`. The fixture's first version kept all 60 columns (floor) and its tests passed vacuously; a precondition test caught it. The SIS gate had no test anywhere before this |
| CORE-2 | `_stability_outer_fit` set 6 fitted attributes where the other terminal paths set the full diagnostic roster, and stored ndarray names as pandas' integer labels | **RESOLVED** — `_seed_default_fitted_attrs` holds the roster once, called by both the stability path and the identity shortcut; the stability path names ndarray columns `feature_<i>` (as the classic path does), stores them as strings, and sets `_feature_names_in_synthesized_` | Found while writing the test: the integer labels did not just break the attribute contract, they crashed every stability replicate (IMPL-9), so every ndarray stability fit silently fell back to classic. The fixture now asserts `stability_freq_` exists, because a classic fallback passes the name assertions on its own. Pre-fix: `the stability outer loop did not complete`; post-fix names are strings, caller `input_features` are honoured, and the roster is complete. 65 neighbouring identity-cache / stability / feature-name tests pass |
| CORE-3 | An explicit `fit()` never reset the `partial_fit` buffer, so `partial_fit(A); fit(B); partial_fit(C)` refit on A + C | **RESOLVED** — `partial_fit` marks its own internal fits; any other `fit` clears the stream buffers | `test_mrmr_state_contracts.py`: pre-fix the buffer held 110 rows (A + C) instead of 50; a control pins that consecutive `partial_fit` calls still accumulate |
| CORE-4 | `_fit_sample_weight_` (an n-length array read by nothing after fit) rode in every pickle | **RESOLVED** — popped in `__getstate__` | Pre-fix the key was in the pickled state; post-fix no row-length arrays remain and a reloaded model transforms identically |
| CORE-5 | `_stability_replay_state_` (subsample x candidates of bin codes, hundreds of MB at production shape) rode in every pickle | **RESOLVED** — dropped above `MLFRAME_MRMR_PICKLE_REPLAY_STATE_MAX_MB` (default 64) with a warning; the report on the reloaded model says it was not persisted | Pre-fix no warning and the reloaded report gave an unexplained empty result; a control pins that a small state still survives pickling |
| CORE-7 | `HybridOrthConfig` had no field for `fe_hybrid_orth_elasticnet_l1_ratio` | **RESOLVED** — field added (0..1), plus a test that every flat `fe_hybrid_orth_*` param has a config field | Pre-fix `HybridOrthConfig(elasticnet_l1_ratio=...)` raised `ValidationError` (extra forbidden) and the coverage test listed it as unmapped |
| CORE-8 | Eight string fields on the config objects were bare `str`, so a typo passed construction; six hybrid-orth string params had no allow-list at all, and their consumers run inside a per-family try/except that turns the consumer's own ValueError into a warning, silently dropping that FE family for the fit | **RESOLVED** — `Literal` types on `DCDConfig.dcd_distance`/`dcd_swap_method` and the hybrid-orth `hsic_kernel`, `ensemble_aggregator`, `ensemble_scorers` (elements), `meta_force_scorer`, `default_scorer`, `basis`, `cluster_basis_aggregator`; the flat params get allow-lists checked at fit start | `test_string_param_allow_lists.py`: pre-fix 25 of 26 failed (no ValidationError on config typos, no ValueError at fit on flat typos, constants absent); post-fix all pass. A sync test pins each allow-list to what its consumer accepts. That test caught `basis` also accepting the four extra bases (`fourier`, `rbf`, `sigmoid`, `pade`) merged into `_POLY_BASES`, which the first allow-list would have wrongly rejected. The meta force scorer stays case-insensitive, as its consumer lower-cases it |
| FEC-8 | Six shipped recipe kinds (`mahalanobis_density`, `random_fourier`, `lof_score`, `conditional_quantile_rank`, `sir_direction`, `ordinal_pattern_te`) were missing from `_RECIPE_KIND_TO_ORIGIN`, so their surviving columns reported as `engineered_unknown` | **RESOLVED** — mapped to `extra_fe`, matching the other Layer-104 families | `test_fe_provenance_kind_registry.py` AST-scans every `kind=` literal under `filters/` and requires a mapping, which generalises the class. Pre-fix it listed exactly the six. A second test pins every mapped origin to a public label, and allows only the deliberate kinds to map to `engineered_unknown` |
| FEC-9 | `get_unlabeled_recipe_kinds` keyed recipes by RAW name but looked them up with the SIMPLIFIED names the provenance frame holds | **RESOLVED** — the key is simplified, mirroring `compute_fe_provenance` | Pre-fix the self-audit returned `{'<no-recipe>': 1}` for a recipe whose name canonicalises; post-fix it returns the recipe's kind |
| FEC-11 | `set(...)` rebuilt per column inside list-comprehension conditions | **RESOLVED** — hoisted at the four `mid_a` sites plus two more found by grep in `_fit_impl_core.py` (passthrough narrowing, unified second-pass gate) | No behaviour change, so no dedicated test (as the finding proposes); neighbouring tests pass |
| FIT_IMPL-4 | `_mrmr_sis_apply` read `getattr(self, "n_features", None)`, an attribute no constructor defines, so `k_target` was always `None` | **RESOLVED** — the dead read is removed and `k_target=None` is passed explicitly, with the reason: MRMR has no requested feature count, since the greedy stops on its own information criterion. `sis_screen` keeps `k_target` for direct callers | No behaviour change (the value was already always `None`); no test, since there is no behaviour to pin |
| FIT_IMPL-5 | The empty-screen rescue tokenised the recipe object's `repr`, so a raw column that only shared a token with `kind`/`extra` metadata was wrongly excluded from the rescue | **RESOLVED** — extracted to `surviving_recipe_operands`, reading `_engineered_recipe_name(r)` | Staged proof in `test_rescue_operand_exclusion.py`: the helper extracted with the old `str(r)` semantics returned `{'a','b'}` for a recipe `log(a)` with `extra={'note':'b'}`; the fix returns `{'a'}`. A control covers `__`-suffixed legs and legacy string entries |
| FEC-10 | `_greedy_rank_for_name` re-simplified the whole predictor log for every provenance name, O(n_names x n_predictors) parser calls | **RESOLVED** — `_greedy_rank_index` builds a first-match `{simplified name: index}` once per report; `_greedy_rank_for_name` stays as the scanning form | `test_fe_provenance_rosters_and_rank_lookup.py`: pre-fix 3561 simplifier calls at 80 names (291 at 20, quadratic); post-fix linear, and every `support_rank` equals the scanning reference |
| FEC-16 | `_ROSTER_ATTR_TO_ORIGIN` covered 13 of the 39 FE rosters, so a surviving column held only in an unmapped roster (no recipe object) never reached `fe_provenance_` | **RESOLVED** — all 24 missing rosters mapped to existing public labels, inserted before the two catch-all rosters so the ordering invariant holds. `_adaptive_fourier_features_` / `_hinge_features_` are exempt by design | Pre-fix: the coverage test listed 24 rosters, and a wavelet column held only in `wavelet_features_` was absent from the provenance frame; post-fix it is reported as `wavelet_basis`. 37 provenance / diagnostic-completeness tests pass |
| REGR-1 | (new, a regression introduced by this audit's own PERIPHERY-2 fix, found by a neighbour test run) `_recipe_reachable_columns` read a recipe's `extra` only when it was a plain `dict`. Recipe objects hold it in another mapping type, so columns read outside `src_names` were left out of the private replay frame, and every temporal lag/expanding recipe raised `KeyError: 'tcol'` at `transform()` | **RESOLVED** — any mapping with `items` is walked, and column names inside list/tuple/set values (`entity_cols`, group keys) are followed | `test_transform_reachable_columns_follow_recipe_extra.py`: pre-fix the narrowed frame was `['entity','x0']` (no `tcol`), and a list-valued group key was missed; post-fix both pass. The two failing `test_fe_target_aware_leak_contract.py` temporal cases pass again, with 45 transform / replay-parity / temporal neighbours. Bisect confirmed the same commit (be24033fe) as the cause of `test_biz_value_rescue_improves_f2_downstream_r2` falling to R2 0.84/0.88: the transform-side column loss hit both rescue paths, and that test passes again after this fix |
| PERIPHERY-7 | Named-frame `transform()` accepted columns the fit never saw with no signal (only missing selected columns raised) | **RESOLVED** — `_warn_unseen_transform_columns` issues a throttled WARNING naming up to 8 unseen columns, on the pandas and polars paths; output unchanged | `test_transform_warns_on_unseen_columns.py`: pre-fix no warning; post-fix the extra column is named and the output equals the transform of the fit columns. A control pins no warning on the fit column set |
| PERIPHERY-9 | The stability report's `np.argpartition` sorts NaN to the high end, so a NaN-relevance candidate counted as selected on every resample | **RESOLVED** — NaN relevance ranks as `-inf` before partitioning | `test_stability_report_nan_relevance.py`: pre-fix `{'degenerate': 1.0, 'signal': 0.0}`; post-fix `degenerate` 0.0 and `signal` 1.0 |
| PERIPHERY-11 | `_content_array_signature`'s outermost fallback returned `('uncached', id(arr))`, which can match a different object at a recycled address | **RESOLVED** — `uuid4().hex`, matching the sibling fallbacks; log messages corrected | `test_fit_cache_key_fallbacks.py`: pre-fix two calls on one unsignable object returned the identical id key; post-fix they differ |
| PERIPHERY-12 | The in-object skip signature hashed `get_params(deep=True)`, the process-wide `_FIT_CACHE` key `deep=False` | **RESOLVED** — both use `deep=True` | No current MRMR constructor parameter is a `get_params`-bearing object, so the two forms produce the same key today and a behavioural test would be vacuous. The change removes the asymmetry before such a parameter appears. 72 fingerprint / identity-cache / stability-report neighbours pass |
| RO-5 | Three held-out R2 gates built their baseline design by iterating a `set` of column names, so design column order depended on `PYTHONHASHSEED` | **RESOLVED** — all three iterate the selection order (`dict.fromkeys(cols[i] for i in selected_vars)`); the sets stay for membership | No reproducer test. Column order changes a least-squares R2 only by FP reordering, so a verdict flips only right at the gate threshold, which a hash-seed subprocess test could not hit deterministically. The change makes the order a function of the selection. 95 protection / friend-graph / DCD neighbours pass |
| RO-7 | The post-selection DCD "numeric only" guard tested `data`'s dtype, but `data` holds integer bin codes for every column, so string/categorical raws passed into the PC1/Pearson combiner | **RESOLVED** — `raw_column_is_numeric` (`_friend_graph_and_redundancy/_raw_dtype.py`) reads the raw pandas/polars column dtype; the code matrix decides only without a named raw frame | Staged proof in `test_dcd_raw_numeric_guard.py`: the helper extracted with the old semantics called a string column numeric; post-fix it does not. Controls cover a float column and the no-frame fallback |
| RO-8 | Sibling passes read the target as `_y_np`, `y.to_numpy()` or `y.values` | **RESOLVED** — every site in `_group2/3/4` uses the fit-wide `_y_np` | For pandas and ndarray targets the three are the same values, so the change is uniformity, not behaviour. The audit's broader point stands: the continuous-target R2 probes do not run for a string-labelled classification target, at any of these sites. That is DOC: a held-out linear R2 on nominal labels has no meaning, and those passes already log the skip at debug |
| RO-9 | `_group2` gated the orth-basis protection on `"_heldout_incr_over_selected" in locals()`, and picked the relevance floor via `in dir()` on a name that is always a parameter | **RESOLVED** — an explicit `_heldout_gate_ready` flag, set where the closure is defined, replaces the `locals()` test; the `dir()` conditional and its unreachable fallback are removed | Behaviour-preserving (the flag is True exactly when the name was in `locals()`). The existing `test_orth_basis_protection_without_hinge.py` contract passes |
| RO-13 | The masked-raw rescue's continuous-y re-binning swallowed every exception at debug and fell back to the coarser screening classes, which can change the keep-rule verdict | **RESOLVED** — the fallback is kept but logged at WARNING with the exception, naming the substitution | Visibility-only; the fallback value is unchanged. It reads the fit-wide `_y_np` since RO-8. No dedicated test (the handler fires only on a coercion failure). 59 neighbours pass |
| RO-14 | The raw monotone-twin drop read a `cached_MIs` miss as 0.0, so a raw re-added by a rescue pass (never scored by the screen) always lost to a screen-selected twin | **RESOLVED** — `monotone_twin_to_drop` (`_friend_graph_and_redundancy/_monotone_twin.py`) skips the drop when either relevance is unknown | Staged proof in `test_monotone_twin_unknown_relevance.py`: the helper extracted with the old semantics dropped candidate 7 with only its twin scored; post-fix it returns None. Controls pin lower-MI drop and tie-keeps-earlier |
| RO-16 | The raw floor-drop protection stacked the full-height (n, p) design, then took train and val boolean copies | **RESOLVED** — `heldout_r2_scorer` accepts a column sequence and slices train/val rows per column, so the full-height design never exists, and the caller releases its column list. The RO-3 scorer extraction had already stopped the closure from holding a full matrix | Bit-identical: the same rows in the same order. `test_raw_protect_r2_rank_deficient_base.py` and neighbours pass |
| PERF-4 | `_su_normalize_relevance` re-factorised the fit-constant target for every candidate although the caller already has `freqs_y` | **RESOLVED** — optional `freqs_y` parameter, passed at both call sites; recomputed only when absent | `test_su_normalize_reuses_target_freqs.py`: a bit-identical SU value with 1 `merge_vars` call instead of 2. Pre-fix the parameter did not exist (TypeError) |
| FEC-17 | `try/except NameError` control flow in the early cascade; its fallback set `_extra_basis_scorer_ok = True`, re-enabling the Fourier extra basis under an alternate default scorer whenever the polynomial stage failed | **RESOLVED** (in d5314ee65) — `_default_scorer` / `_y_for_hybrid` are bound once before the polynomial stage's `try:`, and both `except NameError` handlers are gone | Verified against the tree: no `except NameError` remains in `_fe_stage_cascade_early_a.py`, and the binding comment documents the old inverted fallback |
| FEC-18 | Artifact SU outside [0, 1] was silently clamped, turning an MI/entropy inconsistency into a plausible 0 or 1 | **RESOLVED** — the clamp stays, but a WARNING names the column, the pre-clamp SU, the cached MI and H(X)+H(y) | `test_artifacts_and_repr_contracts.py`: pre-fix no warning for a cached MI of 5.0; post-fix the column is named and an in-range column stays silent |
| FEC-19 | Artifacts used only `target_indices[0]` while the schema advertised "SU(X_j, y)", with nothing recorded for multi-target fits | **RESOLVED** — `target_index_used` key (added to `_ARTIFACT_SCHEMA`), and a WARNING when more than one target is passed | Pre-fix `KeyError: 'target_index_used'`; post-fix the key is present and a two-target call warns |
| CORE-13 | Five process-lifetime class caches in `_mrmr_class_config.py` read-then-write with no lock; the fresh-instance miss constructs a full `MRMR()` | **RESOLVED** — one module `RLock` with double-checked population for all five; their entries were removed from `_unlocked_module_cache_baseline.json` | `test_class_caches_populate_once_under_threads.py`: pre-fix 8 threads on a cold cache constructed `MRMR()` 8 times; post-fix once, all threads get the same dict. `test_no_unlocked_module_cache.py` passes |
| CORE-14 | `_engineered_names_cache_` (a get_feature_names_out memo) was pickled | **RESOLVED** — popped in `__getstate__`; it rebuilds on first use | Pre-fix the key was in `__getstate__()`; post-fix absent, with identical names after a round-trip |
| CORE-15 | `__repr__` spliced `n_workers=` into sklearn's output even when `N_CHAR_MAX` truncation had elided the parameter list | **RESOLVED** — the annotation is skipped when the repr contains `...` | Pre-fix a 40-char repr ended `...n_workers=1)`; post-fix it carries no `n_workers=`, and the full repr still does |
| CORE-16 | `and is_first is False` in partial_fit's weight-length check was always true (the `is_first` path returned earlier) | **RESOLVED** — clause removed | Dead-condition removal, no behaviour change; the partial_fit neighbours pass |
| FEC-20 | Missingness count/pattern families silently take their column scope from `fe_missingness_indicator_cols` | **DOC** — the shared scope is intended (one missingness column set for all three families) and is now stated in the constructor's parameter comment; no per-family overrides were added | Documentation only |
| FEC-21 | `_gate_raw_operands_` / `_gate_col_src_vars_` were seeded only inside `early_b` and mutated in `mid_a` with no default | **RESOLVED** — `seed_empty_fe_rosters` seeds both, and `mid_a` guards both before updating | `test_fe_cascade_contracts_p3.py`: pre-fix a seeded multioutput-style estimator had no `_gate_raw_operands_`; post-fix both are present and empty |
| FEC-22 | Module docstrings and comments carry split-history / dated process markers; `mid_a`/`mid_b` are near the LOC band | **RESOLVED** (module docstrings) / **FUTURE** (the rest) — the split-history text in the `mid_a`, `mid_b` and temporal-agg module docstrings is replaced by what each module is. The dozens of dated in-body markers across `early_a`/`mid_a`/`mid_b` and the `mid_b` Layer-104 carve are left for a dedicated comment-hygiene and carve pass: a mechanical rewrite across 3k LOC is its own reviewable change | Docstrings only, no behaviour change |
| FEC-23 | `validate_artifact_dict` ignored `schema_version`, the `bins`/`nbins_per_feature` pairing and `n_samples_at_fit`, which the schema tells consumers to check | **RESOLVED** — rejects a newer `schema_version` and half-present bins; takes an optional `n_samples` compared against `n_samples_at_fit` | Pre-fix a version-2 dict and bins-without-nbins both validated, and `n_samples` did not exist; post-fix all rejected / checked |
| FEC-24 | The temporal-agg stage re-implemented `_fe_family_on` inline | **RESOLVED** — `_fit_impl_core` passes `_fe_family_on` and the stage calls it; the inline form stays only as the fallback when called without the helper | Behaviour-preserving today. Folding a budget check into `_fe_family_on` (FEC-6) now reaches this stage too. 152 missingness / conditional-gate / temporal / artifact / roster neighbours pass (the run was repeated with CUDA hidden after the first segfaulted natively inside a conditional-gate biz test; the CUDA-hidden run passed) |
| FEC-25 | The batched and per-pair dedup degeneracy floors are written differently (`saa <= 1e-24*n` vs `std <= 1e-12`) | **DOC** — verified numerically equivalent (`saa/n <= 1e-24` is `std <= 1e-12`); no live divergence. Recorded so a future edit to one floor checks the other | No change |
| FE_STEP-15 | `apply_interaction_information_routing` read `fe_ii_routing_enable` with default True, contradicting the ctor default False and the bench-rejected verdict | **RESOLVED** — default False | `test_ii_routing_default_off.py`: pre-fix a bare namespace with 40 pairs ran the routing (returned a new dict); post-fix the input dict is returned untouched and the null floor is never computed |
| FE_STEP-16 | `_gate_cache[raw_vars_pair]` was an unguarded lookup relying on the pre-pass and main loop keeping identical filter chains | **RESOLVED** — `.get` with a throttled WARNING naming the pair, and the pair is skipped | Defensive: no drift exists today, so no reproducer. The FE-step neighbours pass |
| FE_STEP-17 | `_cached_single_corr` did not memoize its `None` result, re-resolving the operand per call on the no-continuous-y path | **RESOLVED** — the call-constant `_yc_cont_ is None` test short-circuits first, and a shape-mismatch `None` is cached | Cache behaviour only; values unchanged |
| FE_STEP-18 | `run_cluster_aggregate_emission` promoted indices with O(n^2) list membership; the `_step_score` sibling already hoisted a set | **RESOLVED** — `_sv_set` hoisted | Behaviour-preserving |
| PERIPHERY-17 | `selection_stability_report` silently coerced `n_boot <= 0` to 1 | **RESOLVED** — raises `ValueError` for `n_boot < 1` | `test_periphery_p3_contracts.py`: pre-fix 0 and -3 did not raise |
| PERIPHERY-18 | The y fingerprint cast a string/categorical target to float, failed, warned and disabled the identity cache on every fit | **RESOLVED** — a non-numeric sample hashes its labels; the unique-token fallback stays for genuinely unhandleable input | Pre-fix two calls on the same string target returned different `yfp_uncacheable_*` tokens with a warning each; post-fix the fingerprint is stable, silent, and differs for different labels |
| PERIPHERY-21 | `_replay_recipe_survival` silently skipped a recipe whose stored codes disagreed with y's length | **RESOLVED** — WARNING naming the recipe and both lengths; the plain no-state skip stays silent | Pre-fix no warning for 150 stored codes vs 200 rows; post-fix named |
| PERF-11 | `use_su_normalization()` was called twice in one argument list per candidate | **RESOLVED** — read once via a walrus and reused for `use_mm` (keyword arguments evaluate left to right) | Identical values; a smaller per-candidate cost. The other two getters stay per call: they are per-round constants, but hoisting them into the driver is a wider signature change than the measured cost justifies (unmeasured, and the audit expects a small win) |
| PERF-10 | `np.ones` mask reallocated per outer iteration of the monotone-twin scan | **FUTURE** — hoisting is trivial but `_group4` was just changed by RO-14; folded into the next `_group4` touch. The allocation is bounded by the final selected-raw count | No change in this batch |
| PERF-12 | `_all_pairs_precomputed` walked all C(k,2) pairs as a membership check | **RESOLVED** (by FE_STEP-6) — the scan runs only when the loky pool is otherwise eligible, which removes it from the default serial path | See FE_STEP-6 |
| PERF-13 | The loky pair-MI pool branch is unreachable at the production default `fe_npermutations=3` | **DOC** — correctly unreachable: the pool measured 0.03-0.38x at that budget. Kept per REJECTED != DELETED; the gate constant's comment already records the measurement | No change |
| NUM-10 | The bandit early-stop margin buried a 1e-9 variance floor as an additive term inside the sqrt | **RESOLVED** — explicit `max(p(1-p)/n, _MIN_BANDIT_VARIANCE)` with a named module constant | Numerically the same floor (variance at least 1e-9); conservative direction unchanged |
| NUM-11 | `u / (abs(v) + 1e-6)` in candidate generation | **DOC** — by design: a protected-ratio feature definition, replayed identically at transform, not a statistical denominator. Recorded so it is not re-flagged | No change |
| TESTGAP-2 | No polars-vs-pandas selection-equivalence test | **RESOLVED** — `test_mrmr_polars_pandas_selection_parity.py`: identical support, names and gains without FE; identical names and engineered recipe order with FE on (the Arrow-bridge path); identical transform values | All pass on the current tree (coverage addition; no divergence found) |
| TESTGAP-3 | float32 parity pinned for y but not X | **RESOLVED** — `test_mrmr_dtype_parity.py`: float32 and float64 X select the same support on a well-separated synthetic, with gains within float32 precision | Passes (coverage addition) |
| TESTGAP-5 | No unfitted / reordered / extra / missing-column transform contract tests | **RESOLVED** — `test_mrmr_transform_column_contract.py`: unfitted `get_feature_names_out` raises NotFittedError; reversed columns realign by name byte-exactly; an extra column leaves output identical; a dropped selected column raises naming it | All pass. The pinned contract is realign-by-name (read from the transform code), not raise |
| TESTGAP-27 | No test pins selection as independent of the global RNG seed | **RESOLVED** — `test_mrmr_determinism.py`: three global numpy/random seeds give identical support and byte-identical gains; two fresh fits with the same seed agree byte-for-byte with the fit cache cleared | Both pass (FE on, so the subsample and permutation paths are exercised) |
| TESTGAP-4 | partial_fit covered for streaming mechanics but not for frozen parameters at transform | **RESOLVED** — `test_partial_fit_transform_frozen.py`: after two batches, `transform(X1[10:40])` equals `transform(X1)[10:40]` byte-exactly and `n_features_` is unchanged | Passes (coverage addition) |
| TESTGAP-7 | No single-feature fit test | **RESOLVED** — `TestDegenerateShapes::test_single_feature_frame_selects_it`: one column fits, is selected, has one gain, and transforms to (n, 1) | Passes |
| TESTGAP-8 | No n_samples < n_bins test | **RESOLVED** — `test_n_samples_below_n_bins_fits`: 12 rows with `quantization_nbins=32` fit and select a feature | Passes |
| TESTGAP-10 | No all-NaN target test | **RESOLVED** — `test_all_nan_target_raises_before_any_fe_work`: raises ValueError and leaves no engineered recipes | Passes |
| TESTGAP-15 | `support_.size >= 0` could not fail | **RESOLVED** — every stability method must select at least one feature | Passes for classic / cluster / complementary_pairs |
| TESTGAP-16 | `gains.size == len(support) or gains.size >= 1` swallowed the alignment invariant | **RESOLVED** — asserts `mrmr_gains_.size == n_features_ == len(get_feature_names_out())` | Passes |
| TESTGAP-17 | `cm is None or isinstance(cm, dict)` passed when DCD silently did not run | **RESOLVED** — `cluster_members_` must be a dict on this `dcd_enable=True` fixture, and `cluster_diagnostics` is indexed directly so a renamed key fails | Passes |
| TESTGAP-18 | `dcd_ is None or dcd_.get('n_swaps', 0) == 0` defaulted a missing key to passing | **RESOLVED** (narrowed) — on the DCD-disabled paths `dcd_` may legitimately be unset, so that half is kept, but when set, `dcd_['n_swaps']` is indexed directly | Passes. Asserting `dcd_ is not None` on a `dcd_enable=False` fixture, as the audit proposed, would pin a value the disabled path does not promise |
| TESTGAP-19 | `n_su_calls >= 0` on a count | **RESOLVED** — `>= 1` at both sites (the fixtures run DCD on collinear / tau='auto' frames) | Passes |
| TESTGAP-20 | `test_skip_retraining_parameter_exists` only checked `hasattr` | **RESOLVED** — renamed `test_skip_retraining_param_survives_fit_unmutated`; asserts the constructor param is unchanged after fit and a feature is selected | Passes |
| TESTGAP-21 | `isinstance(roster, list)` passed on empty lists with all FE enabled | **RESOLVED** (narrowed) — rosters must be lists and not all six may be empty. Requiring every roster non-empty would fail for families the kitchen-sink fixture legitimately does not engage, and no measured allow-set exists yet | Passes |
| TESTGAP-22 | The Layer 29 reference check asserted a file path exists, which breaks on a move and passes for a broken module | **RESOLVED** — the check imports the relocated module and requires it to define test functions or classes | Passes (the first rewrite checked only `test_` functions and failed because the module's tests live in `Test*` classes; widened accordingly) |
| NUM-7 | Stability-cluster correlation z-scored with `std + 1e-12`, deflating a tiny-scale column's correlations so it never clustered with its rescaled duplicate | **RESOLVED** — degenerate columns are guarded with a magnitude-relative tolerance (`std > 32*eps*max|x|`), not padded; they get a zero axis and stay singletons. Same for the p-cap marginal correlation | `test_stability_cluster_tiny_scale_columns.py`: pre-fix `x` and `x*1e-13` landed in different clusters (`[0,1,2]`); post-fix they share one. A constant-column control stays a singleton |
| NUM-8 | Near-constant gates used absolute floors (`SS <= 1e-24*n`, `var <= 1e-18`, `std <= 1e-12`), declaring genuinely tiny-scale columns constant | **RESOLVED** (CPU + resident twin) / **FUTURE** (rest) — both `_pairs_core` \|corr\| kernels, the four njit usability-pool kernels and their GPU resident twin, and the two raw-value checks in the dedup scan now compare the spread with `32*eps*max|value|`. Rank vectors keep the absolute floor (ranks are >= 1, and this matches the batched kernel). The `_orth_dedup.py` numpy/njit/cupy trio and the `_gpu_resident_basis.py` corr kernel are not changed yet: they need a coordinated host/GPU change and GPU validation | `test_abs_corr_kernels_scale_relative_degeneracy.py`: pre-fix a 1e-13-scaled perfect correlate returned \|corr\| 0.0 in both kernels (expected 0.9987); post-fix identical to the unscaled column, and constant columns at any offset still return 0.0. `test_usability_pool_resident_parity.py` passes with CUDA visible, so host and device agree on the new mask |
| FIT_IMPL-9 | The SIS redundancy dedup gathered a full-height float64 (n, m) survivor matrix that the int16-sized survivor RAM cap did not budget | **RESOLVED** — the correlation gather uses a deterministic strided subset of at most `_SIS_DEDUP_MAX_ROWS` (200k) rows; the 0.92 \|corr\| decision is settled well before that | `test_sis_dedup_gather_row_cap.py`: pre-fix the gather took all 2000 rows against a 300-row cap; post-fix <= 300, and the near-duplicate pair still collapses |
| RO-17 | `len(selected_vars) >= 0` was vacuously true on the n-way synergy seeding gate | **RESOLVED** — conjunct removed, with a comment that an empty support is a valid input (the screen seeds operands the greedy never selected) | Behaviour-preserving (the condition was always true); 83 redundancy/protection neighbours pass |
| RO-18 | `_rr_raw_is_relevant_given_engineered` never used its `_raw_idx` parameter | **RESOLVED** — parameter dropped at the definition and the one call site | No behaviour change |
| RO-19 | One log gate read `self.verbose` while its siblings read the threaded `verbose` | **RESOLVED** — uses `verbose` | Log-gating only |
| RO-20 | The masked-raw rescue re-filtered its re-add list against a set the loop had already excluded | **RESOLVED** — extends from `_pcr_readd` directly, so the logged count is exactly what was added | Behaviour-preserving (the filter could never remove anything) |
| RO-21 | `_mt_relevance`'s exception handler was unreachable | **RESOLVED** (by RO-14) — `_mt_relevance` was replaced by `monotone_twin_to_drop`, which has no such handler and makes the unknown-relevance case explicit | See RO-14 |
| RO-22 | A debug message named `classes_y` as the thing that failed when it was the fallback | **RESOLVED** — message says y coercion failed and classes_y is the fallback | Message only |
| RO-23 | `set(self.feature_names_in_)` rebuilt inside the kept-column loop and a comprehension | **RESOLVED** — both reuse `_raw_names_for_redund` | Behaviour-preserving |
| RO-24 | `_mi_x_pair_njit` computed its log argument as a division by a division | **RESOLVED** — `log(v * n / (Px * Pz))`, the sibling kernel's form | Algebraically identical; FP-level difference only, in a RelaxMRMR score |
| RO-25 | A negative `alpha` silently disabled the signed interaction term | **RESOLVED** — `relax_mrmr_score` raises ValueError for `alpha < 0` | `test_redundancy_orth_p3_contracts.py`: on unmodified HEAD `alpha=-1` did not raise; post-fix it raises, and alpha 0 / 1 still score |
| RO-26 | The hybrid-orth package docstring justified skipping the AST name audit with a subset-by-construction argument its sibling package documents as false | **RESOLVED** — replaced with the actual gate (a per-submodule AST audit of Load-context names), citing the shared-import incident | Docstring only |
| RO-27 | The standalone-gate prune warning logged the exception type but not its message | **RESOLVED** — logs both | Log only |
| RO-28 | Tree-rescue ranking broke importance ties toward the highest column index and filled NaN with 0.0 before LightGBM | **RESOLVED** — `_rank_by_importance` uses a stable descending sort (ties keep column order); the fast path leaves NaN for LightGBM's native missing-value handling | `test_tree_rescue_ties_broken_by_column_order` pins the ranking; 83 neighbours incl. the tree-rescue tests pass |
| NUM-40 | Content-free `logger.debug("suppressed: %s", e)` handlers at 13 named sites | **RESOLVED** (named sites) / **FUTURE** (tail) — none of the 13 named sites in `_mrmr_sis_screen.py`, `_mrmr_fingerprints.py`, `_mrmr_fe_provenance.py`, `_group3.py`, `_step_score.py`, `_adaptive_nbins.py` or `info_theory/_cmi_cuda.py` still carries the pattern (cleaned in an earlier wave). 91 similar handlers remain in 45 other files under `filters/` (163 across `mlframe/`), outside this audit's scope; they need a per-site message naming the function and key, as their own pass | Verified by grep against the tree |
| TESTGAP-13 | `pytest.xfail` on a perf assertion | **DOC** — hardware-variance concession: the hard >= 0.7x gate stays live and only a slow-standalone-host sensor is downgraded. Not a hidden defect | No change |
| TESTGAP-14 | CROSS-2 / CROSS-4 cleaned-portion had no ratchet against regrowth | **RESOLVED** (already present) — `tests/test_meta/test_no_audit_metadata_in_comments.py` with `_audit_metadata_baseline.json` ratchets finding-ID, audit-dir, wave, loop-iter and date-stamp markers in comments, failing on new occurrences | Verified against the tree |
| FE_STEP-6 | `_all_pairs_precomputed` walked all C(k,2) pairs with two dict lookups each, unconditionally, even when `n_jobs<=1` or the permutation floor had already chosen the serial sweep | **RESOLVED** — `_use_serial_pair_sweep` takes the scan as a callable and runs it only when the pool is otherwise eligible | Staged proof in `test_fe_step_class_codes_and_serial_sweep.py`: the helper extracted with eager semantics ran the scan at `n_jobs=1`; post-fix it runs 0 times there. A control pins that the scan still decides when the pool is eligible |
| FE_STEP-9 | The CMI gate and the escalation twin built class codes differently: the escalation path int-cast before `np.unique` (merging fractional labels) and skipped `.ravel()` (numpy>=2 returns `(n,1)` codes for a column target) | **RESOLVED** — one `dense_class_codes` helper (`_mrmr_fe_step/_step_class_codes.py`) used by both sites | Staged proof: with the old escalation semantics `[0.2,0.7,1.4,0.2]` gave `[0,0,1,0]` and an `(n,1)` target gave `(3,1)` codes; post-fix `[0,1,2,0]` and `(3,)` |
| FE_STEP-12 | The escalation marginal MI omitted the `kx` hint its gate sibling passes, forcing a blocking device scalar sync per candidate | **RESOLVED** — `kx=int(self.quantization_nbins)` forwarded | Perf-only; the value is unchanged, since `kx` is the bin count the codes were built with. No dedicated test; the escalation neighbours pass |
| FIT_IMPL-6 | A failed SIS scoring block kept its pre-allocated zero scores, which fuse_scores ranks at the bottom, so up to `chunk_width` columns silently left the fit behind one throttled warning | **RESOLVED** — failed blocks are tracked, their columns are unioned into the survivors, and an unthrottled summary warning names the block ranges | `test_sis_block_failure_keeps_columns.py`: with both channels failing on the second block, pre-fix survivors were `[0,1,22,23,25]` (the planted signal column 15 dropped, no summary); post-fix column 15 survives and the summary is logged. A control pins exact top-k on a healthy screen. (A first version that failed only the MI channel had no teeth: the propensity channel still ranked the signal. It was replaced) |
| FIT_IMPL-8 | The group-aware final demotion removed names from `_engineered_features_` after roster reconciliation had already run, so public rosters kept listing demoted columns | **RESOLVED** — reconciliation factored into `reconcile_fe_rosters` (`_fe_roster_attrs.py`) and re-run after the demotion. It now covers every roster in `FE_ROSTER_ATTRS` except `_hinge_features_`; the old inline loop listed 28, missing `conditional_gate`, `row_argmax`, `integer_lattice`, `pairwise_modular` and the Layer-104 rosters | `test_group_demotion_prunes_public_rosters.py`: pre-fix `hybrid_orth_features_` still listed `a*b__He1_He1` after it was demoted out of the output; post-fix no roster names a column absent from `get_feature_names_out()` |
| CORE-9 | With `partial_fit_window` < `partial_fit_min_recompute` the buffer can never reach the threshold, so the refit came late, on window rows only | **RESOLVED** — the effective threshold is clamped to the window for that call, with a warning; the constructor param is not mutated | Pre-fix no refit happened after 140 rows with window=50 and no warning. 114 neighbouring partial_fit / pickle / setstate / config tests pass |
| NUM-2 | The GPU basis-routing \|corr\| RawKernel formed the variance from raw power sums (`n*sum(v^2) - (sum v)^2`), cancelling to noise when a column's mean is far above its spread; the failure value `-1.0` makes the host argmax skip the column as if constant | **RESOLVED** — the kernel now accumulates centred sums: per-column means are computed on device, the target is centred once, and `sum((v-mean)^2)` / `sum((v-mean)*yc)` are accumulated directly (the CPU twins' approach). The degenerate test matches the cupy fallback chain's | `test_gpu_abs_corr_kernel_large_offset.py` (runs on the dev GPU). Pre-fix a column correlating 0.498 with y came back 0.496 at offset 1e7 and **0.003** at offset 1e10; post-fix all match numpy to 1e-6, and a constant column at offset 1e7 still gets the -1.0 skip sentinel |
| NUM-3 | Rényi-α MI built its RBF Gram matrix with `\|a\|^2 + \|b\|^2 - 2a.b` on raw columns: rounding error scales with the offset squared against a spread-sized bandwidth | **RESOLVED** — columns are centred before the Gram trick (exactly invariant for an RBF kernel) | `test_renyi_alpha_shift_invariance.py`: pre-fix MI of a pair carrying 1.25 bits became **0.0** after shifting x by 1e4 or 1e8, and CMI collapsed likewise; post-fix both are shift-invariant to 1e-9. The existing Rényi estimator tests pass |
| PERF-1 | Batch `_pairwise_modular_fe._perm_null_hi`'s 12 host MI calls into one, as the resident path and both sibling modules do | **REJECTED (bench-attempt-rejected)** — implemented and measured NOT bit-identical: rank binning splits TIED values by row order, and the residues `c mod k` are tied by construction. The loop keeps the residue in its original order and permutes y, so the residue bins identically each time; each re-ordered column in a batch splits its ties differently. On 5000-row residues (k=3/7, two seeds) the null band moved by up to 31% (e.g. 0.00298 vs 0.00392). The audit rated this risk LOW, citing an invariance that holds for true MI but not for this estimator. The loop stays, with a `bench-attempt-rejected` note at the call site | `test_pairwise_modular_perm_null_host_batched.py` pins the host null to the per-permutation loop with `==` on tied residues, so a re-batching fails loudly. This led to IMPL-11 |
| NUM-22 | Prewarp clean-form demotion: `_config_corr` returned `-1.0` on a measurement failure, and a failed CLEAN side made `_clean_corr >= 0.0` False, so the distorted prewarp form was kept unchallenged | **RESOLVED** — the decision is `_should_demote_prewarp`; a measurement failure returns `None` (throttled warning) and an unmeasured side resolves toward the clean form, while `-1.0` keeps meaning "clean config genuinely unrecoverable" | Staged: extraction with the old semantics raised `TypeError` on a `None` side; the fix passes the full decision table (unmeasured clean -> demote, unmeasured prewarp -> demote, >5% better prewarp -> keep, within 5% -> demote, unrecoverable clean -> keep) |
| NUM-23 | `_target_entropy_nats` returned 0.0 on failure, disabling the MI-ceiling sanity bound at debug | **RESOLVED** — same return value (0.0 already means "bound off" at its consumer), now a warning naming the exception and `y.dtype` | Pre-fix no warning on an unorderable object target |
| NUM-24 | II routing substituted 0.0 for a missing marginal MI, inflating `ii` and routing the pair as synergy | **RESOLVED** — a pair with a missing marginal is kept unrouted (neither promoted nor demoted) and one warning counts such pairs | Pre-fix the fixture pair routed `synergy` from a cache miss; post-fix it is kept without a route |
| NUM-25 | `_gpu_upload_fits` returned `True` when `memGetInfo` or the cushion probe failed | **RESOLVED** — `ImportError` (no cupy, no GPU dispatch possible) stays permissive; any other probe failure refuses the upload with a warning naming the requested GB | Pre-fix `True` with a raising `memGetInfo`; post-fix `False` + warning (runs on the dev GPU) |
| NUM-26 | CMI CUDA gate: VRAM probe failures swallowed at debug, falling through toward CUDA | **RESOLVED (visibility)** — measured pre-fix the gate already returned `False`: after the first probe failed, the shared `fe_gpu_has_vram_cushion` re-probed and fails closed itself. So the fail-open path the audit described was not reachable; the defect was the silent handlers, which now warn and return `False` directly | Pre-fix the return-value assertion passed and only the warning assertion failed |
| NUM-27 | The RAM headroom guard was disabled at debug when `psutil.virtual_memory()` failed | **RESOLVED (visibility)** — kept permissive (blocking every fit on a sandboxed host would be worse), now a throttled warning naming the unchecked working set | Pre-fix no warning |
| NUM-28 | The inf scan and constant-y check skipped themselves on any exception; the constant-y check sorted y with `np.unique`, so an unorderable target (e.g. all `None`) skipped it | **RESOLVED** — uniqueness is counted with `pd.unique` (hashing, no sort), and both handlers now warn with the consequence | Pre-fix an all-`None` target did not raise; post-fix it raises the documented `only 1 unique value` |
| NUM-29 | Multi-output detection returned `False` (single-target) when `np.asarray(y)` failed | **RESOLVED** — raises `TypeError` naming the container type instead of guessing a fit path | Pre-fix `DID NOT RAISE` |
| NUM-30 | A failed ctor-defaults lookup made the fast-search profile a silent no-op | **RESOLVED (visibility)** — warning stating the profile was NOT applied | Pre-fix no warning |
| NUM-31 | One usability batch failure disabled the admission route for every pair, at debug | **RESOLVED (visibility)** — warning with the exception and the number of affected pairs | Pre-fix no warning. Across NUM-22..31 the new test file plus 20 neighbouring interaction-information / degenerate-input / multi-output / usability files: 173 passed, 1 skipped; the one failure on that run was this test file's own substring typo, fixed and re-run |
| NUM-32 | Two operand-significance guards (emit-both probe in `_assign_support`, subsumption discriminator in `_assign_support_tail`) retained operands on estimator failure at debug; the tail's OUTER handler had already been raised to warning, the inner one not | **RESOLVED (visibility)** — polarity kept (retain, never silently drop genuine signal); both go through `call_or_default`, which logs the fallback at WARNING, throttled per site | Shared helper `_fallback_probe.call_or_default` is tested directly (`test_fallback_probe.py`: value on success and quiet, exact default plus a warning naming the exception on failure for True / False / -inf, independent throttling per key). The call sites are deep inside `fit` with no direct entry point, so they are covered by the helper test plus the neighbouring support-finalisation suites, not by a per-site test |
| NUM-33 | Three relevance probes scored a failed candidate 0.0 (MI's floor), so it tied with genuine zero-MI candidates and iteration order could pick the argmax; a fourth site logged `"suppressed: %s"` naming nothing | **RESOLVED** — the three probes fall back to `-inf` through `call_or_default` (all three argmaxes start at `-inf`, verified, so a failed candidate is excluded rather than tied); the `_step_score` site now warns with the candidate name | Helper test pins that a `-inf` fallback loses to a genuine `0.0` |
| NUM-34 | A failing floor-eligibility check marked each candidate ineligible at debug, so a systematic fault silently emptied the raw floor pool | **RESOLVED (visibility)** — ineligible kept (conservative); throttled warning per site | Helper test |
| NUM-35 | A failed `mrmr_gains_` build left it empty at debug, silently making `uaed_auto_size` a no-op; same shape for the predictors log | **RESOLVED (visibility)** — both warn and state the consequence (`uaed_auto_size` no-op; provenance loses gain mapping) | `test_uaed_auto_size_works.py` and the UAED support-consistency suite pass unchanged |
| NUM-36 | `except Exception: pass` when attaching `cat_code_maps` to a recipe, at debug: the recipe then replays categories without its train-time codes (train/serve skew) | **RESOLVED (visibility)** — warning naming the recipe and the skew; dead `pass` removed | Across NUM-32..36: helper test + raw-tail subsumption, subsumption-verdict and both UAED suites, 20 passed (re-run after replacing the probe lambdas with typed functions for mypy) |
| RO-2 | RelaxMRMR's interaction term differences plug-in MIs estimated on tables of very different sizes, so it carries a systematic bias instead of ~0 under independence | **RESOLVED** — two parts, both needed. (1) The four MIs of each selected pair go through Miller-Madow-corrected, unclamped estimators (`_mi_mm_njit`, `_cmi_mm_njit`), applied once each; relevance and pairwise redundancy keep their plug-in values. (2) A pair contributes only when its largest table, `K_x*K_i*K_j*K_y` cells, holds at least `min_rows_per_cell` (default 5, the repo's `fe_confirm_undersample_rows_per_cell`) rows per cell; below that the term is not estimable | `test_relaxmrmr_interaction_bias.py`. Pre-fix, on fully independent data at n=2000: interaction -0.40 / -0.39 / -0.38 across seeds (the audit predicted a positive bias; measured it is negative and larger), and -0.05 at K=4 vs -0.46 at K=12. Miller-Madow alone left it at -0.37 and -0.50 at K=12: at 4320 cells for 2000 rows no plug-in correction holds, which is what the undersample guard addresses. Post-fix |term| < 0.01 at every seed and independent of K; an XOR control (y = z1 XOR z2, 16 cells at n=4000) still gets a clear non-zero term. The existing orientation / sign / hoist-equivalence RelaxMRMR tests pass (17 total) |
| PERIPHERY-4 | The "cheap O(1)" cache fingerprints called `X.to_numpy()` on the WHOLE frame (a dense upcast of every column, object on mixed frames) before sampling 1024 cells; the full-content hash did the same | **RESOLVED** — frames are read column by column: the signature samples 1024 strided rows per column, and the full hash updates one blake2b per column (categoricals as codes plus their labels, names, dtypes and shape folded in). Plain object columns still return `""` (no deterministic byte form). Docstrings now state the real cost | `test_content_signature_no_whole_frame_copy.py` with `DataFrame.to_numpy` made to raise. Pre-fix the signature fell back to `uncached` and the full hash returned `""` on a mixed int/float/category frame, which also means such frames were never cached at all; post-fix both work, are deterministic, and change with content, names and dtypes. 162 neighbouring cache / fingerprint / identity-cache tests pass |
| TESTGAP-1 | `nbins_method` `"mah_sci"` / `"sci"` / `"marx"` had zero tests; the alias table maps all to `"mah"` | **RESOLVED (test added)** — the aliasing is intentional (MAH/SCI is one method, per the tracker's own correction), so the gap was coverage only. `test_nbins_mah_aliases.py` pins the alias table and asserts each alias yields edges identical to `mah` end to end, with a control that `mah` differs from Sturges so the equality is not vacuous | Passes; a typo or re-pointing in the alias table now fails it |
| TESTGAP-9 | No test of a single-class target reaching `fit` through an imbalanced split | **RESOLVED (test added)** — the existing constant-y guard already covers it and names the class count (`only 1 unique value`). `test_single_class_target.py` drives it the realistic way (the negatives-only slice of a 1%-positive frame) across int / bool / str / categorical labels, with a two-class control that must fit | Passes |
| IMPL-10 | (new, found while planning FEC-3) `_fe_frame_ops.fe_append_columns`, the append seam FE stages share, says "the base frame is not duplicated", but its pandas branch is `pd.concat([X, new], axis=1)`, which under pandas 2.3.3 without copy-on-write copies EVERY existing column on every append (verified: no base column shares memory with the result) | **DOC + REJECTED (as a default change, bench-attempt-rejected)** — the docstring is corrected. A shallow copy + column assignment does share the base and is faster in isolation (`_benchmarks/bench_fe_append_columns.py`: n=2M p=60 k=1 454 -> 6 ms; but slower at n=10k, k>=64, and it trips pandas' fragmentation warning at k=150). End to end it did not win: `_benchmarks/ab_fe_append_columns_e2e.py`, default FE-heavy MRMR fit at n=200k p=30, alternating strategies in fresh processes, the seam's code swapped at the function level so lazy importers are covered (`seam_calls=2` per fit): wall concat 112.8 / 105.9 s vs shallow 147.2 / 130.2 s, peak RSS 3356 / 3381 vs 3291 / 3718 MB, identical selections. The default fit routes only 2 appends through the seam, so its copy is not what dominates. A first A/B run was discarded: its by-module swap reached only 1 binding and both arms ran concat | Both scripts committed in `_benchmarks/` with the numbers; revisit on a workload whose profile shows the seam's concat as a real share of wall or peak memory |
| FEC-3 | Four whole-frame `pd.concat` copies in the discrete-structural stages (`mid_a`), proposed fix: route them through `fe_append_columns` | **REJECTED (as proposed)** — the proposed seam is itself the same `pd.concat(axis=1)` (IMPL-10), so the reroute would change no allocation. The real concern, whole-frame copies on append, is tracked under IMPL-10, where replacing the copy did not win end to end | See IMPL-10 |
| IMPL-11 | (new, found while testing PERF-1) The shipped host-batched permutation nulls in `_integer_lattice_fe._perm_null_hi` and `_conditional_gate_fe._perm_null_hi` claim bit-identity with the per-permutation loop (the lattice docstring cites "0 diff across 30 synthetic scenarios"), but on TIED features the batched band differs, for the same rank-binning tie-order reason as PERF-1 | **RESOLVED (DOC)** — the batched nulls stay: a selection A/B, batched vs the per-permutation loop over integer-lattice (gcd signal + smooth / ordinary-mul controls), conditional-gate and row-argmax (signal + both controls) fixtures across seeds 1/7/13/42, selected identically in **28 of 28** cases, and this FE family is held to selection-equivalence, not bit-identity. Both docstrings now state the real contract: bit-identical on tie-free features, a shifted but valid null on tied ones (lattice 7-13%, gate 10-32% measured). The pairwise-modular sibling stays on its loop (PERF-1) | Probe and A/B scripts in this session's notes |
| RO-3 | The raw-feature floor-drop protection scored each candidate's held-out R^2 with one unpivoted QR of the base design plus a `qr_insert` per candidate. Exact on a full-rank base, but the base is every selected column, where collinear twins routinely survive together: a near-zero diagonal made `solve_triangular` return huge coefficients silently, and an exactly zero one raised and scored every candidate `-inf` | **RESOLVED** — the scorer moved into `_raw_protect_r2.heldout_r2_scorer`. The QR fast path is taken only while the triangular factor is well conditioned (min |diag| > 1e-10 x max), both for the base and for each extended factor; otherwise the rank-revealing minimum-norm `lstsq` decides. The 20x fast path is unchanged on full-rank designs | Staged: extraction with today's behaviour failed on a base holding a scaled twin column (`0.31868 != 0.31928` vs the lstsq reference) and on a candidate collinear with the base (increment `inf`, not ~0); the fix matches lstsq to 1e-8 on both, and a full-rank control matches too, so the fast path still engages. No existing test covered this pass |
| FE_STEP-3 | With `fe_check_pairs_subsample_n` active, FE auto-escalation sliced the admitted support's VALUES to its row subsample but kept each column's FULL-frame marginal MI, then pooled those with survivor MIs estimated on the subsample in one S5 redundancy gate. Plug-in MI is biased upward ~(k_x-1)(k_y-1)/2n, so the admitted support looked weaker than the candidates and the gate's bar was set from the wrong scale, toward admitting | **RESOLVED** — `_slice_admitted_pool` slices AND re-estimates each admitted marginal on the subsample with the survivors' estimator (`_quantile_bin` + `_cmi_from_binned` on the densified subsample target). Called only inside the subsample branch, so the full-data path is byte-identical | Staged: extraction with the old carry-over failed `marginal 999.0 was carried over instead of re-estimated on the subsample (0.2497)`; the fix passes, with an ordering control (informative column still above noise). The existing `test_fe_auto_escalation.py` (10) passes unchanged |
| IMPL-9 | (new, found while testing CORE-2) Any frame with non-string column labels crashed the classic fit: the greedy screen renders candidate names with `"-".join(factors_names[i] ...)`, which raises `TypeError` on an int. `pd.DataFrame(ndarray)` produces exactly such labels | **RESOLVED** — the candidate name uses `str()` | `test_non_string_column_names_fit.py`: pre-fix both tests raised `TypeError: sequence item 0: expected str instance, int found`; post-fix an int-labelled frame fits, selects the signal columns, transforms by the same labels, and selects the same positions as the identical data under string labels |
| NUM-15 | A present-but-unusable `xxhash` (partial wheel, renamed entry point, DLL fault) read as "absent" at debug, pinning the process onto the slower duplicate-column hash for its lifetime | **RESOLVED** — the trial import moved into `_resolve_xxh3_64()`; `ImportError` stays at debug, anything else warns naming the exception | Staged so the warning has its own failing run: extraction with the old debug level failed `test_xxhash_present_but_unusable_is_warned`, the split except makes it pass. Controls: genuine absence stays quiet, a usable module resolves its function |
| NUM-16 | The polars Struct-column check substituted "no Struct columns" on failure, disarming the validator whose only job is to fail early | **RESOLVED** — `ImportError` still means "polars absent"; any other failure warns and re-raises | Pre-fix the fit failed later for an unrelated reason and the check's failure was never reported; post-fix the fit stops at the check with a warning |
| NUM-17 | Two per-column MI failures (sklearn reference loop, numba partial-NaN branch) scored the column 0.0, MRMR's never-select value, at debug | **RESOLVED** — both warn with the column index, dtype and finite count, then re-raise | NaN was considered and not used: `_mi_classif_batch` has about 50 consumers and a NaN would reach sums and comparisons in each, which is its own audit. Re-raising makes the failure visible at the FE-family level instead. Pre-fix both tests: `DID NOT RAISE` |
| NUM-18 | The never-empty raw re-attach treated a failed subsumption verdict as "nothing is subsumed" at debug, making EVERY operand eligible, so it resurrected exactly the subsumed operand the check excludes. The `emit_both` policy was signalled by raising and catching a `RuntimeError`, indistinguishable from a real failure | **RESOLVED** — the verdict moved into `_subsumed_operands_verdict`: `emit_both` is decided before the try and returns an empty set; a genuine failure warns and returns `None`. `_never_empty_eligible` returns no candidates without a verdict (fail closed) | Staged: extraction with the old behaviour failed both behavioural tests (`a failed verdict was reported as set()`; `emit_both was logged as a failure`), the fix makes them pass, with a successful-verdict control and an eligibility table. `test_raw_tail_subsume_survivor_gate.py` passes unchanged. Scope note: with no verdict this block now leaves the support empty; the downstream empty-raw rescue still applies its own marginal-MI top-up policy, which is outside this finding |
| NUM-19 | One failed chunk of the prevalence auto-debias switched debiasing off for every pair of the fit, loosening the gate | **RESOLVED** — the try is per chunk: a failed chunk leaves its own pairs without a bias (read as 0.0 at the gate, i.e. raw joint MI), the others keep theirs, and one summary warning counts failed chunks and pairs. Only an import or setup failure still disables auto for the fit, now at warning | 6 variables, 15 pairs, chunks of 3, first chunk forced to fail. Pre-fix the whole loop aborted on that chunk (`assert 1 >= 2`: no second chunk was ever scored) and the flag came back False; post-fix the flag stays True and 12 of 15 pairs are debiased |
| NUM-20 | `NameError -> True` for the extra-basis routing guard, unlogged | **RESOLVED** — same code as FEC-17 (duplicate finding): `_default_scorer` is bound before the stage and the handler is gone | See FEC-17 |
| NUM-21 | A failed capture of the pre-FE raw safety net emptied it at debug, disabling the re-add that stops genuine raws being dropped | **RESOLVED** — the capture moved into `_capture_prefe_screened_raw`; the failure now warns (it runs at most once per fit) | Staged: extraction with the debug level failed the warning test, the promotion makes it pass; a control pins the healthy capture's raw-only, selection-ordered output |
| FEC-17 | `_fe_stage_cascade_early_a`: `_y_for_hybrid` and `_default_scorer` were bound only inside the polynomial stage's `try:` and read back under `except NameError`; the scorer fallback set `_extra_basis_scorer_ok = True`, silently re-enabling the Fourier extra basis under an alternate `fe_hybrid_orth_default_scorer` whenever that stage raised | **RESOLVED** — both bound once before the stage; both `NameError` handlers removed. Done together with IMPL-4, since the same block carried one of the 19 truncating casts |
| FIT_IMPL-10 | `_is_constant` took `np.ptp` over the non-NaN values; on an all-+inf (or all--inf) column that is inf - inf = nan, so a zero-variance infinite column was not reported as constant | **RESOLVED** — a column with non-finite values is constant when it has exactly one distinct non-NaN value | `test_degenerate_audit_infinite_columns.py` fails pre-fix, passes post-fix |
| FIT_IMPL-11 | `"data" in dir()` / `"cols" in dir()` / `"cached_MIs" in dir()` guards in the PGN cap paths were always true (they are parameters) | **RESOLVED** — dead conjuncts removed in `_assign_support.py` and `_assign_support_tail.py`; behaviour identical | 227 degenerate / assign-support neighbours pass |
| NUM-37 | The hinge/orth held-out R^2 probe returned 0.0 uplift when a solve failed, reading as a measured zero | **RESOLVED** — returns -inf with a debug log; both consumers compare `< floor`, so the reject verdict is unchanged (NaN would have admitted) | Same 227-test run |
| NUM-38 | A GPU-FE-active probe failure silently downgraded to serial dispatch at debug | **RESOLVED (visibility)** — warning naming the exception | Same run |
| NUM-42 | `sys.path.remove(vendored)` failures in `_neural_mi.py` were `pass` | **RESOLVED (visibility)** — debug log names the path | Same run |
| PERIPHERY-16 | The fit-time object-dtype inf scan ran a Python predicate over every cell of every object column, string-only columns included, and never stopped early | **RESOLVED** — columns pandas infers as string/bytes/empty are skipped; a short-circuiting `any()` stops at the first inf | Same run; mypy / ruff / interrogate / black clean on the touched files |
| PERF-14 | The eng-dedup slow path recomputed `np.isfinite` on kept columns already known fully finite | **RESOLVED** — `_eng_fully_finite` lets those reuse the candidate mask | Same run |
| TESTGAP-6 | `test_mrmr_gains_non_increasing_within_raw_screen_prefix` used a fixture on which the raw screen selected one feature, so the monotonicity check compared nothing | **RESOLVED** — own three-independent-signal frame; precondition asserts >= 2 gains; strict `<= 1e-12` | Precondition failed on the old frame (1 gain); passes on the new one |
| TESTGAP-12 | The DCD pinned-method test and both hybrid-orth recipe-corruption tests `pytest.skip`ped when the fixture did not produce the object under test | **RESOLVED** — the skips are fixture-precondition assertions | All 8 selected tests pass, so the fixtures do produce them |
| FIT_IMPL-13 | Group demotion tested `_grp_mi_f <= 0.0` against a documented "exactly zero within-group signal" contract | **RESOLVED** — `== 0.0`; `group_blocked_mi` floors each group's contribution at 0, so behaviour is identical and the test now states the contract | Covered by the FIT_IMPL-14 run below |
| FIT_IMPL-14 | The additional-RFECV rescue pool was `X.columns` minus the selection and only the hybrid-orth / mi-greedy rosters, and its guard counted `X.shape[1] - len(selected_vars)` across two index spaces. RFECV was built and fitted when every raw column was selected, and engineered columns from other FE families (e.g. `add(cbrt(a),cbrt(b))`) entered the pool; had RFECV selected one, mapping it through `feature_names_in_` would raise KeyError | **RESOLVED** — the pool keeps only `feature_names_in_` members; an empty pool skips the rescue with a verbose log | `test_rfecv_rescue_empty_pool.py` (RFECV spy): pre-fix both tests fail (RFECV constructed on an empty raw pool; engineered column in the pool), post-fix both pass |
| PERIPHERY-6 | No fit-time dtypes were stored, so an int column arriving as float, a categorical arriving as string, or an all-missing selected column passed transform silently | **RESOLVED** — `_validate_inputs` snapshots canonical dtypes (`mlframe._dtype_canon.canonicalise_dtype`) into `_fit_dtypes_`; pandas and polars transform warn (throttled) on dtype-kind drift in selected / recipe-source columns and on all-missing columns (strided sample first, full check only when the sample is all-missing) | `test_transform_dtype_drift.py`: pre-fix the drift and all-missing tests fail, the unchanged-input control passes; post-fix all 3 pass |
| PERIPHERY-13 | `explain_selection`'s survivor section counted `len(fe_provenance_)` as "selected", including screened-out engineered columns, and presented the in-screen greedy gain as an unqualified attribution | **RESOLVED** — membership from `get_feature_names_out()` (support_rank >= 0 fallback); screened-out count reported separately; gain-less selected columns (empty-support fallback raws) listed after the ranked ones; label qualified as in-screen greedy gain, placed after the roster so existing consumers of the prefix keep working | `test_explain_survivor_section_counts_selection.py`: pre-fix both fail; post-fix both pass, with `test_biz_value_mrmr_explain_enrichments.py` |
| RO-4 | Ten hybrid-orth families densified a float target with `astype(np.int64)` when it had <= 32 levels (merging `{0.0, 0.5, 1.0, 1.5}` into two classes), with the same truncating cast as the `qcut`-failure fallback | **RESOLVED** (same change as IMPL-4) — all ten copies, plus the nine further sites IMPL-4 found, call `_y_encoding.encode_y_for_classif_mi` (`np.unique` inverse for discrete targets, `qcut` for continuous, warned fallback per IMPL-5) | Verified against the tree: the ten `_group1.py` / `_group2.py` sites call the helper; `test_no_truncating_y_discretisation.py` (AST gate) and `test_y_encoding_preserves_non_integral_labels.py` pass |
| FEC-5 | The missingness stage reinstated fit-entry NaNs with `X[col] = restored_float64`, writing into a frame that can be the caller's own DataFrame (values replaced, integer columns coerced to float64) | **RESOLVED** — restored columns are kept in a local dict and overlaid on a shallow copy (`copy(deep=False)` + column set, which replaces the column in the copy only) that the indicator / count / pattern families and the raw MI floor read; `X` is never written | `test_missingness_stage_does_not_mutate_caller_frame.py`: the direct restore-branch test fails pre-fix (the caller's `a` came back with NaNs written into it) and passes post-fix, and still sees the 50 reinstated NaNs; the full-fit snapshot test passes both ways. 143 missingness / y-encoding / regression-union neighbours pass |
| PERF-2 | About seventeen FE cascade stages each discretised the same fit target (`np.unique` sort + `pd.qcut` on a continuous `y`) | **RESOLVED** — `encode_y_for_classif_mi` memoises float targets (n >= 4096) on a content hash (xxh3, blake2b fallback) of the raw buffer: equal content is served without re-sorting, changed content is re-encoded, codes are stored at their narrowest integer dtype (2 entries max) and every call returns a fresh int64 array | `bench_encode_y_memo.py` (warm, best-of-N): per call 81x at n=10k, 76x at 100k, 50x at 1M, 56x at 4M; 17 stages at n=4M 18.48s -> 1.40s, at 1M 4.16s -> 0.32s, codes identical. `test_encode_y_for_classif_mi_memo.py` pins one qcut per target, re-encoding on changed content, caller mutation not reaching the memo, and identity with the uncached path on five target shapes (its pre-fix failure is only the missing memo hook, so the bench is the evidence for this one) |
| FE_STEP-10 | `_ls_anchor` replayed a nested parent recipe over the whole frame once per log-side candidate to take one `nanmin` | **RESOLVED** — moved to `_mrmr_fe_step/_step_log_anchor.smart_log_anchor`, memoised per materialise call on `(source, id(nested parent))`; the memo holds the parent so its id cannot be reused while the call runs | `test_smart_log_anchor_memo.py`: five candidates sharing a parent cost one replay with the correct anchor, distinct parents and raw columns key separately, a missing column returns None; the 278-test run above covers the FE step |

### int_nondense note (IMPL-8, new finding)

Integer target with labels {1, 2, 5}, n=1500, default `MRMR()`. The pre-RO-4 baseline (captured at 00:01 on the tree of `be24033fe`, third of five fits in one
process) did NOT select `x1__relu_gt1.09186`. Every later run selects it with an otherwise identical set: the RO-4 after-fit (same in-process order), all 19
encoder sites swapped back to the legacy cast, a clean `git archive` of HEAD, and a clean `git archive` of `be24033fe` itself, the latter two each in 2 fresh
processes. So RO-4 and `2e15c07ac` are both ruled out: the same source produced a different selection in that one baseline run. Within a process, two back-to-back
fits always agreed. Candidates, none yet confirmed: state that one fit leaves behind for the next in the same process, the on-disk kernel-tuning cache (the logs show
DEFAULT-cache misses that write regions), or a GPU-dispatched kernel.

Follow-up on `d5314ee65` (clean `git archive`): the baseline's exact five-case sequence in 2 fresh processes, plus 1 process with int_nondense fitted FIRST, 2 fits
each: all 6 select the column. Across every run so far (12 fits, 3 source versions, 2 in-process orders) the baseline's selection has not recurred once, so
in-process fit order is ruled out as a sufficient cause.

**FUTURE (open)** — the one divergent run is so far unreproducible. Remaining candidates are machine-state ones outside the process: on-disk kernel-tuning-cache
regions written between runs, and GPU-dispatched kernels under contention at that moment. Next step is to run the sequence with the tuning cache pinned read-only
and with CUDA hidden, and to diff the selection's per-candidate MI against a GPU-visible run. A selection that depends on anything besides (X, y, params, seed) is
a correctness bug, so this stays open until a cause is named or the divergence is shown to come from a GPU/CPU numeric path already inside documented tolerance.

### Incidental fixes made while committing

| Item | Disposition |
|------|-------------|
| Two pre-existing `ISC004` (unparenthesized implicit string concatenation in a collection) in `tests/feature_selection/biz_val/test_biz_val_synthesis_and_drop_matrix.py:171` and `tests/test_meta/test_no_lazy_from_import_under_joblib_delayed.py:125` | **RESOLVED** — surfaced by the blocking `ruff tests/` gate, untouched by this session's commits. Per CLAUDE.md ("just fix what a linter surfaces, regardless of origin") they were fixed rather than worked around |
