# Training orchestration core - audit 2026-09-20

Scope read: `src/mlframe/training/core/` (phase runners, per-target loop, split/fit-pipeline wiring, dataset-reuse cache, dummy baselines, predict pre-pipeline), `src/mlframe/training/_data_helpers.py` (`_setup_eval_set`), `_eval_helpers.py` (per-split report call), `_predict_guards.py`, `_fixed_splits.py`, `_dataset_cache_fingerprint.py`, `_prediction_envelope_clip.py`, `_trainer_train_and_evaluate.py`, `_training_loop.py`, `xgb_shim.py`, `lgb_shim.py` / `_lgb_shim_helpers.py`, `cb/_cb_pool.py`, `cb/_cb_pool_build.py`, plus the shared `pipeline/_pipeline_cache.py` hashers the CB Pool caches depend on. `training/composite/` was excluded (owned by another audit). Findings are ordered by severity; each is proven from the quoted source, and the already-known retired-key CB val-Pool reader bug is deliberately not re-reported.

| Sev | ID | Finding | Evidence (file:line + quote) | Failure scenario | Suggested fix |
|---|---|---|---|---|---|
| P1 | TRC-01 | Regression predictions are clipped using the **eval split's own target statistics** before metrics, because the honest train-side envelope is threaded by exactly one caller (dummy baselines) and never by the real-model report path. | `reporting/_reporting_regression/_sensors.py:46-57` `elif targets_arr.ndim == 1 and targets_arr.size > 0:` / `_y_eval = targets_arr[np.isfinite(targets_arr)]` / `_env_stats = TrainEnvelopeStats(y_min=float(_y_eval.min()), y_max=float(_y_eval.max()), y_std=_y_std)` / `_envelope_source = "eval-fallback"`. Only producer of `y_train_envelope_stats`: `core/_phase_dummy_baselines.py:211` `_dummy_envelope_stats = compute_train_envelope_stats(current_train_target)`; the real-model call at `_eval_helpers.py:511` `preds, probs = report_model_perf(` passes no envelope kwargs. | Ridge/MLP extrapolates to 1e6 on TEST. The clip bound is derived from the TEST targets themselves, so the wildest predictions are pulled back inside the test range and the reported test RMSE / R2 / MaxError are optimistic in a way that cannot be reproduced at serving time. Meanwhile the dummy baseline gets the honest train-based (k=3 sigma) clip, so the model-vs-floor comparison is between two different clipping regimes. | Compute `compute_train_envelope_stats(train_target)` once per (model, target) in `_trainer_train_and_evaluate` and thread it into `_eval_helpers` -> `report_model_perf`; make the eval-fallback opt-in rather than the silent default. |
| P1 | TRC-02 | Both CatBoost Pool caches decide "did the label change?" by comparing `_full_target_content_hash` outputs, but that helper returns `""` on any failure - so two *different* targets that both fail hashing compare equal and `set_label` is skipped, training/evaluating against the previous target's labels. | `pipeline/_pipeline_cache.py:500-502` `except Exception as exc:` / `logger.debug("_full_target_content_hash: outer dispatch failed ...")` / `return ""` (also `:474`, `:490`). Consumers: `cb/_cb_pool_build.py:220` `if last_target_sig is None or _target_sig != last_target_sig:` and `cb/_cb_pool.py:765` `_label_changed = last_target_sig != _target_sig`. | Target A (an extension dtype whose `.to_numpy()` yields something `blake2b` rejects) trains, caching a Pool with `_mlframe_last_target_sig == ""`. Target B, same X, same failure -> `"" == ""` -> no `set_label`. CatBoost fits target B's model on target A's labels and the metrics are reported under target B's name with no error. The same path on the val Pool corrupts early stopping. | Treat `""` as "unknown" at both call sites: force the `set_label` swap (or evict the entry) whenever either signature is falsy, exactly as the helper's own docstring instructs. |
| P2 | TRC-03 | `_setup_eval_set` accepts `sample_weight_val` / `group_ids_val` but silently drops them on the single-val (no-shard) path, and no other core call site sets `sample_weight_eval_set` / `eval_group` / `eval_qid`. | `_data_helpers.py:578-579` docstring: `Without extra_eval_sets these are ignored (the caller is responsible for setting them up in fit_params directly, as before).` Caller `_trainer_train_and_evaluate.py:592-596` `_setup_eval_set(..., sample_weight_val=sample_weight[val_idx] ..., group_ids_val=group_ids[val_idx] ...)`. The else-branch at `_data_helpers.py:681` writes only `fit_params[param_name] = (val_df, val_target)`. A repo-wide grep finds `sample_weight_eval_set` assigned nowhere outside the shard branch. | A recency-weighted or class-balanced run trains on weighted rows but early-stops on an **unweighted** val metric: the chosen best iteration and the logged val score optimise a different objective than training. For LGB/XGB ranking the absent `eval_group`/`eval_qid` makes the val NDCG treat all rows as one query - a meaningless ES signal, silently. | In the non-shard branch set `sample_weight_eval_set=[sample_weight_val]` (xgb/lgb/cb) and `eval_qid` / `eval_group` from `group_ids_val` when they are not None. |
| P2 | TRC-04 | `compute_signature` - the key for all four booster dataset caches - does **not** include dtypes despite its own docstring claiming it does, and collapses to a cols/shape-only key whenever the row-sample hash fails. | `_dataset_cache_fingerprint.py:24` `1. Reads ``columns``, ``shape``, ``dtypes`` (cheap, O(n_cols)).` vs `:75` `return (cols, n_rows, n_cols, content_hash, extra)` - `dtypes` is never read in the function. `_row_sample_hash` returns `None` on every failure branch (`:141`, `:154`, `:168`, `:181`, `:195-196`). | A tier transition that recasts the same columns float64 -> float32 (or int -> pandas `category`) produces an identical signature, so `xgb_shim.py:511` `if self._cached_train_key == train_key and self._cached_train_dmatrix is not None:` reuses a DMatrix binned from the *other* dtype and the model trains on stale binning with no error. If `_row_sample_hash` also returns `None` for an unusual frame variant, any two same-shape / same-column frames collide outright. | Fold `tuple(str(d) for d in dtypes)` into the returned tuple, and make a `None` content hash a hard cache miss (unique sentinel) instead of a shared key. |
| P2 | TRC-05 | The predict-time extension pipeline silently renames output columns to positional `ext_i` when `get_feature_names_out()` disagrees with the transformed width - a mismatch the train side may never have hit. | `core/_predict_pre_pipeline.py:136-137` `if len(_names) != _n_out:` / `_names = [f"ext_{i}" for i in range(_n_out)]`, with the preceding comment naming `TF-IDF vocabulary drift between fit and predict` as a trigger. | A TF-IDF extension whose vocabulary differs at predict time yields a different `_n_out`; the frame is rebuilt as `ext_0..ext_{N-1}` while the model was fitted on the train-side real names. A name-agnostic estimator (CatBoost on positional columns, a plain numpy pipeline) then consumes misaligned columns and returns plausible-looking but wrong predictions. | Raise - as the `MLFRAME_EXTENSIONS_SOFT_FAIL` branch immediately above already does - when the predict-time width or name list disagrees with what was persisted at fit time, instead of renaming positionally. |
| P2 | TRC-06 | `group_ids` is taken verbatim from the user extractor with no `np.asarray` coercion, then indexed with positional integer arrays. | `core/_phase_helpers.py:721` `df, target_by_type, group_ids_raw, group_ids, timestamps, artifacts, additional_columns_to_drop, sample_weights = features_and_targets_extractor.transform(` then `:779` `ctx.group_ids = group_ids` (no coercion). Consumed as `_trainer_train_and_evaluate.py:537` `_grp_val = group_ids[val_idx] if (group_ids is not None and val_idx is not None) else None`, `:596`, `:764`, and `_eval_helpers.py:533` `group_ids=group_ids[idx] if group_ids is not None and idx is not None else None,`. | An extractor that returns `df["match_id"]` (a pandas Series) makes `group_ids[val_idx]` a **label**-based lookup. With a non-RangeIndex, or an index that survived a reshaping extractor, the returned group vector belongs to different rows than `val_idx` selects, so group-aware val metrics and ranking `eval_group` are computed against the wrong grouping - silently, whenever every label happens to exist. | Coerce once at `core/_phase_helpers.py:779`: `ctx.group_ids = None if group_ids is None else np.asarray(group_ids)` (same for `group_ids_raw`). |
| P2 | TRC-07 | The NaN guard decides whether a frame contains NaN from the **first 500 rows only**, and swallows probe failures into "no NaN". | `_predict_guards.py:266` `_sample = X.iloc[:500] if hasattr(X, "iloc") else X[:500]`; `:274` `_has_nan = bool(np.any(np.isnan(_arr_check[:500])))`; `:276-277` `except Exception as e: ... _has_nan = False`; `:279-280` `if not _has_nan:` / `return np.asarray(fn(X))  # Let the real error surface`. | A frame whose NaNs start at row 5000 (a lagged/rolling feature on a later entity) passes the probe, so the raw frame reaches a NaN-intolerant estimator. Where the estimator does not validate, the output rows are NaN and flow into metrics and ensemble stacking as a degraded score rather than an error. Any probe exception (e.g. a polars frame without `isna`) has the same effect. | Probe the whole frame with a cheap vectorised check (`X.null_count()` / `np.isnan(...).any()` over all rows) and let a probe failure fall through to the guarded path rather than to `fn(X)`. |
| LOW | TRC-08 | `_full_target_content_hash` is a *pointer* hash, not a content hash, for object-dtype arrays: `blake2b` accepts an object ndarray through the buffer protocol and hashes the pointer table. | `pipeline/_pipeline_cache.py:494` `h = hashlib.blake2b(np.ascontiguousarray(np_arr), digest_size=16)`. Verified by snippet: `hashlib.blake2b(np.ascontiguousarray(np.array(['x','y','z'], dtype=object)), digest_size=16)` succeeds instead of raising. | String class labels (pre-encoding) give a fresh digest for every re-materialisation of the same target, so the pre-pipeline cache and the CB Pool label-swap check never see a repeat - a permanent silent cache miss that forfeits the ~1.2s per-call saving this module exists for. The direction is safe (extra swaps, not skipped ones), but the documented "full content hash" contract is not met. | Reject non-numeric dtypes explicitly (`return ""`, with TRC-02 fixed so callers handle it) or hash `np_arr.astype("U").tobytes()` for object/string dtypes. |
| LOW | TRC-09 | The `_pre_pipeline_cache_key` single-slot memo keys the pipeline by bare `id(pipeline)` with no structural guard, while the real key uses the pipeline signature. | `pipeline/_pipeline_cache.py:606` `id(pipeline),` inside `id_tup`, vs `:615` `sig = _pipeline_signature_for_cache(pipeline)` in the real key; `:612-613` `if _LAST_KEY_CACHE["id_tup"] == id_tup:` / `return _LAST_KEY_CACHE["key"]`. Array components carry a shape guard against id recycling; the pipeline component does not. | A pipeline object is freed after target A and a differently-configured pipeline for target B is allocated at the same address. With the same (ctx-pinned) `train_df` / `val_df` / `train_target` objects and the same target name, the memo returns target A's key and the transform cached under A's pipeline is served for B. | Fold `_pipeline_signature_for_cache(pipeline)` (or at minimum `type(pipeline).__name__` plus step count) into `id_tup` alongside the id. |
| LOW | TRC-10 | The supervised signal handed to the composite-FE steps is whichever target `target_by_type` yields first, reused for the whole suite. | `core/_phase_helpers_fit_pipeline.py:387-392` `for _v in target_by_type.values():` / `_cand = next(iter(_v.values())) if hasattr(_v, "values") else _v` / `_y_for_composite = _cand.to_numpy() ...` / `break`. The result is passed to `apply_categorical_composite_fe` (`:421`) and `apply_target_encoding_composite_fe` (`:451`) for every target. | In a multi-target suite the target-encoded / MI-grouped categorical features fed to targets 2..N encode target 1's labels. Target 2's model consumes a feature built from a different label column, and the scores shift with the dict ordering of `target_by_type` - not reproducible if the extractor reorders targets. | Build `_y_for_composite` per target inside the per-target loop, or at minimum record the chosen target name in metadata and log it. |
| LOW | TRC-11 | `_setup_eval_set` derives `model_category` by substring match over an ordered dict and silently returns - no eval_set, no early stopping - when nothing matches. | `_data_helpers.py:594-597` `for key in eval_set_configs:` / `if key in model_type_lower:` / `model_category = key` / `break`; `:599-600` `if model_category is None or model_category not in eval_set_configs:` / `return`. Note `"catboostclassifier"` contains none of the keys - CB only works because callers pass `model_category` explicitly. | Any future call site (or a wrapper class renamed away from the `LGBM*` / `XGB*` conventions) that omits `model_category` gets no eval_set at all: the booster trains to `n_estimators` with no early stopping and is reported as if ES had run. | Log a warning (or raise) on the unmatched branch instead of a bare `return`. |
| LOW | TRC-12 | In `pinned_train_val_test_split`, `test_size` / `val_size` / `calib_size` are applied to the *unpinned pool*, not to `n_rows`, with no log of the effective fraction. | `_fixed_splits.py:360` `pool = np.flatnonzero(unassigned)`; `:368` `_kw.update(test_size=_test_sz, val_size=_val_sz, calib_size=_calib_sz)` passed to `splitter(df=pd.DataFrame(index=pd.RangeIndex(len(pool))), ...)` at `:370`. | With `test` pinned to 20% of rows and `val_size=0.2`, the carved val is 20% of the remaining 80% = 16% of the data, not the 20% the config states. Metrics are computed on a smaller-than-declared val and the difference is invisible in the logs. | Rescale the pool-relative fractions to the configured whole-frame fractions, or log the effective fraction next to the configured one. |
| LOW | TRC-13 | The NaN-guard probe uses `np.isnan` only, so `+/-inf` frames pass through unguarded even though `SimpleImputer(strategy="mean")` would not repair them either. | `_predict_guards.py:271-274` `# use np.isnan (not ~np.isfinite) for parity with the pandas isna() branch above; the prior ~isfinite included +-inf which SimpleImputer(...) does NOT replace, so +-inf rows would pass the gate but then propagate unchanged through imputer+scaler.` / `_has_nan = bool(np.any(np.isnan(_arr_check[:500])))`. | A feature that overflows to `+inf` at predict reaches a linear model unfiltered; the prediction is `inf` and downstream mean-based aggregation (ensemble blending, calibration) turns every aggregated row into `inf` / `nan` without an exception. | Detect non-finite separately and clip or replace infinities before the imputer rather than documenting the hole. |

- **Disposition**: PARTIAL - TRC-02 RESOLVED, the remaining 12 are TODO.

## Dispositions

| ID | Status | Note |
|---|---|---|
| TRC-02 | RESOLVED | The decision is one shared `target_label_changed(last, new)` used by both Pool caches: a falsy signature is "unknown" and forces the swap. `_full_target_content_hash` additionally reports "unknown" for an object-dtype target instead of hashing its POINTER bytes, which described where the elements lived rather than what they were. Pinned by `tests/training/test_cb_pool_label_signature.py`. |
