# Audit report: reporting_b

- Files reviewed: 56
- LOC reviewed: 18733
- Findings: P0=0 P1=1 P2=4 P3=4

(Recovered from the workflow journal's structured return — this cluster's own Write call for the report file did not land on disk during the run, same tooling hiccup noted for `reporting_charts` in the 2026-07-21 audit.)

| ID | Severity | File:Line | Summary | Suggested fix | Meta-test idea |
|----|----------|-----------|---------|----------------|-----------------|
| REPORTING_B-1 | P1 | src/mlframe/reporting/charts/error_analysis.py:310,316 (+ charts/multiclass.py:549, charts/multilabel.py:208) | weak_segment_heatmap digitizes a feature column that still contains NaN (by design, per its own docstring); numpy sorts NaN after every finite value, so np.digitize silently places every NaN row in the HIGHEST bin instead of excluding/flagging it, corrupting the worst-slice localization. Same missing-finite-filter pattern recurs in multiclass/multilabel _calib_grid_panel's digitize(proba_k, ...) calls. | Explicitly route NaN rows to a dedicated bin (as slice_finder._bin_matrix already does correctly) or exclude them from the digitize input and report their count separately; apply the same fix to the two _calib_grid_panel sites. | AST/grep scanner: flag any np.digitize(X, edges) call where X's defining expression is not first passed through np.isfinite-based filtering in the same function. Pair with a property test injecting one NaN into a binning input and asserting it is excluded, not folded into the extreme bin. |
| REPORTING_B-2 | P2 | src/mlframe/reporting/diagnostics_dispatch.py:206 | render_split_error_diagnostics passes timestamps=None to worst_k_table whenever the frame is subsampled (n > DIAG_ROW_CAP = 100k rows) instead of indexing timestamps by sample_idx like yt/yp are, silently dropping temporal context on exactly the large-frame (100GB+) case this subsystem targets. | Index timestamps[sample_idx] (bounded to the same DIAG_ROW_CAP subsample) instead of unconditionally passing None. | Parametrized test calling render_split_error_diagnostics with n > DIAG_ROW_CAP and timestamps supplied; assert the returned worst_k_table has a non-null timestamp column. Generalize to every optional kwarg (ids, feature_importances) threaded through a subsample branch in this dispatcher. |
| REPORTING_B-3 | P2 | src/mlframe/reporting/renderers/matplotlib.py:365 | MatplotlibRenderer._histogram's bar-width expression `float(p.bin_width or (bin_centers[1]-bin_centers[0]) if len(bin_centers)>1 else 1.0)` relies on Python ternary binding looser than `or`, parsing as `(p.bin_width or diff) if len>1 else 1.0` -- an explicitly-supplied bin_width is silently discarded and forced to 1.0 on a single-bin histogram. PlotlyRenderer._histogram (line 477) has the identical expression but correctly parenthesized, so the two backends render a single-bin histogram at different widths from the same spec. | Wrap the ternary in parens to match plotly.py: p.bin_width or ((bin_centers[1]-bin_centers[0]) if len(bin_centers)>1 else 1.0). | Generic AST checker for `A or B if C else D` (no parens) anywhere in the repo. Plus a cross-backend parity test comparing both renderers' resolved bar width across bin_centers length in {0,1,2,N} x bin_width in {None, explicit}. |
| REPORTING_B-4 | P2 | src/mlframe/reporting/renderers/matplotlib.py:438-442,517-521 (+ charts/confusion_matrix_plot.py:185-192) | Three call sites still loop per-cell calling colors.auto_text_color() individually for heatmap cell-text color, even though colors.auto_text_colors_batch() exists specifically to replace this pattern (its own docstring documents the fix) and is already used by PlotlyRenderer._heatmap. Bounded by a 400-cell cap so not catastrophic, but a real missed optimization and 3-way code duplication. | Replace all three per-cell loops with one auto_text_colors_batch(...) call, mirroring renderers/plotly.py:569-574. | Grep scanner flagging any nested double for-loop under reporting/ whose body calls auto_text_color( -- require auto_text_colors_batch instead. |
| REPORTING_B-5 | P2 | src/mlframe/reporting/charts/error_analysis.py:245-251 | _top_split_features's sklearn-unavailable fallback ranks a feature via a median split (np.median, not np.nanmedian); a column with any NaN makes the median NaN, so both comparison masks (> med, <= med) are all-False, hi/lo come back empty, and that column's surrogate importance silently stays 0.0 regardless of its real discriminating power on non-missing rows. | Use np.nanmedian for the split point and mask NaN rows out of hi/lo (or pre-drop NaN per-column before the median). | Property test: force the ImportError fallback path, feed a matrix where exactly one strongly-discriminating column has a single NaN, assert its surrogate importance is not zero. |
| REPORTING_B-6 | P3 | src/mlframe/reporting/charts/decision_curve.py:137 (docstring) vs 147 (code) | build_decision_curve_spec's docstring states the 'useful' verdict fires on 'margin > 1e-4' but the code uses np.nanmax(advantage) > 1e-3 -- a 10x mismatch between documented and actual threshold for a business-facing verdict surfaced into metrics_dict. | Fix the docstring to say 1e-3 to match the shipped code (or vice versa if 1e-4 was the intended value). | Doc-linter: AST-extract numeric literals in a function body and cross-check against numeric literals quoted near comparison words in its docstring; flag any docstring number with no matching body literal. |
| REPORTING_B-7 | P3 | src/mlframe/reporting/renderers/matplotlib.py:633 | In _line's band handling, `_xi(0) if not xs_per_series else p.x[0]` is a dead-code-equivalent ternary: _xi(0) already returns p.x[0] if xs_per_series else p.x, so both branches evaluate to exactly _xi(0). Harmless but confusing. | Simplify to ax.fill_between(_xi(0), lower, upper, ...). | Standard dead-code/simplification lint (ruff SIM rules / vulture-style check) catching a ternary whose both branches are provably equal. |
| REPORTING_B-8 | P3 | src/mlframe/reporting/charts/drift.py:396 | cusum_residual_drift's finite-row mask is `mask &= np.isfinite(...) if numeric else mask` -- for non-numeric (datetime64) timestamps the else branch is a no-op `mask &= mask`, so NaT timestamps are never filtered and can land at an arbitrary sorted position in the CUSUM's time-ordered residual sequence. | Add an explicit NaT check for the datetime branch (e.g. ~np.isnat(ts) when ts.dtype.kind == 'M') instead of the else-mask no-op. | Property test feeding a datetime64 timestamps array containing one NaT and asserting that row is excluded from the standardized-residual series. |
| REPORTING_B-9 | P3 | src/mlframe/reporting/charts/shap_per_instance.py:89 | _is_binary_score guards its np.all(np.isin(...)) check on the pre-NaN-filter yt.size rather than the post-filter size; for an all-NaN y_true, np.all([]) is vacuously True, so label_ok=True is reported for a target carrying zero real label information instead of the intended indeterminate/False. | Guard on the post-filter array size: finite_yt = yt[~np_isnan(yt)]; label_ok = finite_yt.size > 0 and np.all(np.isin(finite_yt, (0,1))). | Property test calling _is_binary_score with an all-NaN y_true and asserting it returns False; generalize into a scanner flagging np.all/np.any over a boolean-filtered subset with no explicit .size guard on the filtered array. |

## Cluster-level meta-test ideas

- AST/grep scanner for np.digitize/np.searchsorted calls lacking a prior isfinite filter on the same variable within the enclosing function, applied repo-wide (not just reporting/) -- this bug class recurred 3 times in this one cluster alone.
- A cross-backend bit-identity test suite for every chart builder's matplotlib vs plotly renderer output (bar widths, bin counts, per-cell text colors) run as a fixture matrix -- the width bug shows the two backends can silently diverge despite the package's own 'renders identically on either backend' design contract.
- A doc/code numeric-literal consistency checker across reporting/: grep every docstring number near a comparison word and cross-check it against the function's actual body literals.
- A generic 'subsample path silently drops an optional kwarg' regression-test generator for every diagnostics_dispatch entry point with a row cap, parametrized over every optional array argument (timestamps, ids, sample weights).
