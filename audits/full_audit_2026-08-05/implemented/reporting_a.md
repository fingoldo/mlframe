# Audit report: reporting_a

- Files reviewed: 9
- LOC reviewed: 1492
- Findings: P0=0 P1=4 P2=3 P3=4

(Recovered from the workflow journal's structured return — this cluster's own Write call for the report file did not land on disk during the run, same tooling hiccup noted for `reporting_charts` in the 2026-07-21 audit.)

| ID | Severity | File:Line | Summary | Suggested fix | Meta-test idea |
|----|----------|-----------|---------|----------------|-----------------|
| REPORTING_A-1 | P1 | src/mlframe/reporting/_diagnostics_dispatch_extra.py:482-512 (esp. 503) | _first_group_column's categorical-dtype detector is broken: `dt is object` never matches a real pandas object-dtype column (identity vs equality), and there is no polars dtype branch at all. | Use pd.api.types.is_object_dtype(col) / dt == object instead of `is`; add a polars branch (Categorical/Enum/Utf8/String via col.dtype, cardinality via col.n_unique()). | AST/grep scanner flagging `\bis\s+(object\|str\|int\|float\|bool\|dict\|list\|tuple)\b` outside isinstance(...) calls. |
| REPORTING_A-2 | P1 | src/mlframe/reporting/_diagnostics_dispatch_extra.py:474-479 | _ranked_top_features picks NaN-importance features as top-ranked because np.argsort(...)[::-1] sorts NaNs last ascending, first after reversal (empirically confirmed). | order = np.argsort(np.nan_to_num(imp, nan=-np.inf))[::-1], or filter NaNs before ranking. | Property test with random importance vectors containing injected NaNs; assert returned top-k never includes a NaN-importance feature when enough non-NaN features exist. Same unguarded pattern recurs 3x more in sibling diagnostics_dispatch.py (out of scope, not fixed here). |
| REPORTING_A-3 | P1 | src/mlframe/reporting/_diagnostics_dispatch_extra.py:216-226 (build_combined_html_report) | Combined-HTML-report image resolver only tries <base>.png and <base>.matplotlib.png, but renderers/save.py's naming policy produces <base>.plotly.png for any multi-format/multi-backend plot_outputs (e.g. the package's own docstring example 'plotly[html,png] + matplotlib[pdf]'), so it silently embeds broken <img> links while still recording success. | Resolve the real saved path via renderers/save.py's own naming policy (shared helper) instead of a hardcoded 2-name guess; or pass through the actual per-chart output paths recorded at save time. | Render via a 2-format/2-backend plot_outputs DSL then call build_combined_html_report; assert every <img src> path in the produced HTML exists on disk. Existing tests only use single-backend 'matplotlib[png]', confirmed via grep of test_diagnostics_dispatch_followup.py. |
| REPORTING_A-4 | P1 | src/mlframe/reporting/_risk_coverage_diagnostic.py; src/mlframe/reporting/_diagnostics_dispatch_extra.py:whole file; render_model_card_diagnostic/render_split_comparison_from_suite/render_decile_table_diagnostic | render_risk_coverage_diagnostic, render_model_card_diagnostic, render_split_comparison_from_suite, and render_decile_table_diagnostic (the dispatch/wiring layer) have zero direct test coverage; existing tests with similar names only exercise the underlying charts.* composer functions. | Add a direct test per wrapper (happy path + a None/empty/shape-mismatch edge case) asserting on metrics_dict['charts'] and any recorded scalar metrics, not just no-crash. | Scanner: for every def render_*_diagnostic(/def render_*_from_suite( in reporting/, grep tests/ for a call to that exact function name; flag zero-hit functions. |
| REPORTING_A-5 | P2 | src/mlframe/reporting/_diagnostics_dispatch_extra.py:556-597 | render_category_discriminability_diagnostic accepts seed: int = 0 but never uses it; the composer it calls (compose_category_discriminability_figure) doesn't even accept a seed kwarg and hardcodes np.random.default_rng(0) internally for its row subsample. Every sibling render_* function in the same file correctly threads its seed through. | Either wire seed through to the composer/panel builder (replacing the hardcoded default_rng(0)), or drop the dead parameter from the signature. | Grep every render_*_diagnostic/render_*_from_suite for a seed param, then grep its body for seed= passed to the composer call; flag accepted-but-unused seed params. |
| REPORTING_A-6 | P2 | src/mlframe/reporting/_diagnostics_dispatch_extra.py:34-37 | DIAG_ROW_CAP=100_000 / DIAG_MAX_FEATURES=200 are independently redefined here with the same names/values as diagnostics_dispatch.py:34,37 (the actual consumer), instead of importing from it; the copy here is dead except for re-export via __all__. | Import DIAG_ROW_CAP/DIAG_MAX_FEATURES from diagnostics_dispatch (or a shared constants module) instead of copy-pasting the literals. | Grep-based duplicate-constant scanner: flag any module-level NAME=<literal> whose exact (name, literal) pair also occurs in a sibling file within the same package with no import relationship between them. |
| REPORTING_A-7 | P2 | src/mlframe/reporting/auto_dispatch.py:267-334 (multiclass + binary branches of render_multi_target_panels) | When target_type authoritatively selects the multiclass or binary branch but the actual probs shape doesn't satisfy that branch's guard, the function silently falls through to return None with no log line; the multilabel branch a few lines above does log a warning on its analogous shape mismatch. | Add logger.warning in the multiclass and binary branches mirroring the multilabel one when the shape guard fails but the target_type/panels gate matched. | Call render_multi_target_panels with target_type='multiclass_classification' and a 2-column probs (below the >=3 classes guard); assert a warning is logged, not just that the return is None. |
| REPORTING_A-8 | P3 | src/mlframe/reporting/_risk_coverage_diagnostic.py; src/mlframe/reporting/auto_dispatch.py:_risk_coverage_diagnostic.py:19-22/36; auto_dispatch.py:88/142 | Implicit-Optional annotation bugs: y_score: np.ndarray (required) is checked via 'y_score is None', and targets: np.ndarray (required) is checked via 'targets is not None' — both should be Optional[np.ndarray]. | Annotate both parameters as Optional[np.ndarray]. | mypy --no-implicit-optional in CI; or an AST check for a bare-typed parameter later compared via Is/IsNot None in the function body. |
| REPORTING_A-9 | P3 | src/mlframe/reporting/_diagnostics_dispatch_extra.py; src/mlframe/reporting/catalog.py:_diagnostics_dispatch_extra.py:474,482,515-524,556-567,600-611; catalog.py:135 | Multiple function parameters carry no type annotation at all (df, y_true, feature_names, feature_importances, timestamps in three render_*_diagnostic functions; names/feature_importances/k in _ranked_top_features; df/names in _first_group_column; file in catalog.describe_available_panels) despite being compared to None / passed to typed builders. | Add explicit annotations matching actual call-site usage (Any, Optional[Sequence[str]], Optional[IO[str]], etc.). | mypy --disallow-untyped-defs scoped to reporting/ as a CI gate. |
| REPORTING_A-10 | P3 | src/mlframe/reporting/auto_dispatch.py; src/mlframe/reporting/__init__.py:auto_dispatch.py:134,147,175; __init__.py:4-5 | Non-ASCII em-dash (U+2014) used in prose comments/docstrings, inconsistent with this same package's own explicit 'Console output is ASCII-only' convention in catalog.py. | Replace em-dash with ' - ' or restructure the sentence at the five listed lines. | Grep/AST scanner over comments and docstrings for characters with ord(c) > 127 outside data-carrying string literals; regression-gate on newly introduced non-ASCII. |
| REPORTING_A-11 | P3 | src/mlframe/reporting/_diagnostics_dispatch_extra.py:123-135 (_flat_scalar_metrics) | Inconsistent merge semantics: top-level scalar keys unconditionally overwrite, but keys inside a nested sub-dict use setdefault, so two different nested sub-dicts sharing a key name silently drop the second value instead of a documented precedence rule. | Either document first-wins for both cases explicitly, or make nested-dict flattening consistently last-wins to match top-level scalar behavior. | Property test: metrics dict with two nested sub-dicts sharing a key with different float values; assert the documented (not accidental) precedence rule holds. |

## Cluster-level meta-test ideas
