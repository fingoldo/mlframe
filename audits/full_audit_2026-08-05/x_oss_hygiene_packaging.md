# Audit report — cluster `x_oss_hygiene_packaging`

**Scope:** `README.md`, `CHANGELOG.md`, `CONTRIBUTING.md`, `LICENSE`, `MANIFEST.in`, `pyproject.toml`
(packaging metadata), `docs/` (all guides, research notes, examples, gallery), `mkdocs.yml`.
`src/mlframe/feature_selection/filters/**` and `src/mlframe/feature_selection/shap_proxied_fs/**` are
out of scope per the assignment (dedicated audit cycle `audits/mrmr_audit_2026-07-25/` closed 10 days ago);
references to those directories from in-scope docs were checked only for path/name existence, never for
code-quality inside them.

**Files reviewed:** 32 (7 root packaging/policy files + `docs/README.md` + 22 other `docs/*.md` +
`docs/examples/{README.md,composite_targets.md}` + `docs/gallery/index.md` +
`docs/gallery/_combined_report_sample.html` + `docs/composite_targets_tutorial.ipynb` cell-count check).
**LOC reviewed:** ~8,085 lines across the scope (per `wc -l`), plus targeted cross-checks against ~40
files in `src/mlframe/` and `tests/` to verify doc claims (file/line/commit references, function/class
existence, config defaults, logger names) and 8 `gh api`/`git` calls against the live GitHub remote
(tags, releases, pinned commit SHAs).

## Findings table

| ID | Severity | File:Line | Summary | Suggested fix | Meta-test idea |
|---|---|---|---|---|---|
| X_OSS_HYGIENE_PACKAGING-1 | P2 | README.md:668, CONTRIBUTING.md:66 | Both claim the sklearn-matrix CI tests "scikit-learn 1.5 through 1.8" / "1.5, 1.6, 1.7, 1.8", but the actual CI matrix and the hard dependency floor are both 1.6+ (sklearn 1.5 cannot even import mlframe). | Change both to "1.6 through 1.8" / "1.6, 1.7, 1.8". | Script that extracts `sklearn-version:` matrix entries from `.github/workflows/sklearn-matrix-ci.yml` and asserts every "scikit-learn A through B" / "sklearn A, B, C" phrase in `README.md`/`CONTRIBUTING.md` matches the extracted minor-version set exactly. |
| X_OSS_HYGIENE_PACKAGING-2 | P2 | CHANGELOG.md:101-102 | The `[Unreleased]` and `[0.9.0]` reference-link footer points at a `v0.9.0` git tag/release that does not exist on the remote GitHub repo (`gh api repos/fingoldo/mlframe/tags` and `gh release list` both return empty). Both links 404. | Either cut the `v0.9.0` tag/release, or drop the dead links until a real release exists. | CI job (or local check gated on network) that parses `CHANGELOG.md`'s bottom link-reference block for every `.../releases/tag/vX.Y.Z` and `.../compare/vA...vB` URL and verifies via `gh api repos/<owner>/<repo>/tags` that each referenced tag exists. |
| X_OSS_HYGIENE_PACKAGING-3 | P2 | README.md:82-103 (Modules table) | Two real, substantial subpackages are missing from the "Modules" table: `mlframe.competition` (~3,956 LOC, 18 modules) and `mlframe.data_valuation` (~1,372 LOC, 8 modules, backs the `gt_04` research doc referenced elsewhere in this same doc set). | Add both rows (competition can note it is exploratory/never imported by production code, per its own package docstring). | Script that lists every `src/mlframe/<name>/__init__.py` directory (excluding `_benchmarks`, dunder dirs) and asserts each has a matching row in README.md's Modules table (or an explicit exclude-list entry with a reason). |
| X_OSS_HYGIENE_PACKAGING-4 | P2 | docs/MRMR_RESEARCH.md:183, 191 vs :199, :390 | Internal self-contradiction: the "Critical gaps" section says the Fleuret synergy-rejection weakness is "still unfixed" / "No protection against synergy destruction", but the "Recommendations" section 8 lines later and the cross-agent backlog table both mark the identical fix as shipped (`redundancy_aggregator='jmim'`/`'auto'`), and the live `fleuret.py` docstring (lines 4-7) explicitly states "The synergy-aware mitigation is shipped." | Strike through / update the two "Critical gaps" bullets to match the rest of the document. | Grep-based doc-consistency checker: for every `~~<claim>~~ — shipped`/`✅ done <claim>` line in a doc, flag any OTHER paragraph in the same file that asserts the negation ("still unfixed", "No protection against X", "TODO") referencing the same named mechanism/flag. |
| X_OSS_HYGIENE_PACKAGING-5 | P2 | docs/MRMR_RESEARCH.md:177 | Cites `wrappers/_rfecv.py:237-499` for the `feature_groups` mechanism; that file no longer exists — RFECV was split into a `wrappers/rfecv/` subpackage (monolith-split), and the relevant logic now lives in `_finalize.py`/`_validate.py`/`_configs.py`. | Update the reference to `wrappers/rfecv/_finalize.py` (or wherever the feature_groups logic currently sits). | See X_OSS_HYGIENE_PACKAGING-9's generalized stale-path scanner. |
| X_OSS_HYGIENE_PACKAGING-6 | P2 | docs/MULTI_OUTPUT.md:169-179 vs :253 | Internal self-contradiction: the "Calibration" section describes `_PerClassIsotonicCalibrator` as already shipped for both MULTICLASS and MULTILABEL ("Session 4"), while the "Out-of-scope (Session 3+)" list at the bottom still lists "Per-class isotonic calibration for multi-output" as not done. Verified `_PerClassIsotonicCalibrator` is real and wired into 9 files (`training/evaluation.py`, `training/trainer.py`, `training/_calibration_models.py`, etc.). | Remove that line from "Out-of-scope" (or narrow it to a genuinely-remaining sub-item, if one exists). | Same generalized contradiction scanner as X_OSS_HYGIENE_PACKAGING-4, applied to "Out-of-scope"/"Not yet done" list items specifically. |
| X_OSS_HYGIENE_PACKAGING-7 | P2 | docs/dummy_baselines_guide.md:98 | "raise the logger level for `mlframe.training.dummy_baselines` to DEBUG" to see the full per-baseline table — this logger name does not exist anywhere in the codebase. The dummy-baselines implementation was split into `mlframe.training.baselines.*` (e.g. `mlframe.training.baselines.dummy`, `.._dummy_bootstrap`, `.._dummy_baseline_regression`, each with its own `logging.getLogger(__name__)`); none of those names is `mlframe.training.dummy_baselines` or a child of it, so following this instruction literally has zero effect on log verbosity. | Update to name the actual package logger prefix, e.g. `logging.getLogger("mlframe.training.baselines").setLevel(logging.DEBUG)`. | Scanner that extracts every `` `pkg.path.name` `` string immediately preceded by "logger"/"logging level for" in docs, and checks that some `logging.getLogger(__name__)` call site's module path actually resolves to that string (or is a parent of it). |
| X_OSS_HYGIENE_PACKAGING-8 | P2 | docs/dummy_baselines_guide.md:464-467 | Both "Profiling + smoke" commands are broken as written: `python -m mlframe.training._profile_dummy_baselines` fails with `ModuleNotFoundError: No module named 'mlframe.training._profile_dummy_baselines'` (verified by running it) — the real module is `mlframe.training.baselines._profile_dummy_baselines`. Likewise `python mlframe/training/_smoke_dummy_baselines_e2e.py` points at a path that doesn't exist; the real file is `src/mlframe/training/baselines/_smoke_dummy_baselines_e2e.py`. | Fix both commands to the current `training/baselines/` paths. | Extend the meta-test in X_OSS_HYGIENE_PACKAGING-9 to also execute (or at minimum `python -c "import importlib; importlib.import_module(...)"`) every ```` `python -m <module>` ```` / ```` `python <path>.py` ```` code span found in docs, failing on `ModuleNotFoundError`/missing file. |
| X_OSS_HYGIENE_PACKAGING-9 | P3 | mkdocs.yml (whole file) | `docs/SHAP_PROXIED_FS_GAME_THEORY.md` is listed in `docs/README.md`'s "Internal / research notes" table (the same category as five other docs that are explicitly excluded from the mkdocs sidebar via `not_in_nav:`), but it appears in neither `nav:` nor `not_in_nav:` in `mkdocs.yml` — inconsistent with its own stated peer group, and likely to trigger an mkdocs "is not included in the 'nav' configuration" build warning. | Add `/SHAP_PROXIED_FS_GAME_THEORY.md` to the `not_in_nav:` list alongside its five siblings. | Script that parses `mkdocs.yml`'s `nav:` + `not_in_nav:` into one set of covered paths and asserts every `*.md`/`*.ipynb` under `docs_dir` appears in that set (or in an explicit, documented exclude list). |
| X_OSS_HYGIENE_PACKAGING-10 | P3 | docs/MULTI_TARGET_REGRESSION.md:13, :80 | Two "Landed in commit `X`" citations (`2d300944` for the MLP-native-MTR commit, `86504f08` for the E1/E2 per-target-charts commit) do not resolve to any commit in the repository's actual history (`git cat-file -e` fails for both against all refs). The real commits, found by matching commit-message text, are `0a6e60ea7` ("feat(mlp): native multi-target regression...") and `c9d5067f0` ("feat(suite): MTR per-target charts (E1) + per-column CT_ENSEMBLE (E2)..."). | Update both short-SHA citations to the real hashes. | Meta-test from X_OSS_HYGIENE_PACKAGING-2's SHA-existence check, generalized to every backtick-quoted 7-10 hex-char token across all of `docs/*.md` (not just CHANGELOG.md), verified via `git cat-file -e $sha` against local history rather than the GitHub API. |
| X_OSS_HYGIENE_PACKAGING-11 | P3 | docs/MULTI_TARGET_REGRESSION.md:97 | Cites `_phase_composite_discovery.py:223` for the "composite discovery filters to REGRESSION only" gate. The actual gate is at lines 81 and 187 of the current `src/mlframe/training/core/_phase_composite_discovery.py`; line 223 is now unrelated log-message code (an extreme-AR skip warning). | Update the line reference (and note the file lives under `training/core/`, not `training/composite/discovery/` as the surrounding prose implies). | Same stale-file:line scanner as X_OSS_HYGIENE_PACKAGING-9/-10: extract every `` `path/to/file.py:N` `` or `` `path/to/file.py:N-M` `` token from docs, verify the file exists and that `N` (or the range) is `<=` the file's current line count, as a cheap drift signal (doesn't prove the content still matches, but catches the common "file moved" and "line count shrank past N" cases for free). |
| X_OSS_HYGIENE_PACKAGING-12 | P3 | docs/honest_diagnostics_guide.md:108 | "Sensors" section cites `tests/training/test_honest_diagnostics_aggregator.py` as covering "schema, gap-warn threshold, dummy-floor delta, calibration ECE wiring, DeLong invocation" — this exact file does not exist. The closest match, `tests/training/test_honest_diagnostics_default.py`, does cover the claimed behaviors (`test_run_honest_diagnostics_populates_all_four_blocks`, etc.) but under a different name; two other split-off files (`test_honest_diagnostics_precast_bootstrap.py`, `test_honest_diagnostics_seed_derivation.py`) also exist and aren't mentioned. | Update the "Sensors" section to name the three actual test files. | Same stale-path scanner (X_OSS_HYGIENE_PACKAGING-9), applied to `` `tests/**/*.py` `` references specifically. |
| X_OSS_HYGIENE_PACKAGING-13 | P3 | docs/composite_config_reference.md:163 (auto-generated from `src/mlframe/training/_composite_target_discovery_config.py:230`) | The `cross_target_ensemble_strategy` field's auto-generated description cites a benchmark file `composite_ensemble_shootout.py` ("NNLS was the only strategy with positive mean improvement...") that does not exist anywhere in the repository under any name. Root cause is the source docstring comment, which surfaces verbatim into the generated doc. | Fix the comment in `_composite_target_discovery_config.py:230` to name the benchmark that actually exists for this claim (or note it was run ad hoc and not committed), then regenerate `composite_config_reference.md`. | Same stale-path scanner (X_OSS_HYGIENE_PACKAGING-9), run over BOTH `docs/**/*.md` and `src/**/*.py` docstrings/comments that feed auto-generated docs, so a source-side staleness is caught before it propagates. |
| X_OSS_HYGIENE_PACKAGING-14 | P3 | docs/NUMERICAL_STABILITY_REPORT.md (whole doc) | This 2026-04-24 report is a rigorous catastrophic-cancellation audit of `feature_engineering/numerical.py`'s moment kernels, but it is the only doc in the repo positioned as a general "numerical stability" reference and it neither mentions nor is cross-referenced by the three separate, much more severe catastrophic-cancellation bugs (up to 5.8e13 relative error) found in *different* modules using the exact same raw-moment-expansion formula, all documented in `CLAUDE.md` on 2026-08-04 (`_binned_numeric_agg_fe.py`'s `_global_stats_all`, `_target_encoding_fe.py`'s per-category skew/kurt — both fixed — and `_binned_numeric_agg_fe.py`'s `_derive_cell_stats`, explicitly flagged there as still unverified/open). A reader consulting this doc for "is the numerics story handled" gets no pointer to the recurring pattern or the still-open follow-up. | Add a short "Related recurring bug class" note linking to the three 2026-08-04 fixes/open item, and/or promote the `s3 = ...; m3 = s3/n - 3*mean*(s2/n) + 2*mean**3`-style scanner CLAUDE.md itself proposes into an actual repo checker. | A structural/AST scanner (candidate for pyutilz's `code_audit`) that flags any function computing skew/kurtosis via the raw-power-sum binomial-expansion identity (`sum(x**3) - 3*mean*sum(x**2) + ...`) instead of a centered-moment accumulation, so the next occurrence of this exact bug class is caught before shipping rather than after a live-data crash. |

## Severity counts

- P0: 0
- P1: 0
- P2: 8
- P3: 6

No P0/P1 findings: nothing in this cluster crashes, silently corrupts a model/prediction, or is a security issue — every finding is a documentation/packaging-hygiene defect (stale reference, internal contradiction, or a broken copy-pasteable command/link). The two broken commands (X_OSS_HYGIENE_PACKAGING-8) and the broken logger-name instruction (X_OSS_HYGIENE_PACKAGING-7) are rated P2 rather than P1 because their blast radius is dev-tooling discoverability, not production training/inference correctness.

## Coverage by review dimension

1. **Correctness bugs** — N/A to this cluster (no executable production code in scope); the closest analogue, "does a documented command/instruction actually work," is covered by findings 7 and 8 (both reproduced as genuine failures).
2. **ML correctness** — N/A directly; finding 14 is the closest tangent (a doc-completeness gap around a real, already-fixed-elsewhere ML-correctness bug class).
3. **Computational efficiency** — N/A; no algorithmic code in scope. `pyproject.toml`'s pytest/ruff/mypy tuning comments were read but are configuration, not algorithms.
4. **Edge cases / robustness** — N/A to prose docs; `MANIFEST.in` / `pyproject.toml` packaging edge cases (sdist prune list, wheel package-data, python-version markers for numpy/pyarrow/pillow/holidays) were reviewed and found internally consistent and well-justified (each conditional dependency pin carries a dated rationale comment); no gaps found.
5. **Test coverage gaps** — findings 12 (doc names a nonexistent test file) is the only direct hit; the underlying behavior IS tested (under different filenames), so this is a doc-accuracy gap, not an actual coverage gap.
6. **Code quality / architecture** — the repeated "doc still says `foo/bar.py`, code has since moved to `foo/bar/` (monolith-split subpackage)" pattern across findings 5, 7, 8, 12 suggests the monolith-split convention (CLAUDE.md) has no accompanying "update every doc reference" step; worth adding to that convention's checklist.
7. **OSS hygiene** — this is the primary dimension; see all 14 findings. General impression: the large majority of docs (composite_config_reference.md's 174-field auto-gen table, the gallery index, baseline_diagnostics_guide.md, calibration_policy.md, honest_diagnostics_guide.md's numeric claims, dummy_baselines_guide.md's config defaults, the env-var inventory table in README.md, the transform-catalogue table in composite_targets_guide.md) were cross-checked against live source and found accurate — the codebase clearly invests in doc-drift CI gates (`test_composite_config_reference_drift.py`, the env-var table's mechanical generation) and it shows. The failures found cluster specifically in **hand-written narrative sections that predate a later refactor/fix** and were never revisited, not in the auto-generated tables.

## Narrative detail

**X_OSS_HYGIENE_PACKAGING-1 (sklearn version claim).** `.github/workflows/sklearn-matrix-ci.yml` lines 82-87 pin the matrix to exactly `1.6.1`, `1.7.2`, `1.8.0`, with an explicit comment: "Floor is 1.6: category-encoders (a hard mlframe dep) imports `sklearn.utils.Tags`, which only exists from 1.6, so sklearn 1.5 cannot import mlframe at all." `pyproject.toml` line 63 declares `scikit-learn>=1.6` for the same reason. Both README.md and CONTRIBUTING.md nonetheless say the matrix covers "1.5 through 1.8" — a real user reading either doc would believe 1.5 is a supported floor when the repo's own CI infrastructure explicitly documents that it cannot be. Found by grepping the workflow YAML directly rather than trusting the prose.

**X_OSS_HYGIENE_PACKAGING-2 (dead CHANGELOG links).** `gh api repos/fingoldo/mlframe/tags` returned `[]` and `gh release list --repo fingoldo/mlframe` returned nothing (exit 0, confirmed no auth error via `gh api repos/fingoldo/mlframe --jq '{full_name,default_branch,private}'` which resolved fine) — the remote repository genuinely has zero tags and zero releases. `CHANGELOG.md`'s bottom two reference links both point at a `v0.9.0` tag/release that has never existed on the remote, even though `src/mlframe/version.py` correctly holds `0.9.0` locally and the CHANGELOG's own `## [0.9.0]` section content looks release-ready. This is either an artifact of the CHANGELOG being written ahead of the actual tag-and-release step, or the release step being silently skipped; either way the links 404 today.

**X_OSS_HYGIENE_PACKAGING-3 (missing Modules-table rows).** `src/mlframe/competition/` (3,956 LOC across 18 files) and `src/mlframe/data_valuation/` (1,372 LOC across 8 files) both exist as real, substantial, `__init__.py`-bearing subpackages sitting as direct siblings of every package that IS listed in README's Modules table (`training`, `feature_engineering`, `metrics`, etc.). `data_valuation` in particular is referenced from `docs/SHAP_PROXIED_FS_GAME_THEORY.md`'s `gt_04` section as a real, tested module (`mlframe.data_valuation`, KNN-Shapley row valuation) — so it's not vestigial or WIP, just missing from the top-level inventory a new user would read first.

**X_OSS_HYGIENE_PACKAGING-4 (MRMR_RESEARCH.md self-contradiction).** Line 183: "The Fleuret docstring (`filters/fleuret.py:4-7`) even acknowledges this as TODO; still unfixed." Line 191: "No protection against synergy destruction." Both are directly contradicted by: (a) the SAME document's own "Recommendations" section at line 199 — `~~Add JMI / JMIM as a mrmr_redundancy_algo option~~ — shipped as redundancy_aggregator='jmim'`; (b) the cross-agent backlog table at line 390 — `✅ done JMIM redundancy ... Fixes Fleuret-CMIM brittleness on multi-collinear (acknowledged TODO)`; (c) reading the ACTUAL current `fleuret.py:1-7` docstring, which says verbatim: "The synergy-aware mitigation is shipped: `MRMR(redundancy_aggregator='jmim')`... and the data-dependent gate `redundancy_aggregator='auto'`... BENCHMARKED DECISION (2026-06-19...)." This is a clean "one section of the doc was updated when the fix shipped, a sibling section describing the same gap was not" bug — high confidence, three independent confirmations.

**X_OSS_HYGIENE_PACKAGING-5 (stale `wrappers/_rfecv.py` path).** `find src/mlframe/feature_selection/wrappers -maxdepth 1 -type f -iname "*.py"` shows no `_rfecv.py`; `ls src/mlframe/feature_selection/wrappers/rfecv/` shows a 17-file subpackage (`_finalize.py`, `_validate.py`, `_configs.py`, `_fit.py`, ...) that is clearly the monolith-split result (consistent with CLAUDE.md's "flat sibling ... convert to a subpackage once a monolith fans out into 2+ siblings" rule). `grep -l feature_groups` inside that subpackage confirms the cited mechanism now lives in `_finalize.py`/`_validate.py`/`_configs.py`, not at the cited path at all.

**X_OSS_HYGIENE_PACKAGING-6 (MULTI_OUTPUT.md self-contradiction).** The doc's own "Calibration" section (lines 161-179) is detailed and specific: "Per-class isotonic semantics (Session 4): Each class k gets its own `IsotonicRegression.fit(...)`... Wrapped in `_PostHocMultiCalibratedModel`..." — this reads as a shipped, tested feature, and `grep -rl _PerClassIsotonicCalibrator src/mlframe` confirms it is wired into 9 production files including `trainer.py` and `evaluation.py`. Yet the "Out-of-scope (Session 3+)" list 70 lines later repeats "Per-class isotonic calibration for multi-output" as something not yet done. The two sections were evidently written/updated at different times without reconciling the roadmap list.

**X_OSS_HYGIENE_PACKAGING-7 (broken logger-name instruction).** `grep -rn getLogger src/mlframe/training/baselines/*.py` shows every submodule uses `logging.getLogger(__name__)`, giving logger names like `mlframe.training.baselines.dummy`, `mlframe.training.baselines._dummy_bootstrap`, `mlframe.training.baselines._dummy_baseline_regression`, etc. — a flat sibling set under `mlframe.training.baselines`, NOT children of `mlframe.training.dummy_baselines` (which isn't even a valid dotted path — no such module exists, confirmed by `find`). Python's logging hierarchy resolves loggers by literal dotted-name prefix matching, so `logging.getLogger("mlframe.training.dummy_baselines").setLevel(logging.DEBUG)` sets the level on a logger object that nothing in the codebase ever writes to — it is a complete no-op with respect to the actual DEBUG-level table the doc promises. A user follows this instruction, sees no new output, and has no way to know why without reading the source.

**X_OSS_HYGIENE_PACKAGING-8 (broken profiling/smoke commands).** Directly reproduced: `python -m mlframe.training._profile_dummy_baselines` → `ModuleNotFoundError: No module named 'mlframe.training._profile_dummy_baselines'` (stderr captured verbatim in this session). `python -m mlframe.training.baselines._profile_dummy_baselines` (the corrected form) resolves and starts running. The doc's second command, `python mlframe/training/_smoke_dummy_baselines_e2e.py`, has the same "moved into `training/baselines/`" problem AND is missing the `src/` prefix a repo-root-relative invocation would need — a copy-paste of either line from the doc fails immediately for any reader.

**X_OSS_HYGIENE_PACKAGING-9 (mkdocs nav gap).** `docs/README.md`'s "Internal / research notes" table lists six docs, five of which (`MRMR_RESEARCH.md`, `pysr_fe_upgrade_research.md`, `date_features_kaggle_research.md`, `NUMERICAL_STABILITY_REPORT.md`, `WAVE5_GPU_ROADMAP.md`, `FE_MRMR_IDEAS_BACKLOG.md`) are deliberately listed in `mkdocs.yml`'s `not_in_nav:` block, with a comment explaining exactly why ("still build ... but are intentionally kept out of the navigation sidebar"). `SHAP_PROXIED_FS_GAME_THEORY.md` is the sixth item in that same table but is absent from both `nav:` and `not_in_nav:` — an odd-one-out that breaks the stated convention and (per mkdocs' own default behavior for pages outside `nav`) is likely to emit a build-time "not included in the nav configuration" warning, which — depending on whether the site's build is run with `--strict` — could even fail CI.

**X_OSS_HYGIENE_PACKAGING-10/-11 (MULTI_TARGET_REGRESSION.md stale commit SHAs and line numbers).** `git cat-file -e <sha>` against every ref in the repo returns nonzero for both `2d300944` and `86504f08`, while `f03a86ef`, `d73bb763`, `2241468f`, `d48245de` (the doc's other four cited SHAs) all resolve cleanly — ruling out a simple "short-hash too short to be unambiguous" explanation for all six uniformly. Grepping `git log --oneline --all` for MTR-related commit-message text turned up `0a6e60ea7 feat(mlp): native multi-target regression (y shape (N, K>=2)) for PytorchLightningRegressor (F-24)` and `c9d5067f0 feat(suite): MTR per-target charts (E1) + per-column CT_ENSEMBLE (E2) (F-34 final)` — near-verbatim matches for what the doc describes at each cited (dead) hash, plus a THIRD commit, `a1b4044c9`, whose own message literally says "...shipped in 86504f08", meaning that SHA was a real, once-valid short-hash that has since been orphaned (consistent with this repo's documented heavy use of rebasing across parallel worktrees). Separately, `_phase_composite_discovery.py:223` in the CURRENT file is an unrelated `log_throttle(...)` call for an "extreme-AR + group-aware skip" warning, not the REGRESSION-only filter the doc describes; that filter is now at lines 81 and 187 of the same file.

**X_OSS_HYGIENE_PACKAGING-12 (honest_diagnostics_guide.md test-file drift).** `find tests -iname "*honest_diagnostics*"` lists exactly three files: `test_honest_diagnostics_default.py`, `test_honest_diagnostics_precast_bootstrap.py`, `test_honest_diagnostics_seed_derivation.py` — no `test_honest_diagnostics_aggregator.py`. `grep "^def test_" tests/training/test_honest_diagnostics_default.py` shows `test_run_honest_diagnostics_populates_all_four_blocks`, `test_run_honest_diagnostics_calibration_block_emits_plot_when_reports_dir_present`, `test_reporting_config_default_honest_diagnostics_on`, `test_run_honest_diagnostics_handles_missing_test_probs` — this file genuinely covers the doc's claimed schema/toggle/edge-case surface, just under a name the doc never mentions, and the doc omits the other two split-off files entirely.

**X_OSS_HYGIENE_PACKAGING-13 (composite_config_reference.md broken benchmark reference).** This doc is explicitly auto-generated ("Auto-generated by `scripts/gen_composite_config_reference.py` from `mlframe.training.configs.CompositeTargetDiscoveryConfig`. Do not edit by hand -- regenerate after changing the config (a doc-drift test fails CI otherwise)"), and `tests/test_composite_config_reference_drift.py` does exist and presumably keeps the FIELD LIST in sync — but the drift test cannot catch a stale reference living inside one field's free-text description, which is copied verbatim from the source comment at `src/mlframe/training/_composite_target_discovery_config.py:230`. `find src/mlframe -iname "*ensemble_shootout*"` and `grep -rn composite_ensemble_shootout src/mlframe --include=*.py` both come back empty except for that one source comment — the benchmark script the doc/comment cites to justify the `nnls_stack` default was apparently never committed (or was renamed without updating the comment).

**X_OSS_HYGIENE_PACKAGING-14 (NUMERICAL_STABILITY_REPORT.md doesn't reference the recurring bug class).** This is the softest finding in the set — not a factual error in the doc itself (it correctly scopes itself to `feature_engineering/numerical.py` and is honest about what's still open there), but a completeness/discoverability gap given what's since been learned. `CLAUDE.md`'s 2026-08-04 entries document, in order: a catastrophic skew/kurt bug in `_binned_numeric_agg_fe.py`'s `_global_stats_all` (errors up to 17 orders of magnitude, found via a 3000-trial sweep after a SILENT REVERT of an earlier fix), a second catastrophic instance in `_target_encoding_fe.py` (errors up to 5.8e13), and an explicit "OPEN FOLLOW-UP" flagging `_binned_numeric_agg_fe.py`'s `_derive_cell_stats` as a third, not-yet-verified suspect — all three using the exact same raw-power-sum binomial-expansion formula this report already identified as unstable back in April. Since this is the one doc in the repo framed as the authoritative numerical-stability reference, and the bug class it warned about has since recurred three times elsewhere, cross-referencing it (or promoting its own "hybrid: detect via cheap pre-check" recommendation into an actual scanner) would have material value going forward.

## Report path

`C:/Users/Admin/Machine learning/mlframe/audits/full_audit_2026-08-05/x_oss_hygiene_packaging.md`
