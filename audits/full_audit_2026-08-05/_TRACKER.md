# mlframe full audit 2026-08-05 — master tracker

36-cluster read-only parallel audit (30 subsystem clusters + 6 cross-cutting specialists), excluding `feature_selection/filters/` (MRMR, ~163k LOC) and `feature_selection/shap_proxied_fs/` (~15k LOC) — both closed out via `audits/mrmr_audit_2026-07-25/` 10 days prior.

**Totals: 3739 files / 431154 LOC reviewed. 15 P0, 67 P1, 109 P2, 137 P3 — 328 findings total.**

Every finding below will be implemented (RESOLVED), or given an explicit non-silent disposition (FUTURE/DOC/REJECTED) per the multi-agent-review disposition convention. Status starts at TODO for all.

## Per-cluster summary

| Cluster | Files | LOC | P0 | P1 | P2 | P3 | Report |
|---------|-------|-----|----|----|----|----|--------|
| calibration | 45 | 7154 | 0 | 2 | 3 | 8 | [calibration.md](calibration.md) |
| competition_evaluation | 43 | 8557 | 0 | 4 | 5 | 2 | [competition_evaluation.md](competition_evaluation.md) |
| core_infra_misc | 100 | 13951 | 0 | 0 | 4 | 7 | [core_infra_misc.md](core_infra_misc.md) |
| fe_root_a | 40 | 10882 | 0 | 5 | 2 | 3 | [fe_root_a.md](fe_root_a.md) |
| fe_root_b | 139 | 18468 | 1 | 5 | 4 | 10 | [fe_root_b.md](fe_root_b.md) |
| fe_transformer_a | 62 | 10416 | 1 | 2 | 3 | 8 | [fe_transformer_a.md](fe_transformer_a.md) |
| fe_transformer_b | 63 | 10259 | 0 | 2 | 3 | 3 | [fe_transformer_b.md](fe_transformer_b.md) |
| fs_benchmarks_a | 67 | 9311 | 0 | 1 | 1 | 1 | [fs_benchmarks_a.md](fs_benchmarks_a.md) |
| fs_benchmarks_b | 147 | 19334 | 0 | 1 | 1 | 7 | [fs_benchmarks_b.md](fs_benchmarks_b.md) |
| fs_benchmarks_c | 77 | 10975 | 0 | 1 | 2 | 7 | [fs_benchmarks_c.md](fs_benchmarks_c.md) |
| fs_boruta_root | 34 | 8469 | 0 | 1 | 2 | 3 | [fs_boruta_root.md](fs_boruta_root.md) |
| fs_wrappers | 36 | 9632 | 1 | 2 | 3 | 3 | [fs_wrappers.md](fs_wrappers.md) |
| metrics | 85 | 18421 | 3 | 1 | 3 | 7 | [metrics.md](metrics.md) |
| models | 41 | 9294 | 0 | 4 | 6 | 5 | [models.md](models.md) |
| preprocessing | 51 | 6387 | 3 | 3 | 4 | 2 | [preprocessing.md](preprocessing.md) |
| reporting_a | 9 | 1492 | 0 | 4 | 3 | 4 | [reporting_a.md](reporting_a.md) |
| reporting_b | 56 | 18733 | 0 | 1 | 4 | 4 | [reporting_b.md](reporting_b.md) |
| training_composite_core_a | 41 | 11865 | 0 | 2 | 3 | 3 | [training_composite_core_a.md](training_composite_core_a.md) |
| training_composite_core_b | 41 | 12150 | 0 | 2 | 4 | 2 | [training_composite_core_b.md](training_composite_core_b.md) |
| training_composite_discovery | 50 | 15941 | 0 | 0 | 3 | 2 | [training_composite_discovery.md](training_composite_discovery.md) |
| training_composite_ensemble_estimator_transforms | 96 | 17562 | 0 | 1 | 1 | 2 | [training_composite_ensemble_estimator_transforms.md](training_composite_ensemble_estimator_transforms.md) |
| training_core_a | 33 | 12000 | 0 | 1 | 1 | 1 | [training_core_a.md](training_core_a.md) |
| training_core_b | 32 | 11790 | 1 | 1 | 1 | 1 | [training_core_b.md](training_core_b.md) |
| training_feature_handling_targets | 62 | 18040 | 0 | 0 | 4 | 3 | [training_feature_handling_targets.md](training_feature_handling_targets.md) |
| training_loose_a | 28 | 11172 | 0 | 1 | 2 | 5 | [training_loose_a.md](training_loose_a.md) |
| training_loose_b | 28 | 9950 | 0 | 2 | 2 | 3 | [training_loose_b.md](training_loose_b.md) |
| training_loose_c | 27 | 13505 | 0 | 5 | 5 | 5 | [training_loose_c.md](training_loose_c.md) |
| training_neural | 56 | 13619 | 2 | 1 | 3 | 2 | [training_neural.md](training_neural.md) |
| training_pipeline_misc | 50 | 16090 | 1 | 0 | 0 | 3 | [training_pipeline_misc.md](training_pipeline_misc.md) |
| votenrank | 38 | 4276 | 0 | 1 | 7 | 6 | [votenrank.md](votenrank.md) |
| x_architecture_api_consistency | 72 | 9500 | 0 | 2 | 2 | 4 | [x_architecture_api_consistency.md](x_architecture_api_consistency.md) |
| x_cicd_dependencies | 19 | 3016 | 0 | 5 | 2 | 1 | [x_cicd_dependencies.md](x_cicd_dependencies.md) |
| x_ml_correctness_meta | 35 | 9700 | 0 | 1 | 1 | 0 | [x_ml_correctness_meta.md](x_ml_correctness_meta.md) |
| x_oss_hygiene_packaging | 32 | 8085 | 0 | 0 | 8 | 6 | [x_oss_hygiene_packaging.md](x_oss_hygiene_packaging.md) |
| x_security_robustness | 1882 | 39158 | 2 | 0 | 3 | 3 | [x_security_robustness.md](x_security_robustness.md) |
| x_test_suite_architecture | 22 | 2000 | 0 | 2 | 4 | 1 | [x_test_suite_architecture.md](x_test_suite_architecture.md) |

## P0 (15)

| ID | Cluster | File:Line | Summary | Status |
|----|---------|-----------|---------|--------|
| FE_ROOT_B-1 | fe_root_b | src/mlframe/feature_engineering/spatial.py:138-141,192-227 | knn_aggregate's docstring promises automatic self-match exclusion ('query is ref is leak-safe by construction') but the non-group-filtered code path never removes a distance-0 self-match, only truncates to the first k of k+1 neighbours. | RESOLVED |
| FE_TRANSFORMER_A-1 | fe_transformer_a | src/mlframe/feature_engineering/transformer/_focal_loss_shared.py:15-31 | make_focal_objective's LightGBM focal-loss gradient/hessian formula is mathematically wrong (verified numerically via finite differences and symbolically via sympy against the loss the code itself computes) -- corrupts every model trained through focal_lgb.py and multi_aux_ensemble.py's focal=True path. | RESOLVED |
| FS_WRAPPERS-1 | fs_wrappers | src/mlframe/feature_selection/wrappers/rfecv/_nan_policy.py:151-155 | apply_nan_in_X_policy's default 'impute' path crashes (ValueError: could not convert string to float) on any object/category/string-dtype column containing NaN, when the configured core estimator(s) don't all natively tolerate NaN -- defeating the module's own documented 'graceful median-impute so RFECV no longer hard-crashes on NaN' contract. | RESOLVED |
| METRICS-1 | metrics | src/mlframe/metrics/calibration/_calibration_plot.py:667-850 | show_calibration_plot(backend="plotly", show_plots=False) raises UnboundLocalError on 'fig' (confirmed live) because `fig` is only assigned inside the matplotlib branch but returned unconditionally. | RESOLVED |
| METRICS-2 | metrics | src/mlframe/metrics/_core_precision_mape.py:136-157 | maximum_absolute_percentage_error crashes with ValueError on empty y_true/y_pred (np.nanmax on zero-size array) instead of returning NaN like every sibling metric in the file. | RESOLVED |
| METRICS-3 | metrics | src/mlframe/metrics/regression/_regression_corr.py:154-177 | fast_concordance_index computes (kendall_tau_b+1)/2, which only equals the true Harrell C-index when there are zero ties; with tied predictions (common for tree-ensemble risk scores) it is systematically wrong (verified: 0.9082 vs correct 0.8333 on a minimal example). | RESOLVED |
| PREPROCESSING-1 | preprocessing | src/mlframe/preprocessing/temporal_drift_augment.py:96-101 | augment_temporal_drift's expanding mean/std uses the catastrophic-cancellation-prone sum(x^2)-(sum(x))^2/n formula; on realistic large-offset/small-spread columns this goes negative, gets clipped to 0, and the augmented z-scored feature silently collapses to a constant 0.0 for every synthetic row (empirically reproduced: offset=1e9, scale=0.5 -> negative variance for 6/7 windows vs. a stable two-pass reference). | RESOLVED |
| PREPROCESSING-2 | preprocessing | src/mlframe/preprocessing/auto_transform_select.py:149-201 | select_column_transforms computes every non-identity candidate transform (all sklearn scalers, RankGauss, and the NaN-median fill) on the WHOLE column before the K-fold CV split, then scores it per fold on that pre-computed array, so the 'honest' CV score is optimistically biased for every candidate except identity/log1p_signed (confirmed: a scaler fit on the whole column vs. fit only on a train subset produces different stats on the held-out test subset). | RESOLVED |
| PREPROCESSING-5 | preprocessing | src/mlframe/preprocessing/cleaning.py:773-783 | analyse_and_clean_features's nunique==2 replacement branch only assigns repl_value for isinstance(real_val,str), col_is_numeric, or col_is_boolean with no final else, so a 2-valued categorical column whose real value is none of those (e.g. decimal.Decimal, pd.Timestamp) raises UnboundLocalError; reproduced live with a Categorical column holding one Decimal value plus None. | RESOLVED |
| TRAINING_CORE_B-2 | training_core_b | src/mlframe/training/core/_predict_main_suite.py:279-384 | predict_mlframe_models_suite (disk-loading predict entry point) lacks the CTE-RAW-X routing, the value-transform re-raise safety net, and the text/embedding passthrough that its sibling predict_from_models has via the shared _apply_pre_pipeline_with_passthrough helper -- composite-target (CompositeTargetEstimator) models loaded from disk silently predict in the wrong (residual/T) scale, with no error raised. | RESOLVED |
| TRAINING_NEURAL-1 | training_neural | src/mlframe/training/neural/base/_base_fit_prep.py:44-111 | partial_fit re-factorizes categorical columns from scratch on every incremental call, desyncing the frozen CategoricalEmbedding table's code assignment from batch to batch. | RESOLVED |
| TRAINING_NEURAL-2 | training_neural | src/mlframe/training/neural/_recurrent_cat_embeddings.py:91 | _apply_cat_codes maps a categorical column via .map(code_maps[col]) without casting to object first; on pandas Categorical dtype this crashes on .fillna(card) trying to add a new category -- the exact bug already fixed in the sibling flat-MLP implementation but never ported here. | RESOLVED |
| TRAINING_PIPELINE_MISC-1 | training_pipeline_misc | src/mlframe/training/pipeline/_composite_fe_shared.py:26-32 | attach_new_columns() joins new feature columns onto train/val/test by pandas index LABEL (df.join), but 6 of the 8 composite-FE sibling modules build the new-columns frame with a fresh RangeIndex(0..n-1) while the caller's df carries the original non-contiguous row-index left over from the upstream df.iloc[train_idx] split. The join silently produces NaN or cross-row-misattributed feature values for most rows on any pandas-backed run. | RESOLVED |
| X_SECURITY_ROBUSTNESS-1 | x_security_robustness | src/mlframe/training/feature_handling/providers.py:279-284 | EmbeddingProvider overrides __repr__ to scrub api_key/token/secret params but never overrides __str__, so str()/f-strings/%s-logging print the raw unscrubbed secret (verified live with a real credential value). | RESOLVED |
| X_SECURITY_ROBUSTNESS-2 | x_security_robustness | src/mlframe/estimators/pipelines.py:92-109 | replay_cv_results(fname, trusted_root=None) only enforces the joblib.load path-containment check when trusted_root is explicitly passed, unlike every sibling joblib.load call site in the codebase which fails closed by default; neither the sole production caller nor the sole test passes trusted_root, so the containment check never runs today. | RESOLVED |

## P1 (67)

| ID | Cluster | File:Line | Summary | Status |
|----|---------|-----------|---------|--------|
| CALIBRATION-1 | calibration | src/mlframe/calibration/group_bias_correction.py:20-35 | _canonical_group_key_series stringifies a genuine None/NaN group label to the literal string "None"/"nan" for non-floating (object/string) dtype arrays instead of preserving it as missing, so it silently becomes a real fitted group instead of being excluded, contradicting the function's own docstring and its own logged warning. | RESOLVED |
| CALIBRATION-2 | calibration | src/mlframe/calibration/policy.py:442-498 | _bootstrap_ece_with_indices's fused idx-aware bootstrap loop (used whenever n_bins is passed, i.e. always from pick_best_calibrator) feeds the njit kernel raw un-normalized y_true labels while the point estimate two lines above normalizes them via _normalize_binary_labels, so for non-{0,1}-encoded labels the bootstrap CI is computed on a different scale than the point estimate it should bracket. | RESOLVED |
| COMPETITION_EVALUATION-1 | competition_evaluation | src/mlframe/evaluation/group_leakage_guard.py:64 | assert_no_group_leakage never materializes cv_splits to a list despite its own docstring promising exactly that ('consumed eagerly... without exhausting it for the caller'); passing a real generator silently exhausts it, so a downstream CV loop reusing the same generator silently runs zero folds. | RESOLVED |
| COMPETITION_EVALUATION-2 | competition_evaluation | src/mlframe/evaluation/reports.py:289 | evaluate_estimators's classification_thresholds: Optional[list] param is used incompatibly across branches: .map() (needs dict-like) for classifiers vs. np.asarray() (needs array-like) for regressors; passing the documented list type to a classifier crashes with TypeError: 'list' object is not callable (reproduced end-to-end with a real LogisticRegression). | RESOLVED |
| COMPETITION_EVALUATION-3 | competition_evaluation | src/mlframe/evaluation/expanding_window_leakage.py:152-155 | detect_expanding_window_feature_leakage's leaky_row_ranges collapses each fold's leaked original-df positions to (min, max+1), which is only a valid contiguous range when df was already time-sorted; on unsorted input (the realistic case, since the function sorts internally) it reports a range spanning nearly the entire dataset instead of the actual leaked rows, contradicting the docstring's 'exact rows' claim (reproduced: 200-row fixture, every fold's reported range spans ~190-200 of 200 rows). | RESOLVED |
| COMPETITION_EVALUATION-4 | competition_evaluation | src/mlframe/evaluation/leak_scan.py:19-38 | scan_temporal_leak's _rank_columns skips tie-averaging, yet split_labels is documented as possibly 'a binary train=0/test=1 indicator' (maximal tie density, the module's primary use case); reproduced a ~0.13 absolute (~15% relative) systematic inflation vs. scipy.stats.spearmanr's tie-aware result (0.996 vs 0.865) on an identical leaky-feature fixture with binary split_labels. | RESOLVED |
| FE_ROOT_A-1 | fe_root_a | src/mlframe/feature_engineering/_numerical_numba.py:86-87,379-436 | compute_numerical_aggregates_numba silently returns a wrong-length list for empty (or recursively-degenerate) input, breaking the documented fixed-width contract with get_basic_feature_names() | RESOLVED |
| FE_ROOT_A-2 | fe_root_a | src/mlframe/feature_engineering/grouped.py:540-552 | per_group_rolling_reduce crashes with ValueError for op in {std,var,median,min,max} whenever min_periods < window_K and a group's length falls in [min_periods, window_K) | RESOLVED |
| FE_ROOT_A-3 | fe_root_a | src/mlframe/feature_engineering/binned_unique_count.py:99-102 | binned_unique_count crashes with ValueError ('bincount... non-negative') whenever entity_col contains any NaN/missing label alongside a valid value_col observation, because pd.factorize's -1 sentinel flows unmasked into np.bincount | RESOLVED |
| FE_ROOT_A-4 | fe_root_a | src/mlframe/feature_engineering/bayesian.py:675-709 | bocpd_features has no validation on NIG prior hyperparameters (kappa0/alpha0/beta0); alpha0=0.0 crashes with an unhandled ZeroDivisionError from inside the njit kernel, and the sibling online_bayesian_linear_regression silently returns all-NaN output for prior_precision<=0 with no warning | RESOLVED |
| FE_ROOT_A-5 | fe_root_a | src/mlframe/feature_engineering/anchor.py:227-300,303-376 | add_anchor_extrapolation_features (the module's primary/flagship function) has no numba-accelerated path, unlike all four sibling functions in the same file which were explicitly converted from Python list append/pop to preallocated njit buffers | RESOLVED |
| FE_ROOT_B-2 | fe_root_b | src/mlframe/feature_engineering/hurst.py:174-194 | _hurst_rs_single's docstring claims 'Same definition as compute_hurst_rs' but uses x.std() (ddof=0) while compute_hurst_rs explicitly uses ddof=1 sample std, a real formula divergence (~2.6% bias at K=20) between the rolling and multi-scale Hurst feature families. | RESOLVED |
| FE_ROOT_B-3 | fe_root_b | src/mlframe/feature_engineering/variance_gated_pairwise_diff.py:1-9,80-83 | Module docstring promises chunk-bounded peak memory, but pruning now happens up-front for ALL pairs via a covariance matrix and every surviving pair's diff column is accumulated into one dict before returning -- chunk_size no longer bounds peak memory, so a low variance threshold with many columns can still blow up to O(C(n_cols,2)*n_rows). | RESOLVED |
| FE_ROOT_B-4 | fe_root_b | src/mlframe/feature_engineering/spatial.py:762-775 | knn_gradient_features wraps a batched np.linalg.solve for ALL query rows in one try/except LinAlgError; one singular neighbourhood (e.g. duplicate/quantized coordinates) sets gradient features to NaN for the ENTIRE batch, not just the offending row. | RESOLVED |
| FE_ROOT_B-5 | fe_root_b | src/mlframe/feature_engineering/timeseries.py:566-592 | create_windowed_features's insufficient-window skip-guard is asymmetric: without targets_creation_fcn it checks an exact expected-window count, but with targets_creation_fcn set it only checks non-emptiness of future_windows_features, so a partially-satisfied future window set silently proceeds instead of being skipped. | RESOLVED |
| FE_ROOT_B-6 | fe_root_b | src/mlframe/feature_engineering/multi_window_aggregate.py:84-101 | Windowed sum/mean features are computed as the difference of two independently-computed CUMULATIVE aggregates (upper - lower); when history span greatly exceeds the horizon, this subtracts two large nearly-equal numbers, risking catastrophic cancellation -- the same bug class this codebase has repeatedly fixed elsewhere in subtractive derivations. | RESOLVED |
| FE_ROOT_B-DISCOVERED-1 | fe_root_b | src/mlframe/feature_engineering/timeseries.py:656-679 (create_and_process_windows) | Discovered while fixing FE_ROOT_B-5: the window_var (cumsum-based) branch pre-sliced window_var_values by base_point, then passed windows_l/windows_r (ABSOLUTE df row indices, also used directly as df.iloc bounds) as find_next_cumsum_*_index's left_index/right_index, which index INTO that already-offset array -- double-offsetting the search for any base_point > 0 (virtually always in a real walk), silently truncating or entirely dropping windows that should have succeeded. | RESOLVED |
| FE_TRANSFORMER_A-2 | fe_transformer_a | src/mlframe/feature_engineering/transformer/disagreement_band.py:41-74 | Band/anchor assignment uses in-sample baseline predictions (fit-then-predict on the same rows) to rank row hardness/disagreement, biasing which rows become anchors; sibling file bidir_residual_band.py already diagnosed and fixed this exact issue via inner-KFold OOF but the fix wasn't propagated here (also affects class_balanced_hard_row.py and fisher_weighted_residual.py in this cluster). | RESOLVED (6ca0320d1) |
| FE_TRANSFORMER_A-3 | fe_transformer_a | src/mlframe/feature_engineering/transformer/band_conditional_anchor.py:140 | Naive Xq_s[:, None, :] - anchors[None, :, :] broadcast materializes an (n_query, n_anchors, d) cube instead of using the GEMM-decomposition squared_dists helper already used by sibling files anchor_attention.py / class_conditional_anchor.py in the same package; recurs in bidir_residual_band.py:151, class_balanced_hard_row.py:162, disagreement_band.py:151 -- real OOM risk at the codebase's documented 100+GB / d<=32768 production scale. | RESOLVED |
| FE_TRANSFORMER_B-1 | fe_transformer_b | src/mlframe/feature_engineering/transformer/performer_attention.py:48-59 | Per-row numerical-stability shift applied to the key-side (training) Performer random features silently reweights each training row's contribution to the linear-attention aggregate by an arbitrary factor, corrupting the kernel approximation. | RESOLVED (27cad4817) |
| FE_TRANSFORMER_B-2 | fe_transformer_b | src/mlframe/feature_engineering/transformer/multi_aux_ensemble.py:45-72,138-140 | The documented 'quantile-loss LightGBM' third aux model for regression is never implemented; _fit_aux_lgb's focal=True branch only special-cases task=='binary', so the regression 'focal' model is a near-duplicate of the first aux model (same hyperparams, seed+1), silently weakening the disagreement feature. | RESOLVED |
| FS_BENCHMARKS_A-1 | fs_benchmarks_a | src/mlframe/feature_selection/_benchmarks/_datasets.py:30-77 | Every consumable name in _datasets.py (SCENARIOS, CPU_SCENARIOS, GPU_SCENARIOS, make_scenario_data, list_scenarios) is defined inside `if __name__ == "__main__":`, so on import (as bench_mrmr.py does) none of them exist, and the package's own documented entry point (`python -m ...bench_mrmr --scenarios all`) crashes with AttributeError. | RESOLVED (27cad4817) |
| FS_BENCHMARKS_B-1 | fs_benchmarks_b | src/mlframe/feature_selection/_benchmarks/bench_mrmr_fe_path_micro.py:102-106 | np.asarray(df, dtype=float) returns a view (not a copy) into the DataFrame's backing buffer; the subsequent in-place a[np.isnan(a)]=0.0 silently mutates the shared df fixture, so all but the first timed call skip the NaN-scrub work, invalidating the benchmark's own reported speedup. | RESOLVED (4abaf47b4) |
| FS_BENCHMARKS_C-1 | fs_benchmarks_c | src/mlframe/feature_selection/_benchmarks/wide_data_scaling/_progress_shared.py:8,13 | ck() writes to a hardcoded D:/Temp/synergy_scale_bench/progress.txt with no directory creation, so it crashes with FileNotFoundError on any host/session where that folder does not already exist, aborting long h1_bench.py/h2_bench.py sweeps immediately. | RESOLVED |
| FS_BORUTA_ROOT-1 | fs_boruta_root | src/mlframe/feature_selection/boruta_shap/__init__.py:753-757 | TentativeRoughFix() computes its accept/reject medians over self.history_x including the phantom all-zero initializer row (row 0), biasing the tentative-feature decision; _io_plot.py's results_to_csv/plot explicitly strip this same row via .iloc[1:] but TentativeRoughFix does not. | RESOLVED |
| FS_WRAPPERS-2 | fs_wrappers | src/mlframe/feature_selection/wrappers/rfecv/_fit_init.py:182-191 | The minority-class-vs-cv validation is gated on y_arr.dtype.kind in "iu" and silently skipped for float-dtype y (even integer-valued labels), so the intended clear early ValueError never fires and fit crashes deep inside sklearn instead. | RESOLVED (27cad4817) |
| FS_WRAPPERS-3 | fs_wrappers | src/mlframe/feature_selection/wrappers/_knockoffs.py:184-200 | select_features_fdr is O(p^2) (nested Python loops scanning W.values() per candidate threshold) -- measured 6.4s at p=5000, contradicting _validate.py's own p>=5000 warning that recommends this exact function for 'sub-second selection on this scale'. | RESOLVED |
| METRICS-4 | metrics | src/mlframe/metrics/calibration/_calibration_metrics.py:432-481 | integral_calibration_error_from_metrics accepts calibration_coverage (always passed by every caller) but never uses it in the formula — confirmed live that varying it 0.0/0.1/0.9 gives identical ICE; traced via git log -S to the function's original 2024 form, so this predates all module splits. | RESOLVED (27cad4817) |
| MODELS-1 | models | src/mlframe/models/_optimization_search.py:613,646,377,543 | MBHOptimizer.submit_evaluations force-casts known_candidates to int, corrupting/mis-tracking a continuous (non-integer) search_space and causing re-suggestion of already-evaluated points. | RESOLVED |
| MODELS-2 | models | src/mlframe/models/ensembling/score_flavours.py:366-477 | apply_quality_gate_kn slices level_models_and_predictions to gate survivors but returns the tag lists unsliced, breaking positional alignment used by compute_high_correlation_pairs / apply_diversity_drop downstream. | RESOLVED |
| MODELS-3 | models | src/mlframe/models/tuning.py:940,947,967,993-998 | CatboostParamsOptimizer's rule DSL references grow_policy/model_shrink_mode as required companion fields but never adds them to self.params, permanently vetoing boosting_type=='Ordered' and posterior_sampling=True from the HPT search space. | RESOLVED |
| MODELS-9 | models | src/mlframe/models/additive_interaction_diagnostic.py:93-97 | additive_interaction_diagnostic's _cv_score indexes y[train_idx]/y[test_idx] directly; for a pandas Series y with a non-default index (the common real-world case), this performs label-based indexing instead of positional, silently misaligning train/test rows. | RESOLVED |
| PREPROCESSING-3 | preprocessing | src/mlframe/preprocessing/sibling_group_cold_start_fill.py:69-75 | interpolate=True fills a missing sibling group by interpolating over the POSITIONAL rank of groups, not the actual order_col value distance the docstring promises; reproduced with groups at order=(0,1,100) and values=(10,NaN,50) -- the missing middle group (order=1, near order=0) is filled with 30.0 (positional midpoint) instead of a value near 10. | RESOLVED |
| PREPROCESSING-4 | preprocessing | src/mlframe/preprocessing/category_support.py:26-51 | smoothed_target_encode_column computes train_encoded via y_train.groupby(train_series) over the same rows it then maps back onto -- every row's own target informs its own category's shrunk mean (in-sample, non-OOF target encoding), with no leakage-warning docstring unlike every other stateful-statistic function in this package. | RESOLVED |
| PREPROCESSING-6 | preprocessing | src/mlframe/preprocessing/gaussian_power_transform_search.py:55-68, 200-220 | apply_gaussian_power_transform's Box-Cox replay path has no positivity guard (unlike the fit phase's explicit np.any(x<=0) check); reproduced: applying a fitted Box-Cox transform to inference data containing 0.0/-1.0 returns those values UNCHANGED (raw, untransformed), silently mixed into an otherwise properly-transformed column with no error or NaN signal. | RESOLVED |
| REPORTING_A-1 | reporting_a | src/mlframe/reporting/_diagnostics_dispatch_extra.py:482-512 (esp. 503) | _first_group_column's categorical-dtype detector is broken: `dt is object` never matches a real pandas object-dtype column (identity vs equality), and there is no polars dtype branch at all. | RESOLVED |
| REPORTING_A-2 | reporting_a | src/mlframe/reporting/_diagnostics_dispatch_extra.py:474-479 | _ranked_top_features picks NaN-importance features as top-ranked because np.argsort(...)[::-1] sorts NaNs last ascending, first after reversal (empirically confirmed). | RESOLVED |
| REPORTING_A-3 | reporting_a | src/mlframe/reporting/_diagnostics_dispatch_extra.py:216-226 (build_combined_html_report) | Combined-HTML-report image resolver only tries <base>.png and <base>.matplotlib.png, but renderers/save.py's naming policy produces <base>.plotly.png for any multi-format/multi-backend plot_outputs (e.g. the package's own docstring example 'plotly[html,png] + matplotlib[pdf]'), so it silently embeds broken <img> links while still recording success. | RESOLVED (e1c5540d8) |
| REPORTING_A-4 | reporting_a | src/mlframe/reporting/_risk_coverage_diagnostic.py; src/mlframe/reporting/_diagnostics_dispatch_extra.py:whole file; render_model_card_diagnostic/render_split_comparison_from_suite/render_decile_table_diagnostic | render_risk_coverage_diagnostic, render_model_card_diagnostic, render_split_comparison_from_suite, and render_decile_table_diagnostic (the dispatch/wiring layer) have zero direct test coverage; existing tests with similar names only exercise the underlying charts.* composer functions. | RESOLVED (74bb765e6) |
| REPORTING_B-1 | reporting_b | src/mlframe/reporting/charts/error_analysis.py:310,316 (+ charts/multiclass.py:549, charts/multilabel.py:208) | weak_segment_heatmap digitizes a feature column that still contains NaN (by design, per its own docstring); numpy sorts NaN after every finite value, so np.digitize silently places every NaN row in the HIGHEST bin instead of excluding/flagging it, corrupting the worst-slice localization. Same missing-finite-filter pattern recurs in multiclass/multilabel _calib_grid_panel's digitize(proba_k, ...) calls. | RESOLVED (2ea4c762a) |
| TRAINING_COMPOSITE_CORE_A-1 | training_composite_core_a | src/mlframe/training/composite/_estimator_dispatch.py:73-116,183 | _pick_base_column(train_df, y_train) receives the unfiltered full train_df while y_train is already sliced to train_idx, so every column length-check fails and the whole E3 distribution-driven-estimator injection feature silently never fires whenever there is a real train/val/test split. | RESOLVED (7b5a3375e) |
| TRAINING_COMPOSITE_CORE_A-2 | training_composite_core_a | src/mlframe/training/composite/gated_outlier.py:129 | Row-subsetting falls back to np.asarray(X)[mask] when X lacks .loc (e.g. a polars DataFrame), silently flattening it to a raw/object-dtype ndarray and breaking fit/predict feature-name and dtype consistency; confirmed to emit a real sklearn feature-name mismatch warning and to produce mixed-type object arrays on any non-numeric polars column, unlike sibling modules (bagging.py, classification.py, glm.py) which handle polars explicitly. | RESOLVED (57ada73f0) |
| TRAINING_COMPOSITE_CORE_A-2b | training_composite_core_a | src/mlframe/training/composite/gated_regression_mixture.py:153,167 | Same bug class as -2 in a sibling file: fit()/_predict_branch() row-subset via np.asarray(X)[mask] when X lacks .iloc, silently down-converting a polars DataFrame (which _concat_feature a few lines away DOES handle correctly, showing polars support was intended) to an untyped/object ndarray. | RESOLVED (f46180507) |
| TRAINING_COMPOSITE_CORE_B-1 | training_composite_core_b | src/mlframe/training/composite/qrf.py:71-97,294-319 | _leaf_weights_kernel / _LeafResidualForest._leaf_weights compute QRF membership weights via a full O(n_train) linear scan per (query, tree) pair instead of a leaf-bucketed lookup, giving O(n_query*n_trees*n_train) predict cost instead of O(n_query*n_trees*avg_leaf_size). | RESOLVED (1c02d22b5) |
| TRAINING_COMPOSITE_CORE_B-2 | training_composite_core_b | src/mlframe/training/composite/pseudo_labeling.py:242-248 | _fit_final uses `except TypeError: model.fit(X, y)` to retry without sample_weight -- the exact anti-pattern post_shim.py (same directory) documents fixing; a TypeError from any other cause silently drops the confirmation-bias down-weighting of pseudo-labeled rows with no warning. | RESOLVED (2c8ff4204) |
| TRAINING_COMPOSITE_ENSEMBLE_ESTIMATOR_TRANSFORMS-1 | training_composite_ensemble_estimator_transforms | src/mlframe/training/composite/estimator/_predict.py:358-536 | predict_quantile() never calls _soft_shrink.compute, so the default-ON out-of-range-base soft-shrink/smart-fallback protection that predict() applies is silently absent from every quantile/interval prediction. | RESOLVED (1b621c29d) |
| TRAINING_CORE_A-1 | training_core_a | src/mlframe/training/core/_main_train_suite_target_distribution.py:74-78 | Near-duplicate-feature auto-drop's greedy chain-collapse can drop every column in a 3+ way mutually-correlated cluster (a triangle in the correlation graph), leaving zero survivors, instead of keeping exactly one. | RESOLVED (919370181) |
| TRAINING_CORE_B-1 | training_core_b | src/mlframe/training/core/_phase_train_one_target_body.py:818-863 (mirrored in _phase_train_one_target_weight_iteration.py:387-437) | The identity-equivalent-pre_pipeline dedup 'break' exits the weight loop before _build_and_record_model_schema / pipeline_cache.set run, so the model that just trained gets no metadata['model_schemas'] entry, silently disabling predict-time schema-drift hard-fail protection for it. | RESOLVED (aa4fa7939) |
| TRAINING_LOOSE_A-1 | training_loose_a | src/mlframe/training/_data_helpers.py:342-355 | _validate_target_values silently swallows its own empty-classification-target diagnostic: the raise ValueError(...) message-format expression indexes arr_np.flat[0] on a 0-length array, raising IndexError before ValueError is constructed; the except ValueError: raise / except Exception: log.debug pair catches the IndexError in the generic branch and returns normally instead of raising anything. | RESOLVED (054aabb9c) |
| TRAINING_LOOSE_B-1 | training_loose_b | src/mlframe/training/_split_helpers.py:157 | Bare `except Exception:` around the njit multilabel-stratification kernel call has zero logging and silently falls back to the ~50x slower pure-Python iterstrat path on any failure, including a genuine bug in the njit kernel. | RESOLVED (cb0b8e336) |
| TRAINING_LOOSE_B-2 | training_loose_b | src/mlframe/training/_trainer_configure.py:143-181 | _get_training_configs_cached keys its process-lifetime cache partly on id(indexed_subgroups), a freshly-built short-lived dict; CPython id-reuse after GC of the previous target's dict can produce a stale cache hit mixing one target's fairness-subgroup-derived calibration metric config into another target's result. | RESOLVED (c7ad2f64c) |
| TRAINING_LOOSE_C-1 | training_loose_c | src/mlframe/training/trainer.py:258 | _compute_oof_preds constructs GroupKFold(n_splits=1) outside its try/except when a train fold has only 1 distinct group, raising an uncaught ValueError that crashes the whole per-model training call after the model already trained. | RESOLVED (96b5cd751) |
| TRAINING_LOOSE_C-2 | training_loose_c | src/mlframe/training/models.py:312-323 | _build_elasticnet_classifier silently ignores the user's configured l1_ratio and always uses the hardcoded fallback 0.15, because _get_l1_ratio gates on config.penalty=='elasticnet' which is never true (penalty defaults to 'l2' and nothing derives it from model_type='elasticnet'). | RESOLVED |
| TRAINING_LOOSE_C-3 | training_loose_c | src/mlframe/training/lgb_shim.py:519-582 | On a train-Dataset cache hit, lgb_shim's fit() re-applies set_label/set_weight but never set_init_score, so a subsequent .fit(X, y, init_score=new_value) call reusing the cached Dataset silently trains against the stale/absent init_score instead of the new one. | RESOLVED (921a49e23) |
| TRAINING_LOOSE_C-4 | training_loose_c | src/mlframe/training/lgb_shim.py:646-649 | On a val-Dataset cache hit, lgb_shim only calls dval.set_weight() when w_val is not None; when a later call omits the eval-set weight after an earlier call supplied one, the reused Dataset keeps the stale weight instead of resetting to uniform -- asymmetric with the train-Dataset path in the same file and with xgb_shim.py's equivalent val-DMatrix handling. | RESOLVED (e41ff2ce5) |
| TRAINING_LOOSE_C-5 | training_loose_c | src/mlframe/training/honest_diagnostics.py:326-338 | _calibration_block falls back to model_entry.test_target when oof_target is absent but oof_probs is present, then positionally truncates and zips oof_arr[:n] against test-split y_arr[:n] -- pairing train-row-ordered OOF probabilities with unrelated test-split labels, the exact leakage/misalignment bug class evaluation.py's post_calibrate_model explicitly guards against elsewhere in the same codebase. | RESOLVED |
| TRAINING_NEURAL-3 | training_neural | src/mlframe/training/neural/_muon_optimizer.py:191-217 | MuonAdamWHybrid.param_groups is a disconnected placeholder list, not delegated to the wrapped Muon/AdamW optimizers, so any torch LR scheduler attached via configure_optimizers silently has zero effect on the real learning rates. | RESOLVED (e7f00cbb7) |
| VOTENRANK-1 | votenrank | src/mlframe/votenrank/shapley_blend.py:192-205 | shapley_blend's degenerate-fallback branch silently returns an all-zero ensemble_pred (and all-zero weights) while still reporting a 'selected' model, when every model's raw Shapley value is <= 0. | RESOLVED (a6aa239d1) |
| X_ARCHITECTURE_API_CONSISTENCY-1 | x_architecture_api_consistency | src/mlframe/__init__.py:9-13 | 3 of 8 example imports in the root package's 'public API convention' docstring are broken ImportErrors against the current tree (MRMR, expected_calibration_error, predict_from_models). | RESOLVED (fc95f8cc9) |
| X_ARCHITECTURE_API_CONSISTENCY-2 | x_architecture_api_consistency | training/composite/direct_multi_horizon.py:196 | 10 of 35 sibling training/composite/*.py BaseEstimator regressors (direct_multi_horizon, dual_direction, orthogonal, per_group_router, regime_split_ensemble, segmented_model_factory, feature_subset_bagging, gated_regression_mixture, meta, simplex) lack sample_weight in fit() while 11+ siblings in the same package accept it, causing a hard TypeError for any caller threading sample_weight uniformly across the family. | RESOLVED (682e6cbc4) |
| X_CICD_DEPENDENCIES-1 | x_cicd_dependencies | .github/workflows/ci.yml:181,262 | 6 workflow files (ci.yml x2, mypy-full.yml, gpu-matrix.yml, gpu-extras-install-matrix.yml, sklearn-matrix-ci.yml, numba-coverage.yml) install sibling dep pyutilz via unpinned `git clone --depth 1` (default branch HEAD, no SHA) instead of a pinned commit, reproducing a documented prior incident class (X_SECURITY_API_PACKAGING-5) where an unpinned pyutilz commit silently broke MRMR.fit() for every caller. | RESOLVED (c3ffeb970) |
| X_CICD_DEPENDENCIES-2 | x_cicd_dependencies | pyproject.toml:405-407 | pyproject.toml's dev extra pins ruff==0.16.0 with a comment claiming it matches .pre-commit-config.yaml's ruff-pre-commit rev, but all three ruff-pre-commit hooks there are pinned to v0.15.22 -- the exact stale-pin drift class the prior audit's F1 finding already fixed once, now recurred. | RESOLVED (f6be483b2) |
| X_CICD_DEPENDENCIES-3 | x_cicd_dependencies | .github/workflows/dependabot-auto-merge.yml:32-54 | The auto-merge eligibility regex requires a single 'from X.Y.Z to A.B.C' pair in the PR title, but dependabot.yml groups both ecosystems' routine bumps into one PR (grouped titles contain no such pair), so auto-merge silently never fires for the majority of PRs it exists to handle. | RESOLVED (daf91e3f5) |
| X_CICD_DEPENDENCIES-4 | x_cicd_dependencies | .pre-commit-config.yaml:618-654 | The mypy-full-manual hook still uses --cache-dir=../.mlframe_mypy_cache_shared (cross-worktree shared cache), the exact pattern two sibling mypy hooks in the same file were moved off on 2026-08-03 after proving it can make a known-buggy revision silently report 'Success'. | RESOLVED (0f6d9d5b0) |
| X_CICD_DEPENDENCIES-5 | x_cicd_dependencies | pyproject.toml:816 | [tool.ruff] extend depends on $PY_CI_SHARED_DIR, which is never set anywhere in the repo (no workflow, no .pre-commit-config.yaml, no CONTRIBUTING.md mention) -- a fresh contributor following the documented setup breaks on the very first pre-commit ruff hook. | RESOLVED (2082e3a59) |
| X_ML_CORRECTNESS_META-1 | x_ml_correctness_meta | src/mlframe/training/honest_diagnostics.py:326-338 | _calibration_block falls back from oof_target to test_target and positionally pairs oof_probs (train-row order) with test_target (a disjoint row set), producing a silently meaningless calibration/ECE diagnostic reported as status=ok. | RESOLVED |
| X_TEST_SUITE_ARCHITECTURE-1 | x_test_suite_architecture | tests/training/test_stress.py:449-484 | test_many_nan_values's sole assertion sits inside a try/except Exception: pass, so AssertionError itself is swallowed -- the test cannot fail regardless of code correctness. | RESOLVED (8adbea9bf) |
| X_TEST_SUITE_ARCHITECTURE-2 | x_test_suite_architecture | tests/training/neural/test_integration.py:376-390 (and tests/training/neural/test_multigpu.py:318-331) | test_best_weights_restoration / test_load_best_weights_in_ddp explicitly enable load_best_weights_on_train_end but their whole assertion body is 'if hasattr(model,"best_epoch"): assert best_epoch is not None' -- zero assertions execute if the attribute is absent, and even when present nothing verifies weights were actually restored. | RESOLVED (12acdc5c4) |

## P2 (109)

| ID | Cluster | File:Line | Summary | Status |
|----|---------|-----------|---------|--------|
| CALIBRATION-3 | calibration | src/mlframe/calibration/sticky_state_persistence_floor.py:44-61 | apply_sticky_state_persistence_floor never validates that a per-class floor vector's length equals k (number of classes); a mismatched length causes an opaque IndexError from floor_arr[active] instead of a clear ValueError, unlike every other shape check in this function. | RESOLVED (b58b94b40) |
| CALIBRATION-4 | calibration | src/mlframe/calibration/policy.py:600-651 | _emit_reliability_plot's build_reliability_overlay_spec call is not wrapped in try/except (only render_and_save is), and pick_best_calibrator calls _emit_reliability_plot with no enclosing handler, so a spec-build failure crashes the whole calibrator-selection call even though selection already succeeded and plotting is documented as an opt-in, failure-tolerant diagnostic. | RESOLVED (01d15eed9) |
| CALIBRATION-5 | calibration | src/mlframe/calibration/policy.py:411-418 | _build_resample_indices's non-stratified branch fills the (n_bootstrap, n) index matrix via a Python loop calling rng.integers once per row instead of a single vectorized rng.integers(0, n, size=(n_bootstrap, n)) call, unlike the stratified branch which has an explicit justification for its own loop. | RESOLVED (b3f9b97e7) |
| COMPETITION_EVALUATION-5 | competition_evaluation | src/mlframe/evaluation/constant_group_leak_scan.py:130-131 | constant_group_target_scan(df, y, candidate_cols=[]) raises KeyError: 'min_group_variance_ratio' (confirmed) instead of returning an empty, correctly-columned DataFrame, because pd.DataFrame([]) has no columns for .sort_values(...) to find. | RESOLVED (29078d872) |
| COMPETITION_EVALUATION-6 | competition_evaluation | src/mlframe/evaluation/subpopulation_drift.py:96-97 | subpopulation_ratio_drift_check raises KeyError: 'prevalence_ratio' (confirmed) when train_df/test_df have zero effective values for subgroup_col (e.g. both empty), same pd.DataFrame(rows).sort_values(...)-without-empty-guard pattern as COMPETITION_EVALUATION-5. | RESOLVED (29078d872) |
| COMPETITION_EVALUATION-7 | competition_evaluation | src/mlframe/evaluation/subpopulation_drift.py:157-158 | rank_subpopulation_drift_severity(train_df, test_df, subgroup_cols=[]) raises KeyError: 'drift_severity_score' (confirmed) instead of returning an empty ranking. | RESOLVED (171c872d6) |
| COMPETITION_EVALUATION-8 | competition_evaluation | src/mlframe/evaluation/subgroup_feature_overfit_risk.py:165-166 | rank_subgroup_feature_overfit_risk(train_df, test_df, candidates=[]) raises KeyError: 'risk_score' (confirmed) instead of returning an empty ranking -- 4th independent occurrence of this same bug class in this cluster. | RESOLVED (171c872d6) |
| COMPETITION_EVALUATION-9 | competition_evaluation | src/mlframe/evaluation/_bootstrap_fused_binary_bundle.py:288-320 | bootstrap_auc_brier_ll_ece_batch drops non-finite per-resample values with no failure-count logging, unlike its documented 'bit-identical' sibling bootstrap_metrics which warns once failures exceed 25%; AUC can legitimately go NaN on unstratified resamples of imbalanced binary targets, so this fast path (now the default route per project history) silently loses the failure-rate diagnostic exactly where class imbalance matters most. | RESOLVED (9ebefbf0d) |
| CORE_INFRA_MISC-1 | core_infra_misc | src/mlframe/signal/hull_moving_average.py:34-41 | _sma_from_cumsum slices cumsum[:-window]; window=0 hits Python's arr[:-0]==arr[:0] trap, causing a shape-mismatch crash instead of a clear validation error. | RESOLVED (03f7a1939) |
| CORE_INFRA_MISC-2 | core_infra_misc | src/mlframe/inference/native_gpu_shap.py:42-51 | native_gpu_shap_available only catches AttributeError around get_booster(), but XGBoost raises NotFittedError for an unfitted model, so the documented 'never raises, returns False' contract is broken. | REJECTED -- NotFittedError already subclasses AttributeError in installed sklearn, contract already holds (17e003833 adds regression coverage) |
| CORE_INFRA_MISC-3 | core_infra_misc | src/mlframe/data_valuation/_knn_shapley.py:25-38,57-67 | knn_shapley's k parameter is never validated; k<=0 divides by k inside the fastmath njit recursion, silently producing inf/nan Shapley values instead of raising. | RESOLVED (629c4a84c) |
| CORE_INFRA_MISC-4 | core_infra_misc | src/mlframe/estimators/pipelines.py:246,258-267 | visualize_prediction_vs_truth calls plt.subplots(1, len(samples)); when len(samples)==1 matplotlib returns a bare Axes (not an array), so axs[i] indexing raises TypeError. | RESOLVED (56357a871) |
| FE_ROOT_A-6 | fe_root_a | src/mlframe/feature_engineering/gmm_bic_membership_features.py:96-109 | gmm_shift_diagnostics' shift_zscore is one-directional (only flags new_avg_loglik < train's), silently missing distribution shift toward HIGHER likelihood (e.g. new data collapsing near one component) | RESOLVED (d4766589b) |
| FE_ROOT_A-7 | fe_root_a | src/mlframe/feature_engineering/_recursion_autotune.py:148-166 | Module-level kernel_tuner registration loop has no try/except despite an adjacent comment claiming it is 'wrapped so a missing pyutilz / circular import never breaks the dispatcher' - the actual safety net lives only in the caller (_recursion_dispatch.py) | RESOLVED (4109dae26) |
| FE_ROOT_B-10 | fe_root_b | src/mlframe/feature_engineering/stationarity.py:119-136 | _frac_diff_single zero-fills any NaN in the input before convolving, so a NaN past the first K rows silently corrupts every downstream window that touches it (substituting 0, not propagating NaN); the public frac_diff docstring never documents this. | RESOLVED (43a1f8dac) |
| FE_ROOT_B-7 | fe_root_b | src/mlframe/feature_engineering/numerical.py:490-507 | compute_numaggs's len(arr)<=1 short-circuit always returns a raw Python NaN-tuple regardless of return_float32, while the normal path returns an np.float32 ndarray when return_float32=True -- a return-type inconsistency for degenerate short-input rows. | RESOLVED (c8ab19127) |
| FE_ROOT_B-8 | fe_root_b | src/mlframe/feature_engineering/recency_density.py:79,104-105,180-222 | n_grid has no minimum-value validation; n_grid=1 triggers division by (n_grid-1)==0 inside the njit KDE grid construction, silently producing inf/NaN grid points instead of a clear error. | RESOLVED (096926c38) |
| FE_ROOT_B-9 | fe_root_b | src/mlframe/feature_engineering/nadaraya_watson.py:89-120 | Both flat and per-group NW smoothers scan the full reference set for every query (O(n_query*n_train)) even for the three compact-support kernels (epanechnikov/boxcar/tricube), which are provably zero outside the bandwidth -- no windowed/two-pointer fast path exploits this. | RESOLVED (7c9d5344a) |
| FE_TRANSFORMER_A-4 | fe_transformer_a | src/mlframe/feature_engineering/transformer/adasyn_smote.py:51 | _adasyn_synthesize's NearestNeighbors(n_neighbors=k_global+1).fit(X_full) is never capped to X_full.shape[0], unlike every other kNN call in this package; crashes with ValueError on small folds/subsets. | RESOLVED (3ca2f8623) |
| FE_TRANSFORMER_A-5 | fe_transformer_a | src/mlframe/feature_engineering/transformer/conformal_locally_adaptive.py:83-92 | Output column names are hardcoded to _width_a01/_width_a02 regardless of the actual configurable alphas parameter values; mislabels columns for non-default alphas and IndexErrors/silently drops columns when len(alphas) != 2. | RESOLVED (00e7198b2) |
| FE_TRANSFORMER_A-6 | fe_transformer_a | src/mlframe/feature_engineering/transformer/cross_feature_reconstruction.py:19-64 | Module docstring claims 'OOF LGB' reconstruction but _process fits and scores in-sample (same rows), understating the MAD residual scale and inflating downstream z-residual outlierness features; sibling files in the same cluster implement the OOF fix for the identical reason. | RESOLVED (81744435a) |
| FE_TRANSFORMER_B-3 | fe_transformer_b | src/mlframe/feature_engineering/transformer/geodesic_kgraph.py:66-74 | kNN graph construction queries a NearestNeighbors index fit on Xt_s with Xt_s itself using n_neighbors=k_graph (not k_graph+1), so each row's self-match occupies one of its k_graph slots, silently weakening graph connectivity vs. the correct pattern used in sibling files (local_density_gradient.py, spectral_attention.py's _build_knn_graph). | RESOLVED (d8a4c275a) |
| FE_TRANSFORMER_B-4 | fe_transformer_b | src/mlframe/feature_engineering/transformer/mdl_binning_pairwise.py:211-219 | Pairwise bin-combo encoding hardcodes bin0*100+bin1 with no validation against max_bins_per_feat (a public, caller-settable parameter); >=100 bins per feature silently collides distinct (bin0,bin1) pairs into the same combo code, corrupting combo_count/n_unique_combos features. | RESOLVED (13a3da04b) |
| FE_TRANSFORMER_B-5 | fe_transformer_b | src/mlframe/feature_engineering/transformer/pairwise_kl_divergence.py, nn_oof_target_mean.py, ib_baseline_codes.py, gradient_direction_agreement.py, multi_baseline_hard_row.py:multiple | Five files each independently duplicate a near-identical _fit_3baselines* helper (LGB depth-3 + LGB depth-5 + LogisticRegression/Ridge with try/except fallback); the duplication has already produced observable drift -- inconsistent log levels (info vs debug) and materially different degenerate-fold fallback semantics (zero-gradient vs class-prior) for the identical LogisticRegression-failure condition. | RESOLVED (089850a91) |
| FS_BENCHMARKS_A-2 | fs_benchmarks_a | src/mlframe/feature_selection/_benchmarks/bench_hermite_fe.py:216-217 | _legacy_hermite draws length_a/length_b via unseeded global np.random.randint(3, 8) while every other RNG use in the same file (data gen, optuna TPESampler seed=42, optimise_hermite_pair seed=42) is fully seeded, so the printed 'legacy' baseline MI is non-deterministic across runs. | RESOLVED (42f23b603) |
| FS_BENCHMARKS_B-2 | fs_benchmarks_b | src/mlframe/feature_selection/_benchmarks/fs_hybrid/run_experiment.py:166 | row["auc_mean"] is gated on any(aucs.values()) instead of any(v is not None for v in aucs.values()); if one model produces a legitimate AUC of exactly 0.0 while the others failed (None), the truthy check misfires and the real result is silently dropped as None. | RESOLVED (f4f643fdb) |
| FS_BENCHMARKS_C-2 | fs_benchmarks_c | src/mlframe/feature_selection/_benchmarks/kernel_tuning_cache/_auto_tune_sweeps_a.py:937 | Legacy ensure_batch_pair_mi_tuning/_run_sweep_batch_pair_mi (and 3 siblings in _auto_tune_sweeps_b.py) remain fully implemented and exported after migration to the new pyutilz kernel_tuner registry; cli.py's own docstring documents that this exact legacy sweep 'silently shadowed the new dispatcher' by writing to the same cache key without a code_version, yet the dangerous function is still reachable by anyone who imports it directly instead of going through the CLI. | RESOLVED (5aa310c9d) |
| FS_BENCHMARKS_C-3 | fs_benchmarks_c | src/mlframe/feature_selection/_benchmarks/wide_data_scaling/h1_bench.py:10-11 | h1_bench.py, h1_gpu_large.py, and h2_bench.py all sys.path.insert(0, r"D:/Upd/Programming/PythonCodeRepository/mlframe/src") — a dev-machine-specific absolute path that, if present but stale on some machine, silently shadows the properly installed mlframe package for the whole process. | RESOLVED (81fcd36d2) |
| FS_BORUTA_ROOT-2 | fs_boruta_root | src/mlframe/feature_selection/boruta_shap/_shadow_stats.py:284-309 | find_sample() starts its KS-test sample search at element=1 (~10% of rows) instead of 0 (~5%), contradicting its own docstring, and raises IndexError on frames with <=2 rows under sample=True because get_5_percent_splits can return an array shorter than 2 elements. | RESOLVED (2c1c809e8) |
| FS_BORUTA_ROOT-3 | fs_boruta_root | src/mlframe/feature_selection/general.py:206-210 | estimate_features_relevancy divides by len(feature_columns) with no zero guard; a bins frame containing only target columns (no candidate features) raises ZeroDivisionError instead of returning an empty/graceful result. | RESOLVED (8d924e66d) |
| FS_WRAPPERS-4 | fs_wrappers | src/mlframe/feature_selection/wrappers/rfecv/_fit.py:289-297 | stability_selection dispatches to _fit_stability_selection BEFORE the estimators_list presence check a few lines later, so RFECV(stability_selection=True) with no estimator crashes with a confusing AttributeError from sklearn.base.clone(None) instead of RFECV's own clear ValueError. | RESOLVED (8d6c5b40f) |
| FS_WRAPPERS-5 | fs_wrappers | src/mlframe/feature_selection/wrappers/rfecv/__init__.py:804-810 | RFECV.transform() unconditionally passes self_destruct=True to polars' to_pandas() for a polars X, unlike the sibling fit-path (_fit_init.py) which gates the identical kwarg combination behind an ownership marker specifically to avoid destroying a caller-owned frame's data. Direct testing on the installed polars 1.41.2 did not reproduce actual corruption, so this is a design-consistency / latent-risk gap rather than a confirmed data-loss bug. | RESOLVED (1aad9b726) |
| FS_WRAPPERS-6 | fs_wrappers | src/mlframe/feature_selection/wrappers/_helpers_importance.py:284-296 | _conditional_permutation_importance's single-feature (p==1, no-conditioning-set) fallback branch calls model.score() with no try/except, unlike the general (p>1) branch a few lines below which explicitly wraps the identical call so a scorer crash degrades to NaN instead of aborting the whole per-fold FI computation. | RESOLVED (0ef4e7d37) |
| METRICS-5 | metrics | src/mlframe/metrics/classification/_classification_report.py:83-95 | format_classification_report's MACRO-AVG CAVEAT docstring describes the deflating macro_over_present=False legacy behavior and claims it matches sklearn, but the function actually inherits fast_classification_report's default macro_over_present=True (present-classes-only, which is what really matches sklearn) — confirmed live with a 3-class example (0.875 actual vs 0.583 as the docstring implies). | RESOLVED (96b512d9f) |
| METRICS-6 | metrics | src/mlframe/metrics/rank_correlation.py:118-154 | _spearmanr_scalar_njit's leading NaN pre-check loop has an early `return` inside `prange`, which Numba cannot parallelize (confirmed via NumbaPerformanceWarning reproduced in isolation); the docstring's '2-way prange... 1.5-2.7x' claim overstates achieved parallelism whenever the loop runs (i.e. every call). | RESOLVED (84052bf4c) |
| METRICS-7 | metrics | src/mlframe/metrics/classification/_threshold_optimization.py:88-116 | optimal_threshold (and optimal_ordinal_cutpoints in _ordinal_cutpoints.py:61-127) fit a parameter by maximizing a metric on the exact (y_true,y_score) supplied, with no HOLDOUT CONTRACT warning — inconsistent with this same package's own established pattern (quantile.py's coverage()/pit_values() carry explicit holdout-contract docstrings for a lower-risk read-only case). | RESOLVED (a4cd9e988) |
| MODELS-15 | models | src/mlframe/models/tuning.py:1 | tuning.py defines no __all__ (unlike sibling rf_proximity.py), so models/__init__.py's star-import leaks 13 third-party symbols (pd, np, db, uniform, loguniform, randint, KFold, train_test_split, cross_validate, check_scoring, Enum, auto) and the mutable global trained_models into mlframe.models's public API, verified empirically via mlframe.models.__all__. | RESOLVED (48b782985) |
| MODELS-4 | models | src/mlframe/models/tuning.py:149-166 | check_rules's allow_if_values_or branch re-uses the gate loop's leftover 'condition' variable; harmless (dead code) when conditions is non-empty but raises NameError when conditions is an empty tuple, an otherwise-legal DSL input. | RESOLVED (439c46269) |
| MODELS-5 | models | src/mlframe/models/tuning.py:343-345,358-381 | favorize_unexplored's order parameter (docstring: 'combinations of order N') is never referenced in the function body; only single categorical values are ever favorized regardless of the order argument. | RESOLVED (8336c832f) |
| MODELS-6 | models | src/mlframe/models/masked_multilabel_objective.py:85-89 | masked_multilabel_logloss_objective's care_mask uses exact float equality against a sentinel that has round-tripped through XGBoost's internal float32 label storage; a non-default sentinel not exactly representable in float32 would silently fail to mask 'don't care' cells. | RESOLVED (d12910b60) |
| MODELS-7 | models | src/mlframe/models/ensembling/base.py:723-728 | combine_probs's 'qube' underflow guard is gated on a global (stacked > 0).all() over the whole tensor; a single exact-zero cell anywhere disables the underflow-protection clip for the entire tensor, including unrelated tiny-positive cells that need it. | RESOLVED (1835c326a) |
| MODELS-8 | models | src/mlframe/models/ensembling/base.py:405-424 | enrich_ensemble_preds_with_numaggs mutates a caller-supplied numaggs_kwds dict in place via .update() instead of working on a local copy. | RESOLVED (73e190748) |
| PREPROCESSING-10 | preprocessing | src/mlframe/preprocessing/cluster.py:61-91 | clusterize's show_plot=True (default) path builds the full matplotlib figure, computes all scatter data, sets title/annotations, then immediately calls plt.close(fig) without ever calling .show(), saving, or returning it -- so the default call produces no visible or persisted output at all despite the docstring's 'render a scatter plot of the clusters' promise. | RESOLVED (c5fea276c) |
| PREPROCESSING-7 | preprocessing | src/mlframe/preprocessing/outlier_policy.py:79-136 | apply_outlier_policy recomputes cap bounds from whatever X it is called on every time (no fit/apply split, no persisted state) and, unlike its sibling outlier_capping_or_missing.py, carries no leakage/fit-on-train-only caveat in its docstring; calling it independently on train and test silently diverges the cap bounds applied to each. | RESOLVED (cbeb42af0) |
| PREPROCESSING-8 | preprocessing | src/mlframe/preprocessing/__init__.py:19-33 | Package-level __init__.py omits the fit_*/apply_* leakage-safe function pairs for rare_count_pruning, missing_indicator_pairing, and regime_conditioned_imputation (only the combined convenience wrapper is exported), even though each module's docstring recommends the split; confirmed 'from mlframe.preprocessing import fit_rare_category_collapse' raises ImportError while the analogous apply_feature_direction IS exported. | RESOLVED (66a3421da) |
| PREPROCESSING-9 | preprocessing | src/mlframe/preprocessing/cleaning.py:76-93 | _get_nunique's float fast path only extracts skip_vals[0]/skip_vals[1] into skip0/skip1; any 3rd+ element of a longer skip_vals tuple is silently dropped with no validation, diverging silently from the np.unique fallback path (which supports arbitrary-length skip_vals) if a future caller passes more than 2. | RESOLVED (ca5c8e8ce) |
| REPORTING_A-5 | reporting_a | src/mlframe/reporting/_diagnostics_dispatch_extra.py:556-597 | render_category_discriminability_diagnostic accepts seed: int = 0 but never uses it; the composer it calls (compose_category_discriminability_figure) doesn't even accept a seed kwarg and hardcodes np.random.default_rng(0) internally for its row subsample. Every sibling render_* function in the same file correctly threads its seed through. | RESOLVED (7b262c471) |
| REPORTING_A-6 | reporting_a | src/mlframe/reporting/_diagnostics_dispatch_extra.py:34-37 | DIAG_ROW_CAP=100_000 / DIAG_MAX_FEATURES=200 are independently redefined here with the same names/values as diagnostics_dispatch.py:34,37 (the actual consumer), instead of importing from it; the copy here is dead except for re-export via __all__. | RESOLVED (628c83c75) |
| REPORTING_A-7 | reporting_a | src/mlframe/reporting/auto_dispatch.py:267-334 (multiclass + binary branches of render_multi_target_panels) | When target_type authoritatively selects the multiclass or binary branch but the actual probs shape doesn't satisfy that branch's guard, the function silently falls through to return None with no log line; the multilabel branch a few lines above does log a warning on its analogous shape mismatch. | RESOLVED (b52be9864) |
| REPORTING_B-2 | reporting_b | src/mlframe/reporting/diagnostics_dispatch.py:206 | render_split_error_diagnostics passes timestamps=None to worst_k_table whenever the frame is subsampled (n > DIAG_ROW_CAP = 100k rows) instead of indexing timestamps by sample_idx like yt/yp are, silently dropping temporal context on exactly the large-frame (100GB+) case this subsystem targets. | RESOLVED (f56887bc6) |
| REPORTING_B-3 | reporting_b | src/mlframe/reporting/renderers/matplotlib.py:365 | MatplotlibRenderer._histogram's bar-width expression `float(p.bin_width or (bin_centers[1]-bin_centers[0]) if len(bin_centers)>1 else 1.0)` relies on Python ternary binding looser than `or`, parsing as `(p.bin_width or diff) if len>1 else 1.0` -- an explicitly-supplied bin_width is silently discarded and forced to 1.0 on a single-bin histogram. PlotlyRenderer._histogram (line 477) has the identical expression but correctly parenthesized, so the two backends render a single-bin histogram at different widths from the same spec. | RESOLVED (80f687fd3) |
| REPORTING_B-4 | reporting_b | src/mlframe/reporting/renderers/matplotlib.py:438-442,517-521 (+ charts/confusion_matrix_plot.py:185-192) | Three call sites still loop per-cell calling colors.auto_text_color() individually for heatmap cell-text color, even though colors.auto_text_colors_batch() exists specifically to replace this pattern (its own docstring documents the fix) and is already used by PlotlyRenderer._heatmap. Bounded by a 400-cell cap so not catastrophic, but a real missed optimization and 3-way code duplication. | RESOLVED (e4f0afdee) |
| REPORTING_B-5 | reporting_b | src/mlframe/reporting/charts/error_analysis.py:245-251 | _top_split_features's sklearn-unavailable fallback ranks a feature via a median split (np.median, not np.nanmedian); a column with any NaN makes the median NaN, so both comparison masks (> med, <= med) are all-False, hi/lo come back empty, and that column's surrogate importance silently stays 0.0 regardless of its real discriminating power on non-missing rows. | RESOLVED (43b0e9b40) |
| TRAINING_COMPOSITE_CORE_A-3 | training_composite_core_a | src/mlframe/training/composite/_composite_report_shared.py:41-48 | factorize()'s no-pandas fallback (np.unique(..., return_inverse=True)) does not map NaN/null group labels to code -1 the way the pandas branch does, contradicting the function's own documented contract; downstream group reports would silently treat a NaN group as real data in the fallback-only path. | RESOLVED (a83aa63f6) |
| TRAINING_COMPOSITE_CORE_A-4 | training_composite_core_a | src/mlframe/training/composite/classification_discovery.py:222-223,256 | Per-candidate-column median imputation is computed once over the whole screening split BEFORE the StratifiedKFold loop (both stage-1 and stage-2), so each fold's train-side imputed values incorporate information from that fold's own held-out validation rows -- a train/val boundary leak of a summary statistic that mildly inflates margin_gain/cv_gain. | RESOLVED (8fa95e350) |
| TRAINING_COMPOSITE_CORE_A-5 | training_composite_core_a | src/mlframe/training/composite/distributional.py:214-239 | predict_cdf builds a dense (n_rows, K, len(y_grid)) boolean broadcast array to derive the step-CDF, an unguarded O(n*K*G) memory allocation inconsistent with the rest of this package's careful bounded-memory reduction discipline; can allocate tens of GB at realistic large-n plus a moderately fine y_grid. | RESOLVED (a2883a007) |
| TRAINING_COMPOSITE_CORE_B-3 | training_composite_core_b | src/mlframe/training/composite/meta.py:178-180 | CompositeOrRawStacker.fit computes n_splits = max(2, min(self.n_splits, n)); for n=0 or n=1 this still returns 2, so KFold(n_splits=2) raises sklearn's raw internal ValueError instead of a clear, class-named error. | RESOLVED (36e09f8bc) |
| TRAINING_COMPOSITE_CORE_B-4 | training_composite_core_b | src/mlframe/training/composite/panel.py:143-172 | _compute_offsets's docstring claims a 'vectorised groupby pass' but the implementation is a plain Python for-loop with .tolist()/dict accumulation -- materially slower than a vectorized groupby on the million-row panel data this estimator targets, and the docstring is misleading about the mechanism. | RESOLVED (a84025510) |
| TRAINING_COMPOSITE_CORE_B-5 | training_composite_core_b | src/mlframe/training/composite/venn_abers.py:86-160 | _ivap_saddle_njit is explicitly self-documented as O(g^2) in the number of unique calibration scores (outer max-over-lo rescan), with no size guard/cap/warning for a caller calibrating on a large, mostly-continuous-score calibration split. | RESOLVED (eaee2f5c0) |
| TRAINING_COMPOSITE_CORE_B-7 | training_composite_core_b | src/mlframe/training/composite/long_format_gbm.py:108-114 | melt_to_long_gbm_features's within-column frequency count uses groupby(['_feature_name','_value']).transform('count') with pandas' default dropna=True, so any row whose original X value is NaN silently gets a NaN '_count' instead of a valid count -- no test/doc coverage for X containing missing values. | RESOLVED (521fee178) |
| TRAINING_COMPOSITE_DISCOVERY-1 | training_composite_discovery | src/mlframe/training/composite/discovery/_eval.py:649-651 | Bootstrap MI-gain CI raises an uncaught IndexError (crashing the whole fit() call) when mi_gain_bootstrap_n=1 and that lone replicate fails, because `boot_finite.size >= bootstrap_n // 2` is trivially true (0>=0) even for an empty array. | RESOLVED (a4e598e64) |
| TRAINING_COMPOSITE_DISCOVERY-2 | training_composite_discovery | src/mlframe/training/composite/discovery/_collinear_numba.py:326-331 | Module-level _KEEP_MASK_CACHE (shared across all CompositeTargetDiscovery instances/threads in-process) is read via .get()+move_to_end() WITHOUT the lock that guards every write/evict path (popitem+__setitem__), risking a KeyError or corrupted LRU order under concurrent fit() calls. | RESOLVED (6c5aa38f9) |
| TRAINING_COMPOSITE_DISCOVERY-3 | training_composite_discovery | src/mlframe/training/composite/discovery/_calibration_gate.py:1-202 | A fully-implemented, unit-tested calibration-aware ranking-penalty module is never imported or called by any discovery gate (_tiny_rerank.py, _filter_and_gate.py, etc.) -- confirmed by repo-wide grep, zero production call sites. | FUTURE (144f96299 -- docs+integration point only; full wiring needs a return_residuals option threaded through _tiny_rerank.py, out of scope for a scoped fix) |
| TRAINING_COMPOSITE_ENSEMBLE_ESTIMATOR_TRANSFORMS-2 | training_composite_ensemble_estimator_transforms | src/mlframe/training/composite/estimator/_soft_shrink.py:55-63 | linear_residual_multi_robust reuses the exact same forward/inverse functions as linear_residual_multi but is missing from ADDITIVE_BASE_TRANSFORMS, so it silently never gets the soft-base-shrink OOD guard its byte-identical sibling gets. | RESOLVED (b82331d2b) |
| TRAINING_CORE_A-3 | training_core_a | src/mlframe/training/core/_main_train_suite.py:722 | Developer-flagged (!TODO!) unresolved inefficiency: CB/LGB/XGB inner feature matrices (Pool/Dataset/DMatrix) are rebuilt once per target inside the per-target loop instead of once per shared feature-set, wasting binning/encoding work on multi-target suites. | RESOLVED (96a8ac16f -- stale comment; the reuse mechanism was already fully implemented) |
| TRAINING_CORE_B-3 | training_core_b | src/mlframe/training/core/_phase_train_one_target_weight_iteration.py:1-451 (function at line 36) | _run_one_weight_iteration (451 LOC) is a full duplicate of the inline per-(model,weight) loop body in _phase_train_one_target_body.py but has zero call sites anywhere in src/ -- an abandoned monolith-split extraction that independently reproduces the TRAINING_CORE_B-1 bug and risks silent fix-divergence. | FUTURE (ceb4f9892 -- dead-code warning documented; wiring the real refactor needs its own dedicated pass, ~30 closure vars, past-regression-flagged code) |
| TRAINING_FEATURE_HANDLING_TARGETS-1 | training_feature_handling_targets | src/mlframe/training/feature_handling/ordered_target_encoder.py:130-139 | ordered_target_encode_batch lacks the noise_count_halflife parameter its sibling ordered_target_encode exposes. | RESOLVED (892b6570c) |
| TRAINING_FEATURE_HANDLING_TARGETS-2 | training_feature_handling_targets | src/mlframe/training/feature_handling/apply.py:457-527 | feature_handling_apply's target-encoder path never threads sample_weight into LeakageSafeEncoder, and no config field exposes one. | RESOLVED (40b09e2f2) |
| TRAINING_FEATURE_HANDLING_TARGETS-3 | training_feature_handling_targets | src/mlframe/training/targets/_target_temporal_plot.py:111 | plot_target_over_time hardcodes ax.set_ylim(-0.02, 1.05) even for target_type='regression', where target_rate is an unbounded mean(y), breaking the chart. | RESOLVED (37b7e4bf4) |
| TRAINING_FEATURE_HANDLING_TARGETS-5 | training_feature_handling_targets | src/mlframe/training/strategies/__init__.py:297-305 | is_neural_model() includes literal 'recurrent' in its alias set but MODEL_STRATEGIES has no 'recurrent' key; live-verified is_neural_model('recurrent')=True while get_strategy('recurrent') warns and mis-routes to TreeModelStrategy. Same bug class already fixed once in _helpers_training_configs.py. | RESOLVED (2fb428173) |
| TRAINING_LOOSE_A-2 | training_loose_a | src/mlframe/training/_eval_helpers.py:39-65 (docstring) vs 151-158 (impl) | _align_xgb_cat_categories's docstring claims the union of category levels spans 'all three splits' (train/val/test) so 'val/test' become a subset of the train universe, but the actual (correct, leak-safe) implementation deliberately unions only train+val, excluding test to avoid feeding test categories back before fit. The stale docstring could mislead a future edit into reintroducing the leak it currently prevents. | RESOLVED (bc5240072) |
| TRAINING_LOOSE_A-3 | training_loose_a | src/mlframe/training/_io_save.py:187-268, 392-412 | save_mlframe_model's pre-pickle walk (_collect_pre_dump_swaps) mutates the SHARED nested object graph in place (torch.compile swap, Lightning-bloat nulling) and restores only in a finally block; lean=True only shallow-copies the top-level SimpleNamespace, so nested sub-objects are the same live references the caller's model still holds. Two concurrent save_mlframe_model calls on bundles sharing a nested sub-object (e.g. parallel per-model saves) can interleave the strip/restore windows. | RESOLVED (f302809d3) |
| TRAINING_LOOSE_B-3 | training_loose_b | src/mlframe/training/_regression_calibration.py:146-161 | smearing_predict builds an unbounded (n_test, n_cal) dense broadcast array with no size gate on n_test, risking OOM on large predict batches (e.g. 32GB at n_test=2M, max_cal=2000). | RESOLVED (e5adb49f4) |
| TRAINING_LOOSE_B-4 | training_loose_b | src/mlframe/training/_model_factories.py:486-504 | _get_neural_components caches only the import-success path; a broken (not merely absent) mlframe.training.neural install causes every subsequent call to retry the same documented 30-180s import chain. | RESOLVED (2e056c455) |
| TRAINING_LOOSE_C-10 | training_loose_c | src/mlframe/training/mlp_runtime_defaults.py:335-380 | _probe_available_memory_bytes's process-wide first-successful-probe cache doesn't distinguish a CPU-RAM probe from a GPU-VRAM probe; a call in one mode followed by a call expecting the other mode silently reuses the first probe's (very different magnitude) value, risking a CUDA OOM the module exists to prevent. | RESOLVED (a1af3c78a) |
| TRAINING_LOOSE_C-6 | training_loose_c | src/mlframe/training/feature_drift_report.py:774 | _compute_drift_invariant forwards feature_names to the numeric z-stat computation but not to compute_categorical_drift_psi, so a caller restricting compute_feature_distribution_drift to a feature subset gets that restriction honored only on the numeric side, not the categorical PSI side. | RESOLVED (d75b098c4) |
| TRAINING_LOOSE_C-7 | training_loose_c | src/mlframe/training/drift_report.py:62-72 | _binary_split_summary has no NaN handling (unlike the regression/multiclass/multilabel summarizers in the same file); a binary target split with missing labels is silently counted as all-negative, producing a misleading p_positive with no signal that labels were missing. | RESOLVED (386df6f77) |
| TRAINING_LOOSE_C-8 | training_loose_c | src/mlframe/training/evaluation.py:155,193-195 | post_calibrate_model's calib_set_size parameter is documented as controlling the multi-output per-class isotonic path's slice size, but is never referenced anywhere in the function body -- a dead, misleading parameter. | RESOLVED (609ed4c2f) |
| TRAINING_LOOSE_C-9 | training_loose_c | src/mlframe/training/metrics_registry.py:409-418 | The exploss builtin metric's pos_label = uniq[1] if uniq.shape[0] > 1 else uniq[-1] raises IndexError on empty y_true; iter_extra_metrics's narrow except tuple doesn't catch IndexError, so an empty-input report crashes the whole metrics pass instead of gracefully omitting exploss. | RESOLVED (81a714d70) |
| TRAINING_NEURAL-4 | training_neural | src/mlframe/training/neural/field_grouped_mlp.py:30-48 | FieldGroupedMLPRegressor never validates that field_groups partitions X's columns exactly once, despite the docstring stating this contract; missing/duplicated indices are silently dropped or double-counted. | RESOLVED (97c65797f) |
| TRAINING_NEURAL-5 | training_neural | src/mlframe/training/neural/keras_compat.py:47-89 | build_keras_mlp(num_layers=N) builds N+1 hidden Dense layers (one unconditional block before the loop, plus N inside it), not N as the parameter name/docstring imply. | RESOLVED (2e25656cf) |
| TRAINING_NEURAL-6 | training_neural | src/mlframe/training/neural/_recurrent_cat_embeddings.py:29-75 | Recurrent wrapper's _factorize_cats_fit only factorizes explicitly-named cat_features columns, unlike the flat-MLP sibling which additionally auto-detects any remaining non-numeric column; an un-named categorical column crashes downstream instead of being handled gracefully. | RESOLVED (7aa6c8131) |
| VOTENRANK-14 | votenrank | src/mlframe/votenrank/data_processing.py:63-64 | get_tracker_table's `model, task, _ = f.split("_")` assumes exactly 3 underscore-separated directory-name components; any real model/task name containing its own underscore breaks the unpack, aborting the whole tracker-table load. | RESOLVED (32549a983) |
| VOTENRANK-2 | votenrank | src/mlframe/votenrank/shapley_blend.py:118-134 | _permutation_shapley recomputes score_fn(y, zeros) (the empty-coalition score) once per permutation even though it's a constant already computed once by the caller as v_empty, wasting ~1/n_models of the total score_fn call budget. | RESOLVED (8ac353579) |
| VOTENRANK-3 | votenrank | src/mlframe/votenrank/shapley_blend.py:23-35 | _default_score_fn silently falls back to negative-RMSE (treating y as continuous) for any y whose cardinality isn't exactly 2, including degenerate single-class or integer-coded multiclass targets, with no warning. | RESOLVED (1dafc4d93) |
| VOTENRANK-4 | votenrank | src/mlframe/votenrank/similarity_blend.py:141-160 | region_similarity_weights's documented 'rows summing to 1' invariant is silently violated when a query row's similarity to every region underflows exp(...) to exactly 0.0, returning an all-zero weight row instead of a sane fallback. | RESOLVED (f01e52182) |
| VOTENRANK-5 | votenrank | src/mlframe/votenrank/correlation_diversity_ablation.py:147-149 | np.nanmax on an all-NaN correlation row (from a constant/zero-variance prediction column) returns NaN, and NaN < correlation_threshold is always False, so a constant-prediction model is silently excluded from the diversity report instead of flagged. | RESOLVED (3b727e058) |
| VOTENRANK-6 | votenrank | src/mlframe/votenrank/hill_climb.py:34-82 | Neither hill_climb_ensemble nor _hill_climb_single_path validates that every oof_preds array shares y_true's shape, so a mismatched-shape array silently broadcasts into a wrong-shaped result instead of raising. | RESOLVED (7ced3aebe) |
| VOTENRANK-7 | votenrank | src/mlframe/votenrank/leaderboard/_rules.py:167 | minimax_ranking's models_scores.drop(model).max() is NaN for a 1-model leaderboard (drop empties the Series), making minimax_election() silently return an empty winner list instead of the one trivially-correct model. | RESOLVED (bac0431a2) |
| X_ARCHITECTURE_API_CONSISTENCY-3 | x_architecture_api_consistency | training/neural/ranker.py:605 | MLPRanker and ShortlistTransformerAdapter (both sklearn BaseEstimator subclasses) name their RNG param 'seed' instead of the sklearn-standard 'random_state' used by ~40 sibling BaseEstimator classes in the codebase. | RESOLVED (9751d817b) |
| X_ARCHITECTURE_API_CONSISTENCY-4 | x_architecture_api_consistency | training/neural/ranker.py:600,918 | MLPRanker's epoch-count parameter is named n_estimators (docstring: 'epochs'; fed to max_epochs=self.n_estimators) while 4 sibling neural regressors in training/neural/ use n_epochs for the identical concept, and n_estimators already has a conflicting sklearn meaning (ensemble size). | RESOLVED (1232ce8b1) |
| X_CICD_DEPENDENCIES-6 | x_cicd_dependencies | pre_test_gate.ps1:53 | Full-suite worker count is computed as physical-cores/2, conflicting with the documented quarter-cores policy for pytest -n on this machine, motivated by a known Windows paging-file exhaustion failure mode under joblib fan-out. | RESOLVED (18442c7a7) |
| X_CICD_DEPENDENCIES-7 | x_cicd_dependencies | .pre-commit-config.yaml:268-292,618-654 | Both mypy-full (whole-project) and mypy-full-manual (changed-files) run as blocking hooks on every commit, doubling mypy wall time paid per commit with the changed-files hook adding no coverage the whole-project superset doesn't already provide. | RESOLVED (12ac6ca24) |
| X_ML_CORRECTNESS_META-2 | x_ml_correctness_meta | src/mlframe/training/feature_handling/target_encoders.py:31-33,575-580 | method='target_james_stein' is documented as variance-aware James-Stein shrinkage but its implementation (shrink = smoothing/(n_c+smoothing)) is algebraically identical to target_mean/target_m_estimate; it is not real James-Stein shrinkage and produces bit-identical output to target_mean given the same smoothing. | RESOLVED (d245b1acb) |
| X_OSS_HYGIENE_PACKAGING-1 | x_oss_hygiene_packaging | README.md:668 | README.md and CONTRIBUTING.md claim the sklearn-matrix CI tests scikit-learn '1.5 through 1.8', but the actual workflow matrix and the hard dependency floor are both 1.6+ (sklearn 1.5 cannot import mlframe at all, per pyproject.toml's own comment). | RESOLVED (07bc2eef3) |
| X_OSS_HYGIENE_PACKAGING-2 | x_oss_hygiene_packaging | CHANGELOG.md:101-102 | The [Unreleased] and [0.9.0] footer links reference a v0.9.0 git tag/GitHub release that does not exist on the remote (gh api tags and gh release list both return empty) -- both links 404. | RESOLVED (3b714c8ef) |
| X_OSS_HYGIENE_PACKAGING-3 | x_oss_hygiene_packaging | README.md:82-103 | README's Modules table omits two real, substantial subpackages: mlframe.competition (~3956 LOC) and mlframe.data_valuation (~1372 LOC, referenced elsewhere in the doc set via the gt_04 research note). | RESOLVED (1ece1fc4d) |
| X_OSS_HYGIENE_PACKAGING-4 | x_oss_hygiene_packaging | docs/MRMR_RESEARCH.md:183,191 | The 'Critical gaps' section says the Fleuret synergy-rejection weakness is 'still unfixed' / has 'No protection against synergy destruction', directly contradicted by the same doc's Recommendations section (line 199) and backlog table (line 390), and by the live fleuret.py docstring which says the mitigation is shipped. | RESOLVED (f1db866e7) |
| X_OSS_HYGIENE_PACKAGING-5 | x_oss_hygiene_packaging | docs/MRMR_RESEARCH.md:177 | Cites wrappers/_rfecv.py:237-499 for the feature_groups mechanism; that file no longer exists -- RFECV was split into a wrappers/rfecv/ subpackage and feature_groups logic now lives across _finalize.py/_validate.py/_configs.py. | RESOLVED (6ff9f117e) |
| X_OSS_HYGIENE_PACKAGING-6 | x_oss_hygiene_packaging | docs/MULTI_OUTPUT.md:169-179,253 | The Calibration section describes _PerClassIsotonicCalibrator as already shipped for MULTICLASS and MULTILABEL, but the 'Out-of-scope (Session 3+)' list still names 'Per-class isotonic calibration for multi-output' as not done; _PerClassIsotonicCalibrator is verified wired into 9 production files. | RESOLVED (82a5449e5) |
| X_OSS_HYGIENE_PACKAGING-7 | x_oss_hygiene_packaging | docs/dummy_baselines_guide.md:98 | Instructs users to 'raise the logger level for mlframe.training.dummy_baselines to DEBUG' to see the full metrics table; that logger name does not exist anywhere -- the code was split into mlframe.training.baselines.* submodules each with their own logging.getLogger(__name__), none a child of the cited name, so the instruction is a complete no-op. | RESOLVED (bdb0b0e4d) |
| X_OSS_HYGIENE_PACKAGING-8 | x_oss_hygiene_packaging | docs/dummy_baselines_guide.md:464-467 | Both documented 'Profiling + smoke' commands are broken as written: `python -m mlframe.training._profile_dummy_baselines` fails with ModuleNotFoundError (verified by running it) and `python mlframe/training/_smoke_dummy_baselines_e2e.py` points at a nonexistent path -- both files actually live under src/mlframe/training/baselines/. | RESOLVED (4833136cc) |
| X_SECURITY_ROBUSTNESS-3 | x_security_robustness | src/mlframe/estimators/pipelines.py:97-105 (also inference/predict.py:163-172, training/_data_helpers.py:38-61, training/core/_misc_helpers.py:209-223) | The abspath+os.path.commonpath trusted-root containment check gating every joblib.load/dill.load in the codebase is independently re-implemented in four separate files with subtly different fail-open/fail-closed defaults, making it easy for a future fix to miss a copy. | RESOLVED (a87b5099a) |
| X_SECURITY_ROBUSTNESS-4 | x_security_robustness | src/mlframe/integrations/mlflow.py:141-151 | get_or_create_mlflow_run scrubs tracking-server userinfo credentials out of the log line via _strip_userinfo but then bare-`raise`s the original unscrubbed exception, so any outer handler that logs str(e) on the propagated exception leaks the credential the scrub was built to hide. | RESOLVED (fc4c1936d) |
| X_SECURITY_ROBUSTNESS-5 | x_security_robustness | src/mlframe/preprocessing/unseen_category_imputer.py:96-114 | UnseenCategoryImputer.fit() raises a raw unhelpful IndexError (reliable.index[0] on an empty index) when a fitted column has zero non-null values -- reproduced live for both an empty DataFrame and, more realistically, an all-NaN column. | RESOLVED (808e63b64) |
| X_TEST_SUITE_ARCHITECTURE-3 | x_test_suite_architecture | tests/feature_selection/gpu/test_numba_utils_coverage.py:39-46 | test_arr2str_uint8's isinstance assertion is inside a try whose except Exception converts an assertion failure into pytest.skip with a misleading 'not supported' reason, indistinguishable from a real environment gap. | RESOLVED (da0268996) |
| X_TEST_SUITE_ARCHITECTURE-4 | x_test_suite_architecture | tests/feature_selection/stability/test_selectors_shared.py:313-328 | test_works_in_sklearn_pipeline's shape assertion is inside a try/except Exception: pytest.skip(...) that reports a genuine shape-mismatch bug as a Pipeline-incompatibility skip rather than a failure. | RESOLVED (f0829864f) |
| X_TEST_SUITE_ARCHITECTURE-5 | x_test_suite_architecture | src/mlframe/testing/parametric.py:1-end (10 public generators) | A shipped-with-package edge-case data generator module (constant/categorical/high-card/inf-heavy/sparse-null columns) is imported by exactly one test file across ~3200, while dozens of files (55 for constant-column-shaped scenarios, 18 categorical, 7 id) hand-roll equivalent fixtures inline instead of reusing it. | RESOLVED (528927fa2) |
| X_TEST_SUITE_ARCHITECTURE-6 | x_test_suite_architecture | src/mlframe/system/kernel_tuning_cache/__init__.py:22-180 | The mlframe-tune-kernels CLI entry point (main + 6 cmd_* subcommands) still has zero test coverage 15 days after the prior audit cycle (F6) flagged this exact gap and deferred it as a tracked follow-up that has not materialized. | RESOLVED (7c7677b13) |

## P3 (137)

| ID | Cluster | File:Line | Summary | Status |
|----|---------|-----------|---------|--------|
| CALIBRATION-10 | calibration | src/mlframe/calibration/threshold_optimizer.py:72-75 | _threshold_stability_report's coefficient_of_variation = std/mean is not protected against mean < 0; a caller-supplied threshold_range spanning negative values can sign-flip the ratio negative, trivially passing the is_stable <= threshold check regardless of true relative spread. | TODO |
| CALIBRATION-11 | calibration | src/mlframe/calibration/group_zero_sum_constraint.py:75-84 | apply_group_zero_sum_constraint_multi's docstring claims the solver works exactly like Dykstra's alternating projection algorithm, but the per-constraint step applies a constant per-group shift (by design) rather than a true weighted orthogonal projection - the algorithm still converges to a feasible point (verified empirically) but the Dykstra/optimality claim over-states what is guaranteed under non-uniform weights. | TODO |
| CALIBRATION-12 | calibration | src/mlframe/calibration/policy.py:247-248 | Pervasive process/audit metadata embedded in source comments across quality.py, policy.py, __init__.py, post.py, _post_train_calibrators.py and a benchmark file (Wave NN, iterNNN, P1-5/P2-1 finding IDs), which this repo's own CLAUDE.md explicitly forbids in code comments. | TODO |
| CALIBRATION-13 | calibration | src/mlframe/calibration/policy.py:1 | Docstrings/comments throughout the cluster use a literal " -- " as an em-dash substitute in prose (124 occurrences across 24 files), which this repo's own CLAUDE.md convention explicitly disallows in favor of a single " - ". | TODO |
| CALIBRATION-6 | calibration | src/mlframe/calibration/post.py:111-115 | BinaryPostCalibrator.fit dynamically sets self.classes_, self.n_features_in_, self.y_cal, self.p_cal but only _resolved_transform_method_name is declared in the class-level annotation block, violating the repo's own convention to declare dynamically-set attributes at class scope for mypy cleanliness. | TODO |
| CALIBRATION-7 | calibration | src/mlframe/calibration/post.py:36-38 | Module-level sentinel _INCLUDE_RE is dead code, never read anywhere, kept per its own comment purely so a source-inspection meta-test can confirm a refactor landed - the exact anti-pattern the repo's own behavioral-testing convention rejects. | TODO |
| CALIBRATION-8 | calibration | src/mlframe/calibration/quality.py:271-276 | estimate_calibration_quality_binned and show_classifier_calibration both default metrics_to_show: dict = METRICS_TO_SHOW, a mutable module-level dict used directly as a parameter default - currently harmless since neither function mutates it, but a latent mutable-default-argument footgun. | TODO |
| CALIBRATION-9 | calibration | src/mlframe/calibration/quality.py:228-268 | bin_predictions's njit kernel computes bin_size = s // nbins with no guard against nbins <= 0; the caller clamps nbins = min(nbins, n_samples) but never validates nbins >= 1, so nbins=0 reaches the compiled kernel and raises an opaque ZeroDivisionError instead of a clear ValueError. | TODO |
| COMPETITION_EVALUATION-10 | competition_evaluation | src/mlframe/evaluation/reports.py:90-92 | get_predicted_classes's docstring doctest is malformed (a >>> line's own echoed output has no expected-output block, immediately followed by a second >>> line) and would fail if ever collected via --doctest-modules. | TODO |
| COMPETITION_EVALUATION-11 | competition_evaluation | src/mlframe/evaluation/reports.py:312-313 | Dead commented-out debug lines (# print(mes), # logger.info(mes)) left in evaluate_estimators, immediately above the display(Markdown(...)) call that superseded them. | TODO |
| CORE_INFRA_MISC-10 | core_infra_misc | src/mlframe/signal/changepoint_detection.py:76-79 | detect_regime_changepoints/pelt_l2 never validate min_segment_length >= 1; min_segment_length=0 causes a zero-length-segment division inside the njit PELT kernel, silently producing inf/nan costs that corrupt detected breakpoints. | TODO |
| CORE_INFRA_MISC-11 | core_infra_misc | src/mlframe/utils/_param_oracle.py:737-743 | _knn_recommend sorts 'scored' by distance, slices the top-k prefix (already sorted), then re-sorts that already-sorted prefix again -- a redundant no-op sort. | TODO |
| CORE_INFRA_MISC-5 | core_infra_misc | src/mlframe/__init__.py:193-194 | _disable_broken_cupy's NVRTC-probe-failure branch appends to gpu_disable_errors twice with near-duplicate messages, contradicting the module docstring's documented '0 or 1 entries' invariant. | TODO |
| CORE_INFRA_MISC-6 | core_infra_misc | src/mlframe/integrations/mlflow.py:113-116 | get_or_create_mlflow_run's 'if runs: for run in runs: return run, True' always returns on the first iteration, making the trailing 'return None, False' unreachable dead code. | TODO |
| CORE_INFRA_MISC-7 | core_infra_misc | src/mlframe/estimators/custom.py:394 | MyDecorrelator's class docstring is still 'TODO: TEST PROPERLY' despite being covered by 5 separate test files, making it misleadingly look untested. | RESOLVED (6df976e41) |
| CORE_INFRA_MISC-8 | core_infra_misc | src/mlframe/estimators/base.py:54-62 | _resolve_stratify sets n_val=n (the whole dataset) when test_size is None, overestimating the real validation fold size (sklearn defaults to test_size=0.25), which can wrongly permit stratification that later fails inside train_test_split. | RESOLVED (19d3c29d6) |
| CORE_INFRA_MISC-9 | core_infra_misc | src/mlframe/core/frame_compat.py:88-109 | to_pandas_or_array's polars-conversion branches catch Exception and silently fall back to np.asarray(X) at DEBUG-only log level, reintroducing the same silent-wrong-dtype failure mode the module was built to prevent. | RESOLVED (03b16ad7a) |
| FE_ROOT_A-10 | fe_root_a | src/mlframe/feature_engineering/numerical.py:413 | compute_entropy_features's variable `nonzero = (~np.isnan(arr)).sum()` is misleadingly named - it counts non-NaN elements, not non-zero-valued elements | TODO |
| FE_ROOT_A-8 | fe_root_a | src/mlframe/feature_engineering/_numerical_numba.py:3,247,294,679,700 | Six comments (this file + _timeseries_emit.py:3) embed 'Wave NN (YYYY-MM-DD)' process/phase markers, which the project's own CLAUDE.md explicitly prohibits in code comments | TODO |
| FE_ROOT_A-9 | fe_root_a | src/mlframe/feature_engineering/ensemble_features.py:119-120 | Stale comment on predictor_disagreement_var references a branch ('the arr.shape[1] > 1 branch below') that no longer exists in the function body (now a single unconditional return) | TODO |
| FE_ROOT_B-11 | fe_root_b | src/mlframe/feature_engineering/row_wise_extremality.py:63-76 | _ordinal_rank is dead code, fully superseded by _extremality_matrix_njit; no longer called anywhere in the file or repo. | TODO |
| FE_ROOT_B-12 | fe_root_b | src/mlframe/feature_engineering/nadaraya_watson.py:89-120,159 | Kernel parameter name x_sorted is misleading for the flat public API path -- nadaraya_watson_smooth never sorts x before passing it in (only the per-group path sorts). | TODO |
| FE_ROOT_B-13 | fe_root_b | src/mlframe/feature_engineering/hurst.py:356 | _per_group_rolling's group branch does np.where(...).copy() -- np.where already allocates a fresh array, so the trailing .copy() is a redundant extra allocation per group segment. | TODO |
| FE_ROOT_B-14 | fe_root_b | src/mlframe/feature_engineering/mps.py:276-278 | find_best_mps_sequence's shift parameter has no validation for negative values; only shift>0 triggers the shift logic, so a negative shift silently does nothing. | TODO |
| FE_ROOT_B-15 | fe_root_b | src/mlframe/feature_engineering/_benchmarks/ab_grouped_10m.py:10-11 | Hardcoded absolute, session-specific worktree/temp paths make this script non-portable and almost certainly broken today. | TODO |
| FE_ROOT_B-16 | fe_root_b | src/mlframe/feature_engineering/_benchmarks/bench_density_ratio_kde_gemm.py:7 | Run-command doc comment references a specific Anaconda interpreter path, inconsistent with every other bench script's 'python -m mlframe...' convention (same issue also present in bench_rff_matmul.py:9). | TODO |
| FE_ROOT_B-17 | fe_root_b | src/mlframe/feature_engineering/_benchmarks/bench_graddir_gradient_colcopy.py:76 | The 'unchanged' verification variable compares X to an unrelated freshly-drawn random array (not a real before/after comparison) and is never used/printed/asserted -- a dead, no-op check whose name implies it verifies restoration correctness. | TODO |
| FE_ROOT_B-18 | fe_root_b | src/mlframe/feature_engineering/nearest_past_join.py:117-125,137-161 | right_value_cols is resolved once from the outer by before the fallback-tier loop; a fallback tier introducing a key column outside the original by that also happens to be a value column would build a duplicate-column frame and likely raise a confusing error. | TODO |
| FE_ROOT_B-19 | fe_root_b | src/mlframe/feature_engineering/spatial.py:373-389 | knn_within_bucket_aggregate supports only median/mean/std/iqr while the sibling knn_aggregate additionally supports min/max/p10/p90 -- an undocumented API-surface inconsistency between two parallel functions. | TODO |
| FE_ROOT_B-20 | fe_root_b | src/mlframe/feature_engineering/spatial.py:192-198,206-223 | knn_aggregate's group-filter overquery is capped heuristically at k*4+1; when a query's own-group density exceeds this cap, its aggregates silently degrade to partial-k/NaN with no counter or warning surfaced. | TODO |
| FE_TRANSFORMER_A-10 | fe_transformer_a | src/mlframe/feature_engineering/transformer/fca_closed_concepts.py:17-44 | standardize: bool = True parameter is accepted (matching every sibling transformer's contract) but never read anywhere in _process -- dead/misleading parameter. | TODO |
| FE_TRANSFORMER_A-11 | fe_transformer_a | src/mlframe/feature_engineering/transformer/_smote_kernels_shared.py:90-138 | smote_synthesize_rowloop and smote_synthesize_vectorized (non-'minority'-named variants) are not imported by any production module -- only referenced in benchmark/test docstring prose, never actually called. | TODO |
| FE_TRANSFORMER_A-12 | fe_transformer_a | src/mlframe/feature_engineering/transformer/_aggregation.py:311 | dedupe_by_correlation's 'if nonzero_var.any():' guard at line 311 is always True because the function already returned early whenever not nonzero_var.any() (line 302-305). | TODO |
| FE_TRANSFORMER_A-13 | fe_transformer_a | src/mlframe/feature_engineering/transformer/apriori_itemsets.py:44-46 | np.in1d = np.isin monkeypatch mutates the process-global numpy module as a side effect of calling a feature-computation function, rather than being scoped at import time. | TODO |
| FE_TRANSFORMER_A-14 | fe_transformer_a | src/mlframe/feature_engineering/transformer/_knn_helper.py:76-91 | Docstring 'Returns' section says output shape is (n_query, min(k, n_subset)), but the documented n_subset==0 case actually (and intentionally) returns (n_query, k) sentinel columns -- self-contradictory docstring. | TODO |
| FE_TRANSFORMER_A-7 | fe_transformer_a | src/mlframe/feature_engineering/transformer/baseline_disagreement_v2.py:107-109 | Docstring claims '= 12 features' but the itemized list and the actual code (n_features=11) both total 11. | TODO |
| FE_TRANSFORMER_A-8 | fe_transformer_a | src/mlframe/feature_engineering/transformer/class_balanced_hard_row.py:92-93 | Docstring claims '2*n_hard_per_side + 8 features' but the itemized 9-term list and the actual code (n_features = n_total_anchors + 9) both total +9. | TODO |
| FE_TRANSFORMER_A-9 | fe_transformer_a | src/mlframe/feature_engineering/transformer/bgmm_quantile_bands.py:8-9 | Module docstring describes 12 cross-band log-ratio features that are never implemented in _process/_make_df; the function-level docstring correctly omits them. | TODO |
| FE_TRANSFORMER_B-6 | fe_transformer_b | src/mlframe/feature_engineering/transformer/y_quintile_baseline_knn.py:131 | Regression quantile edges (np.quantile) have no tie-protection, unlike sibling target_quantile.py which bumps non-increasing adjacent edges by 1e-9; a tied/discrete regression target can silently produce fully-empty strata that degrade to (mean=0,std=0) features. | TODO |
| FE_TRANSFORMER_B-7 | fe_transformer_b | src/mlframe/feature_engineering/transformer/multi_threshold_ordinal.py:78-84 | Binary sub-population classifier branch guards only target.sum()==0, while the sibling regression branch in the same function guards both extremes (==0 or ==shape[0]); analytically unreachable today (median semantics bound target.sum() below half the rows) but a latent trap if the threshold logic changes. | TODO |
| FE_TRANSFORMER_B-8 | fe_transformer_b | src/mlframe/feature_engineering/transformer/local_density_gradient.py:93 | k_eff = min(k_neighbors, Xt_s.shape[0]-1) can reach 0 on a single-row fold; downstream indexing uses Python negative-index wraparound (q_dists[:, -1]) instead of raising, silently producing a numerically-extreme, uninformative log_density with no warning or documented sentinel. | TODO |
| FS_BENCHMARKS_A-3 | fs_benchmarks_a | src/mlframe/feature_selection/_benchmarks/bench_adaptive_nbins_ab.py:160, 174 | Both _treatment_applies and _run_fold_ab build a self-mapping dict {"fayyad_irani": "fayyad_irani", "blocks": "blocks"}.get(method, method), which always equals plain `method`; the adjacent comment claiming an 'mdlp' name alias is resolved here is also inaccurate since no such key exists. | TODO |
| FS_BENCHMARKS_B-3 | fs_benchmarks_b | src/mlframe/feature_selection/_benchmarks/bench_pair_maxt_kernel_hoist.py:94-96 | best() is defined with deliberately obfuscated, always-empty generator logic and is never called anywhere in the file (a separate inline timing loop is used instead). | TODO |
| FS_BENCHMARKS_B-4 | fs_benchmarks_b | src/mlframe/feature_selection/_benchmarks/fs_hybrid/round4_adaptive_n_trials_bench.py:158 | Bare expression statement `traj[min(n_run - 1, len(traj) - 1)][0]` computes a value that is never assigned or printed. | TODO |
| FS_BENCHMARKS_B-5 | fs_benchmarks_b | src/mlframe/feature_selection/_benchmarks/fs_hybrid/round4_mrmr_tree_rescue_bench.py:69 | The round(time.time()-t0,1) timing value is computed and discarded, unlike sibling emit() calls in the same file which log their timing. | TODO |
| FS_BENCHMARKS_B-6 | fs_benchmarks_b | src/mlframe/feature_selection/_benchmarks/fs_hybrid/round4_permodel_emit_bench.py:203 | list(h._Xaug_.columns) is computed as a bare expression statement and never used; cols is later re-derived from Ztr.columns instead. | TODO |
| FS_BENCHMARKS_B-7 | fs_benchmarks_b | src/mlframe/feature_selection/_benchmarks/fs_hybrid/round4_fe_median_gated_recipe_proof.py:59,62 | Two gate_med_apply(...) * b_te expressions are computed and discarded as bare statements; the real comparison on line 63 recomputes both operands fresh. | TODO |
| FS_BENCHMARKS_B-8 | fs_benchmarks_b | src/mlframe/feature_selection/_benchmarks/bench_medoid_corr_method.py:38 | Uses the legacy np.random.RandomState(seed) API while every other file in the cluster uses np.random.default_rng(seed). | TODO |
| FS_BENCHMARKS_B-9 | fs_benchmarks_b | src/mlframe/feature_selection/_benchmarks/fs_quality/qual24_adaptive_nbins_large_n_reg_confirm.py:19-20 | Imports its sibling module via a sys.path.insert hack + bare import, while the other two campaign scripts in the same directory use the clean absolute package import. | TODO |
| FS_BENCHMARKS_C-10 | fs_benchmarks_c | src/mlframe/feature_selection/_benchmarks/wide_data_scaling/h1_gpu_large.py:11-16 | h1_gpu_large.py re-implements the PROG/ck() progress-log helper inline instead of importing the shared _progress_shared.ck that its two siblings use, defeating the shared module's stated purpose; the drift is already observable (inline copy formats timestamps as %H:%M:%S vs the shared helper's %Y-%m-%d %H:%M:%S). | TODO |
| FS_BENCHMARKS_C-4 | fs_benchmarks_c | src/mlframe/feature_selection/_benchmarks/kernel_tuning_cache/cli.py:47,90 | Multiple '# Wave N (date): ...' comments embed process/audit-phase metadata directly in source (also in auto_tune.py:139 and _auto_tune_sweeps_a.py:401,529,892), violating the project's own rule against phase/wave markers and date stamps in code comments. | TODO |
| FS_BENCHMARKS_C-5 | fs_benchmarks_c | src/mlframe/feature_selection/_benchmarks/bench_pr4_methods.py:67,84-86 | _run_knockoffs' docstring claims the Barber-Candes FDR threshold defaults to q=0.2, but the actual parameter default is fdr_q=0.5, and the underlying _bc_threshold has an independent, never-used default of q=0.1 — three different numbers for one policy value. | TODO |
| FS_BENCHMARKS_C-6 | fs_benchmarks_c | src/mlframe/feature_selection/_benchmarks/collision_census.py:14-16 | Module docstring's usage example shows --n-features and --max-order CLI flags that do not exist in the actual argparse.ArgumentParser (only --out is defined). | TODO |
| FS_BENCHMARKS_C-7 | fs_benchmarks_c | src/mlframe/feature_selection/_benchmarks/profile_wellbore_mrmr_only_100k.py:11-13 | Docstring's nsys/ncu example commands reference profile_wellbore_mrmr_only.py, but the actual filename is profile_wellbore_mrmr_only_100k.py. | TODO |
| FS_BENCHMARKS_C-8 | fs_benchmarks_c | src/mlframe/feature_selection/_benchmarks/kernel_tuning_cache/cli.py:10-27 | Module docstring's subcommand list omits refresh-batch-mi-noise-gate and refresh-fe-gpu-pairs-mi, both fully implemented, registered as subparsers, and folded into refresh-all. | TODO |
| FS_BENCHMARKS_C-9 | fs_benchmarks_c | src/mlframe/feature_selection/_benchmarks/bench_usability_corr_f32_f64.py:70 | Module-level _f64_cache dict, keyed by id(y), is defined at the bottom of the file after use and correctness relies entirely on the `pairs` list keeping every y array alive for the process lifetime of id() validity — a fragile identity-keyed cache that would silently break if the pair list were ever streamed/released early. | TODO |
| FS_BORUTA_ROOT-4 | fs_boruta_root | src/mlframe/feature_selection/boruta_shap/_fit_explain.py:521-527 | The 'log every 5 trials' block sits outside the for-trial loop (dedented to the loop's sibling level), so it executes exactly once after the last trial instead of periodically, making it dead/misleading code. | TODO |
| FS_BORUTA_ROOT-5 | fs_boruta_root | src/mlframe/feature_selection/boruta_shap/__init__.py:766-774 | TentativeRoughFix() never clears/narrows self.tentative after resolving those features into accepted/rejected, leaving self.tentative stale (reports resolved features as still undecided) for any caller that reads it directly. | TODO |
| FS_BORUTA_ROOT-6 | fs_boruta_root | src/mlframe/feature_selection/unanimous_permutation_prune.py:94-104 | No validation that cv_splits is non-empty; an empty sequence makes np.stack([], axis=0) raise an opaque numpy ValueError instead of a clear, actionable error naming cv_splits. | TODO |
| FS_WRAPPERS-7 | fs_wrappers | src/mlframe/feature_selection/wrappers/_helpers.py:89-106 | split_into_train_test's X_estimator fast-path crashes with an opaque numpy-cast ValueError when X_estimator is supplied but col_pos=None and features_indices are strings (falls into the else branch that casts names directly to np.intp). Not reachable via any current in-scope caller, but this is a public __all__-exported helper with no guard against the misuse. | TODO |
| FS_WRAPPERS-8 | fs_wrappers | src/mlframe/feature_selection/wrappers/rfecv/_fit_outer_loop.py:233-238 | _eval_fold_pinned calls pin_threads_to_one(estimator) (an estimator.set_params mutation on the single shared outer estimator) from inside the per-fold closure that joblib dispatches across multiple worker threads under force_parallel=True, an unguarded concurrent-mutation pattern that is only safe today because every thread writes identical values. | TODO |
| FS_WRAPPERS-9 | fs_wrappers | src/mlframe/feature_selection/wrappers/rfecv/_sffs.py:131-135 | The SFFS swap-pass summary log reports n_swaps_accepted/len(swap_out), but the loop actually iterates zip(swap_out, swap_in) which truncates to min(len(swap_out), len(swap_in)); when fewer drop-candidates exist than kept-candidates, the logged denominator overstates the pairs actually attempted. | TODO |
| METRICS-10 | metrics | src/mlframe/metrics/regression/_regression_benchmark.py:173-199 | fast_rmspe silently drops y_true==0 rows with no warning, unlike its direct siblings fast_mape_mean and maximum_absolute_percentage_error which both emit a rate-limited RuntimeWarning for the identical situation. | TODO |
| METRICS-11 | metrics | src/mlframe/metrics/_core_precision_mape.py:78,104-109 | fast_classification_report's accuracy divides by len(y_true) (raw count) while weighted_averages divides by support_total (in-range labels only) — inconsistent denominators when out-of-range labels are present. | TODO |
| METRICS-12 | metrics | src/mlframe/metrics/calibration/_calibration_plot.py:220 | Em-dash/en-dash Unicode characters used as prose punctuation across ~7 files in this cluster, inconsistent with the ASCII '--' convention used throughout the rest of the codebase. | TODO |
| METRICS-13 | metrics | src/mlframe/metrics/classification/_classification_extras_blocks.py:311-315 | Comment says a known bug was 'left unfixed in this sibling fused block', but the code directly below actually applies the fix (present_macro filtering) — self-contradictory wording that could mislead a future maintainer. | TODO |
| METRICS-14 | metrics | src/mlframe/metrics/iteration_metrics.py:130-182 | Module docstring claims 'NOT a reimplementation: every number comes from an existing public metric function', but _multiclass_metrics's log_loss is a fresh inline formula (no public multiclass log-loss primitive exists in the package); it also silently drops out-of-range-label rows with no surfaced count, unlike the package's established pattern. | TODO |
| METRICS-8 | metrics | src/mlframe/metrics/_multilabel_extras.py:480-502 | fast_multilabel_classification_metrics_block's jaccard_macro uses 0.0 for zero-support labels while jaccard_score_multilabel (same package) uses 1.0 for empty-union rows, with no cross-reference between the two docstrings. | TODO |
| METRICS-9 | metrics | src/mlframe/metrics/classification/_classification_extras.py:645-671 | _rps_kernel/ranked_probability_score has no bounds validation on y_true class indices, unlike the sibling _multiclass_confusion_kernel in the same file which explicitly guards 0<=t<K; an out-of-range label silently produces a wrong RPS. | TODO |
| MODELS-10 | models | src/mlframe/models/tuning.py:550-560 | create_ctr_params's next(iter(cands)) has no emptiness guard on generate_valid_candidates' result; unreachable today but would raise an opaque StopIteration if a future call site added restrictive rule kwargs. | TODO |
| MODELS-11 | models | src/mlframe/models/_optimization_shared.py:209-215 | Several except Exception handlers across the cluster (also process_method.py:370-372, score_flavours.py:223-225,451-453) log only at DEBUG, inconsistent with sibling broad-excepts in the same package that log at WARNING; a real bug in the guarded code could go unnoticed. | TODO |
| MODELS-12 | models | src/mlframe/models/ensembling/predict.py:44-107 | _gate_cache memoizes the outlier-member gate purely by id() of prediction arrays plus thresholds, with no content fingerprint; in-place mutation of a still-cached array between calls would silently return a stale gate decision. | TODO |
| MODELS-13 | models | src/mlframe/models/_optimization_shared.py:86-109 | compute_candidates_exploration_scores builds a value->index dict from search_space; duplicate values silently collapse to the last occurrence's index, giving wrong distances for earlier duplicates. | TODO |
| MODELS-14 | models | src/mlframe/models/tuning.py:565-567 | create_ctr_params's 'CrossEntropy' in params.get('loss_function', []) silently degrades to a substring test if loss_function is a bare string rather than a list, so e.g. 'QueryCrossEntropy' would incorrectly match. | TODO |
| PREPROCESSING-11 | preprocessing | src/mlframe/preprocessing/regime_conditioned_imputation.py:26-29 | fit_regime_conditioned_median's docstring contains a stray, undefined cross-reference ('same architectural gap as F1/F7') with an odd trailing period on its own line -- meaningless to a reader of this module in isolation, leftover from an external planning document. | TODO |
| PREPROCESSING-12 | preprocessing | src/mlframe/preprocessing/_benchmarks/check74.py:27 | The 'falsy_skip' test case passes skip_vals=(0.0) -- missing the trailing comma for a 1-tuple, so it is actually the scalar float 0.0, which is falsy and makes 'if skip_vals:' skip the skip-value logic entirely on both sides, so the case never actually exercises what its name claims to test. | TODO |
| REPORTING_A-10 | reporting_a | src/mlframe/reporting/auto_dispatch.py; src/mlframe/reporting/__init__.py:auto_dispatch.py:134,147,175; __init__.py:4-5 | Non-ASCII em-dash (U+2014) used in prose comments/docstrings, inconsistent with this same package's own explicit 'Console output is ASCII-only' convention in catalog.py. | TODO |
| REPORTING_A-11 | reporting_a | src/mlframe/reporting/_diagnostics_dispatch_extra.py:123-135 (_flat_scalar_metrics) | Inconsistent merge semantics: top-level scalar keys unconditionally overwrite, but keys inside a nested sub-dict use setdefault, so two different nested sub-dicts sharing a key name silently drop the second value instead of a documented precedence rule. | TODO |
| REPORTING_A-8 | reporting_a | src/mlframe/reporting/_risk_coverage_diagnostic.py; src/mlframe/reporting/auto_dispatch.py:_risk_coverage_diagnostic.py:19-22/36; auto_dispatch.py:88/142 | Implicit-Optional annotation bugs: y_score: np.ndarray (required) is checked via 'y_score is None', and targets: np.ndarray (required) is checked via 'targets is not None' — both should be Optional[np.ndarray]. | TODO |
| REPORTING_A-9 | reporting_a | src/mlframe/reporting/_diagnostics_dispatch_extra.py; src/mlframe/reporting/catalog.py:_diagnostics_dispatch_extra.py:474,482,515-524,556-567,600-611; catalog.py:135 | Multiple function parameters carry no type annotation at all (df, y_true, feature_names, feature_importances, timestamps in three render_*_diagnostic functions; names/feature_importances/k in _ranked_top_features; df/names in _first_group_column; file in catalog.describe_available_panels) despite being compared to None / passed to typed builders. | TODO |
| REPORTING_B-6 | reporting_b | src/mlframe/reporting/charts/decision_curve.py:137 (docstring) vs 147 (code) | build_decision_curve_spec's docstring states the 'useful' verdict fires on 'margin > 1e-4' but the code uses np.nanmax(advantage) > 1e-3 -- a 10x mismatch between documented and actual threshold for a business-facing verdict surfaced into metrics_dict. | TODO |
| REPORTING_B-7 | reporting_b | src/mlframe/reporting/renderers/matplotlib.py:633 | In _line's band handling, `_xi(0) if not xs_per_series else p.x[0]` is a dead-code-equivalent ternary: _xi(0) already returns p.x[0] if xs_per_series else p.x, so both branches evaluate to exactly _xi(0). Harmless but confusing. | TODO |
| REPORTING_B-8 | reporting_b | src/mlframe/reporting/charts/drift.py:396 | cusum_residual_drift's finite-row mask is `mask &= np.isfinite(...) if numeric else mask` -- for non-numeric (datetime64) timestamps the else branch is a no-op `mask &= mask`, so NaT timestamps are never filtered and can land at an arbitrary sorted position in the CUSUM's time-ordered residual sequence. | TODO |
| REPORTING_B-9 | reporting_b | src/mlframe/reporting/charts/shap_per_instance.py:89 | _is_binary_score guards its np.all(np.isin(...)) check on the pre-NaN-filter yt.size rather than the post-filter size; for an all-NaN y_true, np.all([]) is vacuously True, so label_ok=True is reported for a target carrying zero real label information instead of the intended indeterminate/False. | TODO |
| TRAINING_COMPOSITE_CORE_A-6 | training_composite_core_a | src/mlframe/training/composite/_winkler.py:216-259 | winkler_score_per_group reimplements group factorization inline instead of reusing the shared _composite_report_shared.factorize helper the sibling modules were refactored to share, and as a result independently reproduces the same NaN-mapping gap as -3 in its own np.unique fallback. | TODO |
| TRAINING_COMPOSITE_CORE_A-7 | training_composite_core_a | src/mlframe/training/composite/_composite_array_shared.py:12-23 | n_features(X) returns 0 for a 1-D array input instead of treating it as a single feature column. | TODO |
| TRAINING_COMPOSITE_CORE_A-8 | training_composite_core_a | src/mlframe/training/composite/_composite_utils.py:39-43 | sklearn_set_params silently accepts and applies any keyword via a bare setattr loop, unlike sklearn's own set_params contract which raises ValueError on an unrecognized parameter name, so a typo'd kwarg is silently swallowed. | TODO |
| TRAINING_COMPOSITE_CORE_B-6 | training_composite_core_b | src/mlframe/training/composite/glm.py:113-133 | _default_inner builds the default LGBMRegressor with no random_state, unlike the sibling default-builder highlevel.py._default_inner_estimator (same cluster) which explicitly pins random_state=0 -- an implicit, undocumented reproducibility invariant rather than an explicit contract. | TODO |
| TRAINING_COMPOSITE_CORE_B-8 | training_composite_core_b | src/mlframe/training/composite/glm.py:119-124 | _default_inner wraps the lightgbm import in `except Exception as exc: raise ImportError(...)`, relabeling any non-import failure (broken native DLL, version-incompatible binary) as 'lightgbm not installed' -- the same broad-except-masks-the-real-error class this codebase's own change history documents fixing twice elsewhere this session. | RESOLVED (e0cb1cb23) |
| TRAINING_COMPOSITE_DISCOVERY-4 | training_composite_discovery | src/mlframe/training/composite/discovery/bayesian.py:1-332 | bayesian_alpha_fit / bayesian_alpha_fit_bootstrap (posteriors for linear_residual's alpha/beta) are never called from any production discovery path -- only unit-tested standalone -- and unlike forward_stepwise.py the module carries no 'intentionally standalone, not auto-integrated' docstring framing, so its unreachability reads as ambiguous rather than a documented design choice. | RESOLVED (08841c582) |
| TRAINING_COMPOSITE_DISCOVERY-5 | training_composite_discovery | src/mlframe/training/composite/discovery/_screening_tiny_perbin.py:42-43 | Dead, exact-duplicate module-level _KFOLD_SPLIT_CACHE / _KFOLD_SPLIT_CACHE_MAX declarations left over from the _screening_tiny.py module split; this file actually imports and uses the live _cached_kfold_splits (and its underlying cache) from _screening_tiny.py, so these two lines are never read or written. | RESOLVED (03b16ad7a) |
| TRAINING_COMPOSITE_ENSEMBLE_ESTIMATOR_TRANSFORMS-3 | training_composite_ensemble_estimator_transforms | src/mlframe/training/composite/estimator/_soft_shrink.py:55-63 | second_diff's inverse (y = T_hat + 2*b1 - b2) is purely additive-linear in base, same family as diff (which IS covered), yet second_diff is missing from ADDITIVE_BASE_TRANSFORMS. | RESOLVED (196c4dae3) |
| TRAINING_COMPOSITE_ENSEMBLE_ESTIMATOR_TRANSFORMS-4 | training_composite_ensemble_estimator_transforms | src/mlframe/training/composite/transforms/nonlinear.py:146-150 | _MAD_FLOOR_FRAC / _MAD_SOFT_CAP_K are redefined at module scope in nonlinear.py, byte-identical to the parent's actually-used constants, but never referenced anywhere in this file — dead leftover from the monolith split, a drift trap for future edits. | TODO |
| TRAINING_CORE_A-2 | training_core_a | src/mlframe/training/core/_phase_composite_post_xt_ensemble/_post_xt_ensemble_mtr.py:117-126 | MTRPerColumnEqualMeanEnsemble.fit()'s docstring claims NNLS weights are normalised to sum to 1, but the code (and the class/property docstrings in the same file) explicitly keep raw, unnormalised weights. | TODO |
| TRAINING_CORE_B-4 | training_core_b | src/mlframe/training/core/ (systemic, e.g. _phase_helpers_fit_split.py:3-6, _setup_helpers_pipeline_cache.py:60-88):multiple | Comments throughout the cluster embed process/audit metadata (wave numbers, iterNNN refs, date stamps, refactor-history narration) that CLAUDE.md's own comment-style rule explicitly bans as belonging in git history, not code. | TODO |
| TRAINING_FEATURE_HANDLING_TARGETS-4 | training_feature_handling_targets | src/mlframe/training/ranking/_ranker_fs.py:404 | group_aware_mrmr_select's `cap = min(n, int(max_features)) if max_features else n` treats max_features=0 as unset instead of capping at zero. | TODO |
| TRAINING_FEATURE_HANDLING_TARGETS-6 | training_feature_handling_targets | src/mlframe/training/targets/regression_residual_audit.py:182 | EXCESS_KURT_MILD=1.5 is declared with a comment implying it's a real threshold but is never referenced anywhere else; _diagnose reuses EXCESS_KURT_HEAVY (also 1.5) for that boundary instead. | TODO |
| TRAINING_FEATURE_HANDLING_TARGETS-7 | training_feature_handling_targets | src/mlframe/training/ranking/ranker_suite.py:123-183 | ranker_suite.borda_fuse (Sum(size-rank)) and ranking.ensemble_ranker_scores(method='borda') (-Sum(rank)) both claim to be 'Borda' fusion but compute different magnitudes, order-equivalent only within a fixed group size. | TODO |
| TRAINING_LOOSE_A-4 | training_loose_a | src/mlframe/training/_confidence_analysis.py:104-107 | run_confidence_analysis mutates the caller-supplied confidence_model_kwargs dict in place (direct key assignment + setdefault) instead of copying it first; currently masked because the sole in-repo caller always passes a fresh dict(...) copy, but any future/direct caller reusing a shared kwargs dict would see it silently polluted. | TODO |
| TRAINING_LOOSE_A-5 | training_loose_a | src/mlframe/training/_aggregate_cv_early_stopping.py:54-58 | select_best_iteration_by_aggregate_cv validates curves.shape[1]==0 (zero rounds) but never validates curves.shape[0]==0 (zero folds); a 0-fold input silently produces NaN (with a RuntimeWarning) from np.mean/median/trim_mean over an empty axis instead of a clear ValueError like the sibling check two lines above. | TODO |
| TRAINING_LOOSE_A-6 | training_loose_a | src/mlframe/training/_easy_ensemble.py:21, 59-68, 78, 98 | easy_ensemble_fit_predict validates bag_feature_subsample but never validates n_bags; n_bags<=0 leaves bag_preds empty and np.mean(bag_preds, axis=0) raises an opaque 'zero-size array to reduction operation' instead of a clear function-specific message. | TODO |
| TRAINING_LOOSE_A-7 | training_loose_a | src/mlframe/training/_data_helpers.py:36, 64 | logger = logging.getLogger(__name__) is defined twice at module scope (right after imports, and again right after _validate_trusted_path). Harmless but dead/confusing. | TODO |
| TRAINING_LOOSE_A-8 | training_loose_a | src/mlframe/training/_data_helpers.py:665, 669, 673 | In _setup_eval_set's shard fan-out branch, three list-extension expressions of the shape 'X if X is not None else None' (for sample_weight/base_margin/group_ids) are logical no-ops that always evaluate to X; reads as a defensive None-guard but does nothing. | TODO |
| TRAINING_LOOSE_B-5 | training_loose_b | src/mlframe/training/_pseudo_group_reconstruction.py:69-77 | reconstruct_pseudo_group_ids collapses a multi-column key to one 64-bit hash before grouping instead of an exact factorize/groupby; a hash collision could silently merge two distinct rows into the same pseudo-group (safe-direction only, since it only makes GroupKFold more conservative). | TODO |
| TRAINING_LOOSE_B-6 | training_loose_b | src/mlframe/training/_profile_fs_suite_integration.py:76-88 | The cProfile harness's main() prints stats only and never calls pstats.Stats.dump_stats(...), contrary to this project's standing rule that profiling runs must always save stats. | TODO |
| TRAINING_LOOSE_B-7 | training_loose_b | src/mlframe/training/_model_factories.py:26-49 | Process/audit metadata (date stamps, Wave N, iterNNN, Fix N markers) is embedded pervasively in source comments/docstrings across this cluster, contrary to CLAUDE.md's explicit ban on such metadata in code comments. | TODO |
| TRAINING_LOOSE_C-11 | training_loose_c | src/mlframe/training/helpers.py:41 | Comment '_build_classifier_chain_ensemble kept in helpers.py (line 86)' is stale -- the function is defined in _classif_helpers.py:611 and only imported here. | TODO |
| TRAINING_LOOSE_C-12 | training_loose_c | src/mlframe/training/loss_recommendation.py:36 | Docstring references a prefer_l1_above_kurt parameter/mechanism that does not exist anywhere in the module or repo. | TODO |
| TRAINING_LOOSE_C-13 | training_loose_c | src/mlframe/training/mlp_runtime_defaults.py:478 | resolve_mlp_train_batch_size's docstring says it 'preserves the previous 1024 default as the ceiling', but _TRAIN_BATCH_MAX was explicitly raised to 65536 per the comment block directly above the constant. | TODO |
| TRAINING_LOOSE_C-14 | training_loose_c | src/mlframe/training/quantile_wrapper.py:27-33 | Module docstring lists a 3-candidate probe order including 'quantile_alpha', but _ALPHA_PARAM_CANDIDATES only has 2 entries and explicitly excludes quantile_alpha per its own docstring a few lines below. | TODO |
| TRAINING_LOOSE_C-15 | training_loose_c | src/mlframe/training/suite_artefact_cache.py:282-284 | _evict_lru_locked's byte-based eviction gate treats bytes_limit=0 as 'unlimited' (never evicts by size) rather than 'zero-byte budget', so pre-existing on-disk entries survive a budget change to 0 even though put() correctly refuses new nonzero-size entries at that setting. | TODO |
| TRAINING_NEURAL-7 | training_neural | src/mlframe/training/neural/_recurrent_data.py:162-420 | RecurrentDataModule is exported public API (package __all__) but has zero production callers -- the actual fit path builds dataloaders independently via _RecurrentWrapperBase, duplicating the same stratified-sampler logic in two places. | TODO |
| TRAINING_NEURAL-8 | training_neural | src/mlframe/training/neural/__init__.py:57 | Comment claims field_grouped_mlp.py's docstring contains an honest-negative note about a reproduction failure, but no such note exists anywhere in that file. | TODO |
| TRAINING_PIPELINE_MISC-2 | training_pipeline_misc | src/mlframe/training/callbacks/iteration_metrics.py:162-177 | CBIterationMetricsCallback._predict's is_reg = "regression" in self.target_type substring check only works for learning_to_rank today because the sole production wiring site derives target_type from is_classifier(model) rather than the genuine target_type string; if ever wired with the real "learning_to_rank" value it would call predict_proba on a ranker, raise AttributeError, get silently swallowed (debug-only log), and leave iteration_metrics_ permanently empty. | RESOLVED (45e896861) |
| TRAINING_PIPELINE_MISC-3 | training_pipeline_misc | src/mlframe/training/baselines/_dummy_baseline_regression.py:201-202 | The seasonal_naive_pP dummy baseline builds val/test predictions via a Python list comprehension indexing train_y once per output row instead of vectorized modulo indexing (train_y[-P:][idx % P]). | TODO |
| TRAINING_PIPELINE_MISC-4 | training_pipeline_misc | src/mlframe/training/baselines/_dummy_metrics_pick_plot.py:517-518 | A source comment contains an unintentional Cyrillic phrase ("толпа графиков") embedded in otherwise-English prose, plus a date stamp, violating the project's own comment-style convention. | TODO |
| VOTENRANK-10 | votenrank | src/mlframe/votenrank/shapley_blend.py:137-151 | _msr_banzhaf's n_permutations parameter actually represents n_coalitions (independent random coalition masks, not orderings), inherited from the shared shapley_model_values call-site signature. | TODO |
| VOTENRANK-11 | votenrank | src/mlframe/votenrank/confidence_gated_blend.py:140-146 | per_sample_gate_calibration's documented leakage-avoidance contract (calibration data must be disjoint from the blended rows) is entirely caller-enforced -- no runtime check or warning for the common accidental-reuse mistake. | TODO |
| VOTENRANK-12 | votenrank | src/mlframe/votenrank/stability_exp.py:43-44 | spearman_exp injects NaNs one cell at a time via per-index .iloc assignment in a Python loop instead of a single vectorized array write. | TODO |
| VOTENRANK-13 | votenrank | src/mlframe/votenrank/iia_exp.py:32,35 | compute_iia_for_fixed_models assumes models_order has >= 3 entries; a leaderboard with fewer than 3 models silently returns result=0 (looking like 'zero IIA violations') instead of raising a clear error that IIA needs >= 3 models. | TODO |
| VOTENRANK-8 | votenrank | src/mlframe/votenrank/leaderboard/leaderboard_impl.py:64,72,80 | Five comments across data_processing.py:100, dual_optimizer_blend.py:162, and leaderboard_impl.py:64,72,80 embed literal prior-audit finding-ID markers (# F1:, # F2:, # F3:, # F5:, # F10:), which CLAUDE.md explicitly forbids in code comments; the existing test_f13 regression test only checks 2 unrelated literal strings and misses this pattern entirely. | TODO |
| VOTENRANK-9 | votenrank | src/mlframe/votenrank/shapley_blend.py:154-156 | _bootstrap_stderr is named as a bootstrap estimator but its own docstring says it is 'not a true bootstrap' -- it's a plain analytic \|value\|/sqrt(n) proxy. | TODO |
| X_ARCHITECTURE_API_CONSISTENCY-5 | x_architecture_api_consistency | calibration/__init__.py:1-63 | calibration/__init__.py never defines __all__, unlike every sibling package facade (preprocessing, models, data, estimators, inference, utils, core) which explicitly curates __all__ = sorted(...). | TODO |
| X_ARCHITECTURE_API_CONSISTENCY-6 | x_architecture_api_consistency | calibration/post.py:126 | BinaryPostCalibrator uses reflection-based store_params_in_object(get_parent_func_args()) to populate self, an undocumented second idiom alongside the explicit self.param=param pattern used by ~80 other public estimators (though also used validly by RFECV, PytorchLightningEstimator, EarlyStoppingWrapper). | TODO |
| X_ARCHITECTURE_API_CONSISTENCY-7 | x_architecture_api_consistency | feature_engineering/bruteforce.py:86 | verbose is typed bool in some public functions/classes and int (0/1/2 level) in others with no pattern by subsystem (e.g. feature_engineering/bruteforce.py uses int while feature_engineering/timeseries.py uses bool in the same package). | TODO |
| X_ARCHITECTURE_API_CONSISTENCY-8 | x_architecture_api_consistency | feature_selection/wrappers/rfecv/__init__.py:163-232 | RFECV is the sole class in the feature_selection wrapper family mixing grouped pydantic config objects with ~60 additional flat kwargs (documented back-compat choice), while sibling selectors (BorutaShap, HybridSelector, ACESelector) use pure flat kwargs; RFECV's ~70-param constructor is the widest in the audited surface. | TODO |
| X_CICD_DEPENDENCIES-8 | x_cicd_dependencies | .github/workflows/sklearn-matrix-ci.yml:68-72 | The defaults.run.shell: bash block's comment explains it exists for a Windows matrix row, but the Windows leg was dropped 2026-07-09 and the matrix is ubuntu-latest only now -- harmless but misleading. | TODO |
| X_OSS_HYGIENE_PACKAGING-10 | x_oss_hygiene_packaging | docs/MULTI_TARGET_REGRESSION.md:13,80 | Two 'Landed in commit X' citations (2d300944, 86504f08) do not resolve to any commit in the repo's history (git cat-file -e fails for both against all refs), while the doc's other four cited SHAs all resolve fine. Real commits, found via message search, are 0a6e60ea7 and c9d5067f0 respectively. | TODO |
| X_OSS_HYGIENE_PACKAGING-11 | x_oss_hygiene_packaging | docs/MULTI_TARGET_REGRESSION.md:97 | Cites _phase_composite_discovery.py:223 for the REGRESSION-only composite-discovery filter; that line is now unrelated log-message code in the current file (src/mlframe/training/core/_phase_composite_discovery.py) -- the actual gate is at lines 81 and 187. | TODO |
| X_OSS_HYGIENE_PACKAGING-12 | x_oss_hygiene_packaging | docs/honest_diagnostics_guide.md:108 | Cites tests/training/test_honest_diagnostics_aggregator.py as covering the aggregator's schema/thresholds/DeLong wiring; this exact file does not exist. The behavior is covered by test_honest_diagnostics_default.py (under a different name) plus two other split-off files not mentioned in the doc. | TODO |
| X_OSS_HYGIENE_PACKAGING-13 | x_oss_hygiene_packaging | docs/composite_config_reference.md:163 | The auto-generated cross_target_ensemble_strategy field description cites a benchmark file composite_ensemble_shootout.py (justifying the nnls_stack default) that does not exist anywhere in the repo; root cause is the verbatim source comment at src/mlframe/training/_composite_target_discovery_config.py:230. | TODO |
| X_OSS_HYGIENE_PACKAGING-14 | x_oss_hygiene_packaging | docs/NUMERICAL_STABILITY_REPORT.md:1-224 | This is the repo's only general 'numerical stability' reference doc, correctly scoped to feature_engineering/numerical.py, but it neither mentions nor is cross-referenced by three later (2026-08-04) catastrophic-cancellation bugs found in different modules using the exact same raw-moment-expansion formula this report already flagged as unstable -- one still explicitly open per CLAUDE.md. | TODO |
| X_OSS_HYGIENE_PACKAGING-9 | x_oss_hygiene_packaging | mkdocs.yml:57-63 | docs/SHAP_PROXIED_FS_GAME_THEORY.md is listed in docs/README.md's 'Internal / research notes' table alongside five other docs that are all explicitly excluded from the mkdocs sidebar via not_in_nav:, but it alone is missing from both nav: and not_in_nav:, breaking its own stated peer-group convention and likely producing an mkdocs nav-coverage build warning. | TODO |
| X_SECURITY_ROBUSTNESS-6 | x_security_robustness | src/mlframe/training/feature_handling/cache_backend.py:130-136 | LocalDiskBackend builds .bin/.lock paths directly from a caller-supplied key with zero validation in the primitive itself, relying entirely on an unenforced caller contract documented only in a comment; not currently wired to any production caller but a footgun for whoever integrates it next. | TODO |
| X_SECURITY_ROBUSTNESS-7 | x_security_robustness | src/mlframe/integrations/mlflow.py:73-94 | embed_website_to_mlflow's docstring warns that url is embedded with no sanitization but says nothing about fname, which is used unsanitized in open(fname + extension, "w") -- a fname containing '../' could write outside the intended directory. | TODO |
| X_SECURITY_ROBUSTNESS-8 | x_security_robustness | src/mlframe/training/_data_helpers.py:36,64 | logger = logging.getLogger(__name__) is assigned twice in the same module (harmless duplication left over from an extraction/merge). | TODO |
| X_TEST_SUITE_ARCHITECTURE-7 | x_test_suite_architecture | tests/test_meta/test_x_test_suite_architecture_fixes.py:1-152 | The regression file proving the prior cycle's own fixes stuck is itself built on inspect.getsource() string-pattern assertions, the exact antipattern feedback_behavioral_tests and this repo's own meta-linter prohibit for every other test file; it is grandfathered into the linter's whitelist as documented debt rather than converted. | TODO |

| TRAINING_NEURAL-4 | training_neural | src/mlframe/training/neural/_optimizer_wrapper_shared.py:14-30 | (found during TRAINING_NEURAL-3 validation, not in original audit) Lookahead/SAM's param_groups/state getters returned list(...)/dict(...) copies of the base optimizer's live containers; any append-based mutation (e.g. torch's own Optimizer.add_param_group) landed on a throwaway copy and was silently lost. | RESOLVED (9014f8a33) |
| TRAINING_NEURAL-5 | training_neural | tests/training/neural/test_neural_medium_severity_regressions.py:331-341 | (found during TRAINING_NEURAL-3 validation, not in original audit) test_m_neu_11_no_lazy_import_in_create_dataset asserted against recurrent_dataset_helpers (a pure re-export shim) instead of _recurrent_wrapper_base, the module that actually owns _create_dataset since an earlier monolith split -- always failed. | RESOLVED (9014f8a33) |
| MRMR-1 | mrmr | `src/mlframe/feature_selection/filters/_mrmr_validate_transform.py:266-276` | `_validate_inputs`'s object-dtype +/-inf guard (`np.frompyfunc(lambda v: isinstance(v, float) and np.isinf(v), ...)`) only catches values that are instances of the builtin `float` (which covers Python floats and `np.float64`, since `numpy.float64` subclasses `float`), but NOT `np.float32`/`np.float16`/`np.longdouble` infinities stored in an object-dtype column (e.g. a ragged read, a mixed-precision upstream pipeline, or `df['c'] = df['c'].astype(object)` after an earlier `np.float32` computation). Such a value silently passes this guard — the exact "undefined bin on inf" failure mode the guard's own error message and the module docstring say it exists to prevent — and only the float64/native-float path (`select_dtypes(include=["floating"])`) is dtype-complete; the object-column fallback is not. Low likelihood in practice (float32 rarely ends up boxed in an object column) but a real, narrow gap in a guard whose whole job is catching exactly this. | TODO |
| MRMR-2 | mrmr | `src/mlframe/feature_selection/filters/_mrmr_partial_fit.py:83-92` (`_to_series`) | `MRMR.partial_fit` unconditionally rejects a multi-column `y_new` (`ValueError: y as DataFrame must be single-column`) with no ndarray-multicolumn path either (`np.asarray(y).ravel()` flattens any 2-D array into 1-D instead of raising, which is arguably worse — it silently reinterprets a genuine `(n, k)` multilabel/multi-target `y_new` as `n*k` single-target rows). `MRMR.fit` itself supports multilabel/multi-output `y` (see `_fit_multioutput` dispatch in `_mrmr_class.py`), so this is an undocumented capability regression specific to the incremental/streaming API: a caller doing multilabel MRMR via `fit()` who then switches to `partial_fit()` for streaming updates gets either a confusing single-column-only `ValueError` (DataFrame `y_new`) or a silent shape-corrupting `.ravel()` (ndarray `y_new`) instead of a working streaming multilabel path or an explicit "not supported" error. | TODO |
| MRMR-3 | mrmr | `src/mlframe/feature_selection/filters/_mrmr_partial_fit.py:91` | Direct consequence of MRMR-2 on the ndarray branch: `_to_series(y)` does `np.asarray(y).ravel()` with no shape check at all, so a genuine 2-D `y_new` ndarray (multilabel/multi-output) is flattened into a `(n*k,)` series and concatenated against an `(n,)`-row `X_new` buffer with no error — `partial_fit` would proceed on badly misaligned rows/labels rather than failing loudly. A one-line `if arr.ndim > 1: raise ValueError(...)` guard (matching the DataFrame branch's explicit rejection) is missing here. | TODO |
| MRMR-4 | mrmr | `tests/feature_selection/mrmr/**` (coverage gap re: MRMR-1..3) | No test found exercising (a) an object-dtype column holding a non-Python-`float` infinity (e.g. `np.float32('inf')` boxed into an `object` array) against `_validate_inputs`'s inf guard, or (b) `MRMR.partial_fit` called with a genuinely 2-D/multilabel `y_new` (DataFrame or ndarray) to pin either a working streaming-multilabel path or an explicit rejection. `test_mrmr_partial_fit_dtype_mismatch.py` and the `partial_fit`-adjacent tests under `tests/training/feature_selection/` cover dtype/column-alignment edge cases but not the multi-column-`y` case specifically. | TODO |
| MRMR-5 | mrmr | `src/mlframe/feature_selection/filters/_mrmr_fit_impl/_fit_impl_core.py:73-86` (`_fit_impl`, ~10,100 total lines) | Architecture/maintainability note, not a functional bug: `_fit_impl` is one single ~10,000-line function, an order of magnitude past the project's own "carve before ~800-900 LOC" convention (`CLAUDE.md`, "New code goes in focused submodules from the start"). The file's own header comment explicitly claims an LOC-budget exemption ("one giant function cannot be split without distorting the fit control flow"), which is a defensible position for a tightly-coupled greedy-selection state machine — but at this size it is a real audit-surface and onboarding cost (this audit itself could only sample sections of it rather than verify every branch), and the exemption is self-granted rather than reflecting an actual technical impossibility (the file already successfully extracts small nested closures like `_fe_budget_ok`/`_eng_dedup_prefer`; a staged pipeline of well-named private helper functions passing an explicit fit-state object, rather than 10k lines of closures over `_fit_impl`'s locals, is a plausible refactor path, just a large one). Flagging for awareness rather than proposing an immediate rewrite, given the correctness risk of restructuring a function this size without an exhaustive behavioral-equivalence harness. | TODO |
| MRMR-6 | mrmr | `filters/_mrmr_validate_transform.py:114-121` (`_validate_string_params`) | Full-file re-read: the generic string-param validator loop does `if _val is None: continue` before checking allow-list membership. Correct for params whose allow-list legitimately contains `None` as a sentinel (`nbins_strategy`, `redundancy_aggregator`), but for params whose allow-list does **not** include `None` — `quantization_method` (`_VALID_QUANTIZATION_METHODS = ("quantile", "uniform")`), `mrmr_relevance_algo`, `mrmr_redundancy_algo`, `nan_strategy`, `fe_unary_preset`, `fe_binary_preset`, `cluster_aggregate_mode`, `mi_correction`, `stability_selection_method`, `dcd_distance`, `dcd_swap_method`, `additional_rfecv_selection_rule` — an explicit `MRMR(quantization_method=None)` silently bypasses the fail-fast `ValueError` the function's docstring promises ("Raise ValueError on bad constructor strings"). The bad value then reaches `_mrmr_fit_impl/_fit_impl_core.py:6022`'s `method=str(self.quantization_method)`, turning `None` into the literal string `"None"` — not a recognised discretisation method — so the failure (or silent misbehaviour) surfaces far from the actual cause with no message pointing back to the constructor param. Reproducible directly: `MRMR(quantization_method=None).fit(X, y)` skips the intended clean-error path. | TODO |
| MRMR-7 | mrmr | `filters/mrmr/_mrmr_class_config.py:258-270` (`_effective_n_jobs`) | `psutil.cpu_count(logical=False)` can return `None` on hosts (containers/some VMs) where physical-core detection fails — documented `psutil` behaviour. `_effective_n_jobs` does `if n_jobs == -1: return int(psutil.cpu_count(logical=False))` with no `None` guard, so on such a host every default-`n_jobs=-1` fit raises `TypeError: int() argument ... not 'NoneType'` instead of degrading gracefully. Every other host-probe in the MRMR tree (e.g. `_mrmr_sis_screen.py:_free_ram_bytes`) wraps its psutil call in try/except with a safe fallback; this one does not. | TODO |
| MRMR-8 | mrmr | `filters/_mrmr_sis_screen.py:257-271` (`sis_screen`, y-encoding branch chain) | The 3-way `if/elif` chain encoding `y` before the marginal-MI pass (nominal-string → factorize; low-cardinality-relative-to-`n_rows//20` integer → factorize; float/high-cardinality → quantile-bin) has no `else`. A small-`n_rows` integer target whose cardinality is moderate (falls between the `n_rows//20` factorize threshold and the `max(nbins,2)` quantile threshold) skips all three branches and reaches `_mi_classif_batch` unencoded. The downstream kernel (`_plugin_mi_classif_njit`) tolerates non-dense labels via a `y_max - y_min + 1` span rather than requiring 0-based codes, so this is not silently wrong for ordinary small label sets — but a large-magnitude/sparse integer target landing in this gap allocates its MI histogram sized by the raw span rather than the true class count, a latent memory/perf cliff on an otherwise-cheap O(p·n) screen. No test exercises this specific branch combination. | TODO |
