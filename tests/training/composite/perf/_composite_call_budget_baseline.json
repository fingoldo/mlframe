{
  "build_feature_matrix": {
    "measured": 4,
    "note": "ideal 1 per fit (PRF-18): the screen matrix is rebuilt by the tiny rerank, the honest gate and the y-scale gate"
  },
  "near_collinear_keep_mask": {
    "measured": 5,
    "note": "ideal 1 correlation build per target (PRF-01): one per base context plus the unary context"
  },
  "lgb_Dataset": {
    "measured": 54,
    "note": "ideal: one per distinct (base matrix, fold) (PRF-07); today 18 shared-fold builds plus 18 per-spec CV fits, 9 honest-gate fits, 8 WAIC OOF fits and 1 baseline"
  },
  "yscale_predicts_per_wrapper_split": {
    "measured": 6,
    "note": "ideal 2 per (wrapper, split) (PRF-13/14): the wrap pass, its pre-clip score, the per-model hook, the value report and the MoE each re-predict"
  },
  "pp_transforms_per_pipeline_frame": {
    "measured": 7,
    "note": "ideal 1 per (pipeline, frame) (PRF-16): the same frame is re-transformed by the same fitted pre-pipeline across the post-phases"
  }
}
