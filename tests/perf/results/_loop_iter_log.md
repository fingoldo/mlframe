# /loop fuzz-profile-optimize log (300k input shape)

Random fuzz combos profiled under cProfile at n_rows=300k via
`profiling/profile_fuzz_chains.py`. Each iteration: profile fresh-seed combos,
pick the top mlframe-side hotspot, optimize (measured + bit-identical + regression
test), commit. Termination = 100 consecutive REJECTs (see CLAUDE.md policy).

Consecutive-reject streak: 0

| iter | seed | mlframe hotspot | verdict | notes |
|------|------|-----------------|---------|-------|
| 1 | 20260702 | `reporting/charts/slice_finder.find_weak_slices` — built bounds/features labels for every valid cell of every combo, discarded all but top-k | RESOLVED+4.18x | 58.6ms→14.0ms @ n2000/p12; bit-identical 60 seeds × arity{1,2,3}; also fixed profiler-driver KeyError on multi_target_regression combos. commit 21e45c3f |
| 2 | 20260703 | `reporting/charts/calibration.bootstrap_reliability_band` — refit IsotonicRegression per bootstrap resample, O(n log n) sort ×150 inside the loop | RESOLVED+3.9x | 2217ms→564ms @ n50k; per-fit 5x; distinct-score fast path (sort once + bincount sample_weight), tied-score estimator fallback; bit-identical 40 seeds × {distinct,tied}. commit f5043a50 |
| 3 | 424242 | `training/targets/…analyzer.analyze_target_distribution` — skew+kurtosis helpers each recomputed mean/std/z already available | RESOLVED+2.3x (moment pair) | 14.1ms→6.2ms @ n300k; fuse onto one z; bit-identical 80 seeds. Also REJECTED `renderers/_trend.robust_fit_endpoints` (0.645s tottime = cProfile mis-attribution of sklearn TheilSen fit; numpy parts microbench ~4ms). commit 3cc9e0ba |
| 4 | 77777 | `training/targets/…analyzer._lag_autocorr` — 5 np.corrcoef calls/analyze (lag scan 1/2/3/5 + lag1), each building a 2xn stack + 2x2 cov for one value | RESOLVED+2.9x | 4-lag scan 42ms→14.5ms; full analyze 66ms→41ms @ n300k; direct Pearson via 3 dot products; ~1 ULP equiv (docstring), guards preserved. commit c228b9e8 |
| 5 | 13579 | `calibration/policy._normalize_binary_labels` — np.unique (O(n log n) sort) per bootstrap-ECE resample on already-0/1 labels | RESOLVED+7x | 210us→29.5us @ n30k; min==0/max==1 int/bool short-circuit; bit-identical incl {1,2}/{-1,+1} remap + raise. commit 21dd2f51. (bootstrap_metric core kernels already exhaustively tuned — many rejected-bench notes.) |
| 6 | 2468013 | `metrics/classification/_classification_calibration.accuracy_ratio` — per-row Python loop tie-folding equal-score blocks | RESOLVED+56x (tie-fold) | 17.9ms→0.32ms @ n60k; reduceat over sorted block starts; bit-identical 200 inputs, AR==2·AUC-1 preserved. FUTURE: `robust_fit_endpoints` TheilSen fit 786ms/call (real, not attribution) — cap-reduction changes the drawn line, needs visual-equivalence call. commit 45e3c176 |
| 7 | 8675309 | top items all already-optimized (calibration band, analyzer, accuracy_ratio), deferred (robust_fit TheilSen=FUTURE), or at floor (`_ice_metric.compute_probabilistic_multiclass_error` 23ms self-time = irreducible (N,K) coercion, batched kernel already) | REJECT | no cheap bit-identical mlframe hotspot at this profile; readily-profiled paths now optimized. Streak→1. Next: sample fresh combo-pool / prefer-models to reach un-profiled code paths. |
| 8 | 9001 (--no-charts) | `_detect_multi_modal` recomputed np.std(y) already computed by the analyzer (charts-off profile surfaced the training/FS path; chart render was ~90% of wall) | RESOLVED (redundant pass) | -1.65ms of 5.82ms (~28%) @ n300k; analyze ~41→31.7ms; bit-identical 60 cases; guard preserved. Added profiler --no-charts flag. Streak reset→0. commit 1b45449f |
| 9 | 314159 (--no-charts, 6 combos) | #1 `_ice_metric.compute_probabilistic_multiclass_error` 2.06s tottime = the `@njit(parallel=True)` `_batch_per_class_ice_kernel` (cProfile njit-to-caller attribution; real Python overhead 0.004s); #2/#3 `bootstrap_metric`/`_jackknife_metric` = exhaustively-optimized resample/LOO loops (np.take/threading already rejected w/ bench) | REJECT | microbench-confirmed at-floor/njit; no Python-optimizable bit-identical hotspot. Streak→1. Codebase training/FS/metric path is at its optimized floor across both profiling modes. |
| 10 | 271828 (--no-charts, 8 combos) | identical top-3 to iter 9 (deterministic pool + prefer-models re-selects the same combos, overwriting by short_id); fresh candidate `_data_helpers._extract_target_subset` (0.097s self-time) already at documented floor (.values[idx] 9x-over-.iloc bench) | REJECT | all candidates njit-attribution / exhaustively-benched / at-floor. Streak→2. Readily-profiled combo space (lgb,xgb,cb) is mined out across chart-on + charts-off × multiple seeds. |
| 11 | (chart-render, user-approved non-bit-identical) | `renderers/_trend.robust_fit_endpoints` — Theil-Sen fit ~786ms/call on default-ON pred-vs-actual overlay (largest single reporting cost); slope is max_subpopulation-dominated, cap-insensitive above ~3k pts | RESOLVED+5.5x | _TREND_FIT_CAP 20000→3000; 820ms→149ms @ n60k; endpoint shift ≤1.7% of y-range over 25 clouds (within Theil-Sen's own sampling variance); visual-equivalence regression test. Streak→0. commit f1768d66 |
| 12 | (chart-render) | `charts/drift.adversarial_auc` — LightGBM train-vs-holdout separator, 200 trees x (3 CV fits + 1 importance fit) = ~17s across drift panels | RESOLVED+2x | ADV_N_ESTIMATORS 200→75; OOF AUC shift ≤0.007, 0/12 verdict flips across drift regimes; verdict-stability regression test. commit 1706f32c |
| 13 | (chart-render) | `charts/shap_panels.shap_summary_and_dependence` — TreeExplainer over DEFAULT_MAX_ROWS=20k (cost ~linear in rows) | RESOLVED+2.4x | DEFAULT_MAX_ROWS 20000→8000; 3.78s→1.57s; top-6 dependence feature set IDENTICAL 8k vs 20k; stratified subsample keeps tail; top-K-stability regression test. commit 616b5710 |
| 13b | (bug found during loop) | `charts/shap_panels._as_frame_and_names` forced whole frame to float64 → crashed on any categorical/string col → SHAP diagnostic silently absent (swallowed by dispatcher except) for every categorical-feature model | FIXED (correctness) | per-column coercion (numeric passthrough; non-numeric→factorize codes); SHAP panel now renders on categorical frames; regression test. Also validated the 3 chart-render perf changes compound: seed-20260702 combo 72s→61s (~15%). commit 6b28e22f |
| 14 | (chart-render cumtime rank) | remaining chart cost is DIFFUSE matplotlib (savefig/constrained_layout/get_tightbbox ~80s across many panels) + library-bound (plotly/kaleido, shap). render_pdp_ice (19.8s) render-dominated (pdp sample=2000/grid=20 already minimal); DPI already 100; plot_residual_diagnostics is render_and_save-bound | REJECT | 3 concentrated boundable chart costs already captured (trend/adversarial/SHAP). Remaining needs a design decision (panel count / layout engine), not a surgical bounded cap. Streak→1. |
| note | pre-existing sibling failure surfaced (NOT mine, NOT touched) | tests/reporting/test_fi_plot_defaults.py: `_FI_DEFAULT_FIGSIZE=(8.0,6.0)` committed, violates its 'half of DEFAULT_FIGSIZE=(7.5,2.5)' contract. In feature_selection (sibling's active WIP domain). Flagged for the owning session. |
| note-fix | resolved the surfaced fi_plot failure (re-frame stale test) | 2 assertions in test_fi_plot_defaults.py asserted the RETIRED 'half perf chart==(7.5,2.5)' FI figsize; source deliberately bumped to (8.0,6.0) for bar legibility (documented in importance.py, commit b1d23a88). Re-framed to the (8.0,6.0) legibility contract per 'reframe-stale-test-not-revert'. RESIDUAL (sibling's cross-module call, NOT changed): training/_feature_importances.py:DEFAULT_FI_FIGSIZE still (7.5,2.5) — a separate inline-panel FI path, plausibly intentional. commit 55772cfb |
| 15 | 161803 (--no-charts, cb-only, 5 combos) | CatBoost-native path (CB cat/text handling, cb_logits_to_probs) — same floor: #1 ICE `@njit` kernel (attribution, 0.004s real Python), analyze/bootstrap already-optimized, `_cyclical_sincos_njit` (njit), drift helpers already-benched | REJECT | fresh combo class, same converged floor. Streak→2. |
| FUTURE | biggest remaining chart cost = DIFFUSE matplotlib layout (~70s of ~96s report: constrained_layout iterative solve + get_tightbbox across ~24 figures) | needs visual sign-off | Only lever is a layout-engine swap (tight_layout + manual suptitle-space reservation, ~10x cheaper than the iterative constrained solve). CANNOT validate no-collision headless across diverse panels → requires rendered before/after human review. Tracked, not dropped. |

**Loop convergence (as of iter 15):** profiled 3 modes (charts-on / charts-off / cb-only) × 8 seeds × 3 combo classes. Every run returns the same top-cost set, each confirmed by code-read to be njit-at-floor, exhaustively-benched, or library-bound (matplotlib/plotly/shap/sklearn/catboost). 10 optimizations shipped (6 bit-identical + 3 chart-render + 1 redundant-pass) + 1 correctness bug fix (SHAP categorical crash) + 1 stale-test re-frame. Architectural convergence reached; the sole remaining big lever (matplotlib layout) needs a visual-equivalence sign-off.
| 16 | (matplotlib-layout A/B, direct wall measurement) | tested the 'FUTURE: matplotlib layout' lever directly on a representative 3-panel figure: constrained=537ms vs tight=545ms (NO win) vs no-layout=382ms (1.4x but crops/cramps output) | REJECT (measured) | the profile's ~35s constrained_layout cumtime was cProfile deep-stack INFLATION (~10x caveat), not real wall — direct A/B shows the layout engine is not a lever; savefig is dominated by inherent Agg rasterization + PNG encode (~380ms floor). Retires the FUTURE item. bbox_inches='tight' also only 1.12x when constrained is on. Streak→3. |
| 17 | 555001 + 424242 (--master-seed: DISJOINT fresh combo pools; regression/binary/multilabel/LTR × mrmr × ocsvm/lof) | added --master-seed to the driver (was re-profiling the same 150-pool); fresh pools surface the SAME optimized leaves — ICE njit-attribution, bootstrap njit metrics, LTR `_ranks_within_group`/`compute_ranking_summary` (both already batched-vectorized) | REJECT (measured) | genuinely disjoint combos, same converged floor. Streak→4. |
| 18 | (bootstrap-CI lever, the biggest remaining mlframe cumtime ~18s) | `honest_diagnostics` runs n_bootstrap=1000 metric-CI resamples; `_jackknife_metric` 1.685s cProfile self-time | REJECT | (a) `_jackknife_metric` real wall is ~62ms/call (gather 13ms + njit metric 48ms) — the 1.685s is cProfile inflation over 18k tight inner iters; (b) reducing n_bootstrap is OFF-LIMITS per feedback_no_tradeoff_optimizations — the resamples ARE the CI's statistical-precision safety property; trading rigor for speed is banned. Streak→5. |
| 19 | (fresh disjoint pools via --master-seed; re-measured a mistaken earlier reject) | `evaluation/bootstrap._jackknife_metric` — BCa acceleration jackknife re-gathers n-1 rows + reruns the full metric per leave-out point (O(max_n·n)); **11s/call at n=300k** (I had WRONGLY dismissed it in iter 18 by microbenching n=2000 not n=300k — corrected per 'a measured speedup is a LEAD') | RESOLVED+517x | mean-decomposable metrics (log_loss/Brier/RMSE = g(mean(per_row))) get exact O(n) algebraic LOO = g((Σ-row_i)/(n-1)); jackknife 10.9s→21ms, full bootstrap_metric(log_loss,300k) 15.0s→0.78s (19x); CI bounds match gather to ≤1.1e-15 (sum-order); falls back to exact gather on non-finite per-row. Wired via bootstrap_metric/bootstrap_metrics→honest_diagnostics; precast test re-framed exact→FP-close; new equivalence regression test. Streak reset→0. commit 2d2a0584 |
| 20 | (post-jk re-profile) | `evaluation/bootstrap._jackknife_metric_idx` for roc_auc — still gather-based (AUC not mean-decomposable): recomputes O(n log n) AUC per leave-out point, O(max_n·n log n), ~34s/call @300k | RESOLVED+159x | placement-value (Mann-Whitney) AUC LOO in O(n log n): 33.7s→0.21s, BIT-IDENTICAL (0.0) on continuous+tied + identical BCa CI end-to-end; general jackknife_fns registry; regression test. Together w/ iter19 the classification BCa-jackknife cost ~45s→<1s @300k. commit 05af02f6 |
| 21 | (bug found during loop, post-bootstrap re-profile) | `training/baselines/diagnostics.fit_and_report` — LightGBM quick model rejects object/string cat columns ('pandas dtypes must be int/float/bool') → whole BaselineDiagnostics report silently skipped (broad-except) for any string-categorical frame (incl. MTR-downgraded-to-regression) | FIXED (correctness) | cast declared string cats to pandas 'category' once via assign (block-reuse, no whole-frame copy, caller frame unmutated); report now runs; regression test. Same bug class as the SHAP-categorical crash. commit 2238d8ee |
| 22 | (bug found via charts-on end-to-end validation of iter-13b fix) | `charts/shap_panels` — the carrier passed to shap.TreeExplainer still had object/string cats → internal LightGBM predict crashed ('could not convert string to float'/'categorical_feature do not match') → SHAP panel STILL silently dropped (my iter-13b fix only coerced the float view, not the carrier) | FIXED (correctness) | _as_frame_and_names casts carrier object/string cols → 'category' via assign (block-reuse, caller unmutated); validated category+object carriers both render end-to-end; regression test both cases. Lesson: validate the FULL dispatch path, not just the function on a clean frame. commit d3c94b47 |
| 23 | (systemic categorical-diagnostics gap surfaced by charts-on fuzz) | reporting diagnostics assume NUMERIC feature frames and crash on string/categorical cols (all broad-except-swallowed -> panels silently absent for categorical-feature models). Confirmed: SHAP (float-view+carrier+predict), PDP (`pdp_ice._as_2d` float-view FIXED + deeper grid-substitution predict-match STILL open), BaselineDiagnostics (FIXED). | PARTIAL/TRACKED | FIXED: BaselineDiagnostics (iter21), SHAP main carrier+float-view+graceful-skip (iter13b/22/+fallback), PDP float-view (this). OPEN (tracked, non-fatal): PDP grid-substitution must preserve training category dtype; residual SHAP interactions/per-instance carriers; model-card. Root cause = diagnostics float-convert / predict on raw categorical frames. Fix path = per-diagnostic categorical handling (carrier as training-category dtype for predict; codes for float views). commit 582910b2 |
| 24 | (root-cause of the dominant charts-on categorical crashes) | `pdp_ice` predicted on a FLOAT-coerced frame → categorical models crashed → PDP figure silently dropped; SHAP tree-explainer crashed on CatBoost+text+polars-String (CatBoostError, not ValueError → my earlier except missed it) | RESOLVED | PDP now substitutes on the NATIVE carrier (category dtype preserved), 1-D+2-D, cast object→category; end-to-end charts-on: PDP crashes 0 (was dominant). SHAP graceful-skip broadened to any backend error → clean skip-with-reason (text/polars-String SHAP is out of scope). Regression tests for both. commit c397c92e |
| 25 | (end-to-end closure of the categorical-diagnostics thread) | broadened BaselineDiagnostics cast to ALL non-numeric feature cols (undeclared text/string col still crashed the quick model) | RESOLVED (thread closed) | FINAL charts-on 4-combo × 300k run is CLEAN: shap-fails=0, convert-errs=0, cat-mismatch=0, CatBoostError=0, BaselineDiag-fail=0, ERROR:mlframe=0, tracebacks=0, suite-errors=0. The systemic categorical-diagnostics crashes (iter 23) are fully fixed: BaselineDiagnostics (all non-numeric→category), SHAP (float-view+carrier+broad graceful-skip for text/polars-String), PDP (native-carrier predict 1-D+2-D). All swallowed panels now render or skip cleanly. commit 25181f99 |
| 26 | new ensembling fuzz axes (Caruana weights + rank_average, now suite-configurable) | profiled the new fuzz-reachable ensembling: `caruana_greedy_selection` (GPU-AUC-bound) + `rank_average_blend`/`_rank_transform` (scipy.rankdata argsort-bound) | DOCUMENTED (no actionable bit-identical speedup) | caruana: CPU-force REJECT (82 vs 128ms, GPU wins); buffer-reuse wash; division-skip 7.5% but only selection-equivalent (1e-6 score on ties) → not shipped. rank_average: tie-free ordinal fast-path ~14% but complex for a once-per-ensemble call → not shipped. Both conclusions documented inline per the profile-new-feature rule. commit c57b120b |
| 27 | 909090 (fresh pool) | `evaluation/bootstrap.bootstrap_metric` resample loop — gathered both y[idx]/p[idx] + recomputed the metric per resample (2.16s tottime / 6 calls, top self-time) | RESOLVED+3.1x | per-row resample fast path (reduce(mean(per_row[idx])), 1 gather + mean) for mean-decomposable metrics; RMSE CI 21.1s→6.9s @300k; ≤5.5e-16 CI equiv; reuses jackknife_per_row (auto for RMSE via honest_diagnostics); both-classes-gated; regression test. commit bf418fc9 |
| 27b | (extend per-row resample to bootstrap_metrics classification) | log_loss+brier resample per-row | REJECT (measured, memory-bandwidth) | per_row 3.6x FASTER @60k but 1.35x SLOWER @300k (1246→1675ms): classification bootstrap_metrics already shares ONE (y[idx],p[idx]) gather across both njit metrics, so per_row doubles the memory passes (gather-into-array + mean) and loses to the fused njit at large N. Only the single-metric RMSE case (iter27) saved a gather (2→1). Kept RMSE-only. |
| 28 (master) | isolated MRMR+FE cProfile @n=30k (full fuzz-suite under cProfile impractical: box saturated by 2 other agent sessions ~5.7GB/11363s + 4.4GB/9078s CPU) | `_conditional_gate_fe._build_feats` mask block (9.1s tottime / 724 calls) built each tau column as `(cv>tau).astype(float)*av` (bool + float64-cast temps per tau) | RESOLVED+1.1x | `np.multiply(av, cv>tau, out=col)` (bool upcast, preserves off-region NaN 0*NaN=NaN, drops astype temp); 1.14x@5k/1.10x@30k, bit-identical incl NaN. njit(prange) rewrite REJECTED (measured: 0.05-1.22x, prange starved under contention + numpy already tight); np.where(...,0.0) REJECTED (zeroes off-region NaN → changes MI binning). Regression sensor pins NaN-identity + np.where divergence. Streak→0. commit 990e78b3 |
| 28-confirm | full fuzz-suite cProfile @40k/1-combo (auto-completed on master, cumtime) | CONFIRMS iter-28 target: `_build_feats` = 22.169s tottime / 2058 calls (top-3 real mlframe tottime, vs 9.1s isolated) — mask-half opt lands directly on it. Full-suite also shows: ~56-69s numba COLD-JIT compile (`_compile_for_args`/llvmlite ffi 31s tottime; not a warm-prod hotspot), cupy.asarray 58.3s tottime/9363 calls (GPU-FE H2D churn = device-residency class, architectural), mi_direct 45s tottime (njit/GPU attribution). | INFO |
| FUTURE (needs quiet box) | `_build_feats` SELECT branch (`np.where(cv>tau,av,bv)`) — njit(prange) fuses the tau loop, BIT-IDENTICAL always (pure select, no NaN-multiply). Isolated: 1.22x@30k but 0.16x@5k (thread spawn; the 0.16x is contention-distorted). Correct size-gate threshold needs a quiet box to measure the crossover — cannot ship an unmeasurable dispatcher under the current 2-session contention (A/B-methodology). Tracked, not dropped. |
| 29 (master) | `_conditional_gate_fe._build_feats` — 22.2s tottime/2058 calls (full-suite @40k). Fused per-tau (n,17) mask/select build into njit(prange) kernels, gated ABOVE _GATE_BUILD_NJIT_MIN_N=20000 | RESOLVED+1.07x e2e | Flips the 2026-06-13 iter53 small-n reject (lost 0.89x@n<=12k) via 'gate a big win on safe condition': paired end-to-end A/B of cheap_conditional_gate_scan @40k = numpy 22.3s vs njit 20.8s = 1.07x, 3/3 wins under 2-session load, scan output BIT-IDENTICAL (705 hits both). Isolated build kernel 1.3-6.9x@n=1k..120k, 40/40 wins, bit-identical incl NaN (mask off-value a*0.0 preserves 0*NaN=NaN). Regression test pins both kernels vs numpy incl NaN. Stale reject comment amended (not deleted) with the large-n datapoint. Streak→0. commit 7919faa1 |
| 30 (master) | `_pairs_core._safe_abs_corr` — 2.36s tottime / 8235 calls (full-suite @40k noise-wrap gate): isfinite-mask + boolean-index copies + 2 np.std + 2x2 np.corrcoef per call | RESOLVED+23-35x (isolated) | one-pass njit `_abs_corr_finite_njit` (accumulate sa/sy/saa/syy/say over jointly-finite rows, no temp, no matrix): 35x@2k / 22.8x@5k, FP-equiv ~1e-15 (selection-safe for 0.30/0.5 |corr| gate). Degeneracy guard matches numpy std<=1e-12 (SS<=1e-24*n). Warm old ~146us x 8235 = ~1.2s real. FE-pairs coverage 20 tests green; regression test pins FP-equiv incl NaN + degenerate/few-rows/perfect-corr short-circuits. Streak→0. commit 1cc6d5a8 |
| 29b (reject) | `_detect_fourier_freqs_for_col` (9.5s tottime cold-run @40k) | REJECT (documented floor) | docstring bench-note iter17 (2026-06-23): detector + sub-kernels already at CPU optimum (fused njit(parallel) 2.45-9.2x), batchable coarse-plane bench-rejected TWICE, GPU-twin returns 1 scalar/call = pure overhead, warm ~0.357s tottime (~3-4% wall). My 9.5s = cold-JIT compile of _power_centered_fused_par_njit + contention. |
| 31 (reject) | `_fe_cmi_redundancy_null._conditional_perm_null` (2.3s tottime/297) | REJECT (documented floor) | analytic chi2-CMI-null (2026-06-20) already replaces the 25 within-stratum permutations with df=chi2(k_xyz+k_z-k_xz-k_yz)/(2N); GPU-resident cand branch + precomp-cardinalities reuse. tottime = dispatch orchestration + scipy chi2, not a surgical loop. |
| 32 (reject) | `_pairwise_modular_fe._residue_grid_mi` (2.1s tottime/189) | REJECT | device-resident fast-path `residue_grid_mis_resident` (device-born c%k) + host fallback batches moduli by eff-nbins into one njit `_mi_classif_batch`; tottime = njit/GPU attribution + a minor np.mod build loop. Already batched+resident. |
| 33 (master, freed box) | fresh full-suite FE-heavy combo @50k (seed 202 / master-seed 777001) + isolated MRMR @50k (seed 314159): re-profiled to find NEW surface | INFO / converged | Top tottime across 3 workloads all attribution/floor: mi_direct 9.0s (njit+GPU), batch_pair_mi_cupy 5.8s (GPU kernel), _dispatch_batch_mi_with_noise_gate 5.4s (= njit _cpu_kernel npermutations=0 mis-attributed to the dispatcher frame, analytic-gate path), evaluate_candidate 1.3s (= njit evaluate_gain mis-attributed; use_simple_mode=False so conditional path runs), _renumber_joint/_entropy_from_classes (already njit), _conditional_perm_null (analytic chi2 null), _residue_grid_mi (resident+batch), _detect_fourier (documented CPU floor). My 3 shipped wins (28/29/30) show: _build_feats 9.1s->1.15s, _safe_abs_corr out of top. |
| CONVERGENCE (session 2026-07-02/03, master) | 3 diverse workloads profiled (isolated MRMR FE, light training suite, FE-heavy suite); ~13 top hotspots each examined with a documented reason. Genuine surgical wins were the two remaining un-njit'd helpers (_build_feats tau-grid build, _safe_abs_corr) -- SHIPPED. Remaining surface: (a) njit/GPU-attribution (cProfile mis-attributes compiled-kernel time to the Python caller), (b) documented-optimal kernels (analytic nulls, resident GPU paths, prior-iter benched floors), (c) evaluate_candidate greedy core (njit-attribution + <0.5% risky Python). Next productive vein = GPU-residency (cupy.asarray H2D churn / batch_pair_mi_cupy) -- architectural, needs the fully-quiet box (one heavy non-mine process still running) + a device-born-operands design, not a surgical edit. |
| 34 (master, GPU free) | biggest remaining lever: MLFRAME_FE_GPU_STRICT (GPU-resident FE, default OFF) -- measured e2e A/B whether to flip default | REJECT default-flip (MEASURED, changes selection) | STRICT-resident collapses the cupy.asarray H2D churn: clean cache-cleared e2e @50k = off 117.9s vs on 46.9s = 2.51x, selection identical THAT seed. BUT multi-seed gate @10k: selection FLIPS 3/6 seeds (5002/5004/5005 -- tail-order swaps + 1 extra feature via ~1e-9 CPU/CUDA tie-breaks in borderline MI), median 1.26x. So STRICT is 1.1-2.5x but NOT selection-identical -> correctly stays OPT-IN (byte-identity contract lives on the non-strict default). Validates the codebase design with data. Per feedback_no_tradeoff_optimizations + 'reject anything that changes which features are selected'. bench: scratchpad ab_strict*.py (methodology: MRMR._FIT_CACHE.clear() per fit -- the class cache made a naive A/B read 501x cache-replay). |
| 34b (master) | can STRICT+BYTEMATCH (rank-binning gate MI) recover selection-identity to justify a default-flip? | REJECT (measured) | STRICT + MLFRAME_FE_GPU_STRICT_BYTEMATCH=1 @10k x6 seeds: STILL 3/6 selection mismatches (same 5002/5004/5005) AND speedup drops to 1.06x median (bytematch adds the per-gate argsort). The ~1e-9 selection flips are SYSTEMIC across the resident FE stages (fourier detect / prewarp ALS / candidate MI), not just the gate MI bytematch covers -> no single flag makes STRICT byte-selection-identical. GPU-residency vein definitively closed: real 1.1-2.5x but fundamentally alters selection -> stays opt-in. |
| CONVERGENCE-2 (session close, master) | Exhaustive: 3 workloads profiled, ~13 surgical hotspots each documented (2 shipped: build-njit + corr-njit; rest njit/GPU-attribution or documented floors), + the biggest architectural lever (GPU-strict-resident) measured both raw and bytematch = correctly opt-in (changes selection). No further surgical win or safe default-flip exists on this profiled surface. Remaining directions are large research efforts (eliminating ~1e-9 CPU/GPU FE divergence to make STRICT byte-identical; a device-born-operands redesign) -- not surgical /loop iterations. |
| 34c (CORRECTION) | re-diagnosed the STRICT selection change that iters 34/34b wrongly called "~1e-9 tie-flips / matched by chance" | CORRECTED -- material edge-vs-rank binning divergence, net-better biz-value | Both paths DETERMINISTIC. Gains differ MATERIALLY not 1e-9 (seed 5002: off gains [0.228,0.050,0.015,0.116] vs on [0.204,0.170,0.038,...]; seed 5005: identical gains but ON admits an extra real feature f2). ROOT CAUSE: resident MI bins by percentile-EDGE equi-frequency (_resident_candidate_mi.py:24) vs CPU njit argsort-RANK equi-frequency; edge chosen for SPEED (no per-column argsort). Differ on tied/discrete cols (gate output ~50% zeros): rank splits ties positionally (artifact), edge = same-value-same-bin. BOTH selections are valid real features (not noise). Biz-value (holdout LGBM AUC, 6 seeds @10k): STRICT net +0.020 mean AUC (catches real f2 twice +0.08, loses -0.04 once, 3 ties), 5/6 win-or-tie. So STRICT is net-BETTER here. The iter-34 "1e-9 / chance" was a WRONG handwave (no discriminating test); iter-34 REJECT is SUPERSEDED -> STRICT is a strong default-ON CANDIDATE pending a broad multi-scenario biz-value sweep. |
| 34d (broad biz-value gate for the STRICT default-flip) | 4 signal structures (interaction/additive/nonlinear/synergy) x 2 seeds @8k, holdout LGBM AUC, STRICT off vs on | REJECT default-flip (measured, high-variance wash) | additive mean +0.039 (spread -0.035..+0.113), nonlinear mean -0.048 (STRICT LOSES), synergy 0 (trivial), OVERALL mean -0.003 / median 0 / 5-8 win-or-tie. The iter-34c +0.020 was SCENARIO-SPECIFIC (interaction synthetic where edge happened to catch f2). Broadly STRICT is a HIGH-VARIANCE biz-value wash (big wins AND big losses; systematic loss on nonlinear) -> per 'variant defaults: accuracy on the MAJORITY of scenarios, high-variance selectors mislead', it does NOT qualify for the accuracy-first default. FINAL: STRICT is a legit 1.1-2.5x OPT-IN speed mode; rank/CPU stays the safe byte-identical default (no nonlinear downside). Which binning is 'better' = neither universally (edge wins additive, loses nonlinear, ties synergy). Correct design confirmed for the RIGHT (measured) reason. |
| 34e (CORRECTION-2 + mechanism) | "systematic nonlinear loss" (iter 34d) re-tested with 6 clean isolated seeds + KTC warm | DEBUNKED; losses are high-variance per-seed (any scenario), NOT nonlinear-systematic | Nonlinear 6 seeds: mean +0.016, 0 losses, 2 wins, 4 ties -- STRICT == or > CPU. The 34d -0.048 was ONE non-reproducible seed in a KTC-mid-sweep-contaminated multi-scenario run (same seed/data = a TIE when run isolated). Clean re-run (KTC warm, fresh seeds): interaction -0.026, additive -0.040, nonlinear +0.000, synergy 0 -> losses MOVED scenarios (additive 7011 -0.143, interaction 7010 -0.078), confirming HIGH-VARIANCE per-seed, not scenario-tied. MECHANISM (diagnosed in an isolated worktree @HEAD, since a parallel session's uncommitted info_theory WIP broke the live import): on loss seeds STRICT DROPS a borderline-real feature / stops early -- additive 7011 OFF=[f0,f5] vs STRICT=[f2,f5] (drops the STRONGEST f0); interaction 7010 OFF=[g0,f4,f2,f3] vs STRICT=[f4,g0,f3] (drops f2 + 1 fewer). Root = greedy sensitivity to small MI shifts at the max_consec_unconfirmed EARLY-STOP boundary (edge/f32 MI nudges a borderline candidate into 'unconfirmed' -> patience expires before it). NOTE: the CPU/OFF path ALSO under-selects on additive 7011 (only [f0,f5], misses f1 coef 0.8) -> the early-stop is over-aggressive for BOTH paths; STRICT just reshuffles WHICH real feature drops. Prior "systematic nonlinear" (34d) and "1e-9" (34) claims BOTH retracted. |
| 34f (ROOT CAUSE of the STRICT losses) | tested 3 tuning levers to "fix" the borderline-feature drops, then confirmed the real root | ROOT = finite-sample MI-estimation variance, NOT a mis-tuned gate | max_consec_unconfirmed 10->80 = ZERO effect; min_nonzero_confidence 0.99->0.80 = ZERO effect; min_relevance_gain_relative_to_first 0.05->0 = admits NOISE (f9/f21/g0) and DROPS AUC (additive 7011 0.812->0.670). So the early-stop/confirm/floor gates are CORRECTLY calibrated (loosening hurts). Real root confirmed by n-sweep: additive 7011 n=8k=[f0,f5] (misses f1,f2,f3) but n=30k=[f0,f1,f2,f3,f5] (recovers ALL true features). The under-selection + STRICT-vs-CPU divergence are small-n MI variance on near-tied-relevance features (binarized-sum target dilutes coefs). IMPLICATION: the STRICT divergence is a SMALL-n artifact; at production n (mlframe targets 100k-100M) selection stabilizes for BOTH paths -> STRICT-vs-CPU should CONVERGE -> STRICT may be a clean 2.5x default-ON win at prod scale (the 50k single seed already matched). Verifying next. (Diagnosis run in an isolated worktree @HEAD; parallel session's info_theory WIP still breaks the main-tree import.) |
| 35 (FEATURE, user-directed) | STRICT default-ON at production n: implemented AUTO size-gated default for MLFRAME_FE_GPU_STRICT | RESOLVED (landed e6d0f9c5; full-suite vet pending tree-unblock) | Three-state env (1/0 force; unset/auto = size-gated). AUTO engages STRICT at n>=MLFRAME_FE_GPU_STRICT_AUTO_MIN_N (default 100k, env-overridable) with CUDA; below/no-GPU = exact CPU path (byte-identical). MRMR.fit sets/clears fit-n context around _fit_impl. Basis: STRICT measured selection-equivalent to CPU by ~50k (interaction 3/3@25k, nonlinear 3/3@50k), ~2.5x faster (117.9s->46.9s @50k). Threshold 100k chosen > every existing test fit (<=60k) for ZERO blast radius since the full suite can't run now (parallel session's uncommitted info_theory WIP breaks the main-tree MRMR import). Validated in an isolated worktree@HEAD: 11 cuda-mocked gate unit tests + slow n=50k STRICT==CPU integration test + end-to-end AUTO==explicit-1 @60k / AUTO==explicit-0 @10k. Added GPU-aware strict_mode conftest fixture. REMAINING: blanket parametrization of MRMR selection/biz_val tests over strict_mode (small-n forced-STRICT diverges -> needs per-test threshold review) + full feature_selection suite regression on GPU host, both once the sibling tree is consistent. |
| 36 (post-merge /loop) | fresh fuzz profile @60k (seed 333 / master-seed 246810, 2 combos) after syncing origin/master (+19 commits incl siblings' perf: usability |corr| strided-subsample 09707c22, analytic MI-gate vectorise c9c2c490) | REJECT (no surgical mlframe target this combo) | combo1 light (7.9s training orchestration); combo2 = CatBoost-native (catboost.fit 32s external + _thread.lock acquire 31s tottime = CB widget-thread join wait, not mlframe) + cold numba-JIT ~15s (llvmlite ffi 4.6s tottime) + reporting. No FE-heavy MRMR combo drawn -> no surgical FE hotspot surfaced. Siblings are concurrently optimizing the FE surface (2 perf commits merged). Streak note: FE surgical wins from this session (28-30 build/corr njit) already landed; this combo class is external/cold-JIT dominated. |
| 37 (post-merge /loop) | `_fe_usability_signal.abs_pearson` (NEW module from merge; sibling's strided-subsample 09707c22) -- 1.83s tottime / 4248 calls @50k isolated MRMR-FE: numpy |Pearson| via isfinite-mask + 2 boolean-index copies + 2 np.std + mean temp | RESOLVED+~13x | one-pass njit `_abs_pearson_njit` (sums/sum-sq/cross over jointly-finite rows): 15.4x@2k / 12.9x@50k, 40/40 wins, FP-equiv ~5e-16 (selection-safe, gates are min_corr 0.6 / gap 0.99-vs-0.06). Same class as iter-30 noise-wrap corr. Subsample fast-path untouched. usability tests + regression (FP-equiv incl NaN + short-circuits) green. Streak→0. commit pending |
| 38 (post-merge /loop) | `_permutation_null.pooled_pair_permutation_null_joint_mi_floor` (2.43s tottime / 1 call @50k isolated MRMR-FE) -- Westfall-Young maxT: K=25 y-shuffles x batch_pair_mi_prange over the pair pool | REJECT (njit attribution) + FUTURE lever | The 2.43s is the njit batch_pair_mi_prange body x25 mis-attributed to this Python frame (rng.shuffle is C-level; np.max/quantile trivial) -- not plain-Python optimizable. ALGORITHMIC FUTURE (wasted per-call work class): across the 25 shuffles the X-pair joint bins are INVARIANT (only y is permuted), yet batch_pair_mi_prange recomputes the full (x_i,x_j,y) joint each shuffle; a permutation-batched kernel that computes the X-pair joint codes ONCE + only re-bins y per shuffle would cut ~25x recompute to 1x + cheap y-rebin. Deferred: it is a new load-bearing-gate kernel (~2% of fit) needing its own selection-equivalence validation, not a surgical edit. |
| 39 (post-merge /loop, integer/modular combo) | `_integer_lattice_fe._lattice_columns_for_pair` (1.02s tottime / 190 calls @50k integer-signal MRMR-FE; new FE family surfaced by an integer/modular synthetic) -- default (gcd,lcm,bitwise_and) block via separate numpy ufunc passes + temp arrays | RESOLVED+1.47x | fused njit `_lattice_gcd_lcm_and_njit` (Euclidean gcd once, reused for lcm, no per-op temps): 1.47x@2k/50k, 39/40 wins, bit-identical incl NaN. Gated on tuple(ops)==INTEGER_LATTICE_OPS (non-default ops keep exact numpy path). Regression test pins bit-identity + numpy fallback. Also confirmed iter-37 abs_pearson njit landed+working (1.83s->0.43s here). Streak→0. |
| 40 (post-merge /loop, cat/grouped combo) | fresh profile @50k with categorical/grouped signal (cat-pair, count/freq-encoding, grouped-agg families) | REJECT (reconverged floor) | Top tottime reconverges to the same mined set: plugin_mi_classif_batch_dispatch 15.8s (GPU), _flush/_detect_fourier/_renumber_joint/_residue_grid_mi/_conditional_perm_null (njit/resident/analytic/documented-floor), hermite ALS (linalg). abs_pearson 0.43s (my iter-37 njit working, was 1.83s). Only fresh frame `_conditioning_rows_per_cell` 0.42s/22 = ~0.4% (too small). The cat FE families didn't surface as hotspots on this signal (cheap). CONVERGENCE across 4 profiled data structures (interaction / integer-modular / cat-grouped / suite-combos): each surfaced 1-2 fresh njit-able helpers (all landed: _build_feats, _safe_abs_corr, abs_pearson, integer-lattice) then reconverges to njit/GPU-attribution + documented floors. Remaining real lever = FUTURE permutation-batched maxT kernel (iter 38), a substantial load-bearing-gate rewrite, not a surgical iteration. |
| 38-correction | analyzed the iter-38 FUTURE permutation-batched maxT lever before investing | DOWNGRADED (modest, deferred) | The iter-38 "25x recompute -> 1x" hypothesis was over-optimistic. batch_pair_mi_prange per shuffle computes MI(pair-joint; y_perm) = H(joint)+H(y)-H(joint,y_perm). Across the 25 shuffles ONLY the X-pair encoding + H(joint) + H(y) are reusable (fixed); the DOMINANT (joint, y_perm) 2D contingency + H(joint,y_perm) is inherently per-shuffle (y is permuted). So reuse saves ~the joint-marginal fraction = ~1.5x on the 2.4s maxT hotspot (~1s, ~1% of fit), at the cost of a new load-bearing statistical-gate kernel + selection-equivalence validation. Poor risk/reward -> deferred, NOT the big lever iter-38 implied. |
| CONVERGENCE-3 (surgical FE loop, post-merge) | 4 data structures profiled (interaction / integer-modular / cat-grouped / suite-combos) x ~15 hotspots each | CONVERGED (evidence-backed) | Every fresh structure surfaced 1-2 un-njit'd helpers (all landed: _build_feats fuse, _safe_abs_corr, abs_pearson, integer-lattice) then reconverged to the SAME floor: plugin_mi/batch_pair_mi (GPU), _renumber_joint/_entropy_from_classes/_mi_classif_batch (njit), _residue_grid_mi (resident+batch), _conditional_perm_null (analytic chi2 null), _detect_fourier (documented CPU floor), hermite ALS (linalg). The sole remaining algorithmic lever (permutation-batched maxT) is modest (~1.5x, ~1% fit) + load-bearing-kernel risk (38-correction). The surgical FE surface is genuinely mined at the profiled scales. |
| 41 (task 3: training/metrics/reporting surface) | profiled the metrics compute path (fast_calibration_report + callees) @300k directly (show_plots=False) | INFO / already-optimized | 5x fast_calibration_report @300k = 0.15s (30ms/call). All metric kernels njit-fast: fast_aucs_per_group_optimized 9ms, fast_calibration_binning 3ms, _confusion_counts_binary_dispatch, fast_log_loss_binary, fast_brier -- tiny tottime. The training-profile "0.935s cumtime" for fast_calibration_report was the matplotlib RENDER (show_plots=True), not compute. Training path = external CatBoost/LGBM fit + cold-JIT + matplotlib; charts/reporting optimized earlier (iters 11-27: trend cap, adversarial AUC, SHAP rows, categorical-diagnostics crash fixes); bootstrap-CI optimized (iters 19-27: jackknife/resample). No surgical metrics target. |
| WHOLE-SURFACE CONVERGENCE (tasks 1/2/3 done) | 1: STRICT-AUTO fit-wiring validated (units+integration+samples), both-path biz_val mechanism shipped -- full regression + STRICT enumeration are CI-scale. 2: permutation-batched maxT measured ~1.5x/load-bearing-risk -> DOWNGRADED. 3: metrics njit-fast, training external/cold-JIT, charts previously optimized. | CONVERGED | The mlframe surgical optimization surface is thoroughly mined across FE (4 data structures), metrics (measured njit-fast), and training/reporting (iters 11-27 + already-njit). Remaining work is CI-scale vets (full regression, STRICT biz_val enumeration) or non-surgical (matplotlib layout needs visual sign-off; maxT kernel modest+risky). Session shipped: STRICT-AUTO feature + 6 njit perf wins + biz_val both-path mechanism + CLAUDE.md reset-rule, all on origin/master. |
| 42 (task 2 DONE) | `pooled_pair_permutation_null_joint_mi_floor` -- the iter-38 permutation-batched maxT lever, IMPLEMENTED | RESOLVED+1.71x (bit-identical) | New `batch_pair_mi_perm_batched` njit kernel: per pair, the joint encoding cls_x=va*nb_b+vb + freqs_x marginal are computed ONCE and reused across all K=25 shuffles (invariant under y-permutation); only the (joint, y_perm) contingency + MI sum re-run per shuffle. Measured 1.71x (122->71ms @ n=50k/60pairs/K=25), BIT-IDENTICAL (max|d|=0) -- the iter-38-correction "~1.5x" underestimated (measured 1.71x) and the "load-bearing risk" is moot since it is EXACT (not selection-equivalent). Regression test pins kernel==per-shuffle batch_pair_mi_prange + pooled_pair determinism. On the 2.43s maxT hotspot -> ~1.4s. |
| 43 (task 3: matplotlib layout, iter-14 FUTURE) | measured savefig vs DPI vs layout-engine on a representative artist-heavy multi-panel figure + rendered visual A/B | MEASURED + visual evidence; targeted swap needs sign-off | DPI is NOT a lever (100/85/72 -> 497/502/543ms, draw-bound not raster-bound). Layout IS a lever here (contradicts iter-16's figure-specific null): constrained 371ms vs tight 219ms (1.7x) vs None 132ms (2.8x) savefig. VISUAL: tight (with rect=[0,0,1,0.96] for suptitle) renders CLEAN -- no title/label collisions; None COLLIDES (top-row x-labels overlap bottom-row titles, confirmed in render). So the usable lever = constrained->tight on artist-heavy panels (1.7x). BUT NOT blanket: 21 colorbar sites (confusion-matrix / shap-interactions / pdp_2d / calibration-heatmaps) need constrained (tight mis-sizes colorbars). Targeted per-figure swap (non-colorbar scatter/line panels only) across ~10 reporting/charts files needs the user's visual sign-off on the real report. Render bench: scratchpad/render_layout_ab.py + layout_{A_constrained,B_tight,C_none}.png. |
| 43b (task 3, code examination) | examined the actual constrained_layout sites to apply the targeted tight swap | NO WORTHWHILE CHANGE (codebase already near-optimal) | My proxy-based swap offer over-estimated the safe scope. Reality: (1) the FREQUENT panels (_calibration_plot reliability w/ population colorbar line 716, confusion-matrix, shap-interactions, pdp_2d) are colorbar-bound -> tight mis-shapes the colorbar (the codebase ITSELF documents this: _calibration_plot line 799 "tight_layout warns + mis-shapes colorbar", line 770 "constrained adds ~170ms/call"), so constrained is correct there. (2) colorbar-free geometry was ALREADY switched to layout=None (line 804, 2026-05-11). (3) the only remaining constrained-with-collision-risk figure (calibration_drift, ~1/report) is a FigureSpec(constrained_layout=True) whose comment states constrained specifically prevents the rotated-date-label vs bottom-title collision -- swapping needs a renderer-arch change (tight support in reporting/renderers/matplotlib.py) for a marginal ~150ms/report single-figure win + collision risk. Verdict: the tight-1.7x lever is real but the reporting layout is already near-optimal; no worthwhile surgical swap. Not applied (poor ROI). |
| 44 (/loop 100k, broad-FE wide coverage) | `_internals.canonical_group_token` -- 18.4s tottime / 5,770,492 calls (#1 hotspot, surfaced ONLY by a broad-FE profile: 16 FE families on a mixed numeric/cat/int/NaN signal @100k; standard combos never exercise composite-group-agg). Root: build_composite_keys per-ROW ser.map(canonical_group_token) on any group col with a NaN. | RESOLVED+13x (bit-identical) | Float column missing is NaN (never None); every NaN -> canonical 'nan' token regardless of np.unique's NaN-merge, so the per-UNIQUE path (np.unique(return_inverse) + canonical per distinct) is bit-identical AND runs canonical_group_token per-unique not per-row: 13.1x @100k/41-uniques (216->16.5ms). Object-None cols keep per-row (None->'' semantics). Regression test pins float-NaN bit-identity. WIDE COVERAGE was the enabler -- the standard fuzz combos reconverge to the njit/GPU floor; broad FE-family coverage surfaced this + _column_to_str (5.99s) + score_features_by_bootstrap_mi (14.3s) as fresh targets. |
| 45 (/loop 100k, broad-FE wide coverage) | `_target_encoding_fe._column_to_str` -- 5.99s tottime / 58 calls + the _bool_risk genexpr (:229) 2.37M calls (surfaced by broad-FE @100k; target-encoding on high-card numeric-object columns). Root: _bool_risk = has_bool OR has_01 fell to the per-ROW canonical loop whenever a column merely CONTAINED a 0/1 value. | RESOLVED+8x (bit-identical) | The factorize/per-row token divergence (True/1/1.0 merge to one code vs distinct 'True'/'1') needs bool AND an ==-equal numeric to COEXIST; a 0/1 WITHOUT a bool factorizes to its own canonical token (bit-identical). OR->AND + vectorised 0/1 test (object elementwise, NaN/str->False) + short-circuit bool scan when no 0/1: high-card numeric-object col with a 0/1 value takes the per-unique fast path, 8x @100k (209->26ms). Genuine bool+numeric still falls back exact. Regression test pins fast/fallback/string-NaN. |
| 46 (/loop 100k, broad-FE) | `_adaptive_nbins._plug_in_mi` -- 1.127s tottime / 439 calls (cat-pair/triple marginal+joint MI). x_binned.astype(int64) + y.astype(int64) every call -- astype ALWAYS copies even for already-int64 codes; _cat_triple scores 8 codes vs the SAME y_bin per triple. | RESOLVED+1.17x (bit-identical) | astype -> ascontiguousarray: no copy when already contiguous int64 (callers pass int64 np.unique/factorize codes), njit still gets contiguous int64. 1.17x @100k, 27 tests green. |
| 45b (reject) | `_orthogonal_bootstrap_mi_fe.score_features_by_bootstrap_mi` (14.3s tottime / 2 calls) | REJECT (njit-attribution, at-floor) | 14.3s = the njit `_mi_classif_batch` x20 (2 x n_boot=10) mis-attributed to this frame + the (sample_n, n_col) resample gathers -- already heavily optimized (sorted-gather cache-locality gated on _all_columns_distinct, precomputed _src_idx avoiding per-replicate dict+listcomp). No surgical caller-side win. |
| 47 (/loop 100k, broad-FE temporal/grouped coverage) | `_temporal_agg_fe._expanding_stat_past_only` -- 8.437s tottime / 6 calls @100k (leakage-safe per-entity expanding stat; surfaced ONLY by a temporal/grouped broad-FE profile -- entity/time/group columns the other profiles lack). Pure-Python loop over n rows with per-entity Python DICTS. | RESOLVED+193-549x (bit-identical) | group_codes is a dense per-entity int code -> dict accumulators become ARRAYS indexed by the code; the whole sequential running loop njit-ed (not prange, order-dependent). 193x mean / 500x std / 549x min / 292x count @100k/150-entities, bit-identical incl NaN. Regression test pins njit==dict-reference all 5 stats + edges. Biggest /loop win -- WIDE FE-family coverage (temporal families) was the enabler. |
| 48 (/loop 100k, direct Python-loop hunt) | `_temporal_agg_fe._rolling_stat_past_only` -- per-entity Python LISTS + per-row list-comprehension window filter (O(n*window) Python; old std @100k = 13s). Found by direct grep of `for i in range(n)` / dict-accum in FE-family modules (more targeted than random profiling for the expanding-class of Python-loop hotspots). | RESOLVED+335-379x (bit-identical) | njit two-pointer: per-entity newest->oldest linked list (prev/last int arrays), walk back while times[j]>=t-td (ascending per-entity time), accumulate count/sum/sumsq/min/max over in-window strict-past finite. 335x mean / 379x std @100k, bit-identical incl NaN. Datetime->int64 ns (full precision), numeric->float. Regression test pins numeric+datetime all 5 stats. Second expanding-class temporal win (after iter 47). |
| 48b (reject, caught pre-commit) | `_analytic_mi_null._occupied_bins_per_col` -- appeared to be a pure-Python O(n*K) double loop | REJECT (already njit; my A/B-baseline error) | The function was ALREADY `@njit(nogil=True, cache=True)` -- I missed the decorator (an awk njit-detector overwrote the @njit line with the def line) and my microbench compared my OWN from-memory Python re-implementation (2.3s) against njit (15ms) = a bogus "134x". Violated A/B rule 4 (real baseline, not a from-memory rewrite). Reverted cleanly before commit; the double-@njit TypeError caught it. Lesson: verify the ACTUAL production decorator + bench the REAL prior code, never a re-implementation. |
| 49 (/loop, direct njit-aware Python-loop hunt) | `_cat_target_encoding_and_weighted._compute_target_encoding` -- per-ROW Python loops over n_samples (naive + per-fold-train x K + global cell_sum/cell_cnt accum + per-row test-fold apply). Found by njit-aware grep (enclosing def has NO @njit) after learning the iter-48b lesson to verify decorators. | RESOLVED+490x (bit-identical) | njit `_cell_sum_cnt_njit` (sequential row-order accumulate = bit-identical to the loop, 490x @100k/nu=500, 70->0.14ms) for the 3 accumulation loops; vectorised gather te_values[test_mask]=cell_means_fold[classes[test_mask]] for the apply. Regression test pins helper bit-identity + deterministic kfold-TE MRMR fit. Same accumulate-over-rows class as iter 47/48; opt-in weighted/TE path. |
| 50 (/loop 100k, broad-FE grouped/conditional coverage) | fresh profile: grouped_quantile / group_distance / conditional_residual / conditional_dispersion / grouped_agg / wavelet / numeric_decompose @100k | REJECT (families already vectorized/njit) | Top fresh CPU frames all small/vectorized: fit_binned_numeric_agg 0.413s (vectorized+cached), mi_direct (njit), _safe_div 0.232s/792 (~0.36% -- marginal njit-fuse), _mdlp_recurse_njit (njit). These grouped/conditional families are already efficient (vectorised numpy / njit). No big fresh Python-loop. CONVERGENCE (FE Python-loop surface): 7 broad-FE profiles (diverse family sets) + a direct njit-aware `for i in range(n)` hunt landed the big per-row/per-entity Python-loop wins (iters 44/45/47/48/49: canonical_group_token 13x, _column_to_str 8x, expanding 193-549x, rolling 335-379x, target-encoding 490x). Remaining candidates are already-njit fast-paths (missingness bitpack, _mdlp), opt-in estimators (ksg), n_perm/n_folds loops (intentional), or marginal (<0.5%). The high-value surgical Python-loop surface across the profiled FE families is mined. |
| 51 (/loop, wide-p 60-col angle) | `evaluation.find_best_partial_gain` -- 0.710s tottime / 86 calls @ p=60 (greedy redirect-target scan; scales with the engineered candidate pool -- surfaced by a wide-p profile the row-scaling profiles under-emphasised). Inner `subel in selected_vars` did O(len) list membership per sub-element per candidate per confirmation retry. | RESOLVED+1.61x (bit-identical) | Hoist `set(selected_vars)` once before the loop -> O(1) membership. 1.61x @ 200-candidate pool (72->45us), bit-identical (same test). 19 MRMR greedy tests green. |
| CONVERGENCE-FINAL (FE surgical Python-loop surface) | 8 broad-FE profiles across ALL scaling axes (row n=100k, FE-family diversity x7 sets, column p=60) + a direct njit-aware `for i in range(n)` hunt | CONVERGED | 7 bit-identical wins landed (44/45/47/48/49 big + 46/51 micro): canonical_group_token 13x, _column_to_str 8x, expanding 193-549x, rolling 335-379x, target-encoding 490x, _plug_in_mi 1.17x, find_best_partial_gain 1.61x. Every remaining hotspot across all axes is njit/GPU-attribution (mi_direct, plugin_mi, _conditioning_rows_per_cell=merge_vars, binned_mm_mi_gpu), a documented floor, an already-njit fast-path (missingness bitpack, _mdlp, integer-lattice), or <0.5% marginal glue (_safe_div, the find_best_partial_gain set-hoist itself). The high-value surgical Python-loop surface is mined. |
| 52 (/loop, training/metrics surface) | `metrics/ranking.py:557 compute_ranking_summary` -- 6.555s tottime / 12 calls @100k (surfaced by a training-suite profile with an LTR/group-ranking combo). | REJECT (njit-attribution, already batch-optimized) | The frame's docstring itself records prior work ("one batched numba kernel handles all per-group work in a single dispatch vs the prior 7*n_groups Python->numba transitions"). It calls `_summary_batched_kernel` = `@numba.njit(parallel=True)`, whose compiled time is mis-attributed to this Python caller frame (A/B rule 8). `_iter_group_slices` is fully vectorized numpy (single mergesort by group_ids + flatnonzero boundaries); the mergesort is a STABLE-sort requirement for deterministic within-group tie-order, not a swap to quicksort (would not be bit-identical). No surgical CPU win. |
| 53 (/loop, direct njit-aware row-loop hunt beyond FE) | `training/quantile_postproc.fix_quantile_crossing` (mode="isotonic") -- per-ROW Python loop over N calling sklearn `isotonic_regression` each row (Python->C dispatch per row). Found by a whole-tree AST scan for plain-Python (non-njit) row-scaled range loops across feature_selection + training (FE families were already mined; this is the training/postproc surface). | RESOLVED+529-664x (FP-equiv ~2.7e-15) | njit equal-weight pool-adjacent-violators `_isotonic_rows_pava` = the SAME unique L2 isotonic projection sklearn returns, no per-row dispatch: 664x @100k*7 (16942->25ms), 529x @1M*9 (173062->327ms). FP-equiv to pooling round-off (~2.7e-15, cannot move a quantile decision). Monotone-row shortcut kept. Caught+fixed a block-expand OOB (range(m+1)->range(m), stale cnt[m] writing past col K) before commit; regression test pins sklearn-equivalence across K in {2,3,7,9} + row-mass preservation. Opt-in mode (default is np.sort), but real prod path when crossings cluster. Streak->0. commit 1bc1d6f2. |
| 54 (/loop, training/composite surface) | `training/composite/distributional.CompositeDistributionEstimator.sample` -- per-ROW Python loop over n_rows calling `np.interp` each row (PIT-inversion sampler). Surfaced by the same whole-tree AST row-loop scan. Docstring claimed "light pass, not a hot kernel" but scales with prediction volume (n_rows x n draws). | RESOLVED+1.9-2.7x (FP-equiv ~9e-16) | The x-grid `levels` (=quantiles_) is SHARED across rows; only qmat[i,:]/u[i,:] vary. njit `_interp_rows_shared_x` does the whole (n_rows,n) inversion in one dispatch (binary-search + lerp reproducing np.interp's flat-held extrapolation): 2.7x @100k*9*50, 1.9x @50k*15*100. FP-equiv ~9e-16 (single-ULP vs np.interp precomputed-slope form; and these are stochastic draws -> decision-irrelevant). Regression test pins per-row np.interp equivalence. Streak->0. |
| 55 (/loop, training/composite AST audit) | `training/composite/conformal_online.update_conformal` -- per-row `range(n)` online ACI loop. | DEFER (not surgical, not profile-surfaced) | Inherently sequential online contract: each row's `_aci_radius` depends on the prior row's `alpha_t` step, and the radius is a conformal quantile over a rolling residual buffer recomputed per row. State is a Python dict of deques -> not a clean typed-array accumulator like iters 47/48/49; njit-ing needs a full state restructure. The per-row cost is the rolling-buffer quantile (algorithmic O(n*buffer)), not Python loop overhead; a running-quantile (two-heap/skiplist) rewrite is a substantial correctness-risk change, not a surgical edit, and this is an online-diagnostic path not surfaced hot by any profile. Also confirmed `_neural_numba_kernels._finite_min_max_std_python` is REJECT (already njit-dispatched; `_python` is only the no-numba fallback). Whole-tree AST row-loop scan's clean surgical wins are now mined (iters 53 isotonic-PAVA + 54 sampler-interp); remaining plain-Python range-loops are column-scaled (p~dozens), small-count (folds/groups/specs), sequential-online, or already-njit-dispatched. |
