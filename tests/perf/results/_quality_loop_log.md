# mlframe ACCURACY / QUALITY loop log

This loop hunts measurable ML-QUALITY wins (calibration / probability post-processing / metric-correctness),
NOT speed (the perf loop is saturated -- see `_loop_iter_log.md`). Each iteration picks ONE self-contained
accuracy lever in a low-contention area, measures it on the HONEST metric (holdout / OOF / known ground-truth)
across MULTIPLE seeds AND scenarios, and either RESOLVES (flips the default, keeps the old as opt-in per
REJECTED!=DELETED, adds a `biz_value` test) or REJECTS (keeps the committed bench + verdict, no default change).
A flip requires a MAJORITY win across seeds/scenarios -- never a single-seed/single-scenario result.

| iter | lever | scenario / seeds | honest metric: old -> new | verdict | commit |
|------|-------|------------------|---------------------------|---------|--------|
| qual-1 | Debiased binned ECE (subtract per-bin Bernoulli noise floor) as headline-ECE default in `fast_calibration_report` (`ece_debiased=True`; plug-in kept as `ece_debiased=False`) | 3 perfectly-calibrated (uniform / beta-rare / bimodal, true ECE=0) + 2 miscalibrated (sigmoid-shift / overconf, fine-grid truth) x 7-8 seeds x nbins {10,15,20}; honest metric = \|estimate - ground-truth ECE\| | mean bias-vs-truth on calibrated scenarios @nbins=15: uniform 0.0294 -> 0.0127, beta-rare 0.0128 -> 0.0055, bimodal 0.0196 -> 0.0082 (>2x bias reduction); per-cell \|bias\| wins debiased/plugin = 29/6 @nbins=10, 28/7 @15, 32/8 @20 (debiased wins ~80% of cells, majority at EVERY bin count); miscalibrated model still flagged (true-gap term dominates) | RESOLVED (flip default) | 47713b8e |
| qual-2 | Debiased Brier (Murphy) decomposition reliability/resolution as headline REL/RES default in `fast_calibration_report` (`brier_debiased=True`; plug-in kept as `brier_debiased=False`). SIBLING of qual-1: plug-in REL/RES carry the same +Var(acc_b) per-bin Bernoulli inflation. Broecker 2009: subtract Var(acc_b)=acc_b(1-acc_b)/(n_b-1) from BOTH REL and RES (clamp REL>=0), so REL-RES, UNC, BinnedBrier and the Murphy identity are preserved EXACTLY; only the bias split shifts | 3 perfectly-calibrated (uniform / beta-rare / bimodal, true REL=0) + 2 miscalibrated (sigmoid-shift / overconf, fine-grid truth) x 8 seeds x nbins {10,15,20}; honest metric = \|estimated REL - ground-truth REL\| | mean REL bias-vs-truth @nbins=15: uniform 0.00123 -> 0.00057, beta-rare 0.00128 -> 0.00093, bimodal 0.00113 -> 0.00060, miscal-sigmoid 0.00258 -> 0.00144, miscal-overconf 0.00203 -> 0.00046 (>2x on uniform/bimodal/overconf, every scenario DEBIAS-wins on mean bias); per-cell \|REL bias\| wins debiased/plugin = 34/6 @nbins=10, 35/5 @15, 36/4 @20 (majority at EVERY bin count); Murphy-identity error 0.0; bench `metrics/_benchmarks/bench_brier_decomp_debiased.py`; tests `tests/metrics/test_biz_val_calibration_brier_decomp_debiased.py` | RESOLVED (flip default) | 49b92537 |
| qual-3 | Miller-Madow correction on the Entropy-Based Calibration Index (ECI = log(bins) - H(binned PIT)) in `calibration/quality.py:entropy_calibration_index` (`miller_madow=True` default; plug-in kept as `miller_madow=False`). DIFFERENT file/statistic from qual-1/2 (PIT-entropy, not ECE/Brier kernels): plug-in Shannon entropy H_hat=-sum p_i log p_i is NEGATIVELY biased ~-(K-1)/(2N), so ECI is POSITIVELY biased above the true 0. MM adds (K_obs-1)/(2N) (K_obs=non-empty bins), cancelling the leading 1/N term | uniform PIT (perfectly-calibrated, true ECI=0) x 7 seeds x bins {10,20}; honest metric = \|ECI - 0\| | per-cell \|ECI\| wins MM/plugin = 6/1 @bins=10, 6/1 @bins=20 (12/14 total, majority at BOTH scenarios); mean \|ECI\| plugin 0.0132 -> MM 0.0039 (~3.4x closer to true 0); bench `calibration/_benchmarks/bench_eci_miller_madow.py`; tests `tests/calibration/test_biz_val_calibration_eci_miller_madow.py` | RESOLVED (flip default) | 9e9170e7 |
| qual-4 | Equal-MASS (quantile) vs equal-WIDTH binning grid for the headline DEBIASED binned ECE (`compute_ece_debiased`, already shipped with qual-1 Bernoulli noise-floor subtraction). DIFFERENT angle from qual-1/2/3 (binning SCHEME, not a noise-floor / MM debias): equal-width over [min,max] dumps mass into 1-2 bins when preds are concentrated; literature (Nixon 2019 ACE, Roelofs 2022) says equal-mass lowers bias+variance. Tested keeping the SAME Bernoulli debiasing, only switching the grid | 4 perfectly-calibrated (uniform / beta_rare / beta_mid / bimodal, true ECE=0) + 2 miscalibrated (overconf / shift) x 8 seeds x nbins {10,15,20} x n {500,2000,5000}; honest metric = \|estimate - 0\| on calibrated, flag-retention on miscal | per-cell \|ECE\| wins equal-mass/total = 49/96 @n=500 (51%), 44/96 @n=2000 (46%), 45/96 @n=5000 (47%) -- NO scenario-majority at ANY n; equal-mass helps beta_rare but LOSES all 3 bimodal cells @n=500 and flips beta_mid->width @n>=2000. The qual-1 noise-floor subtraction already removes most of the equal-width sparsity penalty, leaving the residual grid choice scenario-dependent + n-reversing. Miscal scenarios stay flagged either way. Bench `calibration/_benchmarks/bench_ece_debiased_equal_mass.py` | REJECTED (no flip; equal-width debiased ECE stays default) | ebd90ef2 |
| qual-5 | BCa (bias-corrected + accelerated) vs percentile bootstrap CI as the default `method` in `mlframe.evaluation.bootstrap.bootstrap_metric` / `bootstrap_metrics` (`method="bca"` default; percentile kept as `method="percentile"`). FRESH area (CI-coverage / uncertainty, NOT calibration ECE/Brier/ECI of qual-1..4; low-contention -- only the perf-loop touched this file). The percentile interval ignores skew + median-bias of the sampling distribution and UNDER-COVERS on skewed/bounded metrics (AUC near 1.0, Pearson r); BCa shifts the cut-points via z0=Phi^-1(#{resample<point}/B) + jackknife acceleration a, falling back to percentile when degenerate | 2 scenarios x 5 seeds in the skewed regime where the percentile deficiency bites: AUC~0.97 @ n=150 + corr rho=0.90 @ n=40; honest metric = empirical Monte-Carlo coverage of a nominal-95% CI over 600 trials/cell, scored as \|coverage - 0.95\| | BCa closer-to-nominal in 10/10 (scenario x seed) cells (majority at BOTH scenarios, all seeds); AUC mean coverage pct=0.919 (gap 0.031) -> bca=0.948 (gap 0.002); corr mean coverage pct=0.930 -> bca=0.938; bench `evaluation/_benchmarks/bench_bootstrap_ci_coverage.py`; tests `tests/evaluation/test_bootstrap.py` (default + degenerate fallback + parity) + `tests/evaluation/test_biz_val_evaluation_bootstrap.py` (coverage-gap win) | RESOLVED (flip default) | 27741f2e |
| qual-6 | Miller-Madow bias correction for the binned KL / JS drift divergence in `metrics/_drift.py` (`bias_correction=True` default on `kl_divergence` / `js_divergence`; plug-in kept as `bias_correction=False`). FRESH area (distributional-drift divergence, NOT calibration ECE/Brier/ECI of qual-1..4 nor the bootstrap-CI of qual-5; low-contention -- only the perf loop touched this file). The binned plug-in over-resolves the two empirical histograms, so KL/JS sit clearly above the true divergence and stay strongly positive even when P,Q are the SAME distribution (true=0). JS == I(label;bin) so its bias is the Miller-Madow MI floor `(K-1)/(2N)` (K occupied pooled bins, N=np+nq); KL's is `(Kp-1)/(2 np)+(Kq-1)/(2 nq)` (empirical -H(P) + cross-entropy plug-in). Subtract + clamp at 0; no-op on `pre_binned=True` (no counts) | same-distribution (true=0) + 0.5/1.0-sigma shift x 8 seeds x (n,nbins) in {200,1000}x{20,50}; honest metric = \|estimate - Monte-Carlo ground-truth divergence\| | corrected closer-to-truth in 77/96 cells for BOTH KL and JS (80%); 8/8 at every same-distribution cell + every small-n/many-bins shifted cell; same-distribution \|bias\| cut ~7-11x (KL 0.225 -> 0.021, JS 0.064 -> 0.008 @ n=200 nbins=50); genuine shift stays flagged; only losing cells n=1000 nbins=20 shifted (low-bias regime, both near truth, small over-shoot); bench `metrics/_benchmarks/bench_kl_js_divergence_debiased.py`; tests `tests/metrics/test_biz_val_metrics_drift_divergence_debiased.py` | RESOLVED (flip default) | 818f786d |
| qual-7 | Macro precision/recall/F1 in `fast_classification_report` (`metrics/_core_precision_mape.py`) now averaged over classes PRESENT in y_true OR y_pred (`macro_over_present=True` default; legacy divide-by-`nclasses` kept as `macro_over_present=False`). FRESH area (classification-report averaging, NOT calibration ECE/Brier/ECI qual-1..4, bootstrap-CI qual-5, drift-divergence qual-6). The macro mean divided by `nclasses`, so a declared-but-absent class (zeroed P/R/F1 after `nan_to_num`) DEFLATED the macro -- the SAME defect already fixed for `balanced_accuracy` in the same kernel but left on macro P/R/F1. sklearn's `classification_report` macro-averages over the union of present classes; ground truth = sklearn macro | declared-but-absent scenarios {one/two/half absent} x nclasses {6,10} x 8 seeds (24-48 cells); honest metric = \|mlframe_macro - sklearn_macro\| averaged over P/R/F1 | new matches sklearn in 48/48 cells (\|err\|=0.0000) vs legacy mean \|err\| 0.07 (one absent) / 0.14 (two) / 0.38 (half) -- deflation scales with absent fraction; per-cell wins new/old = 48/0; binary / all-present reports unchanged (present-mask all-True); bench `metrics/_benchmarks/bench_macro_avg_present_classes.py`; tests `tests/metrics/classification/test_biz_val_classification_macro_present.py` | RESOLVED (flip default) | 3498f086 |
| qual-8 | Bessel-corrected sample std (`ddof=1`) for the across-subgroup performance-dispersion number in `metrics/_fairness_metrics.py`: the `metric_std` column of `compute_fairness_metrics` and the spread penalty of `robust_mlperf_metric` (`ddof=1` default; legacy population std kept as `ddof=0`). FRESH area (fairness / subgroup-disparity reporting, NOT calibration ECE/Brier/ECI qual-1..4, bootstrap-CI qual-5, drift-divergence qual-6, macro-averaging qual-7). The `metric_std` estimates the dispersion of subgroup performance from a small SAMPLE of K subgroups (typically 2-10); numpy default `ddof=0` is the DOWNWARD-biased estimator at small K, systematically UNDER-reporting disparity (model looks fairer than it is) and under-penalising in `robust_mlperf_metric`. `ddof=1` is the unbiased-variance / sample-std convention already used across mlframe FE (`hurst`/`grouped`/`spatial`/`ensemble_features`) + pandas / R. Ground truth = KNOWN sigma_true of N(mu, sigma_true^2) subgroup draws | K in {2,3,4,6,10} x 8 seeds x 4000 Monte-Carlo redraws/cell; honest metrics = SIGNED std bias (the fairness-harm direction), variance signed bias, and \|estimate - sigma_true\| | SIGNED std \|bias\| cut ~2.2-2.7x in EVERY scenario (K2 -0.0217->-0.0100, K3 -0.0138->-0.0057, K4 -0.0204->-0.0080, K6 -0.0105->-0.0039, K10 -0.0037->-0.0012); variance becomes essentially unbiased (ddof1 ~ 0 vs ddof0 systematically negative); signed-\|bias\| ddof1 wins 5/5 scenarios for BOTH std and variance; \|estimate-sigma\| ddof1 wins 33/40 (scenario x seed) cells, majority at every scenario (K2 5/8, K3 7/8, K4 8/8, K6 7/8, K10 6/8); bench `metrics/_benchmarks/bench_fairness_std_bessel.py`; tests `tests/metrics/test_biz_val_metrics_fairness_dispersion_std.py` | RESOLVED (flip default) | 364f1492 |

| qual-9 | Closed-form DeLong single-sample AUC standard-error / CI (`auc_variance` + `auc_ci`, `method="delong"` default) added to `mlframe.evaluation.bootstrap`, vs the de-facto AUC-uncertainty path of bootstrapping `roc_auc_score` (the qual-5 BCa interval). FRESH area (single-sample AUC SE/CI -- mlframe had only the PAIRED `delong_test`, no single-sample SE; low-contention). Sun & Xu 2014 structural components give `Var(AUC) = Var(v10)/n_pos + Var(v01)/n_neg` (`ddof=1`, tie-aware midranks) with NO resampling; `auc_ci` is a logit-scale Wald interval that stays inside (0,1) near AUC=1 | ground truth = Monte-Carlo SD of AUC over 4000 independent test draws of a known normal-shift model; 5 scenarios (auc0.97/n150, auc0.97/n400, auc0.90/n100, auc0.85/n200, auc0.99/n300) x 8 seeds; honest metrics = \|SE - truth-SD\| + empirical 95%-CI coverage | DeLong vs BCa-bootstrap is a STATISTICAL WASH: DeLong \|SE-truth\| closer in 22/40 (scenario x seed) cells (~55%), per-scenario 3/8, 4/8, 4/8, 6/8, 5/8 (DeLong scenario-majority in only 3/5; n150 LOSES 3/8); mean \|SE-truth\| near-identical (n150 DeLong 0.00431 vs boot 0.00422; n200 DeLong 0.00182 vs boot 0.00220; n300 0.00079 vs 0.00080); coverage essentially TIED (auc0.97/n150 both 0.943; auc0.97/n400 DeLong gap 0.013 vs boot 0.017). NO measurable accuracy edge over the (already AUC-well-calibrated) BCa bootstrap -> NOT a default flip on `bootstrap_metric`. Ships as a NEW function (instant / deterministic / RNG-free closed-form SE), DeLong = internal default of `auc_ci` (no prior single-sample AUC-CI default existed to regress) | ADDED-NEW / REJECT-flip (DeLong = WASH vs BCa bootstrap; closed-form fn + bench kept per REJECTED!=DELETED, no default flipped). bench `evaluation/_benchmarks/bench_auc_ci_delong_vs_bootstrap.py`; tests `tests/evaluation/test_biz_val_evaluation_auc_ci.py` | 6fa94c5f |

| qual-10 | Adjusted R^2 (`fast_adjusted_r2_score(y_true, y_pred, n_predictors)`) added to `mlframe.metrics.regression._regression_extras` (re-exported via `metrics.core` / `metrics.regression`), vs plain `fast_r2_score`. FRESH area (REGRESSION-metric small-sample bias -- not calibration qual-1..4/6, bootstrap-CI qual-5/9, macro-avg qual-7, fairness-std qual-8). Plain R^2 on the fitting sample is upward-biased: it never decreases when predictors are added, so it overstates explained variance when the predictor count `p` is non-trivial vs `n`. Adjusted R^2 (Wherry/Ezekiel) penalises by model df: `1-(1-R^2)*(n-1)/(n-p-1)` | ground truth = TRUE population R^2 exactly 0.5 for `y=x0+noise` (x0,noise~N(0,1)); OLS fit on x0 + (p-1) pure-noise predictors (junk contributes 0 in population). honest metric = \|estimate - 0.5\|. 7 seeds x two cell sets: p/n>=0.1 {(40,8),(50,10),(50,20),(100,20),(100,30)} and p/n small {(500,5),(1000,10),(2000,5)} | p/n>=0.1 regime: adjusted closer-to-true in 27/35 cells (77%), mean \|estimate-true\| plain 0.138 -> adjusted 0.080 (42% reduction); largest win at small-n/large-p (n=50,p=20: plain R^2~0.71 vs adjusted~0.51 at true 0.5). p/n small: WASH (adjusted 9/21, plain 12/21), both ~0.022 -- adjusted converges to plain, no harm. Default-FLIP hypothesis (make adjusted the default `fast_r2_score`) REJECTED: the two answer different questions, adjusted needs a predictor count the plain signature has no way to supply, and no win at small p/n. Ships as NEW fn. bench `metrics/_benchmarks/bench_adjusted_r2.py`; tests `tests/metrics/regression/test_regression_extras.py::test_adjusted_r2_*` (5 unit) + `tests/metrics/regression/test_biz_val_metrics_adjusted_r2.py` (2 biz_value) | ADDED-NEW / REJECT-flip (adjusted R^2 is the correct goodness-of-fit when p matters but is a SEPARATE statistic needing `p`, not a drop-in default for `fast_r2_score`; bench + fn kept per REJECTED!=DELETED, no default flipped) | 162c4e25 |

| qual-11 | Probability-clip eps in `fast_log_loss` (`metrics/_log_loss_and_separation.py`): is the dtype-dependent default `eps=np.finfo(y_pred.dtype).eps` (1.19e-7 for float32) a log-loss-inflating bug that should flip to a fixed float64 eps (2.22e-16)? FRESH area (probability post-processing / log-loss clip floor -- NOT calibration qual-1..4/6, bootstrap/AUC-CI qual-5/9, macro-avg qual-7, fairness-std qual-8, adjusted-R^2 qual-10). Hypothesis: float32 probs (common GBM/NN predict_proba output) clip at 1.19e-7, capping the confident-wrong penalty and under-reporting log-loss | overconfident-wrong model (conf in {1e-8,1e-10}, 5% wrong rows), n=100k, 5 seeds x 2 scenarios; honest metric = `\|fast_log_loss - sklearn_log_loss(float64)\|` (the no-clip target) | The legacy float32 eps IS optimistic (ml32~0.78 vs honest~0.90, ~13% low, 5/5 seeds) -- BUT the proposed fix (force float64 eps on float32) is WORSE: it OVERSHOOTS to ~1.34 (+48%), because float32 cannot represent (1-1.19e-7, 1): a confident `1-1e-8` collapses to exactly 1.0 on cast, and clipping that at 2.22e-16 charges `-log(2.22e-16)~=36` per unrepresentable-near-1 confident-wrong row vs the intended `-log(1e-8)~=18`. The bias DIRECTION flips with eps choice; float32 cannot carry the near-1 information, so the dtype-matched eps is the least-wrong floor. float64 path already matches sklearn exactly. NO unambiguously-better dtype-independent default exists -> REJECT, keep dtype-dependent legacy default. bench `metrics/_benchmarks/bench_log_loss_dtype_eps_qual11.py`; reject-pinning tests `tests/metrics/test_biz_val_metrics_log_loss_dtype_eps.py` (3, pin BOTH sides) | REJECTED (no flip; dtype-dependent `eps=finfo(y_pred.dtype).eps` stays default; bench + reject-pinning tests kept per REJECTED!=DELETED) | a9f8908e |

| qual-12 | RANKING-metric tie-handling: do the four `metrics/_ranking_extras.py` LTR kernels (DCG@k, ERR, Hit@k, Precision@k) need an explicit stable `np.argsort(-y_score, kind="mergesort")` to break tied scores deterministically by input order (the Wave-57 convention the core `ranking.py` NDCG/MAP/MRR kernels carry), since they shipped with a bare `np.argsort(-y_score)`? FRESH area (LTR ranking-metric correctness -- NOT calibration ECE/Brier/ECI qual-1..4/6, bootstrap/AUC-CI qual-5/9, macro-avg qual-7, fairness-std qual-8, adjusted-R^2 qual-10, log-loss-eps qual-11). Hypothesis: bare argsort is unstable -> tied-score blocks permuted arbitrarily -> metric is a non-reproducible function of physical row layout, diverging from the sibling core metrics | discriminating tied-block score vector (a tied 0.0 block where numpy quicksort vs mergesort place the sole relevant doc at different ranks: DCG 2.10721 vs 1.95260, ERR 0.09722 vs 0.07955 ~7%); verified against both numpy and numba argsort | DECISIVE NEGATIVE: numba 0.63.1's `np.argsort` WITHOUT `kind=` is ALREADY stable inside `@njit` (only numpy's pure-Python path defaults to unstable quicksort) -- the four kernels were ALREADY producing the stable, deterministic tie-break (numba default order == numba mergesort order == numpy mergesort order; only numpy-default-quicksort differs). The earlier "ERR 6.6x spread" observation was an artifact of permuting the INPUT rows (different inputs legitimately give different metrics), NOT an unstable sort. No bug, no flippable default: pre-fix behaviour was already correct. Kept the explicit `kind="mergesort"` on all four (bit-identical, documents the invariant + guards a future numba default change) + an invariant test pinning the stable-order DCG/ERR values. bench: N/A (decisive via numba/numpy argsort-stability comparison + tied-block vector); test `tests/metrics/test_ranking_drift_extras.py::test_ranking_extras_stable_tiebreak_matches_input_order` | REJECTED (no flip; pre-fix `@njit` argsort already stable. Explicit `kind="mergesort"` kept as a defensive no-op + invariant test, per REJECTED!=DELETED) | 42e7a123 |

| qual-13 | Fold/member PROBABILITY AGGREGATION for ensemble/CV blending: does the GEOMETRIC mean / log-average of member probabilities (logarithmic opinion pool, renormalised to the simplex) beat the ARITHMETIC mean (linear opinion pool, the `_ChainEnsemble.predict_proba` default `stacked.mean(axis=0)` in `training/_classif_helpers.py`) under the log-loss proper scoring rule? FRESH area (classification probability BLEND path in training/composite -- distinct from all prior metric-estimator-bias qual rows). The arithmetic pool is generally better-calibrated and robust to a confidently-wrong member; geometric sharpens and is the proper-scoring-rule-optimal blend for diverse honest members but is fragile to rogue members. MEASURED honestly, not assumed | synthetic K-member binary/3/4-class ensembles on a known target, honest-holdout NLL (headline) + Brier + ECE; 8 scenarios x 7 seeds = 56 cells, varying member diversity / calibration (temperature) / K / class balance + 3 ADVERSARIAL scenarios injecting confidently-wrong rogue members (the linear-pool-wins regime) | GEOMETRIC wins NLL 50/56 cells (89%, 7/8 scenarios), Brier 50/56, ECE 53/56 (8/8 on per-scenario mean); per-scenario NLL mean cut e.g. S1 0.2860->0.2223, S2 0.1676->0.0780, S4 0.2465->0.1598, S5 0.8962->0.7807; the ONLY arithmetic win is S7 (extreme 40%-rogue contamination: NLL 0.9656 vs geom 0.9949, Brier likewise) -- the known log-pool fragility, preserved as the opt-in `proba_aggregation="arithmetic"` path. Geometric wins the headline metric majority WITHOUT regressing Brier/ECE (improves both in aggregate) -> FLIP default. Pickle-replay gated: pre-flip pickles lack the attr -> `getattr(..., "arithmetic")` replays legacy byte-identically; new instances default geometric. Multilabel-chain output handled correctly (per-label complement renorm, NOT cross-label simplex). bench `training/composite/ensemble/_benchmarks/bench_proba_aggregation_arith_vs_geom.py`; tests `tests/training/test_biz_val_classif_proba_aggregation.py` (NLL >=1.20x win + default-is-geometric + simplex/multilabel renorm + unknown-method raise) | RESOLVED (flip default to geometric; arithmetic kept opt-in + replay-safe for old pickles) | 5fbe53af |
| qual-14 | REGRESSION ensemble member-aggregation default: does the across-member MEAN (`BaggedCompositeEstimator.predict` `members.mean(axis=0)` in `training/composite/bagging.py:279`) beat a robust aggregator (TRIMMED-MEAN / MEDIAN) on honest-holdout RMSE+MAE for heavy-tailed / outlier-contaminated regression targets? FRESH area (REGRESSION point-estimate blend in training/composite -- sibling of qual-13's CLASSIFICATION probability pool, different metric/path). The plain mean is the Gaussian-MLE-optimal blend but a single wild bootstrap member skews it; trimmed-mean drops the symmetric `trim_fraction` extremes per row (robust to a rogue member) and converges to the mean as contamination -> 0; median is maximally robust but loses Gaussian efficiency | DecisionTree(max_depth=6) base, 25 members; 3 scenarios (Student-t(df=2) heavy-tail noise / 5%-outlier-contamination / clean Gaussian) x 7 seeds = 21 cells; honest-holdout RMSE + MAE vs noiseless truth; bench `training/composite/_benchmarks/bench_bagging_aggregation_qual14.py` | TRIMMED-MEAN (10%) wins-over-mean: outlier_contam 7/7 RMSE + 7/7 MAE (mean rel RMSE -4.7%), heavy_tail 5/7 RMSE + 5/7 MAE (-2.8%, BEATS median's 4/7), clean 1/7 RMSE + 1/7 MAE (mean rel RMSE delta only +0.42%, worst +1.16% -- a near-tie, NOT a regression). Median wins outlier_contam 7/7 but REGRESSES clean MAE 0/7; trimmed-mean is the efficiency-preserving robust default (majority win on 2/3 scenarios, trivial loss on clean). FLIP default mean -> trimmed_mean (trim_fraction=0.1); `aggregation="mean"` (legacy Gaussian-MLE) + `aggregation="median"` kept opt-in. Pickle-replay gated: pre-flip pickles lack `aggregation`/`trim_fraction` -> `getattr(self,"aggregation","mean")` replays legacy byte-identically; new instances default trimmed_mean. `predict_std`/`predict_interval_epistemic` unchanged (Gaussian mean+std, independent of the point aggregator). tests `tests/training/test_biz_val_training_composite_bagging.py` (trimmed-mean beats mean >=4/5 seeds on contamination + within-5% on clean + default-is-trimmed-mean sensor) | RESOLVED (flip default to trimmed_mean; mean+median kept opt-in + replay-safe for old pickles) | 57f648f7 |
| qual-15 | Cross-target ensemble OUTPUT-CALIBRATION method default (`OutputCalibrator.method` / `cross_target_calibration_method`, `training/composite/ensemble/_calibration.py`): should the post-hoc monotone recalibration of the OOF-blended prediction flip from free-form `isotonic` to the 2-parameter `sigmoid` at the SMALL OOF (default `oof_holdout_frac=0.2`, often a few hundred rows) where the Niculescu-Mizil & Caruana prior says isotonic PAV overfits and Platt/sigmoid generalises better? FRESH area (regression output recalibration in training/composite -- distinct from the metric-estimator-bias surface of qual-1..8 and the aggregation/blend surface of qual-13/14). NOTE: the cross-target NNLS-stack default was NOT re-probed -- it is already benched (`composite_ensemble_shootout.py`, 6 scenarios x 3 seeds x 11 strategies, NNLS the only positive-mean strategy) AND leak-protected (`oof_holdout_source="kfold"` + the OOF-validation gate falls back to best-single), so that lever is resolved | 2 monotone-miscalibration scenarios (S-shape tanh-squash + sqrt-saturation) x OOF sizes {60,150,400,1500} x 8 seeds; calibrator fit on the OOF split, scored on a DISJOINT 4000-row honest holdout; honest metric = holdout RMSE(map(p_hold), y_hold) | DECISIVE NEGATIVE for the flip: `isotonic` beats `sigmoid` on holdout RMSE in 8/8 seeds in EVERY cell, INCLUDING the smallest n_oof=60 (s_shape iso 0.4417 vs sig 0.8998 vs raw 0.5908; saturating iso 0.4250 vs sig 1.1508 vs raw 0.6445; gap WIDENS as OOF shrinks, opposite the prior's prediction). The OutputCalibrator `sigmoid` is an OLS-on-centred-logit affine-link S-map (not a cross-entropy Platt fit), so it forces an S-shape these regression miscalibrations lack and is WORSE than even the raw blend at small n; isotonic's free-form PAV tracks the real distortion. `isotonic` is the most-accurate default across ALL OOF sizes -> no flip. bench `training/composite/ensemble/_benchmarks/bench_calibration_method.py`; verdict-pinning tests `tests/training/test_ensemble_output_calibration.py::test_biz_val_isotonic_beats_sigmoid_on_small_oof_holdout` (3 OOF sizes) + `::test_default_calibration_method_is_isotonic` | REJECTED (no flip; `isotonic` stays the default for both OutputCalibrator + cross_target_calibration_method; bench + verdict-pinning tests kept per REJECTED!=DELETED) | 99234ccc |
| qual-16 | Cross-target ensemble `OutputCalibrator(method="sigmoid")` FIT objective (`training/composite/ensemble/_calibration.py::_fit_sigmoid`): qual-15 identified the `sigmoid` OPTION as an OLS-on-centred-logit affine-link map, NOT a cross-entropy Platt fit, so it barely improved on (and at small OOF was WORSE than) the raw blend. Fix the OPTION (default `method` stays `isotonic`): replace the OLS-logit fit with a proper MAXIMUM-LIKELIHOOD Platt fit (`sigmoid(A*z+B)` minimising weighted binomial cross-entropy of the [0,1]-normalised truth, 2-param Newton solve), exposed via new `sigmoid_fit="platt"` default; legacy kept opt-in as `sigmoid_fit="ols_logit"`. FRESH (the calibrator-FIT objective, distinct from the qual-15 method-CHOICE) | 3 scenarios (genuine logistic miscalibration of a probability target / over-confident probability blend / regression tanh-S-shape) x OOF sizes {60,150,400} x 8 seeds; fit on OOF, scored on a DISJOINT 4000-row honest holdout; honest metric = holdout RMSE(map(p_hold), y_hold) | DECISIVE: MLE Platt beats the legacy OLS-logit map on holdout RMSE in 8/8 seeds in EVERY one of the 9 cells (72/72), e.g. logistic_miscal n_oof=60 platt 0.4253 vs ols 0.4589 vs raw 0.4586, overconfident n_oof=400 platt 0.4465 vs ols 0.5004 vs raw 0.4531; Platt also beats RAW 8/8 on logistic_miscal at every size + 5-8/8 on overconfident, and is strictly better than the broken OLS-logit even on regression_s_shape (where isotonic still wins overall, so `method` default unchanged). Replay-safe: pre-qual-16 pickles lack `sigmoid_fit` -> predict path never reads it (byte-identical) + re-fit getattr-defaults to "platt". bench `training/composite/ensemble/_benchmarks/bench_sigmoid_platt_vs_ols_logit.py`; tests `tests/training/test_ensemble_output_calibration.py::test_biz_val_platt_sigmoid_beats_ols_logit_and_raw_on_holdout` (3 OOF sizes) + `::test_default_sigmoid_fit_is_platt` + `::test_sigmoid_platt_is_monotone_and_rejects_bad_fit` + `::test_ols_logit_sigmoid_fit_still_reachable_for_replay` | RESOLVED (sigmoid_fit default flipped ols_logit -> platt; OLS-logit kept opt-in + replay-safe; headline `method` default stays isotonic per qual-15) | e4b80764 |
| qual-17 | `BaggedCompositeEstimator` trimmed-mean `trim_fraction` default (`training/composite/bagging.py`): qual-14 set `trim_fraction=0.1`; is a DIFFERENT trim a better default across MORE contamination levels? (the qual-14 bench fixed trim at 0.1; this iter sweeps the knob). Same AGGREGATION surface as qual-14, distinct lever (the trim magnitude, not the aggregator choice) | trim sweep {0.0(=mean),0.05,0.10,0.15,0.20,0.25,0.30} x 5 scenarios (clean Gaussian / heavy-tail Student-t(df=2) / 5%,15%,30% large-spike outlier-contam) x 7 seeds; members computed ONCE per fitted bag then every trim applied to the SAME matrix (apples-to-apples); honest-holdout RMSE+MAE vs noiseless truth; bench `training/composite/_benchmarks/bench_bagging_trim_fraction_qual17.py` | Honest RMSE decreases MONOTONICALLY with the trim on every contaminated scenario; trim=0.2 wins per-seed honest RMSE on 28/35 seed-scenario cells vs the 0.1 incumbent -- 7/7 on EVERY contaminated scenario (contam_30 mean RMSE 2.0838@0.1 -> 1.9763@0.2, ~5-9% drop) -- for an immaterial clean cost (clean mean RMSE 1.6717@0.1 -> 1.6765@0.2, +0.29%; deep 20-seed clean check 1.6107 -> 1.6136, +0.18%, 0.2 still wins 7/20 clean seeds). The grid AGGREGATE optimum sat at the 0.30 edge (sum-RMSE 62.89 vs 0.2's 63.46, within 0.9%) but the edge is near-median / contamination-overfit risk, so the default is set to the 0.2 KNEE (captures ~all the contamination benefit off the boundary). FLIP default 0.1 -> 0.2; any explicit `trim_fraction` + `aggregation="mean"`/`"median"` kept. Replay-safe: pre-qual-14 pickles lack `trim_fraction` (+`aggregation`, so replay as plain mean); qual-14+ pickles carry their stored value; only NEW instances default to 0.2. tests `tests/training/test_biz_val_training_composite_bagging.py::test_biz_val_bagging_trim_fraction_default_is_0_2` + `::test_biz_val_bagging_trim_0_2_beats_0_1_on_heavy_contamination` (15%+30%) + `::test_biz_val_bagging_trim_0_2_near_0_1_on_clean_data` | RESOLVED (trim_fraction default flipped 0.1 -> 0.2; legacy 0.1 + mean/median kept reachable; replay-safe) | 7eb0ccd9 |

Streak: 3 consecutive RESOLVED (qual-1, qual-2, qual-3), then qual-4 REJECT, then qual-5 RESOLVED, qual-6 RESOLVED, qual-7 RESOLVED, qual-8 RESOLVED, then qual-9 (new closed-form `auc_variance`/`auc_ci` added; DeLong scored a WASH vs the BCa bootstrap so NO default flip -- REJECT of the "DeLong beats bootstrap" hypothesis, bench + code kept), then qual-10 (new `fast_adjusted_r2_score` added; clear known-ground-truth win in its p/n>=0.1 regime but a SEPARATE statistic needing a predictor count, so NOT a default flip on `fast_r2_score` -- REJECT of the flip hypothesis, bench + fn kept), then qual-11 (log-loss clip eps -- dtype-dependent default is CORRECT; the "always float64 eps" alternative overshoots on float32's unrepresentable-near-1 region, bias direction flips with eps, no dtype-independent better default -- REJECT, dtype-dependent default kept), then qual-12 (LTR ranking-metric tie-handling -- the four `_ranking_extras` `@njit` kernels were ALREADY stable because numba's default `np.argsort` is stable; no bug, no flippable default -- REJECT, explicit `kind="mergesort"` kept as a defensive no-op). Cumulative: 8 RESOLVED / 5 REJECT across the accuracy/quality loop. qual-13 (fold/member probability aggregation, training/composite -- previously an AVOIDED surface, unlocked for this iteration) BREAKS the four-reject streak: the geometric / log-average (logarithmic opinion pool) blend of member probabilities measurably beats the arithmetic mean on honest-holdout NLL (50/56 cells) without regressing Brier/ECE, so the `_ChainEnsemble` probability-blend default flipped arithmetic -> geometric (arithmetic kept opt-in + replay-safe). The earlier "wind down" judgement was scoped to the metric-estimator surface; the AGGREGATION path it flagged as a remaining candidate is now mined and RESOLVED. Cumulative after qual-13: 9 RESOLVED / 5 REJECT. qual-14 EXTENDS the aggregation win to the REGRESSION side: the bagging point-estimate aggregator flips mean -> trimmed_mean (robust to outlier/heavy-tail members, -2.8% to -4.7% holdout RMSE there, only +0.42% on clean), mean+median kept opt-in + replay-safe. Cumulative after qual-14: 10 RESOLVED / 5 REJECT. Both qual-13 and qual-14 confirm the AGGREGATION/blend surface (distinct from the saturated metric-estimator-bias surface) still yields measurable default-flips; remaining unmined candidate = the cross-target ensemble `oof_weighted` gain-weighting + meta-stack defaults (NNLS is current default), and the FS/MRMR core (off-limits). qual-15 probed the cross-target output-CALIBRATION method default (`isotonic` vs `sigmoid`): the "sigmoid generalises better at small OOF" prior is REJECTED -- isotonic wins holdout RMSE 8/8 seeds in EVERY OOF-size cell down to n=60 (the OutputCalibrator `sigmoid` is an OLS-on-logit affine S-map, not a cross-entropy Platt fit, and is worse than even the raw blend at small n), so `isotonic` stays the most-accurate default; the NNLS-stack default was deliberately NOT re-probed (already benched via `composite_ensemble_shootout.py` + leak-protected by `oof_holdout_source="kfold"` and the OOF-validation gate). Cumulative after qual-15: 10 RESOLVED / 6 REJECT. The AGGREGATION surface remains the only one still flipping defaults; the calibration / metric-estimator surfaces are saturated. qual-16 acts on the secondary candidate qual-15 itself surfaced: the `sigmoid` calibration OPTION was a broken OLS-on-logit map (not a Platt fit), so it is FIXED to a proper maximum-likelihood Platt fit (`sigmoid_fit="platt"` new default, OLS-logit kept opt-in + replay-safe) -- the MLE map beats the legacy surrogate 72/72 seeds on honest holdout and beats the raw blend on the probability targets a sigmoid is for. This is a correctness/quality fix to an OPTION, not a headline-default flip (`method` stays `isotonic` per qual-15), but it is a measured RESOLVED win (a previously-useless option is now genuinely useful). Cumulative after qual-16: 11 RESOLVED / 6 REJECT. The calibration surface yielded a fix (the FIT objective) even though its method-CHOICE default was saturated. qual-17 mines the qual-14 lever one level deeper -- the trimmed-mean TRIM MAGNITUDE: a {0.0..0.30} x 5-contamination-level x 7-seed sweep shows honest RMSE falling monotonically with the trim on every contaminated scenario, so the `trim_fraction` default flips 0.1 -> 0.2 (the robust knee: wins 28/35 cells, 7/7 on every contaminated scenario, ~0.2-0.3% clean cost; the 0.30 grid edge was a hair better in aggregate but near-median / overfit-risk, so 0.2 is the off-boundary choice). Cumulative after qual-17: 12 RESOLVED / 6 REJECT. The AGGREGATION surface (qual-13 classif geometric pool, qual-14 regression trimmed-mean, qual-17 trim magnitude) has now yielded three flips; the metric-estimator-bias + calibration-method-choice surfaces remain saturated. This is the 5th composite/ensemble/training iteration in a row to flip or fix a default on the aggregation/blend path; the remaining composite-quality surface is thinning -- the obvious knobs (proba pool, point aggregator, trim magnitude, calibration fit) are now tuned, and the next candidates (cross-target `oof_weighted` gain-weighting / meta-stack, already benched + leak-protected; FS/MRMR core, off-limits) are either resolved or out of scope.

SATURATION JUDGEMENT (qual-12): the ACCESSIBLE accuracy/quality-flip surface is CONFIRMED SATURATED. Four consecutive REJECT-flips (qual-9/10/11/12) now -- the genuinely-biased estimators were all corrected in qual-1..8 (ECE/Brier/ECI/KL-JS debiasing, macro-averaging, Bessel std, BCa CI), and every default-bearing knob probed since is either SOUND as-is (log-loss eps qual-11, LTR tie-handling qual-12) or a separate statistic rather than a flip (DeLong SE qual-9, adjusted R^2 qual-10). qual-12 deliberately probed a DIFFERENT area (LTR ranking-metric correctness: NDCG gain convention = already exponential/LTR-correct; MAP/MRR conventions = textbook with documented denominators; PR-AUC/AP = sklearn-parity-verified; tie-handling = already stable via numba) and found NO flippable default with a measurable majority win. Recommend WINDING DOWN the accessible accuracy/quality loop. Remaining candidate surfaces all live in currently-AVOIDED sessions: the fold/member probability AGGREGATION path (arithmetic vs geometric mean of probabilities for log-loss-proper blending) in the training/composite core, and the MRMR/FE-selection scoring core -- mine those when the live/FS sessions are idle.

SATURATION NOTE (qual-11): the probability post-processing / metric-estimator-bias flip surface is now broadly SATURATED. The clearly-biased estimators have been corrected (ECE/Brier/ECI/KL-JS debiasing qual-1/2/3/6, macro-averaging qual-7, Bessel std qual-8, BCa CI qual-5) and the remaining default-bearing knobs investigated this loop are SOUND as-is (log-loss clip eps qual-11) or are separate statistics rather than flips (DeLong SE qual-9, adjusted R^2 qual-10). The 3 most recent iterations (qual-9/10/11) all ended REJECT-flip. Recommend winding the accuracy/quality loop down or rotating to a different default-bearing surface (e.g. the fold/member probability AGGREGATION path -- arithmetic vs geometric mean of probabilities for log-loss-proper blending -- which lives in the currently-AVOIDED training/composite core and should be mined when that session is idle).
| qual-18 | Composite multi-base DISCOVERY gate default `multi_base_min_marginal_rmse_gain` (`training/_composite_target_discovery_config_base.py`; consumed by `forward_stepwise_multi_base` via `_fit.py:760`): the relative marginal CV-RMSE gain a candidate base must clear to be ADDED to a `linear_residual_multi` composite target. Is the 0.02 (2%) gate too conservative -- does it leave genuinely-helpful weak orthogonal bases out of the discovered target, hurting the DOWNSTREAM honest-holdout metric? FRESH composite surface (the discovery-target SELECTION gate, distinct from qual-13/14/16/17 aggregation/blend; NOT MI-estimator kernels, NOT MRMR/FE) | leakage-safe honest holdout: the gate runs forward-stepwise selection on TRAIN ONLY (internal TimeSeriesSplit/KFold CV + paired-fold majority gate); joint-OLS composite target fit on TRAIN; LGBM trained on the composite target; inverted prediction scored on a DISJOINT 40% holdout the discovery NEVER saw. 3 scenarios (three_real_bases / one_dominant_plus_decoys / graded_weak_real) x up to 10 seeds x thresholds {0.005,0.01,0.02,0.05}; bench `training/composite/discovery/_benchmarks/bench_multibase_min_marginal_gain.py` | 0.005 beats 0.02 on honest-holdout RMSE in 20/20 NON-TIED seeds across the two additive multi-base DGPs: three_real_bases mean 0.7363 vs 0.8749 (-15.8%, 10/10, admits the weak real base -> 3.0 vs 2.0 bases kept), graded_weak_real 0.6761 vs 0.7786 (-13.2%, 10/10). On one_dominant_plus_decoys 0.005 EXACT-TIES 0.02 (10/10 ties, 1 base kept) -- the paired-fold majority gate rejects the noise-decoy bases independent of the relative threshold, so ZERO regression where extra bases would hurt. FLIP default 0.02 -> 0.005; legacy 0.02 kept reachable. Replay-safe: `_fit.py` reads `getattr(config,"multi_base_min_marginal_rmse_gain",0.02)`, so pre-field pickled configs replay with 0.02 and only NEW configs default to 0.005. tests `tests/training/composite/test_biz_val_training_composite_discovery.py::test_biz_val_composite_discovery_multi_base_min_marginal_gain_default_is_0_005` + `::test_biz_val_composite_discovery_low_gain_gate_beats_legacy_on_additive_holdout` + `::test_biz_val_composite_discovery_low_gain_gate_no_regression_on_dominant_base` + `::test_biz_val_composite_discovery_legacy_002_gate_still_reachable` | RESOLVED (multi_base_min_marginal_rmse_gain default flipped 0.02 -> 0.005; legacy 0.02 kept reachable; replay-safe via getattr) | 9c47af8e |
| qual-19 | Cross-target ENSEMBLE member-pruning default `ct_ensemble_dedup_enabled` (`training/_composite_target_discovery_config.py:416`; consumed at `training/core/_phase_composite_post_xt_ensemble/__init__.py:667`): drop near-duplicate ensemble members (\|honest-OOF residual Pearson corr\| > `ct_ensemble_dedup_corr_threshold`=0.95, keeping the lower-OOF-RMSE one) BEFORE the cross-target stack/blend, so a redundant cluster can't split + dominate the weight. Should this corrective-mechanism flag flip ON by default? Distinct from qual-13/14/16/17 (post-pruning aggregation) and qual-18 (pre-ensemble discovery gate): this is the ENSEMBLE MEMBER-SELECTION lever | leakage-safe honest holdout: dedup keys ONLY on the OOF-residual surface the stacker fits, then test RMSE measured on a DISJOINT 50% holdout. Existing committed bench `training/_benchmarks/bench_ct_ensemble_residual_dedup.py` (NNLS + near-dup-of-strongest, 10 seeds) EXTENDED to 3 scenarios in `bench_ct_ensemble_residual_dedup_scenarios.py`: nnls_dup (NNLS, n_dup=4, jitter=0.02) / mean_dup (uniform-mean strategy, same pool) / nnls_tight (NNLS, n_dup=8, jitter=0.005), x 10 seeds | dedup is NEUTRAL-or-HARMFUL on every scenario. NNLS scenarios: ~1e-4 noise either way (nnls_dup mean RMSE full 1.30454 vs dedup 1.30459, dedup wins 1/10; nnls_tight 1.30452 vs 1.30453, 1/10) -- NNLS already learns the optimal combined weight regardless of how members split it, so removing duplicates changes nothing. mean_dup (the case dedup SHOULD win): dedup HURTS, full 1.44867 vs dedup 1.48968 (+2.83%, dedup wins 0/10) -- the redundant cluster's average is a LOWER-VARIANCE estimate of the strong signal than any single survivor, so dropping the dups raises uniform-mean variance. Overall 0/3 scenarios ON (majority-of-scenarios rule). Default `ct_ensemble_dedup_enabled=False` is CORRECT and unchanged; knob + bench kept (REJECTED!=DELETED). test `tests/training/composite/test_biz_val_training_composite_dedup.py::test_biz_val_ct_ensemble_dedup_does_not_help_nnls_or_mean_on_redundant_pool` pins the verdict (fails if dedup ever measurably beats the full pool here). | REJECT (dedup neutral on NNLS, harmful on mean; default OFF kept; bench `bench_ct_ensemble_residual_dedup_scenarios.py` committed) | a7129d2e |

| qual-20 | Numeric-SCALING default `PreprocessingBackendConfig.scaler_name` (default `"standard"`; flows through `create_polarsds_pipeline` -> `_apply_safe_scaler` at `training/pipeline/__init__.py:703`): does `"robust"` (median-center / IQR-scale, breakdown-resistant) beat StandardScaler (mean / std, both dragged by the tail) on the downstream honest holdout when heavy outliers / heavy tails are present? FRESH PREPROCESSING surface (the scaler default -- distinct from calibration/metrics/CI qual-1..12 and composite/ensemble qual-13..19; the sibling imputer mean-vs-median default was already benched-and-kept). Scored only for SCALE-SENSITIVE linear models (LogisticRegression / Ridge) -- tree models ignore any monotone per-column scaling | honest holdout: real pipeline fit on a 70% train, scored on a DISJOINT 30% holdout. 5 scenarios (5%-additive-spike contam clf+regr with heterogeneous per-column scales / Cauchy-tail noise / right-skewed lognormal / clean-Gaussian control) x 5 seeds; bench `training/_benchmarks/bench_scaler_default_standard_vs_robust.py` | robust shows NO scenario-majority win and only ~1e-3-magnitude deltas (robust 8 / standard 12 / tie 5 of 25): standard wins the clean-Gaussian (3/0) + lognormal (4/1) controls, robust edges only spike-contam-binary (3/0) by ~0.0003 AUC. Root cause: once every column is individually normalised, a single global L2 regulariser absorbs the std-vs-IQR scale-family difference -- the honest-holdout AUC/R2 is near-invariant to the choice, and the spike contamination sits in the data BOTH scalers see (robust's IQR only marginally re-shapes per-column dynamic range, never removing the contaminated rows). A flip needs a scenario-majority robust win WITHOUT a clean-data regression; this bench produces neither. Default `"standard"` is the most-accurate default and unchanged; `"robust"`/`"min_max"`/`"abs_max"` + `robust_q_low`/`robust_q_high` stay reachable (REJECTED!=DELETED). tests `tests/training/test_biz_val_training_scaler_default.py::test_biz_val_scaler_default_is_standard` + `::test_biz_val_scaler_robust_still_reachable` + `::test_biz_val_scaler_robust_does_not_dominate_standard_on_contaminated_holdout` (pins the verdict: fails if robust ever beats standard by a material margin on the contaminated holdout). | REJECT (no flip; `scaler_name="standard"` stays default; bench `bench_scaler_default_standard_vs_robust.py` committed) | 849d995e |

| qual-21 | MRMR relevance-MI bias correction `mi_correction` (`mrmr/_mrmr_class.py:332`, default `"none"` | `"miller_madow"` | `"chao_shen"`): FOUND DEAD -- the knob was validated + stored but NEVER activated in the fit path (no `set_*` call beside the JMIM/SU thread-locals at `_mrmr_class.py:3776`), so `mi_correction="miller_madow"` silently did nothing. WIRED it: new `mi_miller_madow` / `compute_mi_mm_from_classes` kernels (`info_theory/_entropy_kernels.py`, `_class_mi_kernels.py`) subtract the closed-form plug-in bias `(k_x-1)(k_y-1)/(2n)` from the OBSERVED relevance via a `set_mi_miller_madow` thread-local read in `permutation.py:592` + activated/reset in `MRMR.fit`. FRESH UNLOCKED MRMR/FE-selection surface (the relevance-MI estimator bias correction; distinct from the qual-1..20 calibration/metrics/composite/preprocessing surfaces, and distinct from the already-REJECTED `redundancy_aggregator=jmim` + `mi_normalization=su` levers) | KNOWN-relevant-set synthetic: low-card true drivers buried under high-cardinality (40-95 level) integer noise at small n=600; 2 scenarios (lowcard regression / ternary classification) x 7 seeds; honest metrics = selected-set precision/recall vs the known relevant index set AND downstream holdout R2/AUC; bench `feature_selection/_benchmarks/bench_mi_correction_miller_madow.py` (-> `tests/perf/results/mi_correction_mm.json`) | NO DEFAULT FLIP. precision 1.000->1.000 + recall 1.000->1.000 on lowcard, 0.667->0.667 + 1.000->1.000 on ternary; downstream R2 0.9154->0.9155 (+0.0001), AUC 0.9447->0.9447 (+0.0000); 3/14 seed-scenario metric cells -- far below majority; selection BIT-IDENTICAL on every cell, AND identical even with the permutation screen fully OFF (`full_npermutations=0`) and under legacy fixed-bin (`nbins_strategy=None`). Root cause (confirmed, not hypothesised): the default `nbins_strategy="mdlp"` (supervised discretization) collapses pure-noise high-card columns to 1-2 occupied bins -> `k_x<=1` -> MM bias term ~0; AND the default permutation null-debias screen subtracts the EMPIRICAL plug-in bias non-parametrically (the MM term is constant across y-permutations -> cancels in `observed - null_mean`). Both production defaults pre-empt exactly the small-sample inflation MM targets. The kernel-level correction is REAL (60-level noise vs binary y: plug-in MI 0.088 -> MM 0.015, 0.074 nats removed) but does not survive end-to-end. Kept as an OPT-IN for the legacy fixed-bin / no-permutation-screen regime. tests `tests/feature_selection/test_biz_val_filters_mrmr_mi_correction.py::test_biz_val_mi_correction_kernel_removes_high_cardinality_bias` (floor >=0.05 nats removed; measured 0.074) + `::test_biz_val_mi_correction_kernel_noop_on_lowcard_signal` (MM must not damage real binary signal) + `::test_mi_correction_knob_activates_and_resets_thread_local` (dead-knob regression: fit now ACTIVATES + resets the MM thread-local) + `::test_mi_correction_invalid_value_rejected` | REJECT-flip + WIRED-DEAD-KNOB (`mi_correction` was inert, now functional; default stays `"none"` -- mdlp + null-debias already neutralize the bias e2e; `"miller_madow"` opt-in now actually works; bench `bench_mi_correction_miller_madow.py` committed per REJECTED!=DELETED) | 7664d0e6 |

Cumulative after qual-21: 13 RESOLVED / 9 REJECT. qual-21 takes the FIRST swing at the now-UNLOCKED MRMR/FE-selection core (qual-1..20 were all off-core). Picked the clearest known-ground-truth lever: the `mi_correction` relevance-MI bias-correction default. Investigation surfaced that the knob was DEAD (validated + stored but never wired into fit -- a latent bug); fixed by wiring the Miller-Madow `(k_x-1)(k_y-1)/(2n)` correction into the observed relevance score (new njit kernels + thread-local, mirroring the existing SU/JMIM toggles). On a KNOWN-relevant-set synthetic (low-card signal vs high-card noise, n=600, 7 seeds x 2 scenarios) the correction is BIT-IDENTICAL in selection and ~0 in downstream metric -- a clean REJECT for the default flip, because the default mdlp discretization (collapses noise to 1-2 bins -> zero MM bias) and the permutation null-debias screen (empirically cancels the constant MM term) ALREADY neutralize the exact small-sample plug-in inflation MM targets. The kernel-level correction is proven real (0.074 nats removed from a 60-level noise feature, pinned in a biz_value test) but redundant with the production defaults end-to-end, so it ships as a now-functional opt-in for the legacy fixed-bin / no-screen regime. No flip manufactured; the dead-knob bug is fixed + regression-pinned in the same change.

Cumulative after qual-20: 13 RESOLVED / 8 REJECT. qual-20 rotates OFF the saturated composite/ensemble + metric-estimator surfaces to the un-mined PREPROCESSING scaler default (`scaler_name`, `"standard"` vs `"robust"`), measured on a leakage-safe downstream honest holdout for scale-sensitive linear models with heavy outliers present (the regime where the "robust scaler wins under contamination" prior should bite). The prior is REJECTED: robust shows no scenario-majority win and only ~1e-3 deltas, because a single global L2 regulariser absorbs the std-vs-IQR difference once columns are individually normalised, and the contamination is in the data both scalers see. The clean-Gaussian and lognormal controls actively (if barely) favour standard, so flipping would regress the no-outlier common case for no contaminated-case gain. `"standard"` stays the most-accurate default; the robust/min_max/abs_max options stay reachable. SATURATION JUDGEMENT (qual-20): with calibration/metrics/CI (qual-1..12), composite/ensemble/training (qual-13..19), and now the accessible preprocessing scaler default all confirmed-sound-or-already-tuned, the ACCESSIBLE accuracy/quality-flip surface is broadly SATURATED. The sibling preprocessing levers are either already benched-and-kept (imputer mean-vs-median) or live in the off-limits FE-selection core (missing-indicator FE, outlier-detection threshold). Recommend WINDING DOWN the accessible accuracy/quality loop -- no flip was manufactured here, and the next candidates require either the off-limits FS/MRMR core or a genuinely new default-bearing surface.

Cumulative after qual-19: 13 RESOLVED / 7 REJECT. qual-19 probes the remaining un-mined composite candidate -- the cross-target ENSEMBLE MEMBER-SELECTION / pruning lever (`ct_ensemble_dedup_enabled`, the residual-correlation dedup that drops near-duplicate stack members). The pre-existing committed bench (NNLS + near-dup pool, 10 seeds) was verdict-pending; this iter ran it (1/10 seeds, ~1e-4 deltas) AND extended it to 3 scenarios incl. the uniform-`mean` strategy where redundant members mechanically split the average weight and dedup SHOULD win. Result is a clean REJECT: dedup is neutral under NNLS (the stacker already learns the optimal combined weight so member-splitting is irrelevant) and actively HARMFUL under uniform-mean (+2.8% holdout RMSE -- the redundant cluster's average is a lower-variance estimate of the shared signal, so pruning it raises variance). The default OFF is the correct, most-accurate default; the knob stays reachable per REJECTED!=DELETED. With the post-discovery aggregation/blend path (qual-13/14/16/17), the discovery-selection gate (qual-18), and now the ensemble member-pruning lever (qual-19) all either flipped or confirmed-correct, the composite/ensemble/training surface is SATURATED: the remaining opt-in gates (`stacking_aware_gate_enabled`, `use_wilcoxon_gate`, `use_stacked_discovery_residual`, `require_beats_raw_baseline`) all carry their own measured rationale for defaulting OFF (the tiny-LGBM proxy lies for downstream linear/neural models -- documented at config_base.py:443-460), the NNLS-stack strategy is benched + leak-protected, and the MI-estimator kernels + MRMR/FE core are off-limits / saturated. An honest REJECT + saturation verdict is the correct outcome here -- no flip was manufactured.

qual-18 opens a genuinely DIFFERENT composite surface from the qual-13/14/16/17 aggregation/blend path: the composite-target DISCOVERY SELECTION gate. The multi-base forward-stepwise auto-promotion (`multi_base_enabled=True`, default ON) was admitting bases only at a 2% marginal-CV-gain bar that, measured on a leakage-safe disjoint holdout (gate selects on TRAIN, composite target scored on rows discovery never saw), stopped short of genuinely-helpful weak orthogonal bases -- lowering it to 0.005 wins 20/20 non-tied seeds on additive multi-base DGPs (~13-16% holdout RMSE) and exact-ties the single-dominant + noise-decoy DGP (the paired-fold majority gate independently blocks noise bases), so there is no scenario where the lower bar regresses. This is the discovery-target SELECTION lever the loop had not yet probed (prior composite iterations all lived on the post-discovery aggregation/blend path); it confirms the discovery surface still carries a default-flip win, while the gate's leakage protection (TimeSeriesSplit + paired-fold majority + train-only selection) was verified SOUND in the process.
