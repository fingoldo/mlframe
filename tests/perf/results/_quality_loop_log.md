# mlframe ACCURACY / QUALITY loop log

This loop hunts measurable ML-QUALITY wins (calibration / probability post-processing / metric-correctness),
NOT speed (the perf loop is saturated -- see `_loop_iter_log.md`). Each iteration picks ONE self-contained
accuracy lever in a low-contention area, measures it on the HONEST metric (holdout / OOF / known ground-truth)
across MULTIPLE seeds AND scenarios, and either RESOLVES (flips the default, keeps the old as opt-in per
REJECTED!=DELETED, adds a `biz_value` test) or REJECTS (keeps the committed bench + verdict, no default change).
A flip requires a MAJORITY win across seeds/scenarios -- never a single-seed/single-scenario result.

| iter | lever | scenario / seeds | honest metric: old -> new | verdict | commit |
|------|-------|------------------|---------------------------|---------|--------|
| qual-1 | Debiased binned ECE (subtract per-bin Bernoulli noise floor) as headline-ECE default in `fast_calibration_report` (`ece_debiased=True`; plug-in kept as `ece_debiased=False`) | 3 perfectly-calibrated (uniform / beta-rare / bimodal, true ECE=0) + 2 miscalibrated (sigmoid-shift / overconf, fine-grid truth) x 7-8 seeds x nbins {10,15,20}; honest metric = \|estimate - ground-truth ECE\| | mean bias-vs-truth on calibrated scenarios @nbins=15: uniform 0.0294 -> 0.0127, beta-rare 0.0128 -> 0.0055, bimodal 0.0196 -> 0.0082 (>2x bias reduction); per-cell \|bias\| wins debiased/plugin = 29/6 @nbins=10, 28/7 @15, 32/8 @20 (debiased wins ~80% of cells, majority at EVERY bin count); miscalibrated model still flagged (true-gap term dominates) | RESOLVED (flip default) | 47713b8e |
| qual-2 | Debiased Brier (Murphy) decomposition reliability/resolution as headline REL/RES default in `fast_calibration_report` (`brier_debiased=True`; plug-in kept as `brier_debiased=False`). SIBLING of qual-1: plug-in REL/RES carry the same +Var(acc_b) per-bin Bernoulli inflation. Broecker 2009: subtract Var(acc_b)=acc_b(1-acc_b)/(n_b-1) from BOTH REL and RES (clamp REL>=0), so REL-RES, UNC, BinnedBrier and the Murphy identity are preserved EXACTLY; only the bias split shifts | 3 perfectly-calibrated (uniform / beta-rare / bimodal, true REL=0) + 2 miscalibrated (sigmoid-shift / overconf, fine-grid truth) x 8 seeds x nbins {10,15,20}; honest metric = \|estimated REL - ground-truth REL\| | mean REL bias-vs-truth @nbins=15: uniform 0.00123 -> 0.00057, beta-rare 0.00128 -> 0.00093, bimodal 0.00113 -> 0.00060, miscal-sigmoid 0.00258 -> 0.00144, miscal-overconf 0.00203 -> 0.00046 (>2x on uniform/bimodal/overconf, every scenario DEBIAS-wins on mean bias); per-cell \|REL bias\| wins debiased/plugin = 34/6 @nbins=10, 35/5 @15, 36/4 @20 (majority at EVERY bin count); Murphy-identity error 0.0; bench `metrics/_benchmarks/bench_brier_decomp_debiased.py`; tests `tests/metrics/test_biz_val_calibration_brier_decomp_debiased.py` | RESOLVED (flip default) | 49b92537 |
| qual-3 | Miller-Madow correction on the Entropy-Based Calibration Index (ECI = log(bins) - H(binned PIT)) in `calibration/quality.py:entropy_calibration_index` (`miller_madow=True` default; plug-in kept as `miller_madow=False`). DIFFERENT file/statistic from qual-1/2 (PIT-entropy, not ECE/Brier kernels): plug-in Shannon entropy H_hat=-sum p_i log p_i is NEGATIVELY biased ~-(K-1)/(2N), so ECI is POSITIVELY biased above the true 0. MM adds (K_obs-1)/(2N) (K_obs=non-empty bins), cancelling the leading 1/N term | uniform PIT (perfectly-calibrated, true ECI=0) x 7 seeds x bins {10,20}; honest metric = \|ECI - 0\| | per-cell \|ECI\| wins MM/plugin = 6/1 @bins=10, 6/1 @bins=20 (12/14 total, majority at BOTH scenarios); mean \|ECI\| plugin 0.0132 -> MM 0.0039 (~3.4x closer to true 0); bench `calibration/_benchmarks/bench_eci_miller_madow.py`; tests `tests/calibration/test_biz_val_calibration_eci_miller_madow.py` | RESOLVED (flip default) | 9e9170e7 |
| qual-4 | Equal-MASS (quantile) vs equal-WIDTH binning grid for the headline DEBIASED binned ECE (`compute_ece_debiased`, already shipped with qual-1 Bernoulli noise-floor subtraction). DIFFERENT angle from qual-1/2/3 (binning SCHEME, not a noise-floor / MM debias): equal-width over [min,max] dumps mass into 1-2 bins when preds are concentrated; literature (Nixon 2019 ACE, Roelofs 2022) says equal-mass lowers bias+variance. Tested keeping the SAME Bernoulli debiasing, only switching the grid | 4 perfectly-calibrated (uniform / beta_rare / beta_mid / bimodal, true ECE=0) + 2 miscalibrated (overconf / shift) x 8 seeds x nbins {10,15,20} x n {500,2000,5000}; honest metric = \|estimate - 0\| on calibrated, flag-retention on miscal | per-cell \|ECE\| wins equal-mass/total = 49/96 @n=500 (51%), 44/96 @n=2000 (46%), 45/96 @n=5000 (47%) -- NO scenario-majority at ANY n; equal-mass helps beta_rare but LOSES all 3 bimodal cells @n=500 and flips beta_mid->width @n>=2000. The qual-1 noise-floor subtraction already removes most of the equal-width sparsity penalty, leaving the residual grid choice scenario-dependent + n-reversing. Miscal scenarios stay flagged either way. Bench `calibration/_benchmarks/bench_ece_debiased_equal_mass.py` | REJECTED (no flip; equal-width debiased ECE stays default) | ebd90ef2 |
| qual-5 | BCa (bias-corrected + accelerated) vs percentile bootstrap CI as the default `method` in `mlframe.evaluation.bootstrap.bootstrap_metric` / `bootstrap_metrics` (`method="bca"` default; percentile kept as `method="percentile"`). FRESH area (CI-coverage / uncertainty, NOT calibration ECE/Brier/ECI of qual-1..4; low-contention -- only the perf-loop touched this file). The percentile interval ignores skew + median-bias of the sampling distribution and UNDER-COVERS on skewed/bounded metrics (AUC near 1.0, Pearson r); BCa shifts the cut-points via z0=Phi^-1(#{resample<point}/B) + jackknife acceleration a, falling back to percentile when degenerate | 2 scenarios x 5 seeds in the skewed regime where the percentile deficiency bites: AUC~0.97 @ n=150 + corr rho=0.90 @ n=40; honest metric = empirical Monte-Carlo coverage of a nominal-95% CI over 600 trials/cell, scored as \|coverage - 0.95\| | BCa closer-to-nominal in 10/10 (scenario x seed) cells (majority at BOTH scenarios, all seeds); AUC mean coverage pct=0.919 (gap 0.031) -> bca=0.948 (gap 0.002); corr mean coverage pct=0.930 -> bca=0.938; bench `evaluation/_benchmarks/bench_bootstrap_ci_coverage.py`; tests `tests/evaluation/test_bootstrap.py` (default + degenerate fallback + parity) + `tests/evaluation/test_biz_val_evaluation_bootstrap.py` (coverage-gap win) | RESOLVED (flip default) | <pending> |

Streak: 3 consecutive RESOLVED (qual-1, qual-2, qual-3), then qual-4 REJECT, then qual-5 RESOLVED. Cumulative: 4 RESOLVED / 1 REJECT across the accuracy/quality loop.
