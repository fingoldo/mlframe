{
  "src/mlframe/feature_engineering/numerical.py::# num_zerocross(arr),": "commented-out code 44 days old - `# num_zerocross(arr),`. Do it, delete it, or reference the issue that tracks it.",
  "src/mlframe/feature_selection/_benchmarks/gen_default_tuning.py::# tune_spec(skip_existing=...) is a no-op if already tuned (unless force);": "commented-out code 39 days old - `# tune_spec(skip_existing=...) is a no-op if already tuned (unless force);`. Do it, delete it, or reference the issue that tracks it.",
  "src/mlframe/feature_selection/boruta_shap/_fit_explain.py::# TODO(perf, 100GB-class frames): this IS a real deep copy (ordinal-encoding + column drops mutate": "TODO 44 days old - `# TODO(perf, 100GB-class frames): this IS a real deep copy (ordinal-encoding + column drops mutate`. Do it, delete it, or reference the issue that tracks it.",
  "src/mlframe/feature_selection/filters/_confirm_predictor_context.py::# representationally (a shallow downstream can use x1**2-1 but not raw x1),": "commented-out code 42 days old - `# representationally (a shallow downstream can use x1**2-1 but not raw x1),`. Do it, delete it, or reference the issue that tracks it.",
  "src/mlframe/feature_selection/filters/_gpu_resident_select_kernels.py::# cp.ascontiguousarray(disc.T) on any failure. The char body is dtype-agnostic for any 1-byte code (": "commented-out code 48 days old - `# cp.ascontiguousarray(disc.T) on any failure. The char body is dtype-agnostic for any 1-byte code (`. Do it, delete it, or reference the issue that tracks it.",
  "src/mlframe/feature_selection/filters/_hermite_fe_mi.py::# cp.where(hxyc>0, hxyc, 1.0) feeds the log so log(0) never fires (matches the safe_xyc guard);": "commented-out code 41 days old - `# cp.where(hxyc>0, hxyc, 1.0) feeds the log so log(0) never fires (matches the safe_xyc guard);`. Do it, delete it, or reference the issue that tracks it.",
  "src/mlframe/feature_selection/filters/_mrmr_fe_step/_step_score.py::#       clean (c,d) survivor, so it is KEPT (same tell-2/tell-3 logic as the composite prune);": "commented-out code 73 days old - `#       clean (c,d) survivor, so it is KEPT (same tell-2/tell-3 logic as the composite prune);`. Do it, delete it, or reference the issue that tracks it.",
  "src/mlframe/feature_selection/filters/_orthogonal_meta_scorer_fe.py::#     dependence (e.g. y = sigmoid(x), heavy-tailed monotone signal);": "commented-out code 95 days old - `#     dependence (e.g. y = sigmoid(x), heavy-tailed monotone signal);`. Do it, delete it, or reference the issue that tracks it.",
  "src/mlframe/feature_selection/filters/info_theory/_batch_kernels.py::# this (n, K) buffer vs the old int32 default for EVERY caller (not just those that opted into int8)": "commented-out code 76 days old - `# this (n, K) buffer vs the old int32 default for EVERY caller (not just those that opted into int8)`. Do it, delete it, or reference the issue that tracks it.",
  "src/mlframe/feature_selection/filters/mrmr/_mrmr_class.py::# usability (the SECOND SLOPE": "commented-out code 47 days old - `# usability (the SECOND SLOPE ``[1, x, relu(x-tau)]`` hands a linear model),`. Do it, delete it, or reference the issue that tracks it.",
  "src/mlframe/feature_selection/shap_proxied_fs/_shap_proxy_prefilter_univariate.py::# check_X_y(..., dtype=np.float64). Unlike f_classif (which honours float32 input),": "commented-out code 97 days old - `# check_X_y(..., dtype=np.float64). Unlike f_classif (which honours float32 input),`. Do it, delete it, or reference the issue that tracks it.",
  "src/mlframe/metrics/quantile.py::# O(K^2), but K is the number of predicted quantile levels in a PIT diagram (typically <=20, often <": "commented-out code 73 days old - `# O(K^2), but K is the number of predicted quantile levels in a PIT diagram (typically <=20, often <`. Do it, delete it, or reference the issue that tracks it.",
  "src/mlframe/training/_training_loop_objectives.py::# natively (sklearn quirk: multi-output ridge regression internally),": "commented-out code 81 days old - `# natively (sklearn quirk: multi-output ridge regression internally),`. Do it, delete it, or reference the issue that tracks it.",
  "src/mlframe/training/baselines/_dummy_baseline_regression.py::# targets (lag1_corr ~ 0.999 within groups),": "commented-out code 101 days old - `# targets (lag1_corr ~ 0.999 within groups),`. Do it, delete it, or reference the issue that tracks it.",
  "src/mlframe/training/callbacks/_callbacks.py::# TODO(train+val refit): when best_iter >= max_iter-1, val was effectively a": "TODO 44 days old - `# TODO(train+val refit): when best_iter >= max_iter-1, val was effectively a`. Do it, delete it, or reference the issue that tracks it.",
  "src/mlframe/training/composite/estimator/_estimator.py::# transform.fit() always sees domain-filter-COMPACTED y_train/base_train (unlike forward(),": "commented-out code 44 days old - `# transform.fit() always sees domain-filter-COMPACTED y_train/base_train (unlike forward(),`. Do it, delete it, or reference the issue that tracks it.",
  "src/mlframe/training/core/_phase_temporal_audit.py::# behavior_config.target_temporal_audit_unit ('s' / 'ms' / 'us' / 'ns');": "commented-out code 106 days old - `# behavior_config.target_temporal_audit_unit ('s' / 'ms' / 'us' / 'ns');`. Do it, delete it, or reference the issue that tracks it.",
  "src/mlframe/training/feature_handling/text_encoder.py::# TODO 2026-05-21: when polars-ds ships native TF-IDF / hashing,": "TODO 105 days old - `# TODO 2026-05-21: when polars-ds ships native TF-IDF / hashing,`. Do it, delete it, or reference the issue that tracks it.",
  "src/mlframe/training/pipeline/_pipeline_helpers.py::# result (e.g. SelectorMixin returning": "commented-out code 41 days old - `# result (e.g. SelectorMixin returning ``X.iloc[:, support_]``);`. Do it, delete it, or reference the issue that tracks it.",
  "src/mlframe/training/targets/regression_residual_audit.py::# threshold (per feedback_use_kernel_tuning_cache_for_gpu, KTC is for GPU);": "commented-out code 41 days old - `# threshold (per feedback_use_kernel_tuning_cache_for_gpu, KTC is for GPU);`. Do it, delete it, or reference the issue that tracks it."
}
