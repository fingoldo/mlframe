# Composite Targets audit 2026-06-10 - Performance & Memory dimension - dispositions

Triage of every finding in `findings_perf.md`. IDs P1-P8 are in the orchestrator's
already-fixed-and-committed list (verified in current code: P1 gating at `_eval.py:162-175`,
P3 `_per_feat_y_full` decomposition at `_fit.py:350-393`, P4 inner-n_jobs cap at
`_tiny_rerank.py:311-324`, P5 build-once+np.delete at `_tiny_rerank.py:171-194`, etc.).
P9-P19 triaged below against current source.

Counts: ALREADY_RESOLVED=8, RESOLVE_NOW=5, FUTURE=6, DOC=0, REJECTED=0.

| id | severity | disposition | file:line | reason | patch-spec-if-RESOLVE_NOW |
|---|---|---|---|---|---|
| P1 | P1 | ALREADY_RESOLVED | discovery/_eval.py:162-175 | Dead x_screen_valid copy now gated on `_need_x_screen_valid`; prebinned slice gated on `valid_screen.all()`. | |
| P2 | P1 | ALREADY_RESOLVED | discovery/screening.py:313-332 | `finite.all()` / `col_valid.all()` no-copy gates landed. | |
| P3 | P1 | ALREADY_RESOLVED | discovery/_fit.py:350-393 | `_mi_per_feature_prebinned` + `_aggregate_mi_per_feature` decomposition + np.delete removal landed. | |
| P4 | P1 | ALREADY_RESOLVED | discovery/_tiny_rerank.py:311-324 | `_rerank_inner_n_jobs = max(1, cpu//rerank_n_jobs)` threaded to `_build_tiny_model`. | |
| P5 | P1 | ALREADY_RESOLVED | discovery/_tiny_rerank.py:171-194 | build `_x_full` once then per-base np.delete; raw baseline reuses x_full. | |
| P6 | P1 | ALREADY_RESOLVED | discovery/screening.py:43-69 | `_extract_column_array` now takes `rows=` and gathers O(rows). | |
| P7 | P1 | ALREADY_RESOLVED | discovery/_filter.py / screening.py | `_safe_abs_corr_all` allocation fix committed. | |
| P8 | P1 | ALREADY_RESOLVED | cache.py:150-158 | `_row_order_fingerprint` slices before hash_rows. | |
| P9 | P2 | FUTURE | discovery/_screening_tiny.py:623-629 | Per-seed redundant domain_check+forward+`x_train_matrix[valid]` copy is real, but the fix is a prepare()/run(seed) refactor of a hot multi-callsite fn and needs measurement. The standalone `valid.all()` no-copy gate (mirror raw-y lines 243-249) is bit-identical but addresses only one of the three copies and not the per-seed re-forward. | |
| P10 | P2 | FUTURE | discovery/_tiny_rerank.py:521-549 | Per-base raw per-bin refit is bin_var-independent, but `per_bin_n_bins=0` (OFF by default) and the fix needs a `_tiny_cv_rmse_raw_y(bin_vars=dict)` contract change + per-fold-prediction plumbing. Out of scope for a safe quick pass. | |
| P11 | P2 | FUTURE | discovery/_screening_tiny.py:594,767-774 + _tiny_rerank.py:442-560 | `early_stop_threshold` is genuinely dead-wired (always inf; no caller passes it). Current behavior is correct, just misses the 30-66% serial-fold saving. Activating it requires reordering `_tiny_model_rerank` to compute the raw baseline before the per-spec loop + a regression test asserting fewer folds on a hopeless spec - a restructure, not a micro-guard. | |
| P12 | P2 | RESOLVE_NOW | discovery/_eval.py:237 | `x_boot = x_screen_valid[idx_b]` runs every bootstrap replicate but is read only on the non-prebinned `else` branch; on the default prebinned path it is a dead full-matrix gather. Bootstrap OFF by default (P2). Bit-identical move. | Delete line 237 `x_boot = x_screen_valid[idx_b]`; insert it as the first statement inside the `else:` block at line 249 (before `mi_t_b = _mi_to_target(x_boot, t_boot, ...)`), e.g. `else:\n    x_boot = x_screen_valid[idx_b]\n    mi_t_b = ...`. |
| P13 | P2 | FUTURE | discovery/_eval.py:113-118 | Residual-std probe at full-train scale (2 float64 gathers + transform.forward over all train rows) per work item. Moving it to the screen sample removes the heavy forward but CHANGES the rows the std-ratio is measured on (sample vs full train) -> not bit-identical, needs measurement to confirm the 0.001-threshold gate decision is unchanged. The bit-identical sub-fix (hoist the line-56 `y_train[valid]`/`base_train[valid]` gathers and reuse at 114-115) only saves two gathers, not the dominant forward. | |
| P14 | P2 | RESOLVE_NOW | discovery/_fit.py:535,644 | Alpha-drift loop (535) and alpha~1-collapse loop (644) re-`_extract_column_array(df, s.base_column)` full columns, but `self._auto_base_pool[s.base_column]` (set at 367 = `base_full[train_idx]`) already holds them; `idx1/idx2` are `train_idx[:half]`/`train_idx[half:]`, so the three drift gathers are `pool[:half]`/`pool[half:]`/`pool`. Bit-identical (pool is the same float32 values). | Drift loop: replace `base_full = _extract_column_array(df, s.base_column)` (535) with `base_full = self._auto_base_pool.get(s.base_column)` and `if base_full is None: base_full = _extract_column_array(df, s.base_column)`; when from pool, `base_full[idx1]->base_full[:half]`, `base_full[idx2]->base_full[half:]`, `base_full[train_idx]->base_full`. Simpler: keep the extract fallback path unchanged and add a pooled fast-path `bt = self._auto_base_pool.get(s.base_column)` then use `bt[:half], bt[half:], bt` (set `base_t=bt`). Collapse loop (644): `base_train = self._auto_base_pool.get(s.base_column); base_train = _extract_column_array(df, s.base_column)[train_idx] if base_train is None else base_train`. |
| P15 | P2 | FUTURE | discovery/screening.py:294,296 + _auto_base.py:403-422 | int64->int16 prebin codes is bit-identical on MI values (with int32/int64 upcast inside `_mi_from_binned_pair` combo) but spans 3 sites and the finding itself requires a gather/bincount benchmark to confirm the cache-effect speedup. Needs measurement. | |
| P16 | P2 | RESOLVE_NOW | ensemble/_oof_split.py:83,89 | Two `np.unique(g, ...)` calls on the identical array; the second (89) discards uniq+first_idx and keeps only counts_orig, all in the SAME sorted order. Merge into one call. Bit-identical. (Finding cited `ensemble/__init__.py:266` but the fn is `_carve_inner_eval_split` in `_oof_split.py`.) | Line 83: change to `uniq, first_idx, counts_orig = np.unique(g, return_index=True, return_counts=True)`; delete line 89 (`_, _, counts_orig = np.unique(...)`). |
| P17 | LOW | RESOLVE_NOW | discovery/_fit.py:290 | `y_train_for_strat = y_full[train_idx]` duplicates `y_train = y_full[train_idx]` already computed at line 221. One train-sized float32 copy per fit wasted. Used only at line 294 (`y=y_train_for_strat`). Bit-identical. | Delete line 290 (`y_train_for_strat = y_full[train_idx]`); at line 294 change `y=y_train_for_strat` to `y=y_train`. |
| P18 | LOW | FUTURE | discovery/_eval.py:181-204 | Shrunk-domain `mi_y_compare` recomputed per transform sharing an identical valid_screen mask. A `(base, hash(valid_screen.tobytes()))` memo is a real optimization but needs thread-safe population (rerank runs threaded) and measurement of the ~0.2-0.5s/mask saving. Out of scope for a safe quick pass. | |
| P19 | LOW | RESOLVE_NOW | discovery/screening.py:303 | `np.nanquantile(col, q_edges)` per column even when the column is all-finite (`col_finite` already computed at 299, guaranteed `>=5*nbins` at 300). Gate to `np.quantile` on the finite subset only when `col_finite.all()` to stay bit-identical (nanquantile drops NaN but NOT inf, so `np.quantile(col, ...)` on a column with inf is identical to nanquantile; gate on `.all()` keeps both paths exact). | Line 303: `cut = np.quantile(col, q_edges) if col_finite.all() else np.nanquantile(col, q_edges)`. (Using `np.quantile(col, ...)` not `col[col_finite]` preserves the current inf-in-edges behavior bit-for-bit; only the all-finite fast path is taken when safe.) |
