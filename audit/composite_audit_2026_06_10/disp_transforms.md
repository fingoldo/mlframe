# Composite Targets audit -- Transform dimension dispositions (2026-06-10)

Dimension: `findings_transforms.md` (T1-T25). Every finding verified against
CURRENT code (full read of simple/linear/nonlinear/unary/extended/registry/
naming/__init__ + discovery `_eval.py`/`_fit.py`, estimator `_estimator.py`,
`_screening_tiny.py`, test file). T1/T2/T3 confirmed already-fixed in-tree
(matches the pre-supplied resolved list).

Counts: ALREADY_RESOLVED 3 | RESOLVE_NOW 6 | DOC 5 | FUTURE 11 | REJECTED 0.

| id | severity | disposition | file:line | reason | patch-spec (RESOLVE_NOW only) |
|---|---|---|---|---|---|
| T1 | P1 | ALREADY_RESOLVED | transforms/unary.py:44,91-101,187,191,219,224 | `_YJ_INV_BASE_FLOOR=1e-12` floor applied in all three YJ-inverse paths (numpy/numba/scalar); asymptote NaN can no longer reach predict / `_screening_tiny.py:682` inverse. | |
| T2 | P1 | ALREADY_RESOLVED | transforms/linear.py:339-341 | cond-number now computed on unit-L2-normalised centered columns (`base_scaled = base_centered/col_norms`) -> BKW scaled condition index, unit-invariant. | |
| T3 | P1 | ALREADY_RESOLVED | transforms/extended.py:368-373 | `s = m * var_noise` with Rice/von-Neumann lag-1 noise-variance estimator replaces the old `m*std(signal)`. (NOTE: registry.py:621 description still reads `s = n_unique * std(y) * 1.0` -- stale doc, fold into T3 cleanup.) | |
| T4 | P2 | RESOLVE_NOW | transforms/simple.py:317-318 | `_ratio_inverse` multiplies by RAW `base` while `_ratio_forward`:313 floors with `safe_base`; in-domain rows with `0<|base|<eps` break round-trip. Mirroring the floor is bit-identical except on near-zero rows (already pathological). | In `_ratio_inverse`, replace `return t_hat * base` with the same floor as forward: `eps = float(params["eps"]); safe_base = np.where(np.abs(base) < eps, np.sign(base + 1e-300) * eps, base); return t_hat * safe_base`. Add eps-edge round-trip test. |
| T5 | P2 | RESOLVE_NOW | transforms/extended.py:154-158 | `_centered_ratio_inverse` multiplies by RAW `(base+c)` while `_centered_ratio_forward`:150 floors `shifted`; same eps-asymmetry round-trip break. (Sign-flip-below-train-envelope half = params-aware domain, deferred to T15.) | In `_centered_ratio_inverse`, after `c=float(params["c"])` add `eps=float(params["eps"]); shifted=np.asarray(base,dtype=np.float64)+c; safe=np.where(np.abs(shifted)<eps, np.sign(shifted+1e-300)*eps, shifted); return np.asarray(t_hat,dtype=np.float64)*safe`. Add eps-edge round-trip test. Sign-flip remains (see T15). |
| T6 | P2 | RESOLVE_NOW | transforms/simple.py:369-376 | `_rolling_quantile_ratio_inverse` multiplies by RAW `roll_med` while forward:365 floors with `safe`; third instance of T4 class. | In `_rolling_quantile_ratio_inverse`, after computing `roll_med`, add `eps=float(params["eps"]); safe=np.where(np.abs(roll_med)<eps, np.sign(roll_med+1e-300)*eps, roll_med); return np.asarray(t_hat,dtype=np.float64)*safe`. Add eps-edge round-trip test. |
| T7 | P2 | FUTURE | transforms/nonlinear.py:174-183 | JS sigma2 proxy drops the `Var(base_g)` term (slope-variance is `sigma2/(n_g*Var(base))`), making shrinkage unit-dependent. Real, but changing the proxy shifts which groups shrink -> needs a biz_value/regression measurement, not a blind edit. | |
| T8 | P2 | FUTURE | transforms/linear.py:525-530 | After shrinking `alpha_g`, `beta_g` is not re-centred, so per-group residual mean drifts from 0. Fix changes residualisation numerics -> needs the "residual mean ~=0 after shrink" biz_value test to pin it. | |
| T9 | P2 | RESOLVE_NOW | transforms/nonlinear.py:426-434,442-450 | Both degenerate early-returns of `_monotonic_residual_fit` omit `is_degenerate`/`var_explained`, so the MOST degenerate fits bypass the `discovery/_eval.py:66` degeneracy gate they exist for. | Add `"is_degenerate": True, "var_explained": 0.0` to BOTH early-return dicts (the `finite.sum()<n_knots*2` branch and the `knots_x.size<3` branch). Pure additive keys; forward/inverse unaffected. Add a test that `_monotonic_residual_fit(y, ones(100))` returns `is_degenerate=True`. |
| T10 | P2 | FUTURE | transforms/nonlinear.py:417-421 | `n_unique//200` auto-knot cap conflates cardinality with discreteness -> continuous bases on small/mid n underfit. Re-tuning the divisor/predicate alters fitted splines on real configs -> threshold change needs a benchmark. | |
| T11 | P2 | FUTURE | transforms/nonlinear.py:822-834 | `_rolling_median` bottleneck path diverges from pandas fallback (even-k off-by-one at every position; odd-k tail constant-filled). Fixing the shift + truncated-centred tail medians changes T-values and needs a backend-parity test across even/odd k + boundaries -- not a one-line safe edit. Docstring claim "consistent with min_periods=1 semantics" (line 822) is false and should be corrected in the same change. | |
| T12 | P2 | DOC | transforms/registry.py:372 (desc); window math nonlinear.py:804-839 | Centred rolling window reads FUTURE base rows -> look-ahead in time-ordered deployment. Transform is already gated out of default discovery for ordering reasons; the trailing-mode (`mode="trailing"`) extension is FUTURE, but the immediately-safe step is a loud look-ahead caveat in the registry description. | |
| T13 | P2 | FUTURE | estimator/_estimator.py:458-459 | Domain-filter compacts the row sequence before time-recurrent forwards (ewma/rolling/frac_diff), so train T near gaps differs from predict-time T. Fix needs a TAG_SEQUENTIAL tag + full-length NaN-aware forward then mask -- architectural. | |
| T14 | P2 | RESOLVE_NOW | transforms/registry.py:376-386; nonlinear.py:892-900 | `frac_diff` is y-only but registered with default `requires_base=True`: discovery emits one IDENTICAL spec per base (misnamed y-fdiff-lag1/lag2/...) and drops rows on non-finite UNUSED base. | (a) Add `requires_base=False` to the `frac_diff` registry `Transform(...)`. (b) In `_frac_diff_domain`, when `y is not None` ignore base: `return np.isfinite(np.asarray(y,dtype=np.float64).reshape(-1))` (keep `base_ok` only for the `y is None` predict-side call). Add a test that two bases yield ONE deduped frac_diff candidate. |
| T15 | P2 | FUTURE | registry.py:182; unary.py:154-159; discovery/_eval.py:44 | Fitted-params-dependent domains unenforceable: pre-fit gate can't see `offset`/`c`, so log_y screen rows below `-offset` NaN, and T5's `base+c<=0` rows pass as valid. Fix = new `domain_check_fitted(y,base,params)` hook on the Transform contract + wiring in screening/eval -- contract extension. | |
| T16 | LOW | RESOLVE_NOW | transforms/unary.py:233-234 | `_yj_forward` dispatch docstring claims "both paths produce bit-identical output", but the forward numba kernel is `fastmath=True` (max diff ~1e-13 on n=50k per the finding). Docstring contradicts code. | Change the closing sentence of `_yj_forward`'s docstring from "both paths produce bit-identical output" to "the numba path matches the numpy reference within ~1e-13 (fastmath=True)". (Inverse kernel is fastmath=False and stays genuinely bit-identical -- leave `_yj_inverse` docstring.) Optionally pin a `max|numba-numpy| < 1e-12` tolerance test. |
| T17 | LOW | RESOLVE_NOW | transforms/linear.py:46-78,87-90 | Dead duplicate `_ewma_kernel` + `_frac_diff_inverse_kernel` and `_MAD_FLOOR_FRAC`/`_MAD_SOFT_CAP_K` in linear.py; live kernels are in nonlinear.py and live constants lazy-imported from the parent (linear.py:101). Verified NOT re-exported (the `from .linear import` lists in __init__.py:245 and registry.py:55 omit them). Drift risk. | Delete linear.py lines 44-78 (the whole `if _HAS_NUMBA:`/`else:` duplicate-kernel block) and the duplicate constants at linear.py:87-90; keep the single definitions in nonlinear.py / __init__.py. Grep-confirm no importer first (done). Pure dead-code removal, no behaviour change. |
| T18 | LOW | DOC | transforms/simple.py:87,170,249,290,341; nonlinear.py:287,402,560,851 | The `_finite_mask` kwarg's comments advertise "the outer Transform.fit dispatcher precomputes one isfinite pass" but NO call site passes it (`discovery/_eval.py:56` calls `transform.fit(y_train[valid], base_train[valid])` plain). Threading it is a (measurement-gated) perf win = FUTURE; the immediately-honest fix is to correct the comments to say the optimisation is not yet wired (don't claim infrastructure that doesn't exist). | |
| T19 | LOW | DOC | transforms/__init__.py:109 | Transform contract says fitted params "dict is JSON-serialisable" but median/quantile/monotonic/rank/smoothing_spline/quantile_normal return ndarray values (frac_diff already `.tolist()`s its weights). `json.dumps` crashes. Amend the contract docstring to "values may be ndarrays; serialise via the spec serialiser" rather than mass-`.tolist()`. | |
| T20 | LOW | FUTURE | transforms/naming.py:91-117 | `is_composite_target_name` substring match (`-diff-`,`-ratio-`,`-spline-`,...) false-positives plausible user columns (`price-diff-7d`, `debt-ratio-q`). Strict parsing must stay robust to both short-alias and legacy `__`-format names -> behaviour change with pickle-compat surface, not a trivial safe edit. | |
| T21 | LOW | FUTURE | discovery/_eval.py:288; _fit.py:441 | Unary (`requires_base=False`) specs are named with the spurious current-loop base (`y-cbrtY-lag1`); misleading + makes dedup keys / report labels base-order-dependent. Fix (sentinel base + 2-segment `is_composite_target_name` form) changes emitted spec names -> touches dedup keys, report labels, possibly v1 pickles -> needs care. | |
| T22 | LOW | RESOLVE_NOW | transforms/simple.py:164-166; nonlinear.py:276-280 | `except Exception: pass` around the v2 pandas-groupby fast paths silently reroutes to v1 forever on a real v2 defect -- the exact silent-error-swallowing class the project tracks. | At both sites, before the silent fallback, log once: `logger.warning("composite_transforms: pandas-groupby fast path failed (%s); using numpy fallback.", _exc)` -- capture the exception (`except Exception as _exc:`), keep the fallback. No behaviour change beyond the log line. |
| T23 | LOW | DOC | transforms/linear.py:120-130; registry.py:269 | logratio soft-cap makes the inverse inexact for in-domain train rows beyond 10*MAD (`|T-med|>10*MAD`). Deliberate safety tradeoff but the registry description doesn't state the round-trip consequence and no test pins the capped branch. Add one sentence to the description (DOC); the two-sided test is a nice-to-have. | |
| T24 | LOW | FUTURE | transforms/nonlinear.py:558-578,785-796 | EWMA/frac-diff inverse cold-starts every predict batch from the train-mean anchor, biasing the first ~k rows where recency matters. Statelessness is deliberate; warm_start/base_history is a new feature -> FUTURE. | |
| T25 | LOW | FUTURE | tests/training/composite/test_biz_val_composite_transforms.py | Adversarial round-trip/parity coverage missing for the whole bug class. The RESOLVE_NOW items (T4/T5/T6/T9/T14) MUST ship their own regression tests in the same change; the remaining adversarial suite (YJ-asymptote, bottleneck parity for T11, grouped residual-mean for T8, mixed-scale non-fallback) is broader than the safe pass -> FUTURE. | |
