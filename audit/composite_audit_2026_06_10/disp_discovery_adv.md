# Disposition table — discovery_adv dimension (A1-A34)

Audit: composite_audit_2026_06_10. Dimension: tiny-CV rerank / stacked / stepwise /
bayesian / auto-detect. Every finding verified against current code (2026-06-11).

ALREADY_RESOLVED (per integrator list, bare-id match): A2, A3, A8, A9.

| id | severity | disposition | file:line | reason | patch-spec (RESOLVE_NOW only) |
|---|---|---|---|---|---|
| A1 | P1 | REJECTED | _tiny_rerank.py:316-324 | Already guarded: `_rerank_inner_n_jobs = max(1, cpu//_rerank_n_jobs)` is threaded into `_build_tiny_model(inner_n_jobs=)`; the 256-thread oversubscription path no longer exists. Raw-baseline multiseed runs serially after the parallel loop (n_jobs default 1), no oversubscription. | — |
| A2 | P1 | ALREADY_RESOLVED | _tiny_rerank.py:664-692 | Min-seeds Wilcoxon guard present (`_min_seeds_wilcoxon = ceil(log2(1/alpha))`, skip+warn below it). | — |
| A3 | P1 | ALREADY_RESOLVED | _screening_tiny.py:418-427,486-491 | Seed-invariant splitter short-circuit to 1 repeat present in both multiseed wrappers. | — |
| A4 | P1 | FUTURE | _tiny_rerank.py:470-517 | Global `_any_base_monotone` flag still gates raw baseline fold-scheme; real cross-scheme mismatch. Fix = per-fold-scheme raw baseline (shuffled + TSS, cached, ~2x); needs restructure + regression test. | — |
| A5 | P1 | FUTURE | _screening_tiny.py:623-628 + _tiny_rerank.py | Composite CV scores only domain-valid rows; raw/sibling score different row populations. Fix = emulate production median fallback on domain-invalid val rows (new fold-scoring logic) — non-trivial, changes numerics, needs test. | — |
| A6 | P1 | FUTURE | _stacked.py:103,121-126 + core/_misc_helpers.py:172-173 | `_oof_*` pass-2 specs reference columns living only in local `df_aug`; `_build_full_column_from_splits` silently returns all-NaN at suite training -> garbage composite trained. Opt-in (`use_stacked_discovery`). Real fix = persist OOF recipe + rebuild before training (architectural). Cheap interim: WARN when `out` is all-NaN. | — |
| A7 | P2 | FUTURE | _stacked.py:289-308 + core/_phase_composite_discovery.py:508-520 | Residual pass-2 specs auto-merged into `specs_` and trained against RAW y with residual-fitted params; `discovered_on_residual` (object.__setattr__, non-field) read only in a test, not in suite routing. Opt-in (`use_stacked_discovery_residual`, default False). Real fix = residual-aware training path or don't merge into `specs_`. | — |
| A8 | P1 | ALREADY_RESOLVED | _stacked.py:69-88 | X_train slice hoisted above the per-spec loop. | — |
| A9 | P1 | ALREADY_RESOLVED | forward_stepwise.py / _fit.py | Finite-row masking for NaN lag bases in stepwise path (per integrator list). | — |
| A10 | P2 | FUTURE | _screening_tiny.py:258-259,638-639 | Silent GroupKFold->KFold fallback when `n_groups < cv_folds`, no log. Real fix = `GroupKFold(n_splits=min(cv_folds,n_groups))` for n_groups>=2 + WARN — changes fold construction (numeric), needs validation. Cheap interim: add the WARN only. | — |
| A11 | P2 | FUTURE | _screening_tiny.py:633-648 | Groups silently win over `time_aware`; `_tiny_cv_rmse_y_scale` lacks the `cv_splitter` escape hatch its raw sibling has. Real fix = grouped forward-chaining splitter (new) + `cv_splitter` param. Cheap interim: WARN once when both requested. | — |
| A12 | P2 | FUTURE | _screening_tiny.py:752-774 (callers in _tiny_rerank.py) | `early_stop_threshold` serial early-stop has no caller (always inf). Wiring needs raw-baseline threshold threaded into rerank multiseed calls + test; serial-path-only. Shipped-but-unwired class. | — |
| A13 | P2 | FUTURE | _screening_tiny.py:449-454,513-518 + _tiny_rerank.py:676-692 | Per-seed arrays drop failed seeds without identity -> paired Wilcoxon can mis-pair. Fix = fixed-length (n_seed_repeats) arrays with NaN for failed seeds + diff on jointly-finite positions (changes multiseed return contract + gate). Needs test. | — |
| A14 | P2 | RESOLVE_NOW | _tiny_rerank.py:530-546 | Raw per-bin baseline `_tiny_cv_rmse_raw_y` call omits `time_aware`, while the global raw baseline (lines 495/515) passes it -> inconsistent estimator on monotone bases. Default-off (`per_bin_n_bins=0`); bit-identical when off. Consistency fix, not a tuning choice. | In the `_tiny_cv_rmse_raw_y(...)` call at ~line 530 (inside `if per_bin_enabled:`), add `time_aware=_any_base_monotone,` to the kwargs (the flag is already in scope from line 470). |
| A15 | P2 | RESOLVE_NOW | _tiny_rerank.py:422-437 | ENS-P2-5 per-bin fallback recompute calls `_tiny_cv_rmse_y_scale` without `time_aware`/`groups`, so cached (TSS/GroupKFold) and recomputed (shuffled KFold) per-bin are different estimators. Default-off; bit-identical when off. | In the fallback `_tiny_cv_rmse_y_scale(...)` call (~line 422), add `time_aware=bool(_is_monotone_nondecreasing(base_screen)),` and `groups=_groups_screen,` (both in scope). |
| A16 | P2 | FUTURE | _tiny_rerank.py:218 + ensemble `_is_monotone_nondecreasing` | Misses descending time-like bases (needs row-reversal before TSS) and misfires on constant arrays. Descending handling is non-trivial; constant-exclusion alone is partial. | — |
| A17 | P2 | FUTURE | _stacked.py:98-102,240-244 + ensemble/feature_stacking.py | Stacked OOF always shuffled KFold; `time_aware`/`cv_splitter` never passed -> future-contaminated pass-2 bases/residual. Opt-in feature. Fix = thread time-awareness through both fit_stacked* + the OOF helper. | — |
| A18 | P2 | FUTURE | _stacked.py:232 | `ranked[: max(1, len(ranked))]` is a no-op slice; residual variant has no pass-1 cap (sibling caps at 3). Honest fix needs a new config field `stacked_max_pass1_specs_to_aggregate` (changing to a hardcoded 3 alters aggregation without a knob). | — |
| A19 | P2 | FUTURE | forward_stepwise.py:105-133 + _fit.py:742-752 | Forward-stepwise CV is group-blind even when discovery is group-aware -> per-group-constant bases show phantom gains. Fix = add `groups` param + GroupKFold path + pass `_group_ids_for_rerank`. New param + splitter path; needs test. | — |
| A20 | P2 | FUTURE | forward_stepwise.py:87-88,114 | Per-call float64 pool re-materialisation + per-trial `column_stack` churn. Perf; needs measurement + preallocated trial buffer refactor. | — |
| A21 | P2 | RESOLVE_NOW | bayesian.py:278-303 | `bayesian_alpha_fit_bootstrap(n_bootstrap=0)` -> `np.percentile(empty)` raises; no `ci_level` range check. Safe input validation. | After `n = y_f.size` (post the `n<4` block, ~line 278) add: `if n_bootstrap < 1: raise ValueError("bayesian_alpha_fit_bootstrap: n_bootstrap must be >= 1, got %d" % n_bootstrap)` and `if not (0.0 < ci_level < 1.0): raise ValueError("bayesian_alpha_fit_bootstrap: ci_level must be in (0,1), got %r" % ci_level)`. |
| A22 | P2 | RESOLVE_NOW | auto_detect.py:340-345 | Polars branch of `detect_cat_columns` omits the int-as-cat heuristic the pandas branch (348-359) and the sibling `detect_group_column_candidates` polars branch (190-207) both have. `detect_cat_columns` has no production consumer (export-only) -> no live blast radius. Parity fix. | Replace the polars `candidate_columns` comprehension (lines 341-345) with the same int-dtype block used in `detect_group_column_candidates` (lines 195-207): keep non-numeric cols, plus integer-dtype cols whose `df.get_column(c).drop_nulls().n_unique() <= max_unique`. Extend the FUw3a1 parity test to cover `detect_cat_columns`. |
| A23 | P2 | RESOLVE_NOW | auto_detect.py:215-219,352-359 | Pandas candidate filters exclude bool dtype (is_numeric True, is_integer False) while polars includes it; docstring (313-316) promises bool. Export-only func + group-detector parity. | Add `or pd.api.types.is_bool_dtype(df[c])` to the pandas candidate comprehensions in both `detect_group_column_candidates` (line 217-218) and `detect_cat_columns` (line 354-357). Parity test with a bool column on both flavours. |
| A24 | P2 | FUTURE | auto_detect.py:402-403 | `info_bonus = n_unique/(log1p(n_unique)+1)` is monotone-increasing, contradicting the comment ("rewards 10-100 over 2 or 500"); ID-like int columns rank first. Fix = unimodal bonus or sparsity penalty — needs a benchmark to set the peak/threshold + a biz_value test. | — |
| A25 | P2 | RESOLVE_NOW | auto_detect.py:125-134 | Strict `diffs>0`/`<0` rejects timestamp columns with duplicate ticks (normal in event data); the sibling `_is_monotone_nondecreasing` accepts ties. Export-only func. Accept weak monotonicity. | Change the asc test (line 125) to `if np.all(diffs >= 0) and np.any(diffs > 0):` and the desc test (line 130) to `elif np.all(diffs <= 0) and np.any(diffs < 0):` (excludes constant; keeps direction reporting). |
| A26 | P2 | RESOLVE_NOW | auto_detect.py:340-359 | Datetime/temporal columns pass the `detect_cat_columns` default filter (neither float nor numeric) -> target-encoding a date = time-leak trap. Export-only func. Exclude temporal dtypes from defaults; reachable via explicit `candidate_columns`. | In both default-candidate branches add a temporal-dtype exclusion: pandas `and not pd.api.types.is_datetime64_any_dtype(df[c])` (also exclude period/timedelta); polars exclude `Date/Datetime/Duration/Time` via `df.schema[c]`. Apply to `detect_cat_columns` (and ideally `detect_group_column_candidates`). |
| A27 | P2 | RESOLVE_NOW | _tiny_rerank.py:229-284,480-517 | `cv_selector_mode/alpha/confidence/quantile_level` honoured by forward stepwise (_fit.py:710-713) but the rerank multiseed calls never pass them -> always "mean". Default `cv_selector_mode="mean"` -> bit-identical at default; consistency fix. | Read the 4 fields once near `n_seed_repeats` (~line 197) via `getattr(self.config, "cv_selector_*", default)`, then add `cv_selector_mode=..., cv_selector_alpha=..., cv_selector_confidence=..., cv_selector_quantile_level=...` to all 4 rerank multiseed call sites (composite wilcoxon ~229, composite plain ~262, raw wilcoxon ~480, raw plain ~501). The multiseed wrappers forward via **kwargs to functions that already accept them. |
| A28 | P2 | RESOLVE_NOW | _screening_tiny.py:166-177 | CatBoost tiny model lacks `allow_writing_files=False`; parallel fold/spec fits race on `catboost_info/` (Windows file-lock -> silent NaN folds) and litter CWD. Bit-identical to model math. | In the `("cb","catboost")` branch add `allow_writing_files=False,` to the `kwargs` dict (line 168-174). |
| A29 | LOW | FUTURE | forward_stepwise.py:110-112 | Zero-base baseline sentinel is full-sample `np.std(y)`, not the CV-RMSE of the train-fold-mean predictor; inflates first-base gain under TSS on trending y. Only `seed_bases=None`. Changes the baseline value -> selection; needs measurement. | — |
| A30 | LOW | RESOLVE_NOW | _screening_tiny.py:621-625,630-631 (+240-241,250-251) | Early-NaN returns ignore `return_per_bin`, returning a bare `nan` where the contract (and the function's own tail) returns `(nan, full(n_bins,nan))`. Caller then unpacks a per-seed array as per-bin. Benign today but fragile; fix the contract on the early-exit lines. | In `_tiny_cv_rmse_y_scale`, change the 3 early `return float("nan")` (n<cv_folds*10, valid.sum<..., non-finite t_clean) to `return (float("nan"), np.full(n_bins, float("nan"))) if return_per_bin else float("nan")`. Mirror in `_tiny_cv_rmse_raw_y` lines 240-241/250-251. |
| A31 | LOW | DOC | bayesian.py:50-78 | Conjugate `bayesian_alpha_fit` lacks the "caller must filter to the valid domain" contract its bootstrap sibling documents (line 237); NaN y/base silently yields NaN posterior. | Add the contract line to the `bayesian_alpha_fit` docstring (mirror bootstrap's "Caller must filter to the valid domain."). |
| A32 | LOW | RESOLVE_NOW | auto_detect.py:205 | Polars `n_unique()` counts null as a distinct value; pandas `nunique()` excludes NaN -> off-by-one at the `max_unique` boundary in the WIRED `detect_group_column_candidates`. | Change line 205 `df.get_column(c).n_unique()` to `df.get_column(c).drop_nulls().n_unique()`. (Also use `.drop_nulls().n_unique()` in any A22 polars int-as-cat block.) |
| A33 | LOW | RESOLVE_NOW | forward_stepwise.py:58,87-88 | Docstring requires all candidate arrays to have `len(y_train)`; a mismatch surfaces later as an opaque `column_stack` ValueError. | After building `candidates` (line 88), add a validation loop: `for _n, _a in candidates.items():` `if _a.size != y.size: raise ValueError(f"forward_stepwise_multi_base: candidate '{_n}' has length {_a.size}, expected {y.size} (== len(y_train)).")`. |
| A34 | P2 | FUTURE | tests/training/test_residual_stacked_suite_integration.py | Test gap for stacked pass-2 suite TRAINING (would catch A6/A7) + wilcoxon-power floor (A2, now resolved) + multiseed-under-monotone variation (A3, now resolved). The pass-2 training tests can only land alongside the A6/A7 FUTURE fixes; the A2/A3 regression tests should land with confirmation of those resolved items. | — |

## Summary

- ALREADY_RESOLVED: 4 (A2, A3, A8, A9)
- RESOLVE_NOW: 12 (A14, A15, A21, A22, A23, A25, A26, A27, A28, A30, A32, A33)
- REJECTED: 1 (A1)
- DOC: 1 (A31)
- FUTURE: 16 (A4, A5, A6, A7, A10, A11, A12, A13, A16, A17, A18, A19, A20, A24, A29, A34)
- Total: 34
