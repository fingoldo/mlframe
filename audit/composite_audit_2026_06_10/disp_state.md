# Composite Targets audit — dimension: Cache / streaming / provenance / diagnostics — DISPOSITIONS

Date: 2026-06-11. All line numbers verified against current working tree before disposition.

| id | severity | disposition | file:line | reason | patch-spec-if-RESOLVE_NOW |
|----|----------|-------------|-----------|--------|---------------------------|
| S1 | P1 | ALREADY_RESOLVED | cache.py:362-363,419-423,164-175 | In the fixed-id list; verified fix present (polars `.to_list()` content hash at 376-377, pandas content hash at 442, `hash_pandas_object` fingerprint at 175-184). | — |
| S2 | P1 | ALREADY_RESOLVED | cache.py:156 | In the fixed-id list; verified fix present: `df.slice(0, n_take).hash_rows()` (slice-before-hash) at cache.py:162. | — |
| S3 | P2 | FUTURE | cache.py:159-184 (pandas), 150-164 (polars) | Real residual blind spot: polars prefix-only (no tail), pandas still `n_edge=8`. Widening invalidates the on-disk digest and needs a coverage/cost decision on edge size. Document as known sampling blind spot. | — |
| S4 | P2 | RESOLVE_NOW | cache.py:846-849 | Sidecar `.sha256` removal runs unconditionally even when value-file `os.remove` raised (Windows lock) → surviving entry permanently unverifiable. | Move the `try: os.remove(path + ".sha256") except OSError: pass` block (846-849) to run only on successful value removal: place it inside the `try` after `lru.pop(stem, None)` (line 843), before the `except OSError: pass` of the value remove. |
| S5 | P2 | RESOLVE_NOW | cache.py:738-742 | Persistent corrupt/tampered entry returns `default` silently every run → unbounded silent multi-minute recomputes, no operator signal (matches the oversize path that DOES warn at 711). | Split the except: keep `except FileNotFoundError: return default` silent; change the typed+blanket excepts to `except Exception as _e: logger.warning("DiscoveryCache: unreadable/unverifiable entry %s (%s: %s); treating as miss", path, type(_e).__name__, _e); return default`. |
| S6 | P2 | RESOLVE_NOW | cache.py:271-276 | pandas `_col_stats` int branch runs `np.unique(arr)` (full O(n log n) sort per int column) — the exact cost the polars branch deliberately dropped for `null=`. Digest-shape change accepted as for polars (one-time cache invalidation). | Replace the int-branch return body (273-276) with `f"intmin={int(np.min(arr))};intmax={int(np.max(arr))};null=0"` (numpy int/bool dtypes hold no NaN), mirroring the polars int digest at 417-419. |
| S7 | P2 | FUTURE | streaming.py:84-110 | Drift detector tests alpha only; a pure intercept (level-shift) drift never refits yet biases every prediction. Needs a beta/Chow test + a biz_value synthetic (pure-level-shift must refit) + measurement of the threshold. | — |
| S8 | P2 | FUTURE | streaming.py:76-105 | Refit uses the FULL rolling buffer (pre+post drift mixed) → lagged blend biased toward the dead regime. Needs change-point detection (two-segment SSE split) — an algorithmic change with measurement. | — |
| S9 | P2 | FUTURE | estimator/_update.py:54-64 | `extend(y_arr.tolist())` boxes + `np.asarray(deque)` unboxes the whole 10k buffer every update — O(buffer_n) Python work per call. Ring-buffer rewrite is a perf refactor needing a benchmark. | — |
| S10 | P2 | RESOLVE_NOW | provenance.py:168-179 (+50-78) | `to_audit_trail` prints legacy `target__transform__base` name that no longer matches `spec.name` (rendered at report 308) → two ids for one spec; no `extra_base_columns` field → multi-base provenance incomplete. | Add `name: str` and `extra_base_columns: tuple[str,...] = ()` to the dataclass (after line 59); set them in `from_spec` (`name=spec.name`, `extra_base_columns=tuple(spec.extra_base_columns)`); add both to `to_dict`; in `to_audit_trail` replace the f"'{target_col}__{transform_name}__{base_column}'" with `'{self.name}'`. |
| S11 | P2 | RESOLVE_NOW | provenance.py:219-226 | logratio inverse formula hardcodes `10*mad_eff` and says "softcap", but the implementation (transforms/linear.py:123-129) uses fitted `soft_cap_k` (default 10) with a hard `np.clip`. Wrong if k != 10; "softcap" misleads a reader reproducing the inverse. | In the logratio branch add `k = float(fitted_params.get("soft_cap_k", 10.0))` and render the inverse as `f"y_hat = {base_column} * exp(clip(T_hat, {median_t:.4g} +/- {k:.4g}*{mad_eff:.4g}))"`. |
| S12 | P2 | FUTURE | provenance.py:183-240 | Formula/description table covers 4 of ~36 registered transforms; rest render a generic stub. Right fix is a registry-driven formula field per transform (or extending the dict to the alpha/beta family + chains) — an extension, not a quick safe edit. | — |
| S13 | P2 | RESOLVE_NOW | provenance.py:249,303-306 | `report_to_markdown(random_state=42)` default fabricates a load-bearing "discovered using random_state=42" claim when the real seed differed (docs callers omit the kwarg) — same trust-failure class as a hallucinated citation. | Change `random_state: int = 42` (249) to `random_state: int | None = None`; in `to_audit_trail` render `f"random_state={'unspecified' if self.discovery_random_state is None else self.discovery_random_state}"`; relax `discovery_random_state` field type to `int | None`. |
| S14 | P2 | RESOLVE_NOW | diagnostics.py:49-50 | `if not matplotlib.get_backend():` is dead — `get_backend()` resolves the sentinel and never returns falsy → documented headless Agg-selection never runs; comment misquotes conftest (unconditional `use('Agg')`). | Replace the check with a sentinel read: `import matplotlib; _raw = dict.__getitem__(matplotlib.rcParams, "backend"); if isinstance(_raw, str) and _raw == matplotlib.rcsetup._auto_backend_sentinel: matplotlib.use("Agg")` — or, if the sentinel API is undesired, delete the dead branch and fix the comment to "caller owns backend selection (tests/training/conftest.py does an unconditional use('Agg'))". |
| S15 | P2 | RESOLVE_NOW | diagnostics.py:383 | `ax.boxplot(..., tick_labels=names)` needs matplotlib>=3.9 but the project pins >=3.7 → TypeError on 3.7/3.8; line 393 `set_xticklabels(names,...)` already labels version-independently. | Remove `tick_labels=names,` from the `ax.boxplot(...)` call at 383 (set_xticklabels at 393 already does it). |
| S16 | P2 | REJECTED | tests/training/composite/test_composite_cache_signature_audit.py | The test gap is already closed: this file adds same-process string/datetime stability (pandas+polars), cross-subprocess stability under PYTHONHASHSEED randomisation, and content-change detection — exactly what S16 asked for. Landed with the S1 fix. | — |
| S17 | LOW | RESOLVE_NOW | cache.py:519, 584-585/597-598 | Class docstring still says "Thread-safe for single-process use only; concurrent writers will race (caller's responsibility)" — contradicted by the filelock/atomic-replace machinery now shipped; and `_maybe_filelock` returns `nullcontext()` silently despite the comment promising a one-time WARN. | Rewrite the docstring line 519 to state actual guarantees (atomic os.replace writes, filelock-guarded LRU/eviction, idempotent invalidate; filelock optional). In `_maybe_filelock` add a module-level `_FILELOCK_WARNED` flag and `logger.warning(...)` once on the `except ImportError` branch before returning `nullcontext()` — or delete the "one-time WARN" claim from the 584-585 comment. |
| S18 | LOW | FUTURE | cache.py:823 vs 978; 855-974 | Eviction `total_bytes` sums only `*.pkl` while `_discovery_cache_bytes_total` counts `.pkl`+`.pkl.sha256` (cap vs footprint disagree); orphan `*.tmp` / `.lru.lock` never swept. Needs a reconcile decision + an age-threshold sweep. (Cheap sub-part: have clear() also rm `*.tmp` + `.lru.lock`.) | — |
| S19 | LOW | RESOLVE_NOW | provenance.py:104 | `composite_id` uses `json.dumps(..., default=str)`; array params (median_residual bin_edges, monotonic knots, spline coefs) stringify via numpy repr → truncate beyond printoptions threshold and obey global printoptions → id not stable / collides. | Change `default=str` (104) to `default=lambda o: o.tolist() if isinstance(o, np.ndarray) else str(o)` (np already imported). |
| S20 | LOW | RESOLVE_NOW | provenance.py:295-302 | Ensemble weight lookup `break`s at first `nm.startswith(spec.name + "#")` → reports one model's weight as the spec's, but components are `f"{spec_name}#{i}"` over multiple models (core/_phase_composite_post_xt_ensemble/__init__.py:206); actual mass is the SUM. `startswith` also prefix-collides. | Replace the loop body (295-302) to accumulate: `_w_sum = 0.0; _k = 0; for nm, w in zip(...): if nm.rsplit("#",1)[0] == spec.name: _w_sum += float(w); _k += 1` then `if _k: ensemble_w = _w_sum; ensemble_strat = ensemble_metadata.get("strategy")`. (Drop the `break`; exact rsplit match kills the `#`-in-column-name collision.) |
| S21 | LOW | RESOLVE_NOW | diagnostics.py:606-615, 1-29 | `__all__` exports only the deprecated alias `plot_mi_gain_with_ci`, not the canonical `plot_mi_gain_with_jitter` (257) → star-import users only reach it by the deprecated name; header docstring says "Four plot helpers" but `__all__` lists 8. | Add `"plot_mi_gain_with_jitter",` to `__all__` (606-615); change the docstring "Four plot helpers" (line 3) to "Eight plot helpers" and refresh the enumerated list to include qq/jitter/per-fold/per-family/alpha-stability/predictions-vs-actual. |
| S22 | LOW | RESOLVE_NOW | diagnostics.py:438,446-451 | Spearman via `np.argsort(np.argsort(...))` gives ties arbitrary distinct ranks (stability order) instead of average ranks → biased rank-corr in the family-disagreement heatmap when two specs are redundant (equal RMSE). NOTE: the finding's "scipy already imported here" is FALSE — scipy is not imported in this module (it is a hard project dep), so the patch must add the import. | At top of `plot_per_family_disagreement` add `from scipy.stats import spearmanr`; replace the inner `corr[i,j]` computation (446-451) with `r = spearmanr(score_matrix[i], score_matrix[j]).statistic; corr[i, j] = float("nan") if not np.isfinite(r) else float(r)` (drops the now-unneeded `rank_matrix` argsort-of-argsort at 438, keeps the std==0 → NaN behaviour, which spearmanr already returns as nan). |
| S23 | LOW | RESOLVE_NOW | diagnostics.py:500-505 | `x = np.arange(len(window_indices))` with `len != len(alpha_arr)` raises a cryptic matplotlib "x and y must have same first dimension" with no hint which arg is wrong. | After computing `alpha_arr` (493), before the `window_indices is None` branch add `if window_indices is not None and len(window_indices) != alpha_arr.size: raise ValueError(f"plot_alpha_stability: window_indices ({len(window_indices)}) and alpha_per_window ({alpha_arr.size}) must have equal length.")`. |
| S24 | LOW | RESOLVE_NOW | provenance.py:17 | `from .transforms import _TRANSFORMS_REGISTRY` imported and never used (verified: sole reference is the import line). Dead + mildly misleading (implies formulas are registry-driven; they are not — see S12). | Delete line 17 (`from .transforms import _TRANSFORMS_REGISTRY`). |
| S25 | LOW | FUTURE | cache.py:450-487, 100-110 | `compute_config_signature_v1` folds versions only when `library_versions` is passed (defaults None); module-recipe (100-110) never mentions it, so a docstring-following R&D user builds keys with no code-version component → stale spec replay across mlframe upgrades. Needs a `_DISCOVERY_CACHE_SCHEMA_VERSION` constant + mlframe.__version__ folded unconditionally + a bump-discipline + cache-invalidation note. | — |

## Summary
ALREADY_RESOLVED: 2 (S1,S2) · RESOLVE_NOW: 15 (S4,S5,S6,S10,S11,S13,S14,S15,S17,S19,S20,S21,S22,S23,S24) · FUTURE: 7 (S3,S7,S8,S9,S12,S18,S25) · REJECTED: 1 (S16) · DOC: 0 · Total: 25
