# Disposition table - dimension: Cross-target ensemble / OOF / stacking (2026-06-11)

Findings file: `audit/composite_audit_2026_06_10/findings_ensemble.md` (N1-N28).
Source verified against CURRENT code. Note: N1-N5 are the P1 findings the orchestrator pre-marked
ALREADY_RESOLVED (group-aware outer split N3, non-monotone-time argsort N4, df.copy size-gate N5,
sw-mask-align N1, kfold-composite eval-carve N2 are all present in current code and covered by
`tests/training/composite/test_composite_ensemble_oof_hygiene_audit.py`). All N6-N28 opened and decided.

| id | severity | disposition | file:line | one-line reason | patch-spec-if-RESOLVE_NOW |
|---|---|---|---|---|---|
| N1 | P1 | ALREADY_RESOLVED | ensemble/_oof_split.py:28-40 | `_align_fit_sw` applies fit_mask; all 5 call sites use it | - |
| N2 | P1 | ALREADY_RESOLVED | ensemble/__init__.py:632-641 | kfold composite branch now carves eval_set | - |
| N3 | P1 | ALREADY_RESOLVED | ensemble/__init__.py:559-562,778-811 | outer split now GroupKFold / group-level holdout carve | - |
| N4 | P1 | ALREADY_RESOLVED | ensemble/__init__.py:756-769 | non-monotone time_ordering now argsort-sorted, WARN logged | - |
| N5 | P1 | ALREADY_RESOLVED | ensemble/feature_stacking.py:78-96 | 2GB size-gate + WARN; copy kept only on small frames | - |
| N6 | P2 | FUTURE | ensemble/__init__.py:606-607,871-872 | real leak (full-train fitted_params reused in fold predict) but per-fold `transform.fit` shifts OOF RMSEs -> needs biz_value benchmark | - |
| N7 | P2 | RESOLVE_NOW | ensemble/__init__.py:529 | cache key omits holdout_frac/time-mode/external/group_ids -> wrong-result cross-serve for external cache callers | Replace `_full_key = (cache_key, int(kfold), int(random_state))` with a tuple also containing `round(float(holdout_frac),6)`, a time-mode int (0 none / 1 monotone / 2 supplied-nonmonotone-> just `int(time_ordering is not None)`), `int(external_holdout_X is not None)`, `int(len(external_holdout_y)) if external_holdout_y is not None else -1`, and a cheap group fingerprint `(int(group_ids.shape[0]), hash((tuple(np.asarray(group_ids)[:64].tolist()), tuple(np.asarray(group_ids)[-64:].tolist()))))` guarded `if group_ids is not None else (0,0)`. Keep `cache_key` first element. |
| N8 | P2 | RESOLVE_NOW | ensemble/__init__.py:540-547 vs 723 | docstring says external-holdout preferred but kfold branch returns first, silently ignoring the frame | At the top of the `int(kfold) > 1` block (line ~547), add a WARN when `external_holdout_X is not None`: `logger.warning("compute_oof_holdout_predictions: both kfold>1 and external_holdout_X supplied; kfold takes precedence and the external holdout frame is IGNORED. Pass kfold=1 to use the external holdout.")`. No control-flow change (avoids altering the untested-in-suite both-supplied path). |
| N9 | P2 | FUTURE | ensemble/__init__.py:296,735-736 | `external_holdout_base_per_spec` accepted+documented-as-required but never read; wiring it as a missing-base fallback is a real feature, deleting removes a fallback contract | - |
| N10 | P2 | RESOLVE_NOW | ensemble/__init__.py:229-237 | weighted-fit fallback silently trains UNWEIGHTED on a length-mismatched sw with no log | Before the inspect/fit block (after line 207) add: `if sw is not None and len(sw) != len(y): raise ValueError(f"_maybe_pass_sample_weight: sample_weight length {len(sw)} != y length {len(y)}")`. In the `except (TypeError, ValueError): pass` blocks at 229-236 add a `logger.debug(...)` naming the dropped kwargs before `pass`. Do NOT narrow the catch to TypeError (that flips currently-silent runs to hard errors -> needs care; left to N-future). |
| N11 | P2 | RESOLVE_NOW | ensemble/__init__.py:65-68 | `_transform_via` swallows pre_pipeline.transform failures silently -> OOF evaluates a different (raw) pipeline than deployed | In `except Exception:` at line 67 add `logger.warning("[ensemble] pre_pipeline.transform failed (%s: %s); falling back to RAW X for this slice -- OOF may evaluate a different space than deployed.", type(exc).__name__, exc)` (bind `as exc`). Keep `return X`. The one-side-only-divergence handling stays FUTURE. |
| N12 | P2 | RESOLVE_NOW | ensemble/__init__.py:116-123,130-132 | dead no-op lightgbm import; xgb USE_CUDA reports BUILD not device -> false GPU warning on CPU-only host | Delete the `if any(... "lgb"...)` block (116-123) entirely (keep no comment debt). For xgb: after the `bi.get("USE_CUDA", False)` true-branch, add a physical-device gate: `import xgboost`-side cannot probe device portably, so wrap with `from pyutilz.system.gpu_dispatch import is_cuda_available` and only `detected.append("xgboost")` when `bi.get("USE_CUDA") and is_cuda_available()`; on ImportError of pyutilz fall back to the build-flag-only behaviour. |
| N13 | P2 | RESOLVE_NOW | ensemble/__init__.py:155-162 | `env_signature` fully `__import__`s 9 libs (catboost/lgb/xgb ~0.5-3s each) on the predict-path drift check | Replace the loop body: use `from importlib.metadata import version, PackageNotFoundError` with a dist-name map `{"sklearn":"scikit-learn"}` (others map 1:1); `try: sig[libname] = version(distmap.get(libname, libname)) except PackageNotFoundError: sig[libname] = None`. Also add `sig["python"] = platform.python_version()`. No module import, identical version strings. |
| N14 | P2 | RESOLVE_NOW | ensemble/_cross_target.py:644-655,623-639 | three docstrings/comments describe a removed dropout-refit path that no longer exists (predict is NO-REFIT) | Rewrite `discard_train_matrix` docstring (644-655) to say the stash is NO-REFIT-era dead weight stripped before persistence and that NO refit/dropout method consumes it (drop the ".refit / dropout-style methods will raise" claim - no such method). Fix the contradictory comment at 623-625/633-639 to state plainly "predict (NO-REFIT) never reads these; the slice keeps the stash column-aligned only for the diagnostic votenrank assertion". Stop-stashing-by-default (memory/RAM win) stays FUTURE (opt-in flag + pickle suite). |
| N15 | P2 | RESOLVE_NOW | ensemble/stacking.py:191 | gate thresholds RAW NNLS coefficients though documented as "5% of the unit weight budget"; drifts with input calibration | Replace `survivors = [n for n, wv in raw_weights.items() if wv >= min_weight]` with a budget-share threshold: compute `_wsum = sum(max(v,0.0) for v in raw_weights.values())` then `survivors = [n for n, wv in raw_weights.items() if (_wsum > 0 and wv / _wsum >= min_weight)]`; when `_wsum <= 0` keep the existing degenerate (uniform) fallback. The renorm block at 192-202 is unchanged. |
| N16 | P2 | RESOLVE_NOW | ensemble/stacking.py:108-125 | `residual_dedup_indices` never checks `len(oof_rmses)==K`; short vector silently drops cols, long vector IndexErrors mid-loop | After `oof_rmses = np.asarray(...).reshape(-1)` (line 112) add: `if oof_rmses.shape[0] != K: raise ValueError(f"residual_dedup_indices: oof_rmses length {oof_rmses.shape[0]} != residual columns {K}")`. |
| N17 | P2 | FUTURE | ensemble/_cross_target.py:391-398,472-474 | `from_train_metrics` keeps a train-scale baseline when ranking OOF rmses (apples-to-oranges, biases gate); fixing the baseline-scale changes weighting numerics -> needs care; notes-relabel (`rmse_source`) is the safe sub-fix to land with it | - |
| N18 | P2 | RESOLVE_NOW | ensemble/_oof_split.py:84,98-108 | when group_ids present but cannot carve (uniq<4 / overshoot), falls through to a group-SPLITTING tail carve -> within-group leakage the docstring itself warns about | In `_carve_inner_eval_split`, when `group_ids is not None` and `g.shape[0] == n` but the group carve cannot be honoured (uniq.size < 4 at line 84, or the `0 < cumulative < n-100 and len(eval_groups) < uniq.size` guard at 98 is false), `return _ret(X, y, None, None, None)` instead of falling through to the tail carve at 103-108. ES degrades gracefully (no eval_set) rather than leaking groups. Add a one-line `# group carve impossible -> skip ES rather than group-blind-split (leakage)` note. |
| N19 | P2 | FUTURE | ensemble/feature_stacking.py:133,151; discovery/_stacked.py:98-102,240-244 | `composite_oof_predictions` can't receive groups/y for the splitter, no per-fold sample_weight slicing, and the sole prod call sites thread neither time nor group signal -> public-API extension + call-site rewiring | - |
| N20 | P2 | FUTURE | ensemble/__init__.py:522,958 (3-tuple return) | OOF helper returns no holdout row indices, so the suite cannot align sample_weight to OOF rows -> weighted stacking unreachable; adding a 4th return element + threading is an API extension | - |
| N21 | P2 | FUTURE | ensemble/__init__.py:540-546 | no time-respecting K-fold; default suite path nulls the time signal to get shuffled K folds (leaks future) -> needs forward-chaining folds + biz_value AR(1) test | - |
| N22 | LOW | RESOLVE_NOW | ensemble/__init__.py:361,390,886,917 | 4 of 5 inner-eval-carve sites hardcode `random_state=0`, ignoring the master seed (kfold sites thread it); inconsistent with derive_seeds philosophy, breaks seed-sweep variance | Change `random_state=0` -> `random_state=int(random_state)` at the four `_carve_inner_eval_split(...)` call sites (361, 390, 886, 917). Only perturbs grouped runs (the carve uses random_state solely in the group branch); the common deterministic-tail path is unchanged. |
| N23 | LOW | RESOLVE_NOW | ensemble/__init__.py:8,9,17,23,513,569; _cross_target.py:10,13,21; stacking.py:120-121 | unused imports + dead locals; stacking.py:121 `order` is dead and its comment describes an unused strategy | Delete: `__init__.py` `import math` (8), `import warnings` (9), `BaseEstimator, RegressorMixin` from the sklearn.base import (17 -> keep only `clone`), `ElasticNetCV` from the linear_model import (23 -> keep `Ridge, RidgeCV`), the in-fn `from sklearn.model_selection import train_test_split` (513), the unused `fold_frac = ...` line (569). `_cross_target.py`: `import hashlib` (10), `import warnings` (13), `ElasticNetCV` from import (21 -> keep `Ridge, RidgeCV`). `stacking.py`: delete the dead `order = list(np.argsort(oof_rmses)[::-1])` (121) and fix the comment at 120 to "Iterate best-RMSE-first; check each candidate against already-kept stronger members." |
| N24 | LOW | RESOLVE_NOW | ensemble/stacking.py:40,67 | `np.corrcoef` on a constant residual column emits a RuntimeWarning (sibling at 118-119 guards it); docstring claims diagonal always 1.0 | Wrap line 67 `return np.corrcoef(M[finite_rows], rowvar=False), names` in `with np.errstate(invalid="ignore"):`. Reword docstring line 40 to "Diagonal is 1.0 for non-degenerate columns; a NaN row/col indicates a constant (zero-variance) residual." |
| N25 | LOW | RESOLVE_NOW | ensemble/stacking.py:178-181,185-189 | degenerate (too-few-rows / NNLS-fail) fallbacks return all-survivors silently; module has no logger so a permanently-off gate is invisible | Add `import logging` + `logger = logging.getLogger(__name__)` at module top. In the too-few-finite-rows branch (178-181) add `logger.warning("stacking_aware_gate: only %d finite rows for %d transforms (need >=%d); gate disabled, keeping all with uniform weights.", int(finite_rows.sum()), X.shape[1], max(3, X.shape[1]+1))`. In the NNLS-failure except (185-189) add `logger.warning("stacking_aware_gate: NNLS failed (%s); gate disabled, keeping all transforms uniformly.", exc)` (bind `as exc`). |
| N26 | LOW | RESOLVE_NOW | ensemble/_cross_target.py:144-157,561-568 | `from_linear_stack` docstring describes removed intercept-folding/predict-renorm behaviour; `export_metadata` omits is_convex (and linear_stack intercept) | Rewrite the two stale docstring sentences (146-148 "intercept is folded into the bias..." and 154-157 "predict re-normalises only the magnitudes...") to: intercept is stored separately and added at predict (NO-REFIT, no renormalisation for non-convex strategies). In `export_metadata` (561-568) add `"is_convex": bool(getattr(self, "is_convex", True))` and `"intercept": float(getattr(self, "_linear_stack_intercept", 0.0))`. |
| N27 | LOW | RESOLVE_NOW | ensemble/__init__.py:626,665,842 (vs ==-checks 557) | kfold/single-split group_ids length checks use `>= max(idx)+1`, silently slicing a longer misaligned array; external path uses exact `==` | Change the three `_g_arr.shape[0] >= int(np.max(<idx>)) + 1` guards to `_g_arr.shape[0] == n_train` (n_train is in scope at all three sites: 626 fold path, 665 fold raw path, 842 single-split). On mismatch the existing `_group_* = None` fallback fires; add `logger.warning("[ensemble] group_ids length %d != n_train %d; group-aware carve skipped for this split.", _g_arr.shape[0], n_train)` before the fallback so a caller off-by-filtered-rows bug is visible. |
| N28 | P2 | FUTURE | tests/training/composite/test_composite_ensemble_oof_hygiene_audit.py (+others) | hygiene-audit test covers N1/N2/N3 only; group_ids+sample_weight-together, non-monotone-time downgrade pin, and cache-key holdout_frac collision (N7) tests are missing; they ship with their fixes (N7/N19/N21 are FUTURE; group+sw test pairs with N7 landing) | - |

## Summary counts (28 findings)

- ALREADY_RESOLVED: 5 - N1, N2, N3, N4, N5
- RESOLVE_NOW: 16 - N7, N8, N10, N11, N12, N13, N14, N15, N16, N18, N22, N23, N24, N25, N26, N27
- FUTURE: 7 - N6, N9, N17, N19, N20, N21, N28
- DOC: 0
- REJECTED: 0
