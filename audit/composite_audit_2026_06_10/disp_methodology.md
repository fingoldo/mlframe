# Disposition - Composite Targets audit, methodology dimension (2026-06-11)

Verified against current code (2026-06-10/11). None of M1-M13 match the
ALREADY_RESOLVED id list (E*/D*/T*/N*/P*/S*/A2/A3/A8/A9/I*/DX1), so all were
freshly inspected.

Counts: RESOLVE_NOW 2, FUTURE 9, DOC 1, REJECTED 1, ALREADY_RESOLVED 0.

| id | severity | disposition | file:line | reason | patch-spec (RESOLVE_NOW only) |
|---|---|---|---|---|---|
| M1 | P2 | FUTURE | discovery/_fit.py:240-265 (with screening.py:558) | Heavy-tail `mi_n_strata` boost is a real no-op under default `mi_sample_strategy="random"` (screening.py:558 returns the uniform draw before any stratification). The substantive fix (also flip strategy to `stratified_quantile`) changes which rows are sampled -> changes MI estimates -> needs the biz_value test M1 describes. A bit-identical log-truthfulness sub-fix (suppress the "boosted" log unless strategy != "random") is available but band-aids the dead corrective mechanism, so deferring the full fix is the honest call. | |
| M2 | P2 | FUTURE | discovery/_screening_tiny.py:629 + _eval.py:56 | Confirmed in-fold transform leakage: `fitted_params` fit on all valid train rows (_eval.py:56), `t_clean` computed over whole sample at line 629 before the KFold split (line 647). Per-fold refit changes numerics + needs the noise-spline regression test -> FUTURE. | |
| M3 | P2 | FUTURE | discovery/__init__.py:219-223 (+ _screening_tiny.py:445) | Confirmed: `fit_with_stability_check` only varies `config.random_state` (no row subsample), and stride `base_seed+i*7919` (line 223) collides with multiseed stride `+s_idx*7919` (line 445). Adding M-B row subsampling changes results + needs a unit test -> FUTURE. | |
| M4 | P2 | FUTURE | discovery/_eval.py:274 + _fit.py + _tiny_rerank.py | Confirmed no family-wise multiplicity control; eps_mi_gain=-10/require_beats_raw_baseline=False/mi_gain_bootstrap_n=0 by default (config:285,434,535) so latent for default users but real when gates re-enabled. BH/maxT is new selective-inference machinery + needs validation -> FUTURE. | |
| M5 | LOW | FUTURE | _cv_aggregation.py:38 + _screening_tiny.py:377,791 + forward_stepwise.py:136 | Confirmed `correlation_inflation` param exists but every composite call site leaves it 1.0; config has no knob. Adding the NB factor needs a new config field + per-split-geometry calibration -> FUTURE. | |
| M6 | P2 | FUTURE | discovery/_tiny_rerank.py:218 + __init__.py fit | Confirmed: discovery `fit` has no `time_ordering` param; rerank infers time-awareness from base monotonicity (line 218), which never fires for non-cumulative lag-of-y bases -> shuffled KFold leaks on temporal data. Adding a time-signal input is an architectural change threaded from the suite -> FUTURE. | |
| M7 | LOW | FUTURE | composite/__init__.py:55 | Confirmed "regression only here". Classification/init_score margin-offset is a new estimator + spec family + biz_value test -> extension, FUTURE. | |
| M8 | LOW | FUTURE | ensemble/__init__.py (OOF infra) | Confirmed OOF holdout infra exists (`compute_oof_holdout_predictions`) and zero conformal/`predict_interval` code. Split-conformal intervals are a new feature + API -> FUTURE. | |
| M9 | LOW | FUTURE | _composite_target_discovery_config.py:80 + auto_detect.py:43,139 | Confirmed `detect_time_column_candidates`/`sort_df_by_time_column` are imported/re-exported but never invoked; the 3 time-series transforms stay excluded. Wiring them needs sort+screen+index-map plumbing + measurement -> FUTURE (paired with M6). | |
| M10 | LOW | RESOLVE_NOW | discovery/_screening_tiny.py:767-772 | Early-stop bound `sum_so_far > thr*cv_folds` only guarantees the MEAN exceeds thr, but the final aggregate uses `aggregate_fold_scores(mode=cv_selector_mode)` (line 791); for median/low-quantile modes a truncated run can pass the gate wrongly. Latent today (rerank threads neither a finite threshold nor a non-mean mode) but it is a soundness bug in the kernel. Guard is bit-identical under the default `cv_selector_mode=="mean"`. | In the serial early-stop break condition (line 767-772), add `and cv_selector_mode == "mean"` to the `if` so the partial-sum break only fires for mean aggregation. E.g. change `if (math.isfinite(early_stop_threshold) and _n_finite_so_far > 0 and _fi < len(splits) - 1 and _sum_so_far > early_stop_threshold * cv_folds):` to also require `and cv_selector_mode == "mean"`. Add a unit test: folds [0.1,0.1,100], thr=10, cv_folds=5, mode="median_minus_mad" must NOT early-stop. |
| M11 | LOW | RESOLVE_NOW | discovery/bayesian.py:283-304 | Confirmed m-out-of-n bootstrap: replicate fits on `sample_size=subsample_n` rows (line 284) but std/percentiles are returned raw (lines 293-299) without the `sqrt(m/n)` width rescale, overstating CI width ~sqrt(n/m). Standard closed-form correction, deterministic, bit-identical for the default `subsample_n=None` (m==n). No internal prod caller passes subsample_n. | When `subsample_n is not None and sample_size < n`, rescale CI half-widths and std toward the mean by `f=sqrt(sample_size/n)`: alpha_std/beta_std *= f; alpha_ci_low=alpha_mean+(pctile_lo-alpha_mean)*f, alpha_ci_high=alpha_mean+(pctile_hi-alpha_mean)*f (same for beta). Keep alpha_mean/beta_mean and *_samples unchanged. Add `subsample_correction_factor` to the returned dict for transparency, and a regression unit test: subsampled CI within tol of full-n CI on Gaussian data. |
| M12 | LOW | DOC | ensemble/__init__.py:78 | Confirmed `derive_seeds` defined but zero call sites (only the def matches). Actually wiring it into the 6 randomness sources changes every draw -> not bit-identical, needs validation. Minimal safe step: docstring note that it is currently provided for external callers and not yet threaded into discovery's randomness sources (so the docstring stops implying production wiring). | |
| M13 | LOW | FUTURE | discovery/forward_stepwise.py:165-171 | Confirmed greedy step picks `argmin(aggregated)` and gates on relative point-estimate gain; per-fold arrays persisted (line 164) but unused for a paired test. Paired majority-of-folds / sign-test selection changes which bases are picked -> needs biz_value/regression validation -> FUTURE. | |
