# Disposition table — dimension: CompositeTargetEstimator / sklearn compliance

Date: 2026-06-11. Verified against current code in
`src/mlframe/training/composite/estimator/*`, `post_shim.py`, `spec.py`,
`transforms/{registry,linear}.py`, `streaming.py`.

ALREADY_RESOLVED ids (per integrator list, confirmed in current code):
E1 (ndim mask at `_predict.py:132`), E2, E3 (unary branch `_predict.py:280-308`),
E4 (no-copy passthrough `_estimator.py:536`), E5 (raise instead of swallow `post_shim.py:44-57`).

| id | severity | disposition | file:line | one-line reason | patch-spec-if-RESOLVE_NOW |
|----|----------|-------------|-----------|-----------------|---------------------------|
| E1 | P0 | ALREADY_RESOLVED | estimator/_predict.py:132 | ndim-aware mask already present (`mask = domain_ok if base_arr.ndim==1 else domain_ok[:,None]`). | — |
| E2 | P1 | ALREADY_RESOLVED | core/_phase_composite_wrapping.py | In integrator's resolved list. | — |
| E3 | P1 | ALREADY_RESOLVED | estimator/_predict.py:280-308 | Unary `requires_base=False` branch already skips base extraction in predict_quantile. | — |
| E4 | P1 | ALREADY_RESOLVED | estimator/_estimator.py:536 | `X_valid = X if bool(valid.all()) else self._subset_rows(...)` already present. | — |
| E5 | P1 | ALREADY_RESOLVED | post_shim.py:44-57 | `_transform` now re-raises RuntimeError with context instead of returning raw X. | — |
| E6 | P2 | FUTURE | estimator/_predict.py:278-308 | predict_quantile still threads no `groups` and never drops group_column; grouped+quantile crashes. Multi-touch plumbing + from_fitted_inner `group_column` param + tests; niche combo. | — |
| E7 | P2 | FUTURE | estimator/_estimator.py:494-504,572-582; post_shim.py:62-68 | TypeError-based sample_weight retry mis-attributes deep TypeErrors at 3 sites; correct fix is signature-gating (`has_fit_parameter`/`inspect.signature`) — behavior change needing a test matrix. | — |
| E8 | P2 | FUTURE | estimator/_utils.py:26-50 | `getattr(inner, attr, None)` still returns None on a fitted inner lacking the attr (hasattr-trap). Switching to no-default raise is a behavior change that can break callers relying on None; needs deliberate change + tests. | — |
| E9 | P2 | RESOLVE_NOW | estimator/_predict.py:25-28,265-268 | Pre-fit predict/predict_quantile raise bespoke RuntimeError; sklearn convention is NotFittedError (subclasses ValueError+AttributeError, preserving existing broad catches). | Import `from sklearn.exceptions import NotFittedError` at top of `_predict.py`; replace both `raise RuntimeError("CompositeTargetEstimator.predict[/_quantile] called before fit.")` with `raise NotFittedError(...)` (same message). |
| E10 | P2 | FUTURE | estimator/_estimator.py:271-291 | from_fitted_inner T-clip centered at 0 mis-centers unary transforms; fix recomputes T_train via `transform.forward(y,zeros,params)` + MAD logic — numerics change needing a biz_value regression. | — |
| E11 | P2 | FUTURE | estimator/_estimator.py:636-644; _utils.py:53-66 | `n_features_in_` (delegated to inner) != `len(feature_names_in_)` for grouped (inner fit on dropped col). Fix must rework the class-level `n_features_in_` property to prefer a stored wrapper value across both fit + from_fitted_inner routes; not a one-liner. | — |
| E12 | P2 | FUTURE | estimator/_estimator.py:543-544,684-690; _predict.py:82-86 | `X.drop` copies the pandas frame per grouped predict; CoW/lazy-subset fix requires per perf-measure-first benchmark. | — |
| E13 | P2 | FUTURE | estimator/_update.py:71-74 | update() refit refreshes only alpha/beta, leaving y_clip/t_clip/y_train_median stale under the drift that fired; corrective-mechanism change needing a test. | — |
| E14 | P2 | FUTURE | estimator/_predict.py:256-349 | predict_quantile lacks domain-mask/fallback, T-clip, runtime_stats parity with predict; refactor + tests. | — |
| E15 | P2 | RESOLVE_NOW | estimator/_estimator.py:417-433 | `fallback_predict` is only validated lazily on the first predict batch that hits a domain violation; sklearn convention is validate-in-fit. Cheap unambiguous membership check. (The unary len(y)!=len(X) check is deferred to E-future since deriving X row count needs a frame-type branch.) | At the top of `fit` (after `transform = get_transform(...)`, before extracting base), add: `if self.fallback_predict not in ("y_train_median", "nan"): raise ValueError(f"CompositeTargetEstimator: unknown fallback_predict {self.fallback_predict!r}; choose 'y_train_median' or 'nan'.")`. |
| E16 | LOW | RESOLVE_NOW | estimator/__init__.py:114-117,189-191 | `_extract_base` docstring + TypeError message promise "structured ndarray with named columns" but no ndarray path exists (any ndarray hits TypeError); `_extract_base_matrix` ndarray fallback delegates to it and also raises. Correct the message/docstrings (REJECT implementing the path — out of scope). | In `_extract_base`: change docstring "pandas / polars / structured ndarray" -> "pandas / polars DataFrame" and the `TypeError` message tail "pass pandas / polars DataFrame or a structured ndarray with named columns." -> "pass a pandas or polars DataFrame." In `_extract_base_matrix` line 189 comment, drop "for ndarray-with-names etc." |
| E17 | LOW | FUTURE | estimator/_predict.py:101-113,179-206 | T-clip hits not in runtime_stats_/callback + per-batch WARNING can spam; adds callback-payload keys + warn-throttle (contract change), better as a deliberate change. | — |
| E18 | LOW | RESOLVE_NOW | spec.py:7,10-56 | `@dataclass(frozen=True)` auto-generates `__hash__`/`__eq__` over `fitted_params: dict`; hashing raises TypeError and `__eq__` raises "truth value of array is ambiguous" when params hold ndarrays (median/monotonic_residual). Exclude the field from eq/hash; drop unused imports. | `from dataclasses import dataclass, field`; change `fitted_params: dict[str, Any]` -> `fitted_params: dict[str, Any] = field(compare=False, hash=False)` — NOTE this makes it a defaulted field, so it must move below `valid_domain_frac`/`n_train_rows` OR give the trailing non-defaulted fields defaults; simplest: keep position and set `eq=False` on the decorator instead: `@dataclass(frozen=True, eq=False)` (identity eq/hash, no crash). Drop unused `Dict, Tuple` from the typing import (keep `Any`). |
| E19 | LOW | FUTURE | post_shim.py:62-88 | shim `estimator_` property makes check_is_fitted pass pre-fit; no predict_quantile delegation. Adds `__sklearn_is_fitted__` + conditional quantile delegation; small feature, deliberate. | — |
| E20 | LOW | RESOLVE_NOW | estimator/__init__.py:7-16,41; estimator/_estimator.py:17,22,24 | Verified-dead imports: `__init__.py` `warnings`,`deque`,`BaseEstimator`,`RegressorMixin`,`clone`,`NotFittedError`,`Callable`,`Dict`,`List`; module `logger` never used. `_estimator.py` `math`,`deque`,`NotFittedError` unused (only in comments/strings). | Remove `import warnings`, `from collections import deque`, narrow typing import to `Any, Optional, Sequence, Tuple`, drop `from sklearn.base import ...` and `from sklearn.exceptions import NotFittedError`, and drop the unused `logger = logging.getLogger(__name__)` (line 41) from `__init__.py`. From `_estimator.py` remove `import math`, `from collections import deque`, `from sklearn.exceptions import NotFittedError`. Add to `from_fitted_inner`/`__init__` docstring a one-line note: "Note: passing a lambda/closure as runtime_stats_callback makes the fitted wrapper unpicklable; pass a module-level callable when the model will be persisted." (Keep all behavioral imports.) |
| E21 | LOW | FUTURE | tests/test_sklearn_compliance_composite.py:93-137 | Compliance matrix covers only diff/pandas/LinearRegression; parametrize over unary/multi-base/grouped/polars/quantile/fallback — test expansion. | — |
| E22 | LOW | FUTURE | estimator/_update.py:54-64 | update() boxes the deque to PyFloats + rebuilds 2 ndarrays per call; ring-buffer redesign needs a benchmark (perf-measure-first). | — |
| E23 | LOW | FUTURE | estimator/_update.py:36-39 | update() rejects linear_residual_robust though it shares {alpha,beta}; extending the allowlist + choosing robust-fit-on-buffer is a behavior choice needing a biz_value/test. | — |
| E24 | LOW | RESOLVE_NOW | estimator/__init__.py:306-312 | The single-batched-call PROBE catches only (TypeError, ValueError); a library-specific error (LightGBM/CatBoost/XGBoost) from a member's predict_quantile crashes the ensemble instead of using the documented per-alpha fallback. Broadening the catch affects only the optimization probe, not the strict per-alpha contract. | Change `except (TypeError, ValueError):` (line 311) to `except Exception as _probe_err:` and add inside the handler `logger.debug("predict_quantile_ensemble: batched probe for member %d (%s) failed (%r); falling back to per-alpha calls.", idx, type(m).__name__, _probe_err)` before `member_mat = None`. |
