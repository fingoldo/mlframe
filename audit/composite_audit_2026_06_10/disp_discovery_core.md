# Disposition — Discovery core + leakage discipline (composite_audit_2026_06_10)

Dimension findings file: `findings_discovery_core.md` (D1–D25). Every finding
verified against CURRENT code before disposition. D1/D2/D3 are on the
integrator's ALREADY_RESOLVED list and were re-confirmed fixed in source.

Counts: ALREADY_RESOLVED 3 · RESOLVE_NOW 15 · DOC 1 · FUTURE 6 · REJECTED 0.

| id | sev | disposition | file:line | reason | patch-spec (RESOLVE_NOW only) |
|----|-----|-------------|-----------|--------|-------------------------------|
| D1 | P0 | ALREADY_RESOLVED | discovery/_filter.py:213-240 | NaN-bearing cols now recomputed exactly in float64 per-pair (`col_has_nan` loop); dilution gone. | |
| D2 | P1 | ALREADY_RESOLVED | discovery/__init__.py:168-175 | iter_transform now column_stacks `(base_column,*extra)` for multi-base specs. | |
| D3 | P1 | ALREADY_RESOLVED | (on integrator list) | Multi-base promotion NaN-masking fix landed (per ALREADY_RESOLVED list). | |
| D4 | P2 | RESOLVE_NOW | discovery/_auto_base.py:119-124 | `hint_strengths[:len(hint_kept)]` reads RAW-ordered strengths after drops; misattributes a dropped strong hint's % to survivors. `hint_raw`↔`hint_strengths` ARE aligned, so align in-file. | Replace the `is_strong_hint` slice + the two `max(hint_strengths[:len(hint_kept)])` log uses: compute `_kept_set=set(hint_kept)`, `_aligned=[s for f,s in zip(hint_raw,(hint_strengths or [])) if f in _kept_set]`; gate `is_strong_hint = hint_strengths is not None and len(_aligned)>0 and max(_aligned)>=strong_hint_threshold`; use `max(_aligned)` in the two log lines. No cross-module contract change (list stays a list). |
| D5 | P2 | RESOLVE_NOW | discovery/_fit.py:588 | z is SE of a SINGLE half but a1-a2 is a difference of two independent half-fits; Var=2σ²/(half·var_base). Every z inflated 1.41x → excess drift WARN/reject. | `se_alpha = sigma_resid * np.sqrt(2.0) / (np.sqrt(half) * base_std)`. Update z-pinning tests (test_composite_*alpha_drift*) to the corrected value (multiply expected z by 1/√2). |
| D6 | P2 | RESOLVE_NOW | discovery/screening.py:592-606 | `out.sort(); out[:sample_n]` truncates the overshoot exclusively from the LARGEST (latest) indices — systematic temporal bias contradicting the docstring. Seeded, deterministic fix. | In `_sample_indices`, before the final `out.sort()`: `if out.size > sample_n: out = rng.choice(out, size=sample_n, replace=False)`; then sort. (Optional: distribute `sample_n % n_strata` remainder.) Removes the latest-row excision; still seeded/reproducible. |
| D7 | P2 | RESOLVE_NOW | discovery/__init__.py:209-247 | `self.config.random_state = ...` mutates the SHARED config in place; the heavy-tail boost swaps `self.config` for a model_copy mid-loop so the line-236 restore writes the COPY, leaving caller's config permanently reseeded. | At entry of `fit_with_stability_check`: `_saved_cfg = self.config; self.config = self.config.model_copy()`. Replace line-236 restore with `self.config = _saved_cfg`. Per-run reseed now mutates only the owned copy. |
| D8 | P2 | FUTURE | discovery/_fit.py:504,569,631,655,740,832 | report_ `kept=True` frozen at the eps gate; later alpha-drift/collapse/rerank drops + multi-base name-swaps make report() misleading. Real, but the fix is a reconciliation pass (track per-gate drop reasons + synthetic upgrade rows) + tests — not a micro-fix. | |
| D9 | P2 | RESOLVE_NOW | discovery/_fit.py:715-788 | Two seeds converging on the same multi-base set yield two specs (`X+Y` vs `Y+X`) → duplicate joint-OLS, 2x downstream training + perfectly-correlated ensemble members. No set-level dedup. | After the upgrade loop builds `_upgraded_specs` (before `kept_specs=_upgraded_specs`), dedup on `(transform_name, frozenset((s.base_column,)+tuple(getattr(s,'extra_base_columns',()) or ())))` keeping first occurrence (already rank-ordered). |
| D10 | P2 | FUTURE | discovery/_fit.py:355-397 | mi_y per-base recompute is ALREADY fixed (lines 387-397 derive via `_aggregate_mi_per_feature(np.delete(_per_feat_y_full,...))`). Remaining np.delete full-matrix copies held alive for all bases need the exclude_col refactor into `_mi_to_target_prebinned` + a RAM/bench check — out of scope for a safe pass. | |
| D11 | P2 | FUTURE | discovery/screening.py:489-491 | `_mi_to_target` knn path uses a global `isfinite(target)&all(isfinite(X),axis=1)` mask; one 99%-NaN column zeroes MI for the whole sweep. Real, but the proper fix is per-column knn handling (whole-matrix→per-column loop, changes algorithm/perf) or a calibrated >X%-NaN drop threshold — needs measurement. | |
| D12 | P2 | FUTURE | discovery/_auto_base.py:184-203 | All-column AND finite-mask shrinks the ranking sample with MNAR bias for mid-range-NaN columns. Same class as D11; the real fix is per-column `_mi_pair_bin` masking when the AND-mask retains <~50% rows — a behavior change needing the per-column rework + calibration. | |
| D13 | P2 | FUTURE | discovery/_fit.py:437-441 + _eval.py:34-44,287 | Unary (requires_base=False) transforms are scored against an arbitrary first base's x_remaining and stamped with that base's name; mi_gain shifts with irrelevant base reordering and the spec claims a fake base dependence. Fix = dedicated unary context (full X, base_column sentinel) across _fit/_eval/spec/name layers — a real refactor + tests. | |
| D14 | P2 | RESOLVE_NOW | discovery/screening.py:78 | `df[col].to_numpy(dtype=np.float32)` on a pandas nullable extension dtype (Int64/Float64/boolean) with NA raises ValueError; `_is_numeric_column` approves those, so discovery crashes at first column pull instead of treating NA as NaN. | In `_extract_column_array` pandas branch, add `na_value=np.nan` to BOTH `to_numpy` calls: `df[col].iloc[rows].to_numpy(dtype=np.float32, na_value=np.nan)` and `df[col].to_numpy(dtype=np.float32, na_value=np.nan)`. (na_value is a no-op on plain numpy dtypes.) |
| D15 | P2 | RESOLVE_NOW | discovery/_fit.py:187 | A boolean-mask `train_idx` (common numpy idiom) detonates later with a cryptic IndexError (`_sample_indices` reads mask LENGTH as row count); float dtype reinterprets silently. | After `train_idx = np.asarray(train_idx)`: `if train_idx.dtype == bool: train_idx = np.flatnonzero(train_idx); elif not np.issubdtype(train_idx.dtype, np.integer): raise TypeError("train_idx must be integer positions or a boolean mask, got dtype %r" % train_idx.dtype)`. Mirror for val_idx/test_idx at lines 833-834. |
| D16 | P2 | RESOLVE_NOW | discovery/_fit.py:157,174-176 | Class docstring promises val/test integrity checks ("Leakage discipline (CRITICAL)") but fit performs none; overlapping train/test silently fits + screens + reranks on test rows. O(n log n), negligible. Enforce per the documented contract. | At fit entry (after idx normalisation), when test_idx/val_idx provided: `if test_idx is not None and np.intersect1d(train_idx, np.asarray(test_idx)).size: raise ValueError("train_idx overlaps test_idx — leakage")`; same for val_idx; `if np.unique(train_idx).size != train_idx.size: logger.warning("duplicated train_idx rows bias MI")`; bounds-check `train_idx.max() < len(df)`. Add a unit test asserting the raise. |
| D17 | LOW | RESOLVE_NOW | discovery/_eval.py:272 | Comment says "Log first per-spec failure" but `if b == 0:` only fires when replicate 0 fails; failures at b≥1 are silent, biasing the LCB with no signal. | Replace `if b == 0:` with a `_boot_warned`/`_boot_fail_count` pattern: init `_boot_fail_count=0` before the loop; in `except`, `_boot_fail_count += 1; if _boot_fail_count == 1: <emit the existing warning>`. Include `_boot_fail_count` in the message. |
| D18 | LOW | RESOLVE_NOW | discovery/_fit.py:219,290,724 | `y_train` / `y_train_for_strat` / `_y_train_local` are the same `y_full[train_idx]` fancy-index copy (3 O(n_train) gathers); line 724 also has a dead `if y_full is not None else` branch (y_full always bound by then). | Reuse the line-219 `y_train`: set `y_train_for_strat = y_train` at line 290; at line 724 replace `_y_train_local = y_full[train_idx] if y_full is not None else _extract_column_array(...)` with `_y_train_local = y_train`. |
| D19 | LOW | RESOLVE_NOW | discovery/_fit.py:636 | `std_y = float(np.std(y_train[isfinite]))  or 1.0` is NaN-truthy on empty/all-NaN slice (bool(NaN) is True) → std_y=NaN silently disables the linres→diff collapse. Guard-pattern bug inviting copy-paste. | Replace with: `_std_y = float(np.std(y_train[np.isfinite(y_train)])) if np.isfinite(y_train).any() else 0.0; std_y = _std_y if (np.isfinite(_std_y) and _std_y > 0) else 1.0`. |
| D20 | LOW | RESOLVE_NOW | discovery/_fit.py:183,193 + __init__.py | `self._df_ref = df` pins a 100+GB frame on the fitted instance with no `__getstate__` exclusion → pickling/deep-copy serialises the whole frame; violates the project runtime-caches-break-pickle rule. | Add to `CompositeTargetDiscovery` (in discovery/__init__.py): `def __getstate__(self): s=self.__dict__.copy(); s.pop("_df_ref",None); s.pop("_auto_base_pool",None); return s` and `def __setstate__(self,s): self.__dict__.update(s); self._df_ref=None; self._auto_base_pool={}`. Docstring on iter_transform: pass df explicitly after unpickling. |
| D21 | LOW | FUTURE | discovery/_eval.py:160-196 | A 0.99-correlated sibling left in X_remaining biases mi_gain DOWN (conservative) for genuine lag-family bases. No leak. Optional accuracy improvement gated on `auto_base_dedup_corr_threshold`, needs a per-base column mask (ties to the D10 exclude-col refactor) + biz_value measurement. | |
| D22 | LOW | RESOLVE_NOW | discovery/_fit.py:376-379 | The `else:` "base outside the FE pool" arm is unreachable (every base ∈ usable_features ⇒ ∈ `_col_index`) AND methodologically wrong (base would sit in its own x_remaining). Convert dead code to a loud guard. | Replace the `else:` body with `logger.error("[CompositeTargetDiscovery] invariant violated: base %r not in usable_features index; skipping (its own column would leak into x_remaining).", base); continue`. Note the load-bearing invariant in `_resolve_base_candidates`. |
| D23 | LOW | RESOLVE_NOW | discovery/_auto_base.py:203 | Degenerate-sample fallback `return list(usable_features)[:top_k]` discards `hint_kept` — the one signal (BD ablation) NOT derived from the broken screening sample; hint features may fall outside the arbitrary top_k. | Replace with `return (hint_kept + [c for c in usable_features if c not in hint_kept])[: self.config.auto_base_top_k]`. |
| D24 | LOW | RESOLVE_NOW | discovery/screening.py:223-224 + _fit.py:194-202 | For 50≤n<5·mi_nbins every bin-MI is silently 0.0; top-K degenerates to the alphabetical tiebreaker. Undocumented at runtime. Log it (do NOT auto-shrink nbins — that changes numerics). | In `fit`, after `sample_idx`/`train_idx_screen` are known, add: `_eff_n = train_idx_screen.size; if self.config.mi_estimator == "bin" and _eff_n < 5*int(self.config.mi_nbins): logger.warning("[CompositeTargetDiscovery] screening sample %d < 5*mi_nbins(%d): bin-MI inactive (all 0.0); ranking deferred to rerank/tiebreaker.", _eff_n, int(self.config.mi_nbins))`. |
| D25 | LOW | DOC | discovery/_fit.py:498,509 | Gate admits on `mi_gain_lcb` but top-K ranking sorts on point `s.mi_gain`; a high-variance big-point/low-LCB spec can outrank a stable better-LCB one. Opt-in only (default bootstrap_n=0). The clean fix needs threading the entry's lcb to the spec-sort (lcb isn't on the spec); document the intentional point-estimate ranking as a known minor inconsistency rather than a one-liner. | |
