# Composite Targets audit — Integration dimension dispositions (2026-06-10)

Every finding in `findings_integration.md` (I1–I25) verified against current code before disposition.
IDs I1, I3, I4 are on the already-fixed+committed list → ALREADY_RESOLVED. All others opened at the cited
file:line in CURRENT code.

| id | severity | disposition | file:line | reason | patch-spec (RESOLVE_NOW only) |
|---|---|---|---|---|---|
| I1 | P1 | ALREADY_RESOLVED | _phase_composite_wrapping.py:156-170 | On the already-fixed list; code confirms the per-model hook now slices `_y_train_arr = _y_full_arr[train_idx]` (train-only envelope). | — |
| I2 | P1 | FUTURE | _phase_composite_post.py:199-217 | CONFIRMED real: raw-only CT_ENSEMBLE synthesis still gates on the GLOBALLY-empty specs dict (`not composite_specs_by_target_type`), so in a mixed suite an extreme-AR-skipped target B (no specs entry) gets no lag-floor ensemble while target A has specs. Fix = per-target fill of empty `[]` specs for every regression target with trained models; behavior change touching synthesis + per-target loop + needs a multi-target regression test. Not a quick safe guard. | — |
| I3 | P1 | ALREADY_RESOLVED | _phase_composite_post_xt_ensemble/__init__.py:221-259 | On the already-fixed list; code confirms train-RMSE proxy is now lazy (`_compute_train_rmse_proxy` + `_defer_train_proxy`, computed only when OOF deferred/failed). | — |
| I4 | P1 | ALREADY_RESOLVED | _phase_composite_post_xt_ensemble/__init__.py:357-366 | On the already-fixed list. (Note: code still drops `_time_ordering` for kfold at :361; honoring the input directive that the time-aware OOF handling was addressed/committed.) | — |
| I5 | P2 | RESOLVE_NOW | _phase_composite_post_xt_ensemble/__init__.py:692-703 | Stacking-aware gate prunes `_pred_matrix`/`_oof_components`/`_oof_names`/`_oof_rmses` but NOT `_oof_pred_matrix`; OOF gate at :799 then does `_oof_pred_matrix * _w_full[None,:]` with pruned `_w_full` → ValueError swallowed at :853, silently disabling the OOF gate + AR(1) failsafe. Confirmed. | In the `if 2 <= len(_survivors) < len(_oof_names):` block (after `_pred_matrix = _pred_matrix[:, _keep_mask]`) add `_oof_pred_matrix = _oof_pred_matrix[:, _keep_mask]` so the OOF-gate matrix matches the pruned weights. |
| I6 | P2 | RESOLVE_NOW | _phase_composite_post_xt_ensemble/__init__.py:310-312 + composite/ensemble/__init__.py:320,596,859 | Base matrices keyed by `spec["base_column"]` collide when a multi-base and a single-base spec share a primary base; last writer wins → other spec's OOF refit raises width-mismatch and is silently dropped. Confirmed. Keying by unique `spec["name"]` is bit-identical when no collision. | Write side: change keys to `_spec_for_oof["name"]` at __init__.py:310 and :312. Lookup side: in composite/ensemble/__init__.py change all three `base_train_full_per_spec.get(spec["base_column"])` (lines 320-322, 596, 859) to `.get(spec["name"])`. One coordinated commit, both sides. |
| I7 | P2 | FUTURE | _phase_composite_post_xt_ensemble/_phase_composite_post_xt_mtr_oof.py:73-77,84-86 | CONFIRMED: any single component fold failure / non-finite returns `None` for the whole MTR weighting (→ equal-mean, forfeiting the benched ~9% win), all at DEBUG. Per-component exclusion changes the return contract (caller injects weights shaped to ALL components at _post_xt_ensemble_mtr.py:265) and must re-validate against bench_mtr_nnls_oof.py — architectural, not a quick guard. (Cheap partial available: bump the 3 log sites to WARNING.) | — |
| I8 | P2 | RESOLVE_NOW | _phase_composite_post_summary.py:63-76 | y-scale verdict branch reads only `val`; when y-scale entries carry no finite val metric `_best_val` stays None → `best_model="-"` even though test metrics exist. The else branch already has the documented test-fallback (:104-118). Same prod incident shape, untreated branch. Mirrors existing code, only flips "-"→value. | In the `if _yscale_entries:` branch, after the val loop, if `_best_val is None` add a second loop over `_ye["metrics"]["test"]` (set `_best_split="test"`); and make the model-list val/test passes run as a further fallback when `_best_val` is still None (don't leave them only in `else`). |
| I9 | P2 | RESOLVE_NOW | _phase_train_one_target_body.py:894 + _phase_composite_wrapping.py:206,357-403 | Per-model hook emits the TEST y-scale chart (one full-test predict/entry), then the wrap-pass `skip_predict=True` branch re-emits the SAME chart (same `_yscale_{composite}` path → overwrite) for every entry — duplicate predict + file write + log. Confirmed. Guard so the wrap-pass only emits for entries the hook missed. | In `emit_per_model_composite_y_scale_test`, on success set `entry._yscale_chart_emitted = True` (best-effort, ignore read-only). In the wrap-pass `skip_predict` chart loop (:362) `if getattr(_entry, "_yscale_chart_emitted", False): continue`. |
| I10 | P2 | RESOLVE_NOW | _phase_composite_discovery.py:235-238 (+ _phase_train_one_target_mlp_helpers.py:192-229) | `_lag1_ar` is the SINGLE picked-target stat (set at _main_train_suite_target_distribution.py:268 as `picked_target_name`), applied to EVERY regression target without name comparison → over-skips non-AR siblings / under-protects AR siblings. `picked_target_name` is in the report, so the guard is free. | Discovery: add `and _td_report.get("picked_target_name") == _tname_disc` to the extreme-AR skip condition at :235-238 (only skip the target the AR stat was measured for). The MLP twin (`extreme_ar_skip_decision`) needs the same guard threaded at its call site (pass `is_picked_target=(target_name==picked_target_name)` and require it for `fired`) — same fix class, small follow-up. |
| I11 | P2 | FUTURE | _phase_composite_discovery.py:198-215,473-482 | CONFIRMED: group-aware tiny-rerank is gated on the analyzer RECOMMENDATION (`prefer_group_aware`) not the ACTUAL splitter config; a user who supplies group_ids + a group-aware splitter without the analyzer recommending it gets plain-KFold rerank (the documented prod failure). Fix needs a real "splitter was group-aware" flag threaded from the suite boundary — wiring/architectural, not a local guard. | — |
| I12 | P2 | RESOLVE_NOW | _phase_composite_discovery.py:319-325,444-457,541-547 | Three silent-swallow gaps: (1) fit-exception (:541) and (2) row-mismatch (:319) log-only with NO `composite_target_failures` entry (every other skip path records one); (3) cache-replay `except Exception: _cached_specs=[]` (:451) has ZERO logging and leaves a metadata/behaviour divergence (specs claimed, no T columns added). All cheap, no numerics. | (1)+(2): before each `continue`, append a failures entry `[{"name":_tname_disc,"kept":False,"rejected":True,"reason": <exc text / "row-align mismatch y[N] vs df[M]">}]` to `metadata["composite_target_failures"][str(_tt_disc)]`. (3): change the bare `except Exception:` at :451 to log a WARNING with the exception and the cached key, AND clear the just-written `metadata["composite_target_specs"][str(_tt_disc)][_tname_disc] = []` so the claimed-specs/no-columns divergence is removed (full re-discovery fallback is the larger fix; the consistency clear + WARN removes the silent divergence now). |
| I13 | P2 | FUTURE | _phase_composite_discovery.py:48-59 | `pd.concat([sub, target_series], axis=1)` copies blocks under pandas<3 / no-CoW — a full peak-RAM duplication of the (100+GB-convention) train frame per regression target. Real, but the prescribed fixes (mutate-and-restore try/finally, or a `y=` kwarg into `CompositeTargetDiscovery.fit`) change a deliberately-anti-mutation path and need a before/after RAM measurement per the perf-measure-first rule. Out of scope for a safe quick pass. | — |
| I14 | P2 | RESOLVE_NOW | _phase_composite_post_lag_predict.py:50-69 (trigger composite/ensemble/__init__.py:681-682) | `_LagPredictDeployableModel.predict` returns the lag column verbatim incl. NaN at group starts; the OOF helper raises "non-finite holdout predictions" → lag_predict dropped from the surviving set on EXACTLY the strong-AR grouped targets it exists for. Imputing non-finite with a fit-time train-median is a self-contained, clearly-correct edge-case guard (NaN preds are never useful; no copy, no bench). Also fixes deploy-time NaN-lag rows. | In `__init__` add `self._impute_value = None`. In `fit` compute `m = np.nanmedian(np.asarray(X[self.lag_column] ... astype float))` (reuse the predict extraction) and store `self._impute_value = m if np.isfinite(m) else 0.0`. In `predict`, after building `col`, do `bad = ~np.isfinite(col); if bad.any(): col[col-is-readonly→col=col.copy(); col[bad] = self._impute_value if self._impute_value is not None else (np.nanmedian(col) or 0.0)`. `_impute_value` is a fitted attr (not in get_params) so sklearn.clone re-fits it per fold. |
| I15 | P2 | FUTURE | _phase_composite_wrapping.py:546-600 | CONFIRMED: both watchdog blocks build a 1-D `_base_uni`/`_base_arr` from the single `base_column`; for `linear_residual_multi` (in `_ADDITIVE_TRANSFORMS`, :548) inverse/forward need the (n,1+K) matrix → ValueError swallowed at DEBUG → ZERO watchdog coverage for the spec family most prone to wrapper-math bugs. Fix = build the multi-column base in both blocks + a new regression test that corrupts a multi-base wrapper and asserts the WARNING fires. Test-gap + 2-site multi-base matrix build — beyond a one-line guard. | — |
| I16 | LOW | RESOLVE_NOW | _train_eval_select_target.py:117-125 (call site _phase_train_one_target_model_setup.py:539) | `is_composite_target_name(model_name)` runs on the concatenated `f"{target_name} {model_name} {cur_target_name}"` (passed at model_setup.py:539), so a raw target containing "-diff-"/"-ratio-" is mis-stamped MTRESID. Reporting-only; `select_target` has exactly ONE caller and `cur_target_name` is available there. | Add param `cur_target_name: Optional[str] = None` to `select_target`; at :120 use `_is_composite = is_composite_target_name(cur_target_name or model_name)`. At model_setup.py:538 pass `cur_target_name=cur_target_name`. |
| I17 | LOW | RESOLVE_NOW | _phase_composite_wrapping.py:64-71 | `_mttr/_mtts` are computed on the y TEST slice (`y_target`) but the chart header renders "MTTR/MTTS=" (suite convention: MTTR = TRAIN mean) → cross-reading drift gives inconsistent numbers. Also `_human_inner` (:64) is computed and never used. | At :70 change the header label `MTTR/MTTS=` to `test_mean/test_std=` (or `MTeR/MTeS=`); delete the unused `_human_inner = ...` line (:64). |
| I18 | LOW | RESOLVE_NOW | _phase_composite_post.py:55-68 | `recover_composite_y_scale_metrics` is annotated `-> dict[int, np.ndarray]` but it delegates to `_run_composite_target_wrapping`, whose cache is keyed by `(id(wrapper), id(frame), shape)` tuples (wrapping.py:245,457). | Change the return annotation at :68 from `dict[int, np.ndarray]` to `dict[tuple, np.ndarray]`. |
| I19 | LOW | RESOLVE_NOW | _phase_composite_wrapping.py:334-338 | "wrapped %d model(s)" logs `len(_entries)`, counting entries that had no predict, were already wrapped (continue at :294), or failed `from_fitted_inner` (continue at :318) — operator can be told "wrapped 4" when 1 stayed T-scale. | Initialize `_n_wrapped = 0` before the entry loop; increment it only where the wrapper is actually assigned (after :328/:331/:333 success); log `_n_wrapped` instead of `len(_entries)` at :336. |
| I20 | LOW | FUTURE | _phase_composite_post_xt_ensemble/_post_xt_ensemble_mtr.py:267-288 | CONFIRMED parity gaps vs single-target: (a) MTR ensemble is APPENDED into `models[tt][orig_tname]` (:281) not a dedicated `_CT_ENSEMBLE__{name}` key → it appears alongside its own component siblings (votenrank/per-entry tables misread it); (b) SimpleNamespace lacks `metrics={}` (single-target sets it, __init__.py:880); (c) no `ensembles_chosen["cross_target"]` stamp. (a) is a registration-shape change needing downstream save/predict verification; (b)/(c) are cheap but best landed with (a). | — |
| I21 | LOW | RESOLVE_NOW | _post_xt_ensemble_mtr.py:28-34,106-112 | Class docstring (:31 "Weights are normalised to sum to 1") and the `weights` property doc (:108-112 "Each column sums to 1.0 ... post-normalisation (nnls)") contradict the code, which DELIBERATELY keeps RAW NNLS weights (:162-174 rationale comment). A maintainer trusting the doc could re-add normalisation and regress fits. | Update the class docstring (:31) and the `weights` property docstring (:108-112) to state nnls keeps RAW (non-normalised) NNLS weights — convex/sum-to-1 only for equal_mean — referencing the rationale at :164-173. |
| I22 | LOW | RESOLVE_NOW | _phase_composite_discovery.py:318-325 | When `filtered_train_idx is None`, `_y_arr[filtered_train_idx]` becomes `_y_arr[None]` → shape (1,n), so the guard fires the confusing "y[1] vs filtered_train_df[N]" instead of stating the index is missing. The BaselineDiagnostics block 5 lines up (:279-281) already handles None explicitly. | Before `_y_train_aligned = _y_arr[filtered_train_idx]` (:318) add `if filtered_train_idx is None: <log a clear "filtered_train_idx missing; skipping discovery for '%s'" warning, append a failures entry per I12, and continue>`. |
| I23 | LOW | FUTURE | _phase_composite_post_xt_ensemble/__init__.py:243-245 | `_arr[~_fin] = median(finite)` imputes a component whose train predict FAILED with the MEDIAN RMSE → from_train_metrics grants it mid-pack weight on the fallback path. Safer = impute `max(finite)` (worst) or drop. Only reachable on the double-failure path (OOF failed AND component scoring failed). Changing imputation alters fallback weights → wants a small bench/eyeball before flipping; low reach, low value-per-risk for the quick pass. | — |
| I24 | LOW | RESOLVE_NOW | _phase_composite_post.py:13-30 | Verified dead (grep: only appear on their import lines, no call site): `SimpleNamespace, report_model_perf, get_transform, compute_oof_holdout_predictions, PrePipelinePredictShim, _CrossEns, CompositeTargetEstimator, format_suite_end_summary, _build_full_column_from_splits, _entry_metric, _fmt, _short_tag_fn, _strip, _DEFAULT_OOF_RANDOM_STATE, _PROB_NORM_EPS`. No other module imports these from here (only `run_composite_post_processing` + `_run_suite_end_dummy_baselines_summary` are external imports). CORRECTION to the finding: KEEP `np` (used in annotations :68,:139) and KEEP the three noqa-F401 re-exports (:51,:52,:270). | Delete the listed dead imports/constants at :13,:18-25,:29-30 (drop the now-empty `_DEFAULT_OOF_RANDOM_STATE`/`_PROB_NORM_EPS`). Keep `import numpy as np`, `is_composite_target_name`, `logging`, the typing imports actually used, and the three documented re-exports. |
| I25 | LOW | DOC | _phase_composite_post_xt_ensemble/__init__.py:415-422,539-554,727-740 | The dummy-floor / prescreen / oof_weighted gates read the strongest-dummy `primary_metric` generically and compare against component RMSEs; today the regression primary is hard-coded `val_RMSE` (_dummy_metrics_pick_plot.py:108) so units match. The mixed-units risk only materialises if a non-RMSE regression primary is introduced — a config path that does not exist today. Document the RMSE-family assumption at the gate sites rather than add speculative endswith guards for an unreachable case. | — |

Summary (25 findings): ALREADY_RESOLVED=3 (I1,I3,I4); RESOLVE_NOW=14 (I5,I6,I8,I9,I10,I12,I14,I16,I17,I18,I19,I21,I22,I24); FUTURE=7 (I2,I7,I11,I13,I15,I20,I23); DOC=1 (I25).
