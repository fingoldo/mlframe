# FE-creation test-suite gap audit (KEY=gaps_fe_masking, 2026-06-10)

Scope: feature CREATION inside MRMR + FE transformers under tests/feature_selection/ (bases, pair/unary-binary FE, prewarp incl. Huber-IRLS robust fit f98d1d9, fe_max_steps=2 default 7d70af60, FE accuracy gate, CMI redundancy gate, raw-redundancy drop, stability vote, rung schedule, provenance, golden machinery).

Sampling statement (so the verifier can check): deep-read in full or near-full: test_mrmr_engineered_replay.py, test_fe_rung_schedule.py, test_fe_cmi_redundancy_gate.py, test_fe_stability_vote.py, test_biz_value_mrmr_prefer_engineered.py, test_noise_floor.py, test_adaptive_fe_usability_gate.py, test_robust_warp_fit_gate.py, test_biz_val_robust_warp_fit.py, test_mrmr_fe_composite_feedforward.py, test_mrmr_create_keep_drop.py (lines 760-1034 + registry grep), test_biz_value_mrmr_fe_canonical.py (lines 100-214), test_biz_value_mrmr_pair_prewarp.py (lines 295-409), golden/_capture.py, golden/capture_baseline.py, golden/__init__.py, conftest.py (structure). Greps across all 465 files for: noise/null-dataset e2e, prefer_engineered, raw_redundancy, winsor/robust_fit, dedup, is_missing__, nullable dtypes, hijack, golden consumers, accuracy-gate consumers, fe_max_steps. Source cross-checks: mrmr.py, _mrmr_fit_impl.py (accuracy-gate + recipe-drop + raw-redundancy sites), _fe_accuracy_gate.py, _hermite_prewarp.py, _hermite_robust wiring, _recipe_unary_binary.py, feature_engineering.py (binary ops + GPU ops), _fe_rung_schedule/_fe_stability_vote/_fe_cmi_redundancy_gate consumers. NOT deep-read: the 97 layer files (sampled layer37/39/58/59 + LAYER_INDEX.md), cat-FE family files (names + dup-column/state/tier files only), wrappers/boruta/shap_proxy families (out of mission). Per instructions I did not run pytest. Known-dispositioned items (orth-basis recipe parity, GPU SU gate, shap binning) excluded.

---

### [gaps_fe_masking-01] No all-noise end-to-end test of the DEFAULT MRMR pipeline (joint FE family-wise error rate untested)
Severity: P1
Kind: gap
Files: tests/feature_selection/test_fe_stability_vote.py:53-62,172-192; tests/feature_selection/test_biz_value_mrmr_pair_prewarp.py:323-343; tests/feature_selection/test_biz_value_mrmr_layer58.py:357-371; tests/feature_selection/test_biz_value_mrmr_adaptive_fourier.py:188; tests/feature_selection/test_hinge_basis_fe.py:84; tests/feature_selection/test_wavelet_basis_fe.py:178
Evidence: Every noise control is PER-MECHANISM: stability vote tests noise only with gates deliberately RELAXED ("fe_pair_maxt_null_permutations=0 ... so chance-max noise pairs slip the in-fit per-pair gate", RELAXED dict at test_fe_stability_vote.py:53-62, n=1500); prewarp/hinge/wavelet/fourier/chirp/routing noise tests each pin their OWN generator with non-default kwargs. Grep across tests/feature_selection found NO test that fits a plain default `MRMR()` on a frame where y is independent of ALL columns at realistic n and asserts empty selection. With ~8+ FE families default-ON (hybrid orth, adaptive fourier, chirp, hinge, wavelet, pair FE, count-freq, missingness, cat FE) plus fe_max_steps=2 (composites of chance survivors of step 1), the candidate count multiplies across families and the JOINT false-discovery behavior of the default config is exactly what no test measures.
Proposal: new test_biz_value_mrmr_default_null_dataset.py. Recipe: for seed in range(5): X = pd.DataFrame(rng.standard_normal((20_000, 30)), columns=[f"n{i}"...]) plus 3 low-card int cols (rng.integers(0,5)) to wake the cat/count-freq paths; y_clf = rng.integers(0,2,n); y_reg = rng.standard_normal(n). Fit MRMR(verbose=0, random_seed=seed) (NO other kwargs - the point is the shipped default). Assert per-seed len(get_feature_names_out()) == 0 and _engineered_recipes_ == [], with a total-budget fallback assertion (sum of survivors across 5 seeds <= 1) if a strict 0 proves seed-flaky during calibration - then pin the measured rate. Mark @pytest.mark.slow; fast mode (MLFRAME_FAST=1) runs 1 seed at n=4000/p=10 via fast_subset. This is the FWER sensor every per-mechanism noise test assumes but none provides.
Effort: M
VERDICT: CORRECTED — the absolute "NO test fits a plain default MRMR() on an all-noise frame" claim is refuted by TestFalsePositiveRate::test_all_noise_bounded_false_positive_rate (tests/feature_selection/test_biz_value_mrmr_quality_metrics.py:307-336), which fits MRMR(verbose=0, min_features_fallback=0) on a 10-column pure-noise frame with random binary y. However that test runs at n=800/p=10, asserts only the catastrophic ceiling `n_selected < 10`, tolerates the fallback path, and never inspects _engineered_recipes_ — so the SUBSTANTIVE gap (a strict empty-selection / measured-FP-rate sensor at realistic n under the shipped fe_max_steps=2 multi-family default) stands. All six cited per-mechanism noise controls verified at the stated lines (RELAXED dict verbatim at test_fe_stability_vote.py:53-62; layer58 routing-helper noise at 357-371; adaptive-fourier TestGateCNoiseControl at ~186-194 uses _make_mrmr(verbose=0, random_seed=0) wrapper; hinge:84 and wavelet:178 are helper-level). The proposal should cite and supersede/strengthen the existing quality_metrics test rather than claim greenfield.

### [gaps_fe_masking-02] fe_max_steps=2 default shipped "[validation pending]" - the keep/drop registry was deliberately re-pinned so NO formula validates the new default
Severity: P1
Kind: bizvalue
Files: src/mlframe/feature_selection/filters/mrmr.py:372-386; tests/feature_selection/test_mrmr_create_keep_drop.py:819-842 (commit 7d70af60 diff); tests/feature_selection/test_mrmr_fe_composite_feedforward.py:279-317
Evidence: Commit 7d70af60 "feat(fs): fe_max_steps=2 default [validation pending]" changed `_fit_and_eval` to "Pass fe_max_steps EXPLICITLY (was conditionally omitted when ==1 ... omitting it would have run these step-1 cases at step-2 and changed their selections)". Net effect: all ~25 registry formulas (incl. the entire noise-traps family NT_F1..NT_F5) run pinned at step-1; only the 2 cases registered with fe_max_steps=2 (F5/F6 nests) plus the composite-feedforward strict pin run at 2. The strict pin validates SIGNAL recovery (clean additive composite at n=30k x 3 seeds) but nothing validates that the default flip does not ADMIT MORE NOISE/redundancy across the adversarial registry - which is precisely the "[validation pending]" debt named in the commit subject.
Proposal: add test_create_keep_drop_default_steps2(formula) parametrized over a representative subset {NT_F1..NT_F5, F1_easy_single_ratio_plus_noise, F2_easy_two_pairs_marginal_zero_guard, ws2_log_sin_product} at BROAD_N, calling _fit_and_eval(formula, BROAD_N, fe_max_steps=2). Maintain a SEPARATE EXPECTED_XFAILS_STEPS2 registry seeded from one calibration run; the binding assertion: the step-2 failure set for each cell must be a SUBSET of the step-1 failure set union the documented step-2 xfails (i.e. step-2 must not introduce NEW admitted_noise/admitted_redundant kinds). When validation completes, remove "[validation pending]" from the docstring comment block at mrmr.py:373-385.
Effort: M
VERDICT: CORRECTED — every factual claim verified: commit subject is exactly "feat(fs): fe_max_steps=2 default [validation pending]" (git log confirmed); the explicit-threading comment is verbatim at test_mrmr_create_keep_drop.py:823-826; the registry has 27 _reg() formulas of which exactly 2 (F5 at :380-390, F6 at :403-412) carry fe_max_steps=2; NT_F1..NT_F5 exist (:541-614 region) and omit fe_max_steps so they pin step-1; the strict pin (test_mrmr_fe_composite_feedforward.py:279-317, n=30k x seeds 0/1/2) validates signal recovery only. ONE correction: "[validation pending]" exists ONLY in the commit subject — the mrmr.py:372-386 comment block contains no such marker (it instead asserts the n-dependence probe "confirmed" recovery), so the proposal's final step ("remove '[validation pending]' from the docstring comment block") references text that is not there; reword to "document completed validation in the mrmr.py:372-385 comment + CHANGELOG".

### [gaps_fe_masking-03] _fe_accuracy_gate has ZERO direct tests, and its exception path contradicts the documented fail-open contract
Severity: P1
Kind: quality
Files: src/mlframe/feature_selection/filters/_fe_accuracy_gate.py:106-111,127-158; src/mlframe/feature_selection/filters/_mrmr_fit_impl.py:4638-4691
Evidence: grep for measure_feature_uplift / keep_engineered_over_source across tests/ returns NOTHING - the module is consumed only by _mrmr_fit_impl.py. Module docstring claims "Verified to separate all four cases (Hermite/Fourier x win/hijack) where a single MI or R^2 metric cannot" - unpinned by any test. Contradiction: _mrmr_fit_impl.py:4638 comment says "Best-effort: any failure falls back to KEEPING the column" and keep_engineered_over_source docstring says "Fail-open (True) on degenerate input", BUT measure_feature_uplift:106-108 catches `Exception` and returns 0.0, which is < _FE_UPLIFT_MIN=0.015, so keep_engineered_over_source returns False and the candidate is silently DROPPED - a probe exception (sklearn raise on odd y, singular scaler fold) behaves fail-CLOSED, the opposite of the documented contract, with only a logger.debug trace.
Proposal: new tests/feature_selection/test_fe_accuracy_gate.py: (1) the four advertised cases as biz_value pins - He2 on y=x^2 target KEPT (uplift >> 0.015), Fourier transform of a strongly monotone column DROPPED, routed adaptive Fourier on a genuine oscillation KEPT, He of a leak column DROPPED; (2) degenerate fail-open paths (n<40, single class, shape mismatch -> True); (3) MNAR fail-closed (>2% NaN source -> False) at line 152; (4) regression test for the exception path: monkeypatch sklearn.linear_model.LogisticRegression in the module namespace to raise, assert the documented behavior - and first FIX the prod side to match the docs (have measure_feature_uplift return None on exception and keep_engineered_over_source treat None as keep). Verify the test fails pre-fix per the regression-test rule.
Effort: M
VERDICT: CONFIRMED — independent grep over ALL of tests/ for measure_feature_uplift / keep_engineered_over_source / _fe_accuracy_gate / infer_classification returns zero files. Module docstring quote verbatim at _fe_accuracy_gate.py:21-22; `except Exception ... return 0.0` verbatim at :106-108; _FE_UPLIFT_MIN=0.015 at :33; fail-open docstring at :139-140; MNAR >2% fail-closed at :152-153. The caller comment "Best-effort: any failure falls back to keeping the column" verified in the ACCURACY GATE block of _mrmr_fit_impl.py (~:4638), and the call site `if keep_engineered_over_source(...) else drop` confirms the fail-closed exception path: an in-probe sklearn exception is swallowed at :106-108 into 0.0 < 0.015 → drop, never reaching the caller's best-effort except. Contradiction real; proposal coherent with the source (degenerate paths at :57-58, :73-78, :149-150 return as described). Minor caps-for-emphasis: actual caller text reads "falls back to keeping the column" (lowercase).

### [gaps_fe_masking-04] Silent DROP of recipe-less engineered features at transform is untested; names/columns consistency unverified (parity pinned only at fe_max_steps=0)
Severity: P1
Kind: gap-mask
Files: src/mlframe/feature_selection/filters/_mrmr_fit_impl.py:6686-6690; tests/feature_selection/test_biz_value_mrmr_layer39.py:180-205
Evidence: _mrmr_fit_impl.py:6689 logs 'engineered feature(s) selected without replayable recipe (nested-engineered parents at fe_max_steps=%d); they will be DROPPED from transform output'. Layer39's strict recipe-count parity is explicitly scoped away from this branch: "On the kitchen-sink fixture all engineered cols are 1-deep (fe_max_steps=0), so every engineered name MUST have a matching recipe" (layer39:188-190). No test exercises branch (b) ("recorded by name only and DROPPED"). With fe_max_steps=2 now default, this branch is reachable in production, and nothing verifies that get_feature_names_out(), fe_provenance_ (which "zips positionally against transform output", _mrmr_fe_provenance.py:266) and the actual transform column set stay mutually consistent when a selected name is dropped - a mismatch breaks every downstream sklearn consumer silently.
Proposal: two tests. (a) Unit: fit a small default MRMR, then simulate the drop state (append a name to _engineered_features_ / support-name list without a recipe - mirroring what fit does at 6686) and assert transform(X).columns == get_feature_names_out() EXACTLY and fe_provenance_ rows align positionally - i.e. pin which of the two surfaces the dropped name is removed from. (b) E2E: construct a fixture that forces a non-replayable nested parent (e.g. fe_max_steps=3 with a step-3 composite whose parent recipe is suppressed, or monkeypatch build_unary_binary_recipe to return None for one candidate) and assert the WARNING text is emitted (caplog) and transform/get_feature_names_out parity still holds.
Effort: M
VERDICT: CORRECTED — warning text verbatim at _mrmr_fit_impl.py (~:6689-6691); layer39 scoping quote verbatim (test_biz_value_mrmr_layer39.py:189-192); _mrmr_fe_provenance.py:266 "zips positionally against transform output" verbatim; independent grep for tests exercising the drop branch (caplog on "without replayable recipe" / "DROPPED from transform") finds none — the no-test claim survives. ONE overstatement: "With fe_max_steps=2 now default, this branch is reachable in production" understates the 2026-06-08 feed-forward fix — test_mrmr_fe_composite_feedforward.py:17-19 documents that nested-parent recipes (nested_parent_a/b) now make step-2 engineered×engineered composites "fully replayable", so at the shipped default the drop branch fires only on recipe-construction FAILURE (or non-unary_binary engineered parents), not generically for step-2 composites. The finding's own proposal (b) (force via fe_max_steps=3 or monkeypatch build_unary_binary_recipe→None) already implicitly concedes this; the gap and both proposed tests remain valid.

### [gaps_fe_masking-05] EXPECTED_XFAILS registry in create_keep_drop is not strict - cells that start passing go silently stale (XPASS invisible)
Severity: P2
Kind: quality
Files: tests/feature_selection/test_mrmr_create_keep_drop.py:956-962,976-984,1007-1013
Evidence: `_maybe_xfail` is called only inside `if failures:`. A registered cell that begins to PASS (fix landed, threshold drifted) simply passes - nothing reports the stale row. The file itself documents exactly this rot happening manually: "MS_sin_phase_weak now PASSES ... Both removed from the xfail registry (2026-06-08)" (lines 947-950) - removals that relied on someone noticing. A silent-pass registry also masks the opposite drift: a cell can swap one documented failure class for a DIFFERENT, undocumented failure kind and still hit the same xfail row.
Proposal: at the end of test_create_keep_drop_broad / _nsweep add: `if not failures and (formula, n_used) in EXPECTED_XFAILS: pytest.fail(f"{formula} n={n}: registered expected-xfail now PASSES; remove the registry row (reason was: {EXPECTED_XFAILS[(formula,n_used)]})")`. Optionally tighten _maybe_xfail to match on failure KINDS: store (reason, frozenset_of_kinds) per row and only xfail when observed kinds are a subset; new kinds fail loudly.
Effort: S
VERDICT: CONFIRMED — _maybe_xfail defined at :956-962 and invoked only inside `if failures:` in both test_create_keep_drop_broad (:976-977) and test_create_keep_drop_nsweep (:1007-1008); the manual-rot comment is verbatim at :947-950 ("MS_sin_phase_weak now PASSES ... Both removed from the xfail registry (2026-06-08)"); EXPECTED_XFAILS keys are (formula, n) with bare reason strings (:923-953), so kind-drift inside a registered cell is indeed indistinguishable. Proposal coherent with the file structure.

### [gaps_fe_masking-06] keep/drop contract is blind to selected engineered features built ENTIRELY from noise operands
Severity: P2
Kind: gap-mask
Files: tests/feature_selection/test_mrmr_create_keep_drop.py:805-816; tests/feature_selection/test_mrmr_create_keep_drop.py:931,934
Evidence: the drop loop checks only `col in selected` for RAW column names. An engineered all-noise feature like add(sin(e1),abs(e2)) in the selection is counted as a failure ONLY if it also drags a raw noise operand into support - the xfail comments prove the mechanism is real ("a noise-only engineered col add(sin(e1),abs(e2)) drags e2 in", line 931) but a noise composite whose raw operands do NOT enter support passes the contract clean. The suite therefore under-reports FE noise admission - the exact quantity finding 01/02 care about.
Proposal: extend `_evaluate`: for every selected name containing "(", extract operand tokens with the existing bare-token regex generalized to the formula's column alphabet (the registry already knows each formula's noise column names from spec["drop"]); if ALL extracted raw tokens are in spec["drop"], append failure kind "admitted_noise_engineered". Calibrate EXPECTED_XFAILS once for cells where this legitimately fires today (e.g. ws2/F1 residuals) so the new kind is documented, not padded green.
Effort: S
VERDICT: CONFIRMED — drop loop verbatim at :806-815 (`for col in sorted(spec["drop"]): admitted = col in selected` — exact-string membership, so an engineered name like "add(sin(e1),abs(e2))" in `selected` never matches raw "e1"/"e2"); xfail quotes verbatim at :931 and :934. The blindness claim is logically airtight given the exact-match check. Proposal coherent (bare-token regex exists in sibling test files, spec["drop"] carries the noise alphabet).

### [gaps_fe_masking-07] Robust-prewarp (Huber-IRLS) recipe provenance is untested through the recipe layer; replay-on-contaminated-NEW-data behavior unpinned
Severity: P2
Kind: gap
Files: src/mlframe/feature_selection/filters/engineered_recipes/_recipe_unary_binary.py:245-252; src/mlframe/feature_selection/filters/hermite_fe/_hermite_prewarp.py:259-266; tests/feature_selection/test_robust_warp_fit_gate.py:192-207; tests/feature_selection/test_biz_val_robust_warp_fit.py (whole file)
Evidence: commit f98d1d9 states "MAD-anchored winsor bounds + a robust_fit flag are persisted flat in the recipe extra for provenance". The persistence code exists (`extra[f"prewarp_{_side}_winsor_lo"]` etc. at _recipe_unary_binary.py:251-252) but grep shows winsor/robust_fit asserted ONLY against the raw spec dict from fit_operand_prewarp - no test builds a unary_binary RECIPE with a robust prewarp side, so the extra-key persistence, the MappingProxyType frozen-extra interaction (test_engineered_recipe_extra_frozen.py contract), and pickle round-trip of the robust keys are all unexercised. Additionally _hermite_prewarp.py:262 documents "apply_operand_prewarp IGNORES these" (winsor recorded, not applied) - yet both replay tests evaluate only on rows from the SAME distribution (test_biz_value_mrmr_pair_prewarp.py:385 seed=909 same generator); no test pins what a transform-time spike (x far outside the fit range) produces through the polynomial.
Proposal: (1) build a unary_binary recipe via the production builder with a contaminated operand side (reuse `_spike` helper), assert extra contains prewarp_a_winsor_lo/hi + robust flag, pickle round-trip preserves them, and recipe.extra mutation raises (frozen contract); (2) replay parity on NEW data containing spikes at 20x IQR: assert apply_recipe output is finite (nan_to_num scrub at _recipe_unary_binary.py:120 catches inf) and assert the non-spiked rows are bit-equal to the clean-replay values - pinning the documented "winsor recorded, not applied" contract so a future change to APPLY winsor at replay must consciously update the test; (3) one e2e: MRMR fit on a fixture with one contaminated signal operand -> a selected engineered feature's recipe carries the robust keys.
Effort: M
VERDICT: CONFIRMED — persistence block verified at _recipe_unary_binary.py:~244-252 (`if _spec.get("robust_fit"): extra[f"prewarp_{_side}_robust_fit"]=True; ...winsor_lo/hi`); "apply_operand_prewarp ignores these" verbatim at _hermite_prewarp.py:~262 ("Replay itself is closed-form on ``coef`` (apply_operand_prewarp ignores these)"); test_robust_warp_fit_gate.py:192-207 asserts robust_fit/winsor only on the raw spec dict; independent grep across tests/ for prewarp_a_winsor / prewarp_b_winsor / recipe-extra robust keys finds ZERO assertions (robust_fit asserted only at test_biz_val_robust_warp_fit.py:96,132 — again raw spec level); pair_prewarp replay uses _make_poly(seed=909), same generator/distribution (:385). test_engineered_recipe_extra_frozen.py exists; nan_to_num scrub verified at _recipe_unary_binary.py:~120. All claims and the proposal check out.

### [gaps_fe_masking-08] prefer-engineered tie-break has no large-n / raw-should-win calibration test
Severity: P2
Kind: gap
Files: tests/feature_selection/test_biz_value_mrmr_prefer_engineered.py:34-52 (n=1500, all 3 tests)
Evidence: the whole file tests one direction - even-symmetric y where the engineered He2 MUST win - at n=1500 only. There is no control where the RAW column should win the near-tie (monotone target, large n where MI bias shrinks and raw is the honest representation), and no test of the `prefer_engineered_rel_eps` boundary the docstring itself names as the falsifiability knob ("set prefer_engineered_rel_eps=0.0 and the AUC assertion drops"). The project memory for this exact gate family says: calibrate so large-n recovery is untouched. A future eps inflation (or a biased engineered-MI estimate) that starts swapping raw winners for engineered surrogates at large n would pass this file.
Proposal: add two tests to the same file: (a) raw-wins control: y = x1 + 0.2*noise (strictly monotone), n=50_000, same _mrmr_hybrid_kw(); assert "x1" in support and NO "x1__He*" column present (engineered adds nothing on a monotone target; the tie-break must not fire); mark slow, fast-mode n=8000. (b) eps boundary: with prefer_engineered_rel_eps=0.0 on the even-symmetric fixture assert raw x1 wins (legacy behavior reachable), with the default eps assert x1__He2 wins - pinning both sides of the knob.
Effort: S
VERDICT: CORRECTED — gap verified: the file has exactly 3 tests, fixture n=1500, all one direction; the docstring names the knob (verbatim: "delete the tie-break (or set ``prefer_engineered_rel_eps=0.0``) and the AUC assertion drops to ~0.5"); independent grep confirms this is the ONLY test file mentioning prefer_engineered and no raw-wins/large-n control exists anywhere. CORRECTION on proposal (b) coherence: `prefer_engineered_rel_eps` is NOT an MRMR constructor parameter — it is a screen_predictors() parameter (default 0.15 at _screen_predictors.py:174, forwarded to the confirm-ctx at :560), and the screen_predictors call in _mrmr_fit_impl.py:5639 does NOT pass it (so the 0.15 function default always applies and `MRMR(prefer_engineered_rel_eps=0.0)` would be rejected by the sklearn-style ctor). The test-file docstring's falsifiability claim itself references an unexposed knob. Proposal (b) requires first threading the param through the MRMR ctor (or testing at the screen_predictors level); proposal (a) is implementable as written.

### [gaps_fe_masking-09] No pandas nullable-dtype (Int64/Float64, pd.NA) coverage anywhere in the FE test surface
Severity: P2
Kind: gap
Files: tests/feature_selection/test_group_aware.py, tests/feature_selection/test_pre_screen.py, tests/feature_selection/test_wrappers_audit_p1_extra.py (only hits for nullable dtypes - none are FE)
Evidence: grep for Int64Dtype / astype("Int64") / pd.NA across tests/feature_selection returns 3 files, none touching the FE pair search, recipes, or transform replay. Object-dtype (test_polynom_pair_fe_object_dtype.py) and narrow int codes (test_fe_narrow_code_dtype.py) are covered, but a nullable-backed column converts via .to_numpy() to OBJECT dtype with pd.NA sentinels - a classic silent-coercion bug class (memory: silent numeric coercion checklist) that would either crash the njit kernels or silently degrade through an object->float64 cast with NaN conversion differences between fit and replay.
Proposal: parametrized test over dtype in {"Int64", "Float64"}: take the canonical a**2/b fixture at n=5000, cast a/b/c/d via .astype(dtype) and inject 1% pd.NA; assert (1) MRMR(verbose=0).fit completes, (2) the (a,b) signal pair is still recovered, (3) transform on a nullable-dtype test frame returns float columns with the same NaN-handling as the float64 path (compare against the float64-fit baseline selection set), (4) recipes replay (apply_recipe) accepts the nullable frame. If the prod path crashes, that is a prod fix first (fix-on-the-spot rule), with this test as its regression sensor.
Effort: M
VERDICT: CONFIRMED — independent grep (Int64|Float64|pd\.NA|Int64Dtype|nullable) over tests/feature_selection returns exactly the 3 named files (pre_screen: constant-Int64 drop + dtype preservation at :95-101,188-194; group_aware; wrappers_audit_p1_extra) — none FE. A second sweep with different keywords (convert_dtypes / BooleanDtype / pd.array) surfaced only test_orth_fe_numeric_cols.py, which uses pd.array(dtype="bool") (numpy bool, NOT nullable boolean) — no refutation. Both named coverage files (test_polynom_pair_fe_object_dtype.py, test_fe_narrow_code_dtype.py) exist. The .to_numpy()→object-with-pd.NA claim is accurate for parameterless to_numpy() on nullable-backed Series.

### [gaps_fe_masking-10] golden/ snapshot machinery is dead: capture path has a NameError typo, the comparer test was deleted, no test loads the snapshots, and the FE scenario was never captured
Severity: P2
Kind: integration
Files: tests/feature_selection/golden/_capture.py:79; tests/feature_selection/golden/__init__.py:14-16; tests/feature_selection/golden/capture_baseline.py:31-37,104; tests/feature_selection/golden/pre_refactor/ + intermediate/ (4 JSONs each)
Evidence: _capture.py:79 reads `path.write_text(ororjson.dumps(snap, ...).decode().decode(), ...)` - `ororjson` is an undefined name and the double .decode() would fail anyway, so save_snapshot raises NameError on first call: nobody has successfully re-captured since the typo landed. golden/__init__.py points to "tests/feature_selection/test_screen_golden.py for the comparison machinery" - that file does not exist. grep shows NO test imports load_snapshot / assert_support_equivalent / assert_cached_close: the 8 snapshot JSONs are asserted by nothing, so any selection drift on those scenarios is invisible and any future "re-bless" is impossible. The single FE scenario (g_n5k_p100_fe, fe_max_steps=1, capture_baseline.py:37) has NO snapshot file in either tier - golden FE coverage is zero even on paper.
Proposal: decide explicitly: (a) if the etap-refactor golden tier is finished business, DELETE golden/ (and the __init__ pointer) so the dead machinery stops implying coverage; or (b) revive: fix the typo to `orjson.dumps(...).decode()`, add test_golden_capture_roundtrip.py that captures a tiny n=500 scenario into tmp_path and round-trips load_snapshot (smoke for the writer), add test_golden_screen.py that loads the intermediate snapshots and asserts assert_support_equivalent against a fresh fit (slow-marked), and capture the missing FE scenario. If reviving, ALSO extend capture to engineered-feature names + recipe (kind, src_names, binary/unary names) so FE drift is observable - support_set alone cannot see an engineered-form swap.
Proposal: Effort: M
VERDICT: CONFIRMED — every claim verified by direct inspection: _capture.py:79 verbatim `path.write_text(ororjson.dumps(snap, option=orjson.OPT_INDENT_2 | orjson.OPT_SORT_KEYS).decode().decode(), encoding="utf-8")` (undefined `ororjson` + double .decode()); golden/__init__.py:14-16 points to test_screen_golden.py which does NOT exist (ls confirmed); independent grep for load_snapshot/assert_support_equivalent/assert_cached_close consumers outside golden/ found only comment-level "golden" word matches in test_biz_value_mrmr_fe_canonical.py:211, layer31:27, conftest.py:118 — zero actual snapshot consumers; pre_refactor/ and intermediate/ each contain exactly 4 JSONs (g_n2k_p50_clf, g_n5k_p100_clf, g_n2k_p200_clf, g_n2k_p50_reg) with NO g_n5k_p100_fe.json in either tier despite GOLDEN_SCENARIOS listing it at capture_baseline.py:37 (fe_max_steps=1).

### [gaps_fe_masking-11] Stability vote structural blind spots are untested: single-survivor bypass and non-unary_binary exemption
Severity: P2
Kind: gap-mask
Files: tests/feature_selection/test_fe_stability_vote.py:119-148; src/mlframe/feature_selection/filters/_fe_stability_vote.py (confirm_recipes_cross_fold no-op contract)
Evidence: the no-op tests pin that the vote does NOTHING below 2 unary_binary recipes (test:131-137) - so a noise frame producing exactly ONE fold-quirk engineered survivor bypasses the consensus layer entirely, and recipes of every other kind (orth hermite-pair, hinge, wavelet, missingness, cat) are never voted regardless of count. Both exemptions are design decisions but neither has a test on the CONSEQUENCE side: the noise-survivor-reduction biz_value test (lines 172-192) aggregates 4 seeds and only asserts a strict total reduction, so a world where most noise frames yield 1 survivor each (all bypassing the vote) is indistinguishable from the vote working.
Proposal: (a) single-survivor pin: craft a noise frame + RELAXED gates tuned (calibrate seed) to yield exactly one unary_binary survivor without the vote; assert it ALSO survives with the vote on, with a comment marking this as the documented bypass contract - so a future fix that extends voting to 1 recipe must update the test consciously; (b) kind-exemption pin: fit a fixture where a hinge or hermite-pair recipe survives, assert the vote leaves non-unary_binary recipe counts untouched even at quorum=1.0/k=5 (would cull them if they were voted); (c) optional improvement ticket: vote single survivors too (k replays of 1 recipe is cheap), measured against the noise fixtures of finding 01.
Effort: S
VERDICT: CORRECTED — structural exemptions verified in source: _fe_stability_vote.py:171 filters `voted = {... if getattr(r, "kind", None) == "unary_binary"}` and :172 `if len(voted) < 2:` no-op; the no-op pins at test:119-148 verified (single unary_binary recipe → set() at :131-136); independent grep confirms test_fe_stability_vote.py is the ONLY test consumer of confirm_recipes_cross_fold/stability_vote, and no consequence-side test exists. CORRECTION on the biz_value characterization: "only asserts a strict total reduction" is incomplete — the test (:184-192) ALSO asserts `tot_off >= 3` (fixture sanity) AND `tot_on <= max(1, tot_off // 4)` (strong drop). Consequence: the extreme "most noise frames yield 1 survivor each (all bypassing)" world is NOT fully indistinguishable — if all frames yield single bypassing survivors, tot_on == tot_off and the test FAILS at `tot_on < tot_off`. The bypass is invisible only when bypassing singles fit inside the max(1, tot_off//4) budget alongside multi-survivor frames (e.g. survivor profile {1,1,1,9}→tot_on=3<=3 passes with 3 shipped fold-quirks). The gap and all three proposals remain valid with that narrower masking window.

### [gaps_fe_masking-12] Rung-schedule "deeper search at equal wall" biz_value test never asserts the needle; speed assert is wall-clock-tight on shared CI
Severity: P2
Kind: quality
Files: tests/feature_selection/test_fe_rung_schedule.py:227-255 (needle), 185-208 (wall-clock)
Evidence: test_bizvalue_equal_wall_deeper_needle's docstring promises the rung path "RECOVERED" the (c,d) second-signal needle that the flat top-K misses, but the only assertion is `rung_eng >= flat_eng` (count of engineered features, line 252) - a completely broken rung schedule that degenerates to the flat sweep over its larger pool passes, as does a rung that recovers a NOISE pair instead of (c,d). The headline capability (needle recovery) is unpinned. Separately test_bizvalue_speedup_on_wide_pool asserts `t_on <= t_off * 1.05` raw wall-clock (line 206) - on a contended CI box (the same RAM-tight host the file's own comments worry about) this is a flake source; the suite convention elsewhere is to compare work done, not single-shot wall.
Proposal: (a) strengthen the needle test: reuse the _bare_vars-style token check from the CMI-gate file and assert the rung selection contains an engineered feature with {"c","d"} operands (excluding a/b) while asserting the flat capped run does NOT (or drop the flat-side assert if seed-unstable; the rung-side presence is the needle claim); (b) for the wall-clock test, either widen to t_off*1.25 with a comment, or assert the countable proxy instead: info dict from apply_rung_schedule (kept-pair count < pool size) plus identical selection - the speed claim is already covered by the cProfile test.
Effort: S
VERDICT: CONFIRMED — needle test verified at :227-256: docstring promises "(c,d) second signal ... RECOVERED by the rung schedule" but the sole substantive assertion is `rung_eng >= flat_eng` (engineered-recipe COUNT, :252-255); no token/operand check on (c,d); the test ends there (next block is the cProfile section). Wall-clock assert `t_on <= t_off * 1.05` verified at :206-208 (full test name: test_bizvalue_speedup_on_wide_pool_at_identical_selection — the finding's shortened name is the only inaccuracy, trivial). Proposal coherent: apply_rung_schedule exists at _fe_rung_schedule.py:144 and populates an info dict (keep_frac/applied/n_kept at :177,207,212-213), so the countable-proxy alternative is implementable.

### [gaps_fe_masking-13] Adaptive-FE usability gate (anti-hijack) tested only at helper level - no MRMR-level e2e that a monotone/heavy-tail column stays raw
Severity: P2
Kind: gap-mask
Files: tests/feature_selection/test_adaptive_fe_usability_gate.py (whole file, 44 LOC); src/mlframe/feature_selection/filters/_orthogonal_univariate_fe (gate wiring)
Evidence: the file's docstring describes the regression ("adaptive Fourier/chirp operators were hijacking selection on linear / monotone / heavy-tailed / leak columns -- emitting __sin/__cos/__qcos columns that REPLACED the raw signal in support_", naming cauchy_sig) but all three tests call only the private helper `_heldout_smooth_r2` against `_ADAPTIVE_FE_RAW_USABILITY_CAP`. The fourier/chirp biz_value files cover the POSITIVE direction (oscillation -> adaptive column in support) and pure noise, but grep finds no e2e where a strong monotone or Cauchy-monotone column goes through MRMR.fit and the test asserts NO __sin/__cos/__qcos child enters support_ while the raw column survives. If the gate wiring regresses (cap comparison inverted, gate skipped on a refactor) the helper tests stay green and the hijack returns silently - exactly the masked-failure shape this audit hunts.
Proposal: add to test_adaptive_fe_usability_gate.py an e2e pair (slow-marked, n=20_000, seeded): (a) y = 2*x_mono + noise with x_mono = rng.standard_normal -> fit default MRMR -> assert "x_mono" in support and no selected name matches re.compile(r"__(q?sin|q?cos)\d*$") over x_mono; (b) the docstring's own cauchy fixture: x = rng.standard_cauchy, y = (x > median) -> same assertions. Both should fail on pre-gate code (verifiable via MLFRAME env or by setting the cap to 0 in a monkeypatch control leg, which doubles as falsifiability proof).
Effort: M
VERDICT: CONFIRMED — file is exactly 44 lines with exactly 3 tests, all calling only _heldout_smooth_r2 vs _ADAPTIVE_FE_RAW_USABILITY_CAP (imports at :13-17); docstring quote verbatim (":3-9, 'were hijacking selection on linear / monotone / heavy-tailed / leak columns -- emitting ``__sin``/``__cos``/``__qcos`` columns that replaced the raw signal in ``support_``'"); cauchy_sig named at :33-35. Gate wiring located at _orthogonal_univariate_fe/_orth_extra_basis_fe.py:634-637 (`_adaptive_fe_ok = _heldout_smooth_r2(x, _y_adapt) < _ADAPTIVE_FE_RAW_USABILITY_CAP`, cap=0.5 at :375) — a single un-tested comparison, exactly the inversion-fragile shape claimed. Independent refutation attempt across 15 hijack/__sin/__cos/cauchy-matching test files found NO monotone/Cauchy anti-hijack e2e: layer10 is the high-card cat hijack (different mechanism), layer20 is high-dim noise, layer11 explicitly TOLERATES x1__sin children under outlier injection, layer104 is rare-category vs fourier competition, adaptive fourier/chirp files cover positive direction + pure noise only. Claim survives.

### [gaps_fe_masking-14] E2E replay parity for unary_binary engineered columns is correlation-based, never exact - a systematic offset/scale/edge bug at transform would pass
Severity: P2
Kind: quality
Files: tests/feature_selection/test_biz_value_mrmr_fe_canonical.py:143-146 (|rho| > 0.5); tests/feature_selection/test_biz_value_mrmr_pair_prewarp.py:404-408 (|corr| > 0.999); tests/feature_selection/test_mrmr_fe_composite_feedforward.py:222-227 (spearman >= 0.95)
Evidence: all three MRMR-level replay checks for fit-produced unary_binary recipes compare via correlation against the true formula or against apply_recipe. By contrast the hermite-pair recipe family pins assert_allclose(atol=1e-9) including unseen rows and pickle (test_hermite_pair_recipe.py:103-150), and hinge/wavelet pin "bit_exact" replay. |rho|>0.5 (canonical:144) tolerates near-arbitrary distortion; even |corr|>0.999 (prewarp:405) tolerates a constant offset, a scale factor, or wrong quantile edges - the exact bug class the Wave 9.1 fit-time-edges fix (referenced at _recipe_unary_binary.py:125-128) existed to kill. Since transform() and apply_recipe() should execute the SAME recipe code path, the correlation tolerance is hiding either (a) nothing - in which case exact equality is free - or (b) a real divergence worth surfacing.
Proposal: in test_prewarp_recipe_replay_is_deterministic_and_leak_free replace the |corr|>0.999 check with np.testing.assert_allclose(direct, Xt1[:, i], rtol=0, atol=0) (both sides are the recipe path; bit-equality should hold) - if it fails, that failure is the finding and must be root-caused, not re-tolerated. In the canonical replay test keep the |rho| formula check (it compares against the TRUE math, where the recipe's quantization legitimately decorrelates) but ADD an exact transform-vs-apply_recipe assertion per engineered column, mirroring the hermite-pair standard.
Effort: S
VERDICT: CONFIRMED — all three tolerance sites verified: canonical `abs(rho) > 0.5` at test_biz_value_mrmr_fe_canonical.py:~144-146 (against true log(c)*sin(d)); prewarp `r > 0.999` transform-vs-apply_recipe at test_biz_value_mrmr_pair_prewarp.py:~405-409; feedforward `abs(rho) >= 0.95` spearman vs true signal at test_mrmr_fe_composite_feedforward.py:~223-227. Contrast standard verified: test_hermite_pair_recipe.py asserts assert_allclose atol=1e-9 (:114,:131) + pickle round-trip atol=1e-12 (:138-153). Wave 9.1 fit-time-edges comment verified at _recipe_unary_binary.py:~124-130. The same-code-path premise is sound: _mrmr_fit_impl's own comment states engineered columns are appended in transform() via _append_engineered using self._engineered_recipes_ — the same recipes apply_recipe replays — so exact equality on the prewarp check is a reasonable demand (and the proposal correctly keeps the |rho| check where the comparison target is the true math, not the recipe path).

### [gaps_fe_masking-15] Fit-produced recipe-building test is vacuous when FE finds nothing (tolerated-empty assertion)
Severity: LOW
Kind: quality
Files: tests/feature_selection/test_mrmr_engineered_replay.py:135-147
Evidence: test_fe_max_steps_2_records_recipe_for_selected_engineered loops `for r in recipes:` after the comment "regardless of whether the synergy was actually found in this small fixture -- some FE runs converge without picking the engineered col". If discovery yields zero recipes the body never runs and the test passes asserting only isinstance(recipes, list) - the file's only fit-level recipe-construction check is conditionally empty. The contract IS covered elsewhere at 20k rows (test_biz_value_mrmr_fe_canonical.py:112-114 asserts recipe_names non-empty), so severity is LOW, but inside THIS file the test name promises more than it pins.
Evidence note: n=200 fixture, fe presets medium - genuinely flaky discovery territory, which is why the tolerance was added.
Proposal: make the precondition explicit and falsifiable: skip-or-strengthen pattern - `if not recipes: pytest.skip("FE found no synergy at n=200; recipe-construction contract pinned at n=20k in test_biz_value_mrmr_fe_canonical")` so the vacuous-green path is visible in reports, or raise n to 2000 with a deterministic seed where discovery is stable and assert recipes non-empty.
Effort: S
VERDICT: CONFIRMED — test verified at test_mrmr_engineered_replay.py:~116-147: comment verbatim ("regardless of whether the synergy was actually found in this small fixture -- some FE runs converge without picking the engineered col"), `assert isinstance(recipes, list)` followed by the conditionally-empty `for r in recipes:` loop; fixture n_train=200 confirmed (multiplicative_synergy_train_test at :35-39, n_train, n_test = 200, 80); cross-reference accurate — test_biz_value_mrmr_fe_canonical.py:113-114 asserts `recipe_names` non-empty ("engineered recipes empty despite engineered features").

### [gaps_fe_masking-16] Dead GPU binary path uses raw a/b division - CPU/GPU div semantics will diverge the day it is wired
Severity: LOW
Kind: integration
Files: src/mlframe/feature_selection/filters/feature_engineering.py:503-504 (raw `a_gpu / b_gpu`), 649-656 (_safe_div with sign-stable eps); tests/test_meta/test_dead_helpers.py:114-115 (allowlisted dead)
Evidence: the CPU binary registry maps "div" to _safe_div (eps-floored, returns huge-but-finite on zero denominators - covered by test_div_handles_divide_by_zero in test_biz_value_fe_presets.py:75-81). apply_gpu_binary_batched returns raw `a_gpu / b_gpu` -> +/-inf on zero denominators, later scrubbed to 0.0 by nan_to_num - a DIFFERENT value than CPU's huge-finite result, which can flip discretization bins and hence MI/selection. The helper is currently allowlisted dead, so no live bug - but the keep-all-kernels culture means it is a wiring trap with no parity test guarding it.
Proposal: cheapest now (per cheap-now rule): change line 503-504 to mirror _safe_div on GPU (`a_gpu / (b_gpu + cp.sign(b_gpu) * 1e-9 + 1e-9)`) and add a CPU/GPU binary-op parity test (skippable without cupy) over all gpu_compatible_binary_names() on inputs containing exact zeros and denormals: assert allclose(cpu_func(a,b), cp.asnumpy(gpu(a,b)), rtol=1e-6) - so whoever un-deadens the helper inherits a green parity gate instead of a silent selection divergence.
Effort: S
VERDICT: CONFIRMED — raw `a_gpu / b_gpu` verified at feature_engineering.py:503-504; _safe_div with sign-stable eps verified at :649-656 (`return x / (y + np.sign(y) * eps + eps)`, eps=1e-9); CPU registry maps `"div": _safe_div` at :676; both apply_gpu_unary_batched and apply_gpu_binary_batched on the dead-helper allowlist at test_dead_helpers.py:114-115; test_div_handles_divide_by_zero verified at test_biz_value_fe_presets.py:75-81 (asserts all-finite on /0); gpu_compatible_binary_names() exists at feature_engineering.py:399, so the proposed parity sweep is implementable. The divergence reasoning (inf→nan_to_num→0.0 vs CPU huge-finite, bin-flip risk) is sound.

### [gaps_fe_masking-17] create_keep_drop ledger writes to hardcoded D:/Temp with double silent except
Severity: LOW
Kind: quality
Files: tests/feature_selection/test_mrmr_create_keep_drop.py:1016-1034
Evidence: the session-teardown fixture writes the full pass/fail ledger to the hardcoded path `D:/Temp/ckd_suite_ledger.json`, wrapped in try/except Exception/pass twice (orjson then json fallback, both swallowed). On any box without a D: drive (or with D:/Temp absent) the ledger silently vanishes - and the suite's own design comment says the ledger exists so "the harness has the complete pass/fail data even when pytest exits non-zero". Project convention (profiling/artifacts rule) is in-repo artifact paths, never D:/Temp.
Proposal: write to an env-overridable in-repo default: `os.environ.get("MLFRAME_CKD_LEDGER", os.path.join(os.path.dirname(__file__), "_artifacts", "ckd_suite_ledger.json"))`, create the dir, and on failure emit one warnings.warn (not silence) so a lost ledger is at least visible in the run log. Keep orjson-first (project rule) with the json fallback.
Effort: S
VERDICT: CONFIRMED — fixture verified verbatim at test_mrmr_create_keep_drop.py:1016-1034: hardcoded `r"D:/Temp/ckd_suite_ledger.json"` in BOTH branches (:1025, :1031), outer try/except Exception with bare-pass inner except (:1027-1034), design-comment quote verbatim at :1018-1019 ("the harness has the complete pass/fail data even when pytest exits non-zero"). Matches the CLAUDE.md profiling-artifact convention ("never to D:/Temp").

### [gaps_fe_masking-18] Golden snapshots capture support_set + MI caches only - engineered-FE drift is invisible to the golden tier BY DESIGN
Severity: LOW
Kind: gap
Files: tests/feature_selection/golden/_capture.py:31-73; tests/feature_selection/golden/capture_baseline.py:31-37
Evidence: capture_pre_refactor/capture_intermediate persist support_set, n_features, cache sizes, and keyed MI values. No engineered-feature names, no recipe descriptors, no fe_provenance digest. Even if finding 10's revival lands, a refactor that silently changes WHICH engineered form is built (mul(log(c),sin(d)) -> a c-only surrogate - the exact regression the composite strict-pin hunts) while keeping the same support indices would re-bless clean. Four of five GOLDEN_SCENARIOS run fe_max_steps=0, so the tier was scoped to selector-core drift only; with FE now the dominant behavior surface (fe_max_steps=2 default), the golden contract no longer covers what changes most.
Proposal: when reviving golden (finding 10b), extend the capture dict with: sorted engineered names from _engineered_features_, per-recipe tuple (kind, src_names, unary_a, unary_b, binary, quantization_nbins) hashed via orjson.dumps(..., OPT_SORT_KEYS) (sort-keys rule), and the fe_provenance_ support_rank ordering; comparison uses exact equality on the recipe-descriptor list with the tied-score comparator reserved for support indices only. Add 2 FE-on scenarios (the canonical F1 at n=5000 and a noise frame) to GOLDEN_SCENARIOS.
Effort: M
VERDICT: CONFIRMED — capture dict contents verified at _capture.py:29-73: capture_pre_refactor returns {scenario, seed, support_set, n_features_in, n_features_selected, cache_sizes}; capture_intermediate adds keyed cached_MIs/cached_cond_MIs + expected_gains; NO engineered names / recipes / fe_provenance anywhere in either dict. GOLDEN_SCENARIOS verified at capture_baseline.py:32-38: exactly 4 of 5 scenarios have fe_max_steps=0, the fifth (g_n5k_p100_fe) fe_max_steps=1 and was never captured (see finding 10). Proposal coherent with available MRMR attributes (_engineered_features_, _engineered_recipes_, fe_provenance_ all exist in the fit impl).

## Summary

| Severity | Count |
| --- | --- |
| P0 | 0 |
| P1 | 4 |
| P2 | 10 |
| LOW | 4 |
| Total | 18 |

Verdict: the per-mechanism FE test coverage is unusually deep (every basis family ships noise controls, bit-exact replay, default-on pins, and the create_keep_drop registry is an honest adversarial bench with verified per-cell xfail causes), but the suite is weakest exactly at the JOINTS: nothing tests the shipped default pipeline on a null dataset (01), the fe_max_steps=2 default flip is self-declared "[validation pending]" and the registry was re-pinned away from it (02), the default-ON accuracy gate has zero direct tests plus a fail-open/fail-closed contradiction (03), and the recipe-less-drop transform branch that the new default makes reachable is untested (04). A second theme is silent staleness: non-strict xfail registry (05), dead golden machinery with a NameError in its writer (10, 18), and correlation-based replay parity where exactness is achievable (14). All proposals are behavioral, seeded, fast-mode aware, and include the discriminating dataset recipes.

## Verification summary

Verified independently against source + tests on 2026-06-10 (read every cited file:line; ran fresh greps with alternate keywords for every "no test covers X" claim; opened all referenced src modules; checked commit 7d70af60 via git log/show).

| Verdict | Count | Findings |
| --- | --- | --- |
| CONFIRMED | 13 | 03, 05, 06, 07, 09, 10, 12, 13, 14, 15, 16, 17, 18 |
| CORRECTED | 5 | 01, 02, 04, 08, 11 |
| REFUTED | 0 | — |

The 3 most load-bearing corrections:
1. **[01]** An all-noise near-default e2e DOES exist — test_biz_value_mrmr_quality_metrics.py:307-336 (TestFalsePositiveRate) fits MRMR(verbose=0, min_features_fallback=0) on a 10-col pure-noise frame — so the finding's absolute "NO test" premise is wrong; the real gap is its weakness (n=800, ceiling assert `n_selected < 10` only, fallback escape, no engineered-recipes check). The proposed FWER sensor is still warranted but must be framed as strengthening/superseding the existing test.
2. **[08]** The proposal's eps-boundary leg is not currently implementable as written: `prefer_engineered_rel_eps` is a screen_predictors() parameter (default 0.15, _screen_predictors.py:174) NOT exposed on the MRMR constructor and not passed at the _mrmr_fit_impl.py:5639 call site — the test docstring's own falsifiability claim references an unexposed knob. Threading the param (or testing at screen_predictors level) is a prerequisite.
3. **[04]** "Reachable in production at the fe_max_steps=2 default" is overstated: the 2026-06-08 feed-forward fix gives step-2 engineered×engineered composites nested_parent_a/b recipes (test_mrmr_fe_composite_feedforward.py:17-19), so the recipe-less DROP branch fires at the default only on recipe-construction failure — the untested-branch gap stands, but the e2e must force the failure path (fe_max_steps=3 or monkeypatched recipe builder), as the proposal's option (b) already anticipates.
