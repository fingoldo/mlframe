# FS test-suite audit — KEY=test_code_quality (2026-06-10)

Mission: quality + efficiency of the FS/FE test CODE itself (not coverage). Repo: tests/feature_selection (465 files, ~126k LOC; 97 `test_biz_value_mrmr_layerNN.py` files = 47,923 LOC, 1,095 test funcs, 509 `.fit(` calls) + the 4 training-level FS test files.

Sampling statement (for the verifier): mapped the whole dir with greps (def-counts, helper-def dedup hash pass, marker/fixture/rng/json/sleep/xfail/path greps); deep-read in full: `conftest.py` (412 LOC), `_biz_val_synth.py` (head + property-test tail), layer54 (full structure + body 160-270), layer31:340-403, layer39:300-365, layer49:255-275, layer52 head, layer96:130-200, `test_wrappers_default_args.py:120-165`, `test_cached_cond_mi_nexisting_collision.py:1-60`, `test_mrmr_create_keep_drop.py` (ledger/xfail/checkpoint sections), `test_perf_regression.py` structure, `test_bc_caller_rate_check.py:70-85`. Ran the one allowed command: `pytest tests/feature_selection --collect-only -q --no-cov -p no:randomly` -> **6,619 tests collected in 6.89s, zero collection errors**. No pytest duration cache found (`.pytest_cache/v/cache` has only `lastfailed` with 1 entry, `nodeids`). Skimmed `audit_disposition.md` + sibling reports (`param_axes.md`, `shared_lift.md`, `gaps_selection_masking.md`); known-blocked items (orth-basis recipe parity, GPU SU gate, shap binning) NOT re-reported; where a finding is adjacent to a sibling's, the overlap is flagged in-place and this report keeps only the code-quality angle with distinct evidence.

---

### [test_code_quality-01] ~6.6k LOC of copy-pasted dataset-builder/helper code across the 97 layer files; 1,488 LOC are byte-identical copies; shared `_biz_val_synth.py` is imported by only 10/465 files
Severity: P1
Kind: share
Files: tests/feature_selection/test_biz_value_mrmr_layer*.py (85 of 97 files define module-level `def _` helpers); tests/feature_selection/_biz_val_synth.py (509 LOC, the designated home); examples: test_biz_value_mrmr_layer100.py:135-149 (`_make_mrmr`), test_biz_value_mrmr_layer56.py / layer57.py / layer58.py (`_build_linear`, bodies identical to the comment).
Evidence (all verified by grep/hash pass this session):
- `def _make_mrmr(**overrides):` defined **48 times**; an md5 pass over normalized bodies shows 45/48 fall into just 4 equivalence classes (23+12+7+3 byte-identical copies).
- `def _build_linear(seed...)` defined **23 times** (17 with identical 5-col x1/x2/noise_a-c body, e.g. layer56/57/58 differ only in docstring wording).
- Total: **264** `def _build_/_make_/_gen_` defs, only **177 unique signatures**; helper LOC across layer files ~**6,592**; exact-duplicate groups: **33**, redundant byte-identical LOC (copies beyond the first): **1,488**. Other recurring copies: `_train_holdout_split` (7), `_logreg_auc` (4), `_import_plug_in_fe` (9, two variants — layer65 vs layer66 differ only in returning 3 vs 2 functions), `_fit_quiet` (5), `_quantile_bin_local`/`_mi_one` (3 each, reimplementing MI-by-binning per file).
- Only **10/465** files import `_biz_val_synth`; `downstream_auc`/`support_indices` there already subsume the per-file `_logreg_auc`/`_downstream_auc`/`_score_classification` copies.
Proposal: (a) move the 4 `_make_mrmr` equivalence classes into `conftest.py` as one factory: `def make_fast_mrmr(*, fe=False, dcd=False, **overrides) -> MRMR` with the layer100 kwargs dict as base preset and the layer14 docstring rationale; lazy-import MRMR inside the factory (preserves the deliberate import-cost avoidance documented at conftest.py:34-38). (b) move `_build_linear`, `_build_quadratic_classif`, `_build_redundant_multi`, `_build_xor_redundant`, `_train_holdout_split`, `_logreg_auc`, `_quantile_bin_local`, `_mi_one` into `_biz_val_synth.py` with explicit kwargs covering the observed per-file deltas (n, n_noise, coef of x2). (c) mechanical migration: per layer file, replace the local def with `from tests.feature_selection._biz_val_synth import ...` ONLY when the md5-normalized body matches the canonical one; files with genuinely divergent bodies keep theirs (AST-gate per the repo's monolith-split rule). Estimated dedup win: 1,488 LOC guaranteed (byte-identical) + ~1,500-2,500 LOC near-duplicate = **~3,000-4,000 LOC**, and one canonical place to fix dataset bugs. Effort: L (mechanical but 85 files; do it folder-by-folder with the layer-theme batches from finding 16).

### [test_code_quality-02] Zero fast-mode / slow-marker adoption in the 97 layer files: 1,095 tests and 509 MRMR fits always run at full weight
Severity: P1
Kind: quality
Files: tests/feature_selection/test_biz_value_mrmr_layer*.py (grep `pytest.mark.slow` -> **0/97** files; grep `fast_subset|IS_FAST_MODE|is_fast_mode|MLFRAME_FAST` -> **2/97** files); conftest.py:101-139 (fast_subset + slow-skip hook exist and work); pyproject.toml markers list (`slow`, `fast` both registered).
Evidence: the conftest ships full infra — `fast_subset()` (conftest.py:101), `slow` marker auto-skip under `MLFRAME_FAST=1` (conftest.py:129-139), and a repo-wide `fast` marker ("Every new ML feature ships with a representative subset marked @pytest.mark.fast", pyproject.toml). The layer suite (47.9k LOC, 509 fits at n=300-3000, multi-seed loops e.g. layer96:182 five-seed loop) uses none of it: an `MLFRAME_FAST=1` run still executes every layer fit. Only 39/465 FS files use `mark.fast` at all.
Proposal: per layer file, (a) add `pytest.mark.slow` to every class that does >2 fits or any n>=1500 multi-seed sweep (mechanical: grep `.fit(` count per class); (b) pick ONE representative contract test per file (the headline biz-value assert, usually "Contract 1") and tag it `@pytest.mark.fast`. Acceptance: `MLFRAME_FAST=1 pytest tests/feature_selection -m "not slow"` keeps >=1 behavioral assert per layer theme while cutting layer-suite fit count from 509 to <150; `-m fast` gives a <60s smoke. No assertion deletion — only markers. Effort: M.

### [test_code_quality-03] `gpu` marker missing on 11/12 gpu-named FS test files AND on 4 GPU-using non-gpu-named files — the documented `--ignore-glob="*gpu*"` crash workaround leaks
Severity: P1
Kind: integration
Files: tests/feature_selection/test_batch_mi_noise_gate_gpu.py, test_batch_pair_mi_gpu.py, test_biz_val_filters_gpu.py, test_friend_graph_gpu.py, test_gpu_cpu_threshold_parity.py, test_gpu_kernel_equivalence.py, test_gpu_pairs_module_split.py, test_shap_proxy_cluster_su_gpu.py, test_shap_proxy_gpu.py, test_shap_proxy_su_gpu_oom_gate.py, test_shap_proxy_treeshap_interactions_gpu.py (all: 0 `mark.gpu`); only test_gpu_coverage.py has it (14 uses). GPU-touching files WITHOUT "gpu" in the name: test_biz_value_mrmr_layer40.py (whole file is "VERIFY GPU PATH", uses `importorskip("cupy")`), test_kernel_tuning_cache_critic3.py, test_screen_predictors_preamble.py, test_shap_proxy_cluster.py.
Evidence: `gpu` marker is registered in pyproject.toml ("gpu: marks tests requiring GPU hardware"). The known full-suite mitigation (memory: friend_graph_gpu cupy hang + CatBoost CUDA OOM abort the whole suite; run with `CUDA_VISIBLE_DEVICES="" --ignore-glob="*gpu*"`) is filename-glob-based, so layer40 + the 3 other non-gpu-named GPU users are NOT excluded by it and can still native-AV the suite; conversely `-m "not gpu"` currently exempts almost nothing.
Proposal: add module-level `pytestmark = pytest.mark.gpu` to the 11 gpu-named files; in the 4 mixed files mark only the GPU classes/tests (layer40: the cupy-backed bit-equivalence classes; CPU-fallback contract tests stay unmarked since they must pass without cupy). Then the suite-crash mitigation becomes `-m "not gpu"` and stops depending on file naming. Behavioral check: `pytest tests/feature_selection -m gpu --collect-only -q` must list every test that imports/uses cupy (verify via grep cross-check). Effort: S.

### [test_code_quality-04] Redundant model retraining: per-test refits of identical configs where a class/module-scoped fitted fixture serves all asserts (layer49 already demonstrates the correct in-repo pattern)
Severity: P2
Kind: quality
Files: tests/feature_selection/test_biz_value_mrmr_layer54.py:199-258 (TestLayer54_C1/C2: 5 tests, each `_fast_mrmr().fit(X, y)` on `_simple_binary_frame(n=300-400)` differing only in seed 1/2/3/10/11 — the seed variation adds no discriminating power to "attr present / schema columns / non-empty / row-count" contract asserts); layer54 total: 21 `.fit(`; test_biz_value_mrmr_layer23.py: 21 fits; layer28/26: 16; layer43: 14; layer35/27: 13. Counter-example (the target pattern): test_biz_value_mrmr_layer49.py:258-263 — `@pytest.fixture(scope="class") def fits(self)` fits 3 modes ONCE, ~6 tests assert on them. Only 8/97 layer files use any fixture; only layer35/49 use class scope.
Evidence: quoted layer54 bodies above; 509 total fits across layers; conftest dataset fixtures are all function-scope (conftest.py:146-413 — zero `scope=` args).
Proposal: for each layer class whose tests assert on a fit RESULT (not on fit side-effects/state isolation), hoist one `scope="class"` fitted fixture (layer49 shape): `fits()` returns `(X, y, fitted)`; tests become pure asserts. Guard against cross-test mutation by returning the fitted estimator plus pre-extracted frozen views (e.g. `tuple(m.fe_provenance_.columns)`) where tests only read. Conservative scope: the ~15 files with >=12 fits first — cuts ~80-120 fits (~15-25% of layer wall) with zero assertion changes. Do NOT pool fits for tests asserting cache/state semantics (TestMRMRFitCache-style) — function scope is semantic there. Effort: M.

### [test_code_quality-05] Wall-clock perf assertions scattered through 49 regular test files, 42 of them with no slow/perf marker — flaky under contention and unskippable as a group
Severity: P2
Kind: quality
Files: 49 FS files grep `perf_counter` (25 timing-based asserts), e.g. tests/feature_selection/test_biz_value_mrmr_layer96.py:130-137 (`assert speedup >= 1.5` from single-shot naive-vs-cached timing at p=30 n=5000); test_biz_val_filters_permutation.py (BC 3-9x speedup asserts); test_biz_val_filters_gpu.py; layer30/31/35/39/40/50/51/52...; dedicated home exists: test_perf_regression.py (358 LOC, warmup + thresholds documented, `@pytest.mark.fast`/`@pytest.mark.gpu` used).
Evidence: 42/49 files have neither `mark.slow` nor any perf marker; the user's own environment notes transient paging/OOM under concurrent load (memory: retry-on-OOM rule) — single-shot wall-time ratios are exactly what flakes there. `benchmark` marker is already registered in pyproject.toml but unused in FS.
Proposal: (a) register/reuse `benchmark` marker and apply `pytestmark`-level or per-test marks to all 25 timing asserts so `-m "not benchmark"` gives a contention-safe run; (b) harden each ratio assert to median-of-3 with warmup (pattern already written down in test_perf_regression.py:16); (c) keep quantitative speedup floors (biz_value contract — do NOT delete), just make them deselectable and noise-robust. Effort: M.

### [test_code_quality-06] Hardcoded `D:/Temp/...` artifact paths in 3 sweep test files, written from a session-autouse fixture with double `except Exception: pass`
Severity: P2
Kind: quality
Files: tests/feature_selection/test_mrmr_create_keep_drop.py:76 (`_PROGRESS = r"D:/Temp/ckd_suite_progress.txt"`), :1025+:1031 (`D:/Temp/ckd_suite_ledger.json`, session-scope autouse `_dump_ledger`); test_mrmr_distribution_profiles.py:47, :356-361; test_mrmr_weak_f2_seed_stability.py:30 (docstring contract points at `D:/Temp/weak_f2_stability.json`).
Evidence: verbatim `with open(r"D:/Temp/ckd_suite_ledger.json", "wb")` inside `try/except Exception:` nested in another `except Exception: pass` — on any box without `D:/Temp` the ledger/liveness data the files' docstrings promise silently vanishes; the autouse session fixture also runs for every collection that touches the module. CLAUDE.md separately bans D:/Temp as an artifact home for repo tooling.
Proposal: one shared helper in conftest.py: `def artifact_path(name) -> Path` returning `Path(os.environ.get("MLFRAME_TEST_ARTIFACT_DIR", tempfile.gettempdir())) / name`, used by all three files; replace the silent double-swallow with `warnings.warn(f"ledger dump failed: {exc!r}")` so a broken dump is at least visible in `-W` runs. Keep orjson-primary/json-fallback write (correct per orjson rule). Effort: S.

### [test_code_quality-07] Source-presence sensor via `read_text` deliberately evades the `inspect.getsource` meta-test — string-position asserts on prod source instead of behavior
Severity: P2
Kind: quality
Files: tests/feature_selection/test_cached_cond_mi_nexisting_collision.py:40-60 (and the rest of `test_cache_raw_value_independent_of_nexisting`); meta-test it sidesteps: tests/test_meta/test_no_inspect_getsource.py:73.
Evidence: verbatim comment — "Source-presence sensor via file read (no ``inspect.getsource`` -- the meta-test ... enforces the behavioural-tests rule; reading the file directly carries the same signal without the AST-detected antipattern)." The test then asserts `src.find("cached_cond_MIs[key] = additional_knowledge")` comes before the second `additional_knowledge ** (nexisting + 1)` occurrence — a brittle text-order assert that breaks on any rename/refactor of evaluation.py while proving nothing about runtime values.
Proposal: replace with the behavioral discriminator the module docstring itself specifies: build a tiny deterministic dataset, call the Fleuret/CMIM path (or `evaluation.py`'s cache-mediated scorer directly) twice for the SAME (X, Z) with `nexisting=0` then `nexisting=2`, and assert `score(nexisting=2) == pytest.approx(score(nexisting=0) ** 3)` (raw cached value re-exponentiated per read). Pre-fix code returns the FIRST call's exponentiated value on the second lookup, so this fails on pre-fix and passes post-fix — a true regression sensor. Extend test_no_inspect_getsource.py to also flag `read_text` of `src/mlframe/**` from test files (allowlist: the sanctioned monolith-split facade-size sensors test_monolith_split_w11a/w12a_rfecv_fit.py, test_gpu_pairs_module_split.py, and the test-source-scanning anti-mask meta-tests like test_wrappers_default_args.py:127 which scan TEST files, not prod). Effort: S-M.

### [test_code_quality-08] conftest dataset fixtures are near-dead weight: 1 fixture has ZERO consumers, 8 have exactly 1 consumer file, while 4 per-file `small_clf_data` clones exist elsewhere
Severity: P2
Kind: share
Files: tests/feature_selection/conftest.py:402-412 (`known_mi_data` — 0 consumer files); conftest.py:219-238 (`high_dimensional_data` — 1), :275-399 (`synergistic_features_data`, `multiplicative_synergy_data`, `additive_synergy_data`, `nonlinear_transform_data`, `feature_engineering_example_data` — each 1 consumer: test_mrmr_feature_engineering.py); per-file duplicates: test_wrappers_default_args.py:60 + test_wrappers_phase4.py:17 + test_wrappers_phase4_n3_n6.py:34 (all `def small_clf_data`), test_selectors_shared.py:74 (`small_clf_problem`).
Evidence: consumer counts measured by grep this session (table in my working notes; `simple_classification_data` is the only well-shared one at 7 files). The three `small_clf_data` copies are file-local fixtures duplicating what conftest's `simple_classification_data` already provides (same make-classification-with-noise concept).
Proposal: (a) delete `known_mi_data` (dead) — or wire it into a real MI-estimator unit test if one of the MI files wants it (check test_biz_val_mi_estimators.py first; if unused there too, delete). (b) Promote ONE canonical `small_clf_data` to conftest (smallest of the four: n~120, p~8, seed pinned) and point the 4 per-file copies at it. (c) Leave the 1-consumer n=10_000 fixtures but move them INTO test_mrmr_feature_engineering.py (file-local fixtures for single-consumer data keep conftest lean and import cost down). Effort: S.

### [test_code_quality-09] `_biz_val_synth.py` ships ~100 LOC of dead "property tests": `_property_*` functions never called by anything, bodies are doctests ending in `# doctest: +SKIP`, and doctests are not enabled anyway
Severity: P2
Kind: quality
Files: tests/feature_selection/_biz_val_synth.py:411-509 (`_property_make_signal_plus_noise_structural`, `_property_make_3way_xor_structural`, `_property_make_correlated_redundant_structural`, `_property_make_as_df_roundtrip`, `integers_or_skip`); pyproject.toml addopts (no `--doctest-modules`).
Evidence: grep for the four names across all FS test files -> zero callers; each body is a docstring whose final line is `>>> _check()  # doctest: +SKIP` — triply dead (not a test name pytest collects, not invoked, doctest-skipped even if doctests ran). The generators' own header doctests (e.g. make_signal_plus_noise:42-49) are likewise never executed.
Proposal: create `tests/feature_selection/test_biz_val_synth_properties.py` with real `@given`-decorated hypothesis tests transplanted from those docstrings (they are already fully written — `max_examples=10, deadline=None`, structural shape/finite/binary-y asserts), plus one `pytest.importorskip("hypothesis")` guard per CI-optional-deps rule; delete the `_property_*` shells. Also add a single doctest-runner test for the generator examples: `def test_biz_val_synth_doctests(): assert doctest.testmod(_biz_val_synth).failed == 0`. Effort: S.

### [test_code_quality-10] `_biz_val_synth` imported under TWO module identities (`sys.path.insert` + bare name vs package path) — double module objects; layer39 also leaves `sys.path` mutated
Severity: P2
Kind: share
Files: tests/feature_selection/test_biz_val_cluster_aggregate.py:20-22 (`sys.path.insert(0, <dir of __file__>)` then `from _biz_val_synth import make_latent_reflections, ...`); every other consumer uses `from tests.feature_selection._biz_val_synth import ...` (e.g. test_biz_val_filters_mrmr.py:142); test_biz_value_mrmr_layer39.py:328-330 (`sys.path.insert(0, tests_dir)` with no removal).
Evidence: with both idioms live in one session, Python materializes `_biz_val_synth` AND `tests.feature_selection._biz_val_synth` as DISTINCT module objects (the canonical class-identity/pollution mechanism flagged in the repo's test-pollution rule). Today the module exports only functions+ndarray data so the blast radius is double execution + double memory, but any future module-level cache/registry/class added there silently splits. layer39's path insert persists for the whole session (unbalanced global mutation).
Proposal: in test_biz_val_cluster_aggregate.py delete lines 20-22's hack and use the package import like every sibling (the file already lives in the package; `tests/feature_selection/__init__.py` exists). In layer39, wrap the insert in try/finally removal, or drop it entirely — pytest's rootdir conftest already makes `tests.*` importable (that is exactly how the other 9 consumers work). Effort: S.

### [test_code_quality-11] Multi-seed `for seed in (...)` loops inside test bodies: first failing seed masks the rest and the loop defeats per-seed timing/selection
Severity: P2
Kind: param
Files: tests/feature_selection/test_biz_value_mrmr_layer96.py:150 (`for seed in (0, 7, 42)` — bit-equivalence over 3 seeds × n=4000 fits), :182 (`for seed in (1, 7, 13, 42, 101)` — 5 seeds × XOR scoring); grep `for seed in` across layers -> 6 sites total (others in layer13/…).
Evidence: quoted above; on a failure at seed 0 the remaining 2-4 seeds never run, so "stable across seeds" claims degrade to "first seed passes". (Adjacent to sibling param_axes-11/-12 which cover quantization-method and layer83 matrix loops — these 6 seed-loop sites are additional, distinct instances of the same class; per the grep-all-instances rule they should be fixed in the same pass.)
Proposal: `@pytest.mark.parametrize("seed", (0, 7, 42))` on the test (move the body's loop interior into the test body); where the loop wraps an expensive shared scorer call, keep one class-scope fixture computing per-seed results dict and parametrize the assert layer. Apply to all 6 sites in one pass + the sibling-flagged loops. Effort: S.

### [test_code_quality-15] `test_fs_selector_contract.py` converts ANY Pipeline-compat exception into a runtime xfail — a permanent escape hatch for a fixable framework bug class
Severity: P2
Kind: gap-mask
Files: tests/feature_selection/test_fs_selector_contract.py:237 (`pytest.xfail(f"{name}: not Pipeline-compatible yet ({type(exc).__name__}: {exc})")`); same shape at test_wrappers_sklearn_coverage_gaps.py:58+:69 (sparse-X xfail — that one cites a genuine unimplemented input type, acceptable).
Evidence: the :237 hatch fires on ANY exception type from ANY registered selector, so a tomorrow-regression (e.g. a renamed attr breaking `clone()` inside Pipeline) reports as xfail, not failure — exactly the "runtime xfail-on-exception" anti-pattern; `xfail_strict = true` in pyproject does not protect against imperative `pytest.xfail()` calls. Complementary to sibling gaps_selection_masking-15 (which covers the skip hatches in test_selectors_shared.py); this is the OTHER contract file and the xfail (not skip) variant.
Proposal: replace the blanket xfail with an explicit allowlist: `KNOWN_PIPELINE_INCOMPAT = {"BorutaShap": "no get_feature_names_out yet (issue #...)"}`; xfail only when `name in KNOWN_PIPELINE_INCOMPAT` AND the exception type matches the documented one; all other exceptions propagate as failures. Behavioral acceptance: temporarily breaking MRMR's `get_feature_names_out` must turn the contract test red, not xfailed. Effort: S.

### [test_code_quality-12] Bare `except Exception: pass` sites in FS tests swallow infrastructure failures
Severity: LOW
Kind: quality
Files: tests/feature_selection/test_bases_coverage.py:16-17 (module-level import probe); test_mrmr_create_keep_drop.py:84-85 (`_checkpoint` liveness writer — best-effort by design, but invisible when broken) and :1033-1034; test_mrmr_distribution_profiles.py:55-56 + :363-364; test_mrmr_fixes_p0_p1.py:192-193.
Evidence: grep output verified each site this session. None of these swallow ASSERTION failures (checked context), so severity stays LOW — but the ledger/checkpoint swallows (overlap with finding 06) mean liveness/result artifacts can break with zero signal, which the repo's own agent-heartbeat practice depends on.
Proposal: import probes -> `pytest.importorskip` (the repo's own CI convention); best-effort writers -> `except OSError: warnings.warn(...)` (narrow the type, surface once). Effort: S.

### [test_code_quality-13] Global `np.random.seed` + legacy global-state RNG calls in 8 FS test files instead of `default_rng`
Severity: LOW
Kind: quality
Files: tests/feature_selection/test_bc_caller_rate_check.py:77-82 (`np.random.seed(7)` then `np.random.randint/np.random.random`); test_discretization.py:36; test_fleuret.py; test_general_efs_relevancy.py; test_mrmr_fixes_p0_p1.py; test_permutation_coverage.py; test_pid_decomposition_validation.py (test_boruta_shap_rng_isolation.py is exempt — it deliberately TESTS global-RNG isolation).
Evidence: grep verified; all sites do seed first (no missing-seed bugs found — the suite is clean on that axis), but global-state seeding is order-fragile under pytest-randomly/xdist if any other test consumes draws between seed and use, and the layer files uniformly use `default_rng` (355 local-rng calls) so these 8 are the stragglers.
Proposal: mechanical migration to `rng = np.random.default_rng(<same seed>)` + method calls; where exact legacy draw values are load-bearing (pinned thresholds derived from the old MT19937 stream), re-derive the pinned constants once and note the new seed in the assert message. Effort: S.

### [test_code_quality-14] Function-scope n=10_000 conftest fixtures rebuilt per test; all conftest dataset fixtures lack scope tuning
Severity: LOW
Kind: quality
Files: tests/feature_selection/conftest.py:275-301 (`synergistic_features_data`, n=10_000), :369-399 (`feature_engineering_example_data`, n=10_000), :304-366 (n=5_000 trio) — all function scope; consumer: test_mrmr_feature_engineering.py (6 tests, 8 fixture references).
Evidence: conftest.py has zero `scope=` arguments on the 12 dataset fixtures (read in full). Rebuild cost is numpy-only (~ms each) so the wall impact is small — severity LOW, the real cost is the pattern being copied into new files.
Proposal: `@pytest.fixture(scope="module")` for the 5 large generators + return defensive read-only views (`X_df.copy()` is what function scope buys; instead set `arr.setflags(write=False)` on the underlying ndarray, or document the no-mutate contract) — combine with finding 08's relocation of single-consumer fixtures so scope and ownership are fixed in one edit. Effort: S.

### [test_code_quality-17] `tests/training/_fuzz_results.*.jsonl.bak` exist on disk (untracked) and `__pycache__` dirs sit in the test tree — confirm-and-fence hygiene
Severity: LOW
Kind: quality
Files: tests/training/_fuzz_results.prebughunt.jsonl.bak, tests/training/_fuzz_results.run571.jsonl.bak (on disk; `git ls-files` confirms NOT committed); tests/feature_selection/__pycache__/ (on disk, NOT committed; 0 `__pycache__` paths tracked anywhere).
Evidence: verified via `git ls-files` vs `ls` this session — repo tracking is clean; the risk is purely future accidental `git add -A` sweeping them in.
Proposal: add `*.bak` and `tests/**/__pycache__/` to .gitignore (check existing .gitignore first; `__pycache__/` is probably already there — add `*.bak` next to it); optionally relocate the two fuzz .bak snapshots into `tests/training/_fuzz_archive/` with a README line if they are still referenced by fuzz-resume tooling (grep `prebughunt|run571` in tests/training first — if zero refs, delete). Effort: S.

### [test_code_quality-16] Layer-file architecture verdict: keep the per-layer CONTRACT granularity, but consolidate the 97 files into ~12 themed modules with an AST-level assertion-inventory gate — never deleting an assertion
Severity: P1
Kind: quality
Files: tests/feature_selection/test_biz_value_mrmr_layer6..104.py (97 files, 47,923 LOC, 1,095 tests); LAYER_INDEX.md (theme map, "generated from the docstrings ... regenerate after adding a layer"); coupling constraints: test_biz_value_mrmr_layer39.py:300-365 (globs prior layer TEST modules, asserts `len(_LAYER_MODULES) >= 33` + imports each) and test_biz_value_mrmr_layer52.py:232-247 (roster of PROD modules, `>= 50` — unaffected by test-file renames).
Evidence: the accumulation is append-only by construction (~100 iterative campaigns); LAYER_INDEX.md themes cluster naturally: contracts/L6-20 (robustness axes), hybrid-orth FE/L21-32, encodings/L33-38, regression-roster/L39+52, DCD/L41-49, provenance+reporting/L50-54, per-mechanism FE/L55-80s, showdowns+state-of-union/L83+, cat-pair caching/L96, etc. No cross-layer imports exist (grep verified: zero `from test_biz_value_mrmr_layer` imports), so merging is mechanically safe except for layer39's test-module glob. Duplication findings 01/02/04 multiply per-file, so consolidation is also the enabler for those fixes. Verdict: the 97-file form is an artifact of the campaign process, not a designed architecture; but the CONTRACTS inside are the crown jewels — the merge must be assertion-lossless.
Proposal (migration plan, in order):
1. **Inventory gate first**: script `audit/fs_tests_audit_2026_06_10/_assertion_inventory.py` (new file, allowed location) — for each layer file, `ast.parse` and record (test function/method qualname, count of `ast.Assert` nodes, count of `pytest.fail/xfail/skip` calls, the assert-message string literals). Emit JSON. This is the before-snapshot.
2. **Merge by theme**: create `test_biz_value_mrmr_contracts_robustness.py` (L6-20), `..._fe_hybrid_orth.py` (L21-32), `..._fe_encodings.py` (L33-38), `..._dcd.py` (L41-49), `..._provenance.py` (L50-54), `..._fe_mechanisms.py` (L55-82), `..._showdowns.py` (L83+), `..._cat_pair.py` (L90s) etc. (~12 modules, each <=1k LOC per the module-size rule — split further if a theme exceeds it). Move test classes VERBATIM (class names already carry `TestLayer54_C1_...` provenance); de-dup only module-level helpers per finding 01. Explicitly forbidden: deleting/merging/weakening any assert; thresholds stay byte-identical.
3. **Re-run inventory** on the merged modules; gate: identical multiset of (qualname-suffix, assert-count, assert-message-literals). Any delta = abort.
4. **Update the roster sensors**: layer39's `_discover_prior_layer_modules` glob switches to the new `test_biz_value_mrmr_*.py` pattern with floor re-pinned to the new module count AND a new assert pinning the inventory JSON's total assertion count (>= the before-snapshot total) — converting the "file count" tripwire into an "assertion count" tripwire, which is the thing we actually care about. layer52's prod-module roster is untouched.
5. **Regenerate LAYER_INDEX.md** mapping layer-number -> new module::class so the historical campaign narrative stays greppable; keep the old layer numbers in class names forever.
6. Do the move with `git mv`-style adds in <=3-file batches (commit -o per batch per the concurrent-agents rule), running `--collect-only -q` after each batch (collected-test-count must stay 6,619 minus nothing).
Effort: L (but each batch is S and independently shippable; batches also carry findings 01/02/11 fixes for the files they touch).

---

## Summary

| Severity | Count |
|---|---|
| P0 | 0 |
| P1 | 4 (01, 02, 03, 16) |
| P2 | 9 (04, 05, 06, 07, 08, 09, 10, 11, 15) |
| LOW | 4 (12, 13, 14, 17) |
| **Total** | **17** |

Overall verdict: the FS test suite is behaviorally rich and collection-clean (6,619 tests, 0 errors), with strong per-contract assertion discipline and good in-repo exemplars (layer49 class-scoped fits, test_perf_regression's warmup discipline, snapshot/restore sys.modules handling in layer31/39) — but the 97-layer append-only campaign left ~3-4k LOC of consolidatable copy-paste, zero fast/slow-mode adoption in the heaviest 48k-LOC slice, and a GPU-marker gap that keeps the documented suite-crash workaround leaky. No P0s: nothing found that silently breaks correctness of the assertions themselves; the two worst quality items (source-text sensor 07, blanket xfail hatch 15) each weaken exactly one sensor. The consolidation plan in finding 16 is the keystone — it carries findings 01/02/04/11 along with it batch-by-batch under an AST assertion-inventory gate that makes assertion loss mechanically impossible.
