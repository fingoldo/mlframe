# FS/FE test-suite audit — mission: bizvalue_value_proofs (2026-06-10)

Finder: bizvalue_value_proofs. Scope: design NEW biz-value tests that PROVE the value of mlframe's FS/FE approaches (the showcase dimension).

Sampling method (so the verifier can check): inventoried all `test_biz_val*`/`test_biz_value*` under tests/feature_selection (118 files) + tests/training (21 files) via Glob; grepped for external-baseline imports (`SelectKBest`, `mutual_info_classif`, pip `mrmr`, `RFE(`, random-baseline patterns), stability indices (nogueira/jaccard/kuncheva), label-noise, real-dataset fetchers, perf_counter/runtime asserts, GBDT-downstream usage; deep-read 12 most informative files: test_wrappers_h2h_sklearn.py (229 LOC, full), test_fe_baselines.py (330, full), test_wrappers_biz_value.py (267, full), tests/training/test_bizvalue_feature_selection.py (423, full), _biz_val_synth.py (509, full), layer83 (header+roster+test names), test_biz_value_mrmr_quality_metrics.py (header+test names), test_biz_value_mrmr_extreme.py / rc2_undersampled / prefer_engineered / shap_proxied_fs / boruta_shap / cluster_aggregate / mi_estimators (headers), test_rfecv_pareto.py, fs_hybrid bench package listing, tests/training/test_feature_selection.py (selected ranges). Verified external-package importability with the local python (pythoncore-3.14-64): AVAILABLE = shap, xgboost, lightgbm, catboost, sklearn, polars, numba, cupy; NOT INSTALLED = mrmr (pip mrmr_selection), skfeature, BorutaShap, boruta, arfs, openml, featuretools, ITMO_FS, mlxtend, skrebate. Skimmed audit_disposition.md + sibling reports (gaps_selection_masking.md, shared_lift.md, param_axes.md) to avoid duplication; known-blocked items (orth-basis recipe parity, GPU SU gate, shap binning) are not re-reported.

[verifier note on the preamble: current count of `tests/feature_selection/test_biz_val*.py` is 136, not 118 (possible glob-pattern difference or drift); tests/training biz files = 21 + 10 legacy `test_bizvalue*` — does not affect any finding. Verifier independently re-confirmed: pip `mrmr` and `BorutaShap` are NOT importable with the local python.]

What already exists (verified baseline inventory):
- vs sklearn RFECV: regression GUARD only (`test_wrappers_h2h_sklearn.py`), epsilon one-sided; full goodness benchmark lives outside CI in `src/mlframe/feature_selection/_benchmarks/bench_rfecv_vs_sklearn.py`.
- vs all-features: `test_wrappers_biz_value.py::test_score_lift_vs_all_features` (RFECV, +0.01 acc); layer83 real-data matrix is "best-of-10 mechanisms within 0.02 of all-features baseline".
- vs random-same-size subset + SHAP-top-k: ShapProxiedFS only (`test_biz_val_shap_proxied_fs.py`, `test_biz_val_shap_proxy_cluster.py`).
- random-feature MI floor: `test_fe_baselines.py::test_biz_random_baseline_below_signal_floor`.
- end-to-end FS via train_mlframe_models_suite: no-harm only (`test_bizvalue_feature_selection.py`, AUROC >= baseline − 0.03).
- stability: cross-seed Jaccard >= 0.5 for our RFECV only (`test_wrappers_biz_value.py:216`); same/different-seed overlap for MRMR (`test_biz_value_mrmr_quality_metrics.py:204,230`). No bootstrap-resample stability, no baseline comparison, Nogueira index absent from tests (only `fs_hybrid/round2_rfecv_crossseed_bench.py::jaccard` as a bench).
  [verifier: two corrections to this inventory line — (1) `test_biz_value_mrmr_layer12.py` DOES test bootstrap-resample stability (StabilityMRMR, 15 bootstraps, per-feature selection-frequency contracts); (2) `test_wrappers_phase4.py:80-84` exercises `selection_stability_(metric="kuncheva")` (plus jaccard/dice) as unit-range sanity checks. See finding 04 verdict.]
- real data: layer29/layer83 (5-7 sklearn datasets, MRMR FE mechanisms only); FE-transformer matrix `tests/feature_engineering/transformer/test_biz_val_real_datasets.py` (honest informational matrix + one guaranteed-lift synthetic).
- runtime: GPU-kernel speedups (`test_biz_val_filters_gpu.py` >=1.5x at n=10k), ShapProxied prefilter cost comparisons, layer83 300s combined-all-on budget. No selector-level scaling envelope, no max_runtime_mins respect contract.
  [verifier: the "no max_runtime_mins respect contract" half is overstated — see finding 11 verdict.]
- Zero head-to-heads against any EXTERNAL selector library beyond sklearn RFECV (no pip mrmr, no SelectKBest-as-selector, no plain sklearn RFE, no BorutaShap-pip).

---

### [bizvalue_value_proofs-01] No head-to-head vs external selector baselines anywhere (SelectKBest-MI top-K, sklearn RFE, pip mrmr, random-K) for the flagship MRMR
Severity: P1
Kind: bizvalue
Files: tests/feature_selection/ (entire dir — absence verified); tests/training/pipeline/test_prepare_test_split_id_equiv.py:22 and tests/training/pipeline/test_pipeline_stale_fit_recovery.py:18 (the ONLY SelectKBest imports in the repo's tests, both pipeline-mechanics, not quality baselines); tests/feature_selection/test_biz_value_mrmr_layer65.py:8-9 (sklearn mutual_info_* used only as an MI-ESTIMATOR reference, never as a competing selector).
Evidence: grep for `SelectKBest|RFE\(` over tests/feature_selection biz files: zero quality-comparison hits; grep `from mrmr import|mrmr_classif`: zero (all `mrmr` matches are mlframe's own module). MRMR's downstream-AUC assertions compare only against the all-raw-signal oracle (`_biz_val_synth.py:384 baseline_signal_auc`) — i.e. against the answer key, never against a competitor a user would actually reach for first.
Proposal: new `tests/feature_selection/test_biz_val_h2h_external_selectors.py`. Baseline roster (sklearn-only guaranteed; pip extras behind importorskip):
  - `SelectKBest(mutual_info_classif, k=K)` — the default "filter" a user tries first;
  - `sklearn.feature_selection.RFE(LogisticRegression, n_features_to_select=K)`;
  - random-K subset (5 draws, take mean) — the sanity floor;
  - all-features (no selection);
  - `pytest.importorskip("mrmr")` block for pip mrmr_selection (`mrmr_classif(X, y, K=K)`) — skipped today (verified not installed), activates the day it's pip-installed.
  Datasets (from `_biz_val_synth.py`, n=1500, seeds {0,1,2}): (a) `make_signal_plus_noise` (linear, no redundancy — HONEST frontier: assert mlframe MRMR downstream AUC >= SelectKBest − 0.02, a documented tie, NOT a fake win); (b) `make_correlated_redundant` with 3 clusters x 4 copies + 20 noise — the canonical mRMR terrain where top-K-MI provably wastes budget on copies: assert MRMR AUC at K=4 >= SelectKBest AUC at K=4 + 0.03 (majority of 3 seeds); (c) `make_3way_xor`/`make_polynomial_target` with `fe_max_steps=1` — FE-inside-MRMR vs any raw-only baseline: assert MRMR+FE >= SelectKBest + 0.10. Metric: `downstream_auc` helper (already in `_biz_val_synth.py:361`), matched K for every method. Every comparative assert uses majority-of-seeds (2 of 3) per the project's "single-seed wins do not count" rule. Fast mode: `is_fast_mode()` -> seed {0} + scenario (b) only.
Effort: M
VERDICT: CORRECTED — every factual coverage claim verified true (SelectKBest only at test_prepare_test_split_id_equiv.py:22/29 + test_pipeline_stale_fit_recovery.py:18/28/47, both pipeline-mechanics; zero `from mrmr import`/`mrmr_classif` in tests, the only hit is the string "mrmr_classification" model name at tests/training/test_feature_selection.py:426; sklearn RFE appears in tests only in docstrings, never as a competitor; layer65:8-9, `baseline_signal_auc` at _biz_val_synth.py:384, `downstream_auc` at :361 all verbatim; verifier's independent grep for mutual_info_classif found only MI-estimator/reference usages, e.g. test_hermite_e2e_strong.py:150 ranks columns by MI but is not a competing selector; note test_biz_value_mrmr_quality_metrics.py:263 has a class literally titled "MRMR must beat top-K by raw MI" but its body never builds an MI-top-K baseline — coverage gap stands). Two proposal corrections: (1) `make_correlated_redundant` (_biz_val_synth.py:62-88) builds ONE correlated cluster (n_corr copies of one base + 1 unique + p_noise), NOT "3 clusters x 4 copies" — scenario (b) needs a custom fixture (the TestClusterRecall pattern at test_biz_value_mrmr_quality_metrics.py:157-195 shows how); (2) for `make_3way_xor`, `fe_max_steps=1` is likely insufficient — src/mlframe/feature_selection/filters/mrmr.py:380-386 documents that step-1 FE "recovers only the two separate halves" and the default is fe_max_steps=2; calibrate scenario (c) with the default depth.

### [bizvalue_value_proofs-02] sklearn-RFECV head-to-head never asserts a WIN on any axis — the refit-count/runtime advantage is unproven in CI
Severity: P2
Kind: bizvalue
Files: tests/feature_selection/test_wrappers_h2h_sklearn.py:14-16, 154; src/mlframe/feature_selection/_benchmarks/bench_rfecv_vs_sklearn.py (bench only, not CI).
Evidence: "Tolerances are generous (epsilon margin), the goal is to catch REGRESSIONS, not to prove our selector is strictly better" (line 14-16); the only assert is `our_score >= sk_score - eps` (line 154). Our RFECV's MBH search explores `max_refits=8` points vs sklearn's exhaustive step-1 backward elimination over 20-80 features — a structural refit-count advantage that is the selector's actual selling point, and no test pins it.
Proposal: add `test_h2h_runtime_at_score_parity` to the same file: 300x80 `make_sklearn_classification_df` (n_informative=5, shuffle=False), LogisticRegression estimator, 3-fold CV. Fit sklearn `RFECV(step=1)` and ours (`max_refits=10`) each 3 times, take per-method min wall (warm-run protocol per the perf-measurement memory). Assert BOTH: `our_score >= sk_score - 0.02` (existing parity) AND `our_wall_min <= 0.7 * sk_wall_min` (measured during development; set floor 15% above measured ratio). On an 80-feature problem sklearn pays ~70 refit rounds vs our <=10 — the expected ratio is >3x, so 0.7 is a conservative regression floor, not flakiness bait. Skip the runtime half under `running_under_xdist()` (contended-box noise), keep the parity half always-on.
Effort: S
VERDICT: CORRECTED — core claims verified: the :14-16 quote is verbatim (it continues "than sklearn's on every problem"), line 154 is exactly `assert our_score >= sk_score - eps`, max_refits=8 confirmed at :139, the bench file exists at src/mlframe/feature_selection/_benchmarks/bench_rfecv_vs_sklearn.py and is outside pytest collection, and NO assert in the file claims a win on any axis. Two precision fixes: (1) the file's actual problems have 15-25 features (`_clf_redundant` 20, `_clf_noisy` 25, `_reg_correlated` 15) — "20-80 features" is wrong for the existing tests; 80 exists only in the PROPOSED fixture, so sklearn's existing refit count here is ~15-25 rounds, not ~70; (2) "the only assert is line 154" is imprecise — the file also asserts non-NaN (:153), subset-size cap (:192-196), and informative recall >= 0.5 (:226), but none of them asserts superiority over sklearn, so the headline stands.

### [bizvalue_value_proofs-03] No quality-vs-n_features frontier test: "reaches quality Q with K features where baseline needs K' > K" is never asserted for any selector
Severity: P1
Kind: bizvalue
Files: tests/feature_selection/test_rfecv_pareto.py:13-41 (only frontier-adjacent test: asserts pareto_front_ SHAPE properties of one RFECV fit, no cross-method comparison); tests/feature_selection/test_biz_value_mrmr_quality_metrics.py (precision/recall at fixed top-k, no K-sweep).
Evidence: `test_rfecv_pareto_front_and_knee` checks "front must be sorted by n" + non-domination within OUR OWN cv_results_ — it never compares a frontier against another method's frontier or even against MI-ranking's curve. Grep `for k in`/`K_sweep`/`frontier` over biz files: no downstream-quality-at-K sweep exists.
Proposal: new `tests/feature_selection/test_biz_val_quality_vs_k_frontier.py`. Recipe: `make_correlated_redundant`-style data with 3 latent clusters x 4 noisy copies + 2 independent signals + 20 noise (n=2000, seed-parametrized {0,1,2}). Obtain a feature RANKING from (a) MRMR selection order (`support_` order, fe off for cleanliness) and (b) `mutual_info_classif` descending. For K in {1,2,3,4,5,6,8}: downstream 5-fold LogReg AUC on each method's top-K prefix. Assertions: (i) K-efficiency: `auc_mrmr[K=4] >= auc_mi[K=8] - 0.01` — MRMR with half the budget matches MI's double budget because MI's first 4 picks are copies of one cluster (this is THE mRMR pitch, currently never demonstrated); (ii) dominance: `mean_K(auc_mrmr[K] - auc_mi[K]) >= 0.02` on majority of seeds; (iii) print the full frontier table (ASCII only) for the showcase artifact. Fast mode: K in {2,4,8}, seed {0}. Wall: <30s (LogReg-only).
Effort: M
VERDICT: CONFIRMED — test_rfecv_pareto.py:13-44 verified: `test_rfecv_pareto_front_and_knee` asserts only shape/sortedness ("front must be sorted by n" verbatim at :32) + non-domination within the selector's own cv_results_ (:34-40) + knee membership; the second test (:47-65, plateau rule) is also a within-RFECV property. quality_metrics uses fixed top-5/top-3 precision only (:41,:119). Verifier's independent refutation attempt with different keywords (`for k in [...(`, `top_k=`, `n_features_to_select`) found only data-construction loops and fixed-k usages — no downstream-quality-at-K sweep across methods exists anywhere in tests/feature_selection. Same fixture caveat as finding 01 applies ("make_correlated_redundant-style" honestly flags the needed custom construction).

### [bizvalue_value_proofs-04] Selection STABILITY under bootstrap resampling (Nogueira index) is untested, and stability is never compared against any baseline selector
Severity: P1
Kind: bizvalue
Files: tests/feature_selection/test_wrappers_biz_value.py:216-267 (cross-SEED Jaccard >= 0.5, our RFECV vs itself only); tests/feature_selection/test_biz_value_mrmr_quality_metrics.py:204,230 (same-seed 100% overlap / different-seed overlap, MRMR vs itself); tests/test_meta/_annotation_baseline.json:219 (`fs_hybrid/round2_rfecv_crossseed_bench.py::jaccard` — a bench, not CI).
Evidence: grep `nogueira|kuncheva|stability_index` over tests/feature_selection: zero hits (only metrics-package PSI matches elsewhere). All existing stability checks are self-referential (selector vs itself across seeds); no test demonstrates the marketable claim "mlframe's selection is MORE stable than the naive baseline's" — which is exactly where DCD/cluster-collapse should shine, because plain MI top-K flips arbitrarily between near-duplicate copies across resamples.
Proposal: new `tests/feature_selection/test_biz_val_selection_stability_bootstrap.py` + a ~15-line `nogueira_stability(list_of_masks, p)` helper in `_biz_val_synth.py` (Nogueira/Sechidis/Brown JMLR 2018: 1 − mean per-feature selection variance / expected variance at fixed k̄). Recipe: `make_correlated_redundant` (4 copies of one latent + 1 indep + 15 noise, n=1200). B=16 bootstrap resamples (fixed `default_rng(seed)` index draws); on each, fit (a) MRMR with default redundancy/DCD, (b) `SelectKBest(mutual_info_classif, k=K)`, K matched to MRMR's median support size. Assertions: (i) absolute floor `stab_mrmr >= 0.5`; (ii) comparative: `stab_mrmr >= stab_mi_topk + 0.10` — measured expectation is large because MI top-K alternates among the 4 interchangeable copies while DCD canonicalizes the cluster representative (set final margin 10-15% below the measured delta during development); (iii) on the no-redundancy `make_signal_plus_noise` control, assert within-epsilon (`>= stab_mi − 0.05`) — honest frontier, no fake win where the mechanism has no edge. Fast mode: B=6.
Effort: M
VERDICT: CORRECTED — two evidence claims are factually wrong, the residual gap is narrower than stated but real. (1) The grep claim "nogueira|kuncheva|stability_index ... zero hits" is FALSE: tests/feature_selection/test_wrappers_phase4.py:80-84 contains `test_kuncheva_in_unit_range` calling `rfecv.selection_stability_(metric="kuncheva")` (plus jaccard :68-72 and dice :74-78 variants) — these are unit-range sanity checks (`0 <= s <= 1`) on one fitted RFECV, not value proofs, but the hits exist; `nogueira` alone has zero hits (verified). (2) "Selection STABILITY under bootstrap resampling ... is untested" overlooks tests/feature_selection/test_biz_value_mrmr_layer12.py: StabilityMRMR is fitted over N_BOOTSTRAPS=15 resamples (75% subsamples) with pinned per-feature selection-frequency contracts (stable_x freq >= 0.80 at :60, stable > flaky discrimination, noise below the 0.6 support gate) — that IS bootstrap-resample stability testing, albeit of the dedicated StabilityMRMR wrapper's frequencies, not a stability INDEX over a base selector's masks. What survives verification and keeps this P1 alive: (a) no Nogueira index anywhere in tests; (b) every existing stability check (phase4 kuncheva, layer12, test_wrappers_biz_value.py:216-267 — verified, `test_stable_selection_across_seeds` at :216 with mean pairwise Jaccard >= 0.5 at :264; quality_metrics :204/:230 — verified) is SELF-referential; no test compares any mlframe selector's stability against an external baseline selector. _annotation_baseline.json:219 verified (`round2_rfecv_crossseed_bench.py::jaccard`). Reframe the proposal's headline as "no stability-index + no baseline comparison" rather than "untested".

### [bizvalue_value_proofs-05] End-to-end suite FS test proves only no-harm (−0.03 AUROC) and its "speeds up" assertion actually permits a 10-25x SLOWDOWN
Severity: P2
Kind: bizvalue
Files: tests/training/test_bizvalue_feature_selection.py:264 (test name `test_mrmr_preserves_auroc_and_speeds_up_wide_training`), :291 (`assert auroc_b >= auroc_a - 0.03`), :313-320 (`bound = 25.0 if running_under_xdist() else 10.0; assert t_fs <= t_baseline * bound`).
Evidence: the test's NAME promises a speedup; the code asserts "FS run took disproportionately longer ... should not exceed {bound}x" — i.e. FS may be up to 25x slower and the suite stays green. No configuration in tests/training ever demonstrates FS WINNING on either metric end-to-end (quality OR wall), which is the headline value of `use_mrmr_fs=True`.
Proposal: add `test_mrmr_fs_wins_on_very_wide_data` to the same file: regime where FS must win — `_make_noisy_classification(n=1500, k_noise=400)` (405 columns, 5 informative), `iters=300` LGB so the boosting fit dominates MRMR's fixed cost, `use_simple_mode=True` for cheap MRMR. Assert (i) `auroc_fs >= auroc_baseline - 0.005` AND (ii) `t_fs < t_baseline` (strict, achievable: 300-iteration LGB on 405 cols vs ~6 cols; MRMR simple-mode on 405 cols at n=1500 is seconds) AND (iii) suite trains on <=10% of input columns (read `metadata['selected_features']`). Mark `slow_only`; fast-mode variant: k_noise=120, iters=120, assertion (ii) relaxed to `t_fs <= 1.2 * t_baseline`. Also rename or split the existing test so its name matches its actual contract ("preserves_auroc_with_bounded_overhead").
Effort: M
VERDICT: CONFIRMED — all three line references verbatim-verified: test name at :264, AUROC no-harm assert at :291, `bound = 25.0 if running_under_xdist() else 10.0` at :316 with the slowdown-tolerant assert at :317-320 (additionally gated on `t_baseline >= 2.0` at :313, so on a fast baseline NO timing assert fires at all — slightly stronger than the finding states); the assert message quote is accurate; module docstring :16-18 even promises "training wall-time is strictly lower", deepening the name-vs-contract mismatch. Verifier's independent grep over tests/training for FS-speedup asserts (t_fs/faster/wall) found no strict `t_fs < t_baseline` anywhere — the win is indeed never demonstrated end-to-end. Proposal is coherent: `_make_noisy_classification(n, k_noise, seed)` exists (:59), `metadata['selected_features']` is the wired surface (:112, :103), `slow_only` marker is registered (pyproject.toml:338), `use_simple_mode` is a real mrmr_kwargs knob (:190).

### [bizvalue_value_proofs-06] Suite-level RFECV value coverage is promised by the docstring but the `rfecv=True` branch is dead — no test exercises it
Severity: P1
Kind: integration
Files: tests/training/test_bizvalue_feature_selection.py:27-28 (docstring: "RFECV is covered by a single smoke parametrize in Test 2 to prove both selector surfaces honor the runtime-lower contract"), :167+202-204 (`_run_suite(..., rfecv=False)` defines the `rfecv_models=["cb_rfecv"]` branch).
Evidence: grep `rfecv` over the file: the only hits are the docstring, the helper signature, and the helper body — no test invokes `_run_suite(..., rfecv=True)`. The promised "both selector surfaces" contract exists for MRMR only; suite-level RFECV value (no-harm + bounded overhead through `train_mlframe_models_suite`) is asserted nowhere (tests/training/test_feature_selection.py covers RFECV wiring/smoke, not quality/runtime contracts).
Proposal: parametrize Test 2 over `selector in ("mrmr", "rfecv")`: for "rfecv" pass `rfecv=True, use_mrmr=False` with the existing `{"max_runtime_mins": 1, "max_refits": 3, "cv": 2}` kwargs and `iters=60`; reuse the identical asserts (`auroc >= baseline − 0.03`, bounded overhead). Single seed for the rfecv leg (CatBoost RFECV is the slow one), `slow_only` mark; fast mode skips the rfecv leg. Verify the new leg actually exercises the path by asserting `metadata['selected_features']` is non-None (the wire-up sensor already used in Test 1).
Effort: S
VERDICT: CONFIRMED — verified by full-file read: docstring promise at :27-28 verbatim; `_run_suite` signature with `rfecv=False` default at :167; the rfecv branch (`rfecv_models=["cb_rfecv"]`, `rfecv_kwargs={"max_runtime_mins": 1, "max_refits": 3, "cv": 2}`) at :202-204; zero call sites pass `rfecv=True` (Test 2 at :263 is parametrized over seeds only). Verifier independently checked every suite-level RFECV test in tests/training: test_feature_selection.py :316-341/:344-377/:531+/:580+/:604+ all end in smoke asserts (`TargetTypes.X in models`), test_core.py:2139 and test_all_models.py:673 likewise wiring-level — no quality/runtime contract exists for suite-level RFECV anywhere. Proposal kwargs match the existing helper exactly.

### [bizvalue_value_proofs-07] BorutaShap biz-value is a single floor test with no baseline: never compared to random-K, SHAP-top-k, or the reference BorutaShap implementation
Severity: P2
Kind: bizvalue
Files: tests/feature_selection/test_biz_val_filters_boruta_shap.py (1 test in file: `informative_kept >= 2, noise_kept <= 5` floors).
Evidence: file's only test pins absolute floors on a 10-feature synthetic. Contrast with the sibling selector: ShapProxiedFS proves "beats both a random same-size subset AND a plain SHAP-importance-top-k subset" (test_biz_val_shap_proxied_fs.py:3-6, 10 tests). BorutaShap — the selector users know by name from the pip package — has no downstream-quality proof, no redundancy-trap case, no comparison demonstrating shadow-feature gating beats naive SHAP-importance ranking.
Proposal: extend `test_biz_val_filters_boruta_shap.py` (mirror the ShapProxied pattern, `pytest.importorskip("shap")` already used): synthetic with 3 informative + 3 marginally-correlated decoys (corr 0.4 with an informative, no incremental signal) + 14 noise, n=800. (i) downstream 5-fold LogReg AUC on BorutaShap-accepted set >= AUC on a SHAP-importance-top-k set of the SAME size − 0.02 (shadow gate must not lose to the ranking it post-processes), and >= random-same-size mean AUC + 0.05; (ii) decoy rejection: <=1 of 3 decoys accepted while >=2 of 3 informative kept (the shadow-threshold pitch); (iii) optional `pytest.importorskip("BorutaShap")` head-to-head vs the reference pip implementation (selected-set Jaccard + downstream AUC within-epsilon) — skipped today, becomes live on install. Seeds {0,1}, majority. Fast mode: n=500, single seed.
Effort: M
VERDICT: CONFIRMED — test_biz_val_filters_boruta_shap.py verified: exactly one test (`test_biz_val_boruta_shap_filters_noise_keeps_informative`, :17-64), floors as described (informative assert is the equality `informative_kept == {"inf1","inf2"}` at :49, i.e. 2-of-2; `len(noise_kept) <= 5` at :55; dataset is 2 informative + 8 noise); no downstream-AUC, no baseline comparison. ShapProxied contrast verified: quote verbatim at test_biz_val_shap_proxied_fs.py:3-5, exactly 10 test functions counted. Verifier swept all 19 other test_boruta_shap_*.py files: all are mechanics/unit tests (rng isolation, polars input, shadow fast path, premerge, multiclass axes, etc.); the closest to a comparison is test_boruta_shap_subsample_stability.py ("stability never accepts MORE noise than a single fit") — a within-BorutaShap mechanism gate, not an external-baseline value proof, so the finding stands. pip BorutaShap confirmed not installed (importorskip leg correct).

### [bizvalue_value_proofs-08] HybridSelector and hetero_vote ship with ZERO biz-value tests — the "compute-once-share-many" speedup and "quality >= best member" claims are unproven
Severity: P1
Kind: bizvalue
Files: tests/feature_selection/test_hybrid_selector.py:1-6 ("the bulk of the behaviour is tested by injecting that state into `_combine` directly -- no expensive MRMR/SHAP/Boruta fits. A single end-to-end test ... exercises the real plumbing"); tests/feature_selection/test_hetero_vote.py (3 tests, absolute signal/noise floors only); src/mlframe/feature_selection/hybrid_selector.py (551 LOC prod module).
Evidence: glob `test_biz*hybrid*` / `test_biz*hetero*`: no files. HybridSelector's entire pitch — shared FI/cluster artifacts make running 3 selectors barely costlier than one, and the cluster-aware combine beats each member — exists only as offline benches (`src/mlframe/feature_selection/_benchmarks/fs_hybrid/round2_*.py`); a refactor could silently invert either property and CI stays green. hetero_vote's docstring claims it "drops the model-specific noise leak that a single-model shadow gate admits" (test_hetero_vote.py:1-2) but no test compares it against the single-model gate it improves on.
Proposal: (a) new `tests/feature_selection/test_biz_val_hybrid_selector.py`: synthetic with 4 informative + 1 redundant cluster (3 copies) + 20 noise, n=1200. Fit HybridSelector with members {mrmr, shap_proxy, boruta-lite} and each member STANDALONE (same configs). Assert (i) downstream 5-fold LogReg AUC of the hybrid combine >= max(member AUCs) − 0.02 (combine never loses to its best member); (ii) share-many economics: `t_hybrid <= 0.85 * sum(t_member_standalone)` — the shared-artifact saving (set from a measured dev run minus headroom; skip the timing half under xdist); (iii) redundant cluster contributes <=1 representative to the combined support. Mark `slow_only`, fast mode drops boruta member. (b) extend `test_hetero_vote.py` with the comparative leg: single-model shadow gate (same panel size 1) vs hetero vote on a synthetic where one model family memorizes a high-cardinality noise column — assert hetero noise-kept < single-model noise-kept (the exact claimed delta), plus downstream AUC no-harm.
Effort: L
VERDICT: CONFIRMED — all claims verified: test_hybrid_selector.py:1-6 quote verbatim (tests inject state into `_combine`, members keyed "mrmr"/"shap"/"boruta" at :34); src/mlframe/feature_selection/hybrid_selector.py is exactly 551 LOC with `fe_max_steps`/`use_fe`/`boruta_driver` ctor params (:78); no `test_biz*hybrid*`/`test_biz*hetero*` files exist (dir listing: only test_hetero_vote.py, test_hybrid_selector.py, test_hybrid_selector_production.py, test_regression_fs_hybrid_fixes.py); test_hetero_vote.py has exactly 3 tests with absolute floors (signal kept, noise <= 1, weights well-formed) and its :1-2 docstring quote is verbatim — the "single-model gates leak more" claim appears only as a code comment (:31), never as a tested comparison. One addition that does NOT refute: test_hybrid_selector_production.py (verified in full) covers import-identity/interface/pickle only — still zero biz-value coverage. round2_*.py benches confirmed present in fs_hybrid/.

### [bizvalue_value_proofs-09] Real-dataset value proofs never show a WIN for any selector: layer83/29 only assert "best-of-10 within 0.02 of all-features", and RFECV/BorutaShap/ShapProxied/Hybrid have zero real-data coverage
Severity: P1
Kind: bizvalue
Files: tests/feature_selection/test_biz_value_mrmr_layer83.py:24-30 ("the BEST of the 10 mechanism scores must >= baseline_score - tolerance ... 'the toolkit is at least useful' contract"), :83 (`ACC_TOLERANCE = 0.02`); tests/feature_engineering/transformer/test_biz_val_real_datasets.py:5-9 (honestly documents "no headroom ... transformer-FE doesn't help boostings on these specific workloads"); grep real-dataset fetchers over tests/feature_selection: only layers 29/69/83 + rc2_undersampled (all MRMR-family).
Evidence: every real-data FS contract is tolerance-shaped (no regression vs all-features); none demonstrates selection ADDING value on real data. Raw sklearn datasets have little noise so this is structurally impossible as-is — but the standard, honest showcase recipe (noise/duplicate injection into a real dataset) is absent.
Proposal: new `tests/feature_selection/test_biz_val_real_data_noise_injection.py`, `slow_only` + fast subset. Datasets: `load_breast_cancer` (binary, 30 raw cols) and `load_diabetes` (regression) — both already used by layer83, offline-safe. Augment: +90 permuted-row copies of real columns (realistic-marginal noise) +10 near-duplicates (real col + N(0, 0.05*sd)). Selectors parametrized over the registry families practical at this scale: MRMR, RFECV(LogReg/Ridge), ShapProxiedFS, BorutaShap (importorskip shap). Assertions per selector: (i) value: downstream 5-fold AUC/R2 on selected set >= all-(augmented)-features score for an unregularized model (`LogisticRegression(C=1e6)` / `LinearRegression`) + 0.01 — noise injection guarantees headroom, same trick as test_wrappers_biz_value.py:83; (ii) honesty leg: for LGBM downstream assert within-epsilon only (>= all-features − 0.02) — boostings self-regularize, claiming a tree win on real data would be a fake (per the FE-transformer file's own finding); (iii) noise rejection >= 80%, real-column recall >= 50%. Fast mode: breast_cancer only, MRMR+RFECV only, +30 noise cols.
Effort: L
VERDICT: CORRECTED — quotes verified: layer83 "BEST of the 10 mechanism scores must >= baseline_score - tolerance / 'the toolkit is at least useful' contract" verbatim at :24-27, `ACC_TOLERANCE = 0.02` exactly at :83; FE-transformer honesty quote verbatim at test_biz_val_real_datasets.py:6-8; test_wrappers_biz_value.py:83 is indeed `test_score_lift_vs_all_features` using the C=1e6 noise-headroom trick. Two corrections: (1) the fetcher grep missed tests/feature_selection/test_wrappers_sklearn_coverage_gaps.py (load_iris at :24) — RFECV therefore does NOT have literally "zero real-data coverage"; however that file is input-type/contract mechanics (sklearn test_rfe.py parity), not a value proof, so "zero real-data VALUE coverage for RFECV/BorutaShap/ShapProxied/Hybrid" is the accurate residual claim and it holds. (2) "every real-data FS contract is tolerance-shaped (no regression vs all-features)" is slightly off for rc2_undersampled: it pins an ABSOLUTE real-data floor (diabetes full-mode: >= 6 features AND downstream Ridge R2 >= 0.35, header contract (a)) — an absolute floor, not an all-features tolerance, but still not a selection-beats-all-features win, and it is MRMR-family as stated. Headline survives both corrections.

### [bizvalue_value_proofs-10] Label-noise robustness is "signal still found" for MRMR only — no graceful-degradation comparison vs a baseline selector, no noise-level sweep
Severity: P2
Kind: bizvalue
Files: tests/feature_selection/test_biz_value_mrmr_extreme.py:67 (`test_30pct_label_noise_signal_still_found`), :95 (noisy XOR); cross-ref sibling finding gaps_selection_masking-06 (heavy-tail+noise untested for OTHER selectors — different angle: theirs is coverage, this one is comparative value).
Evidence: the existing noise tests assert MRMR survives one fixed noise level; nothing pins the SHAPE of degradation or that mlframe's permutation-confirmed gating degrades more gracefully than the naive baseline (whose MI estimates absorb noise silently and start admitting noise features).
Proposal: new `test_biz_val_label_noise_degradation.py` in tests/feature_selection: `make_signal_plus_noise(n=2000, p_signal=4, p_noise=16)`, flip-rate sweep {0.0, 0.1, 0.2, 0.3} (fixed rng per rate), 3 seeds. For each rate: signal-recall at K=4 and noise-count for (a) MRMR (default confirmation gates), (b) `SelectKBest(mutual_info_classif, k=4)`. Assertions: (i) dominance-or-tie at every rate: `recall_mrmr >= recall_mi − 0.1` (majority of seeds); (ii) graceful floor: at rate 0.3, `recall_mrmr >= 0.5`; (iii) false-positive guard the baseline lacks: MRMR's selected-noise count at rate 0.3 <= its rate-0.0 count + 1 (the permutation gate should hold the FDR roughly flat while plain MI's ranking noise-floor rises). Calibrate margins from a dev run, floors 10% below measured. Fast mode: rates {0.0, 0.3}, seed {0}.
Effort: M
VERDICT: CONFIRMED — line refs verified: `test_30pct_label_noise_signal_still_found` at :67 (single fixed 30% flip, asserts only "signal" in support), `test_noisy_xor_synergy` at :95 (single fixed 20% flip). Verifier's independent sweep grep (label_noise/flip_rate/noise_rate, case-insensitive, all tests/) found only fixed-level data construction (layers 68/88/89/94 at 2-5%, prefer_engineered, supervised_binning) — no flip-rate sweep, no MRMR-vs-baseline degradation comparison anywhere. Proposal fixture `make_signal_plus_noise(n, p_signal, p_noise)` exists with exactly those params (_biz_val_synth.py:34).

### [bizvalue_value_proofs-11] No runtime-budget-respect or scaling-envelope contract: `max_runtime_mins` is passed in ~10 biz files but never asserted to actually bound wall time
Severity: P2
Kind: bizvalue
Files: grep `max_runtime_mins` -> tests/feature_selection/test_biz_val_filters_mrmr.py, test_biz_val_wrappers_rfecv.py, layers 27/87/88/89/90/92/104, test_biz_value_mrmr_hard_cases.py (all pass it as a fit-speed knob; grep for an assert that elapsed <= budget: zero hits); tests/feature_selection/test_biz_value_mrmr_layer83.py:591 (single-point 300s budget, one config); src/mlframe/feature_selection/_benchmarks/bench_1M_stress.py + bench_iter33_scaling.py (scaling exists only as benches).
Evidence: a regression that makes MRMR ignore `max_runtime_mins` (e.g. the check moved out of the candidate loop) would pass the entire suite — tests use the knob to keep CI fast, so they'd just get slower, not red. No test pins "budget exceeded -> selector returns a VALID partial selection promptly".
Proposal: new `test_biz_val_runtime_budget_contract.py`: (i) budget-respect: MRMR on a deliberately oversized problem (n=4000, p=120, full mode, `max_runtime_mins=0.05` = 3s) — assert `elapsed <= 4 * budget` (generous 4x absorbs the in-flight candidate evaluation granularity + JIT warm; warm numba via a tiny prior fit) AND the partial fit is usable: `support_` non-empty, `transform` round-trips; same for RFECV via `SearchConfig(max_runtime_mins=...)`. (ii) scaling envelope (catch quadratic blowups, not micro-perf): simple-mode MRMR wall at (n=2000,p=40) vs (n=8000,p=40): assert `t(4n) <= 8 * t(n)`; and (n=2000,p=160) vs p=40: `t(4p) <= 25 * t(p)` — bounds are 2x above the theoretical growth so only a complexity-class regression trips them. Skip timing asserts under `running_under_xdist()`; mark not-fast.
Effort: M
VERDICT: CORRECTED — the narrow claim survives, the broad one is refuted. Verified true: 28 files in tests/feature_selection pass `max_runtime_mins` (the ~10 named biz files all among them); layer83's 300s budget test (`test_combined_all_on_fit_under_300s_on_largest_dataset`, ~:591) is a one-config wall cap not tied to the knob; NO test anywhere asserts `elapsed <= f(max_runtime_mins)` wall-clock for MRMR or RFECV; bench_1M_stress.py / bench_iter33_scaling.py exist and scaling lives only there. REFUTED sub-claim: "No test pins 'budget exceeded -> selector returns a VALID partial selection promptly'" — tests/feature_selection/test_boruta_shapproxied_budget_stop.py (docstring: "honor a wall-clock budget (``max_runtime_mins``) ... still expose a valid (possibly partial) selection") pins exactly that for BorutaShap (`max_runtime_mins=1e-6` -> `n_trials_run_ < 300` + `selected_features_` present, :39-50) and stop-file aborts for BorutaShap + ShapProxiedFS (:22-36, :53-70); additionally test_wrappers.py:551-565 fits RFECV with `max_runtime_mins=0.01` and asserts a usable `n_features_` (no promptness bound), tests/feature_selection/test_screen_coverage.py:134-138 pins the budget-exhausted guard for screen_predictors, and tests/feature_selection/test_evaluation.py:233-241 unit-pins `handle_best_candidate` runtime-exhaustion detection (`run_out is True`). The genuinely missing pieces, re-scoped: (a) a wall-clock `elapsed <= k * budget` promptness assert for MRMR and RFECV (the existing tests assert valid OUTPUT, never bounded TIME), (b) the scaling envelope. Downgrade-or-keep at P2 is reasonable; the proposal remains the right shape for (a)+(b).

### [bizvalue_value_proofs-12] FE downstream-value is proven only for linear models — the documented "complementarity across model families" claim (lift linear, no-harm GBDT) has no end-to-end pin
Severity: P2
Kind: bizvalue
Files: tests/feature_selection/test_biz_value_mrmr_prefer_engineered.py:14-18 (LogReg AUC 0.5 -> 0.99 — the canonical linear-lift proof); grep LGBM/XGB/CatBoost over tests/feature_selection/test_biz*.py: only test_biz_value_gbm_seeder_order3_floor.py + layer19 (neither is an engineered-vs-raw downstream comparison); tests/feature_selection/test_biz_val_cluster_aggregate.py:6-9 (the one family that DOES state the honest cross-model contract: "the downstream contract is NO-HARM across model classes ... genuine lift is for capacity-limited linear models").
Evidence: the project's own benchmark history (poly/Fourier gate rejection: "no tree-downstream win") says trees rarely gain from MRMR's engineered features — so the defensible showcase claim is exactly the cluster_aggregate-style two-sided contract, and it is pinned for cluster_aggregate ONLY, not for the main FE-inside-MRMR path (hermite/hinge/prewarp bases).
Proposal: new `test_biz_val_fe_value_across_model_families.py`: fixtures `make_polynomial_target` (even-symmetric) + `make_3way_xor` (n=1500, seeds {0,1}); fit MRMR with `fe_max_steps=1` vs `fe_max_steps=0`, transform, then parametrize downstream over `LogisticRegression(max_iter=400)` and `lgb.LGBMClassifier(n_estimators=80)` (importorskip lightgbm). Assert per fixture: (i) linear lift: `auc_fe_logreg >= auc_raw_logreg + 0.25` (measured ~+0.45 on these targets, floor at ~55% of measured); (ii) tree no-harm: `auc_fe_lgbm >= auc_raw_lgbm - 0.02` — pinning that FE does not pollute tree inputs (the realistic failure mode: hundreds of junk engineered columns diluting LGBM's split search). This converts the team's honest "complementarity, not universal lift" position into an executable regression sensor. Fast mode: one fixture, one seed.
Effort: S
VERDICT: CORRECTED — core claims verified: prefer_engineered's 0.5 -> 0.99 LogReg lift documented at :12-17 (finder cited :14-18, overlapping; the test pins both the selection decision and the downstream win); cluster_aggregate :6-9 quote verbatim. Two corrections: (1) the GBDT grep is incomplete — `lightgbm|xgboost|catboost` also matches test_biz_val_shap_proxied_fs.py / test_biz_val_shap_proxy_cluster.py (xgboost/lightgbm as the selector's INTERNAL model, not an engineered-vs-raw downstream) and layer40 (catboost in a comment only), and cluster_aggregate's own GBDT downstream uses sklearn `HistGradientBoostingClassifier` (:35-36) which the keyword grep cannot see — none of these adds an engineered-vs-raw cross-family pin for the main FE path, so the gap stands, but the stated grep result was not the full picture; (2) proposal nit: on `make_3way_xor`, `fe_max_steps=1` likely cannot linearize the 3-way product — src/mlframe/feature_selection/filters/mrmr.py:380-386 documents step-1 recovers "only the two separate halves" (default fe_max_steps=2); use the default depth or swap in a pair-product fixture for the 1-step variant.

### [bizvalue_value_proofs-13] No persisted value-leaderboard: 97 layer files pin one-off local thresholds, but no golden ledger tracks downstream-quality-per-(selector, scenario) across versions
Severity: P2
Kind: quality
Files: tests/feature_selection/golden/ (`_capture.py`, `capture_baseline.py`, `pre_refactor/`, `intermediate/` — refactor-parity captures of selector OUTPUTS, not quality metrics); tests/feature_selection/test_biz_value_mrmr_layer83.py:31-34 (the one leaderboard-ish artifact: "per-dataset best-mechanism is recorded via `_get_best_mechanism` so future layers can compare" — but it is recomputed in-process, asserted only `>= median`, and covers MRMR FE mechanisms only).
Evidence: each of the ~97 layers froze its threshold at authoring time against THEN-current code; a slow quality drift (e.g. −0.01 AUC per release across many scenarios) stays under every local floor forever. The golden/ infra proves the team already accepts the capture-compare pattern — it just was never applied to VALUE metrics.
Proposal: `tests/feature_selection/test_biz_val_golden_value_matrix.py` + `golden/value_matrix.json` (orjson, sort_keys per the hashing convention). Matrix rows: (selector in {MRMR, MRMR+FE, RFECV, ShapProxiedFS}) x (scenario in {signal_plus_noise, correlated_redundant, 3way_xor, heavy_tail}) — 16 cells, each = mean downstream 5-fold LogReg AUC over seeds {0,1} (one fixed config per selector, n=1200, total wall target <5min, `slow_only`). Test asserts every cell >= golden_value − 0.03 and prints the full current-vs-golden table; a deliberate improvement updates the JSON via `capture_baseline.py`-style script (same workflow as the existing golden captures). Catches cross-cutting drift no single layer threshold can see.
Effort: M
VERDICT: CONFIRMED — verified: tests/feature_selection/golden/ contains exactly `__init__.py`, `_capture.py`, `capture_baseline.py`, `pre_refactor/`, `intermediate/`; layer file count is EXACTLY 97 (`ls test_biz_value_mrmr_layer*.py | wc -l` = 97); the best-mechanism leaderboard quote is at layer83:29-31 (finder cited :31-34 — off by ~2 lines, same passage), `_get_best_mechanism` defined at :421, and the only assertion on the recorded best is `test_best_mechanism_score_geq_median` (:450-472, "best >= median, impossible by construction" sanity) — "asserted only >= median" verbatim-accurate. MRMR-FE-mechanisms-only scope confirmed by the 10-mechanism roster in the header. `slow_only` marker registered (pyproject.toml:338); golden-capture workflow exists to mirror.

### [bizvalue_value_proofs-14] Cross-selector value evidence (selector shootouts, cross-seed stability, RFECV goodness) lives ONLY in committed benches; CI has no thin smoke tier over them
Severity: LOW
Kind: bizvalue
Files: src/mlframe/feature_selection/_benchmarks/bench_rfecv_vs_sklearn.py; _benchmarks/fs_hybrid/{fs_selectors.py, importance_shootout.py, round2_rfecv_crossseed_bench.py, hard_synth.py}; _benchmarks/bench_feature_value_demo.py.
Evidence: test_wrappers_h2h_sklearn.py:3-5 explicitly delegates goodness to the bench ("the standalone report at ... bench_rfecv_vs_sklearn.py covers that in depth"). The fs_hybrid package even ships uniform selector adapters (`fs_selectors.py:1-6`) that are exactly the harness findings 01/03/04 need — but nothing imports them from tests/, so adapter rot (e.g. an MRMR ctor-param rename) is detected only when someone manually reruns a bench.
Proposal: smallest useful step — one smoke test `test_biz_val_bench_adapters_alive.py` that imports `fs_selectors` adapters and runs each `.fit(X,y).transform(X)` on a 300x12 synthetic asserting non-empty ASCII-safe output (<20s, keeps the bench harness permanently runnable). Second step (optional, folds into 01/03/04): implement those findings' tests ON TOP of the fs_selectors adapters instead of fresh wrappers — single source of truth for "how to run each selector uniformly".
Effort: S
VERDICT: CONFIRMED — all five bench files verified present (bench_rfecv_vs_sklearn.py, bench_feature_value_demo.py, fs_hybrid/fs_selectors.py, fs_hybrid/importance_shootout.py, fs_hybrid/round2_rfecv_crossseed_bench.py, fs_hybrid/hard_synth.py); h2h docstring delegation quote verbatim at test_wrappers_h2h_sklearn.py:3-5; fs_selectors.py:1-6 is indeed "Uniform adapters over the four mlframe selectors + cascade/ensemble combinators" exposing .fit/.transform with ASCII-safe names; verifier's independent grep for `fs_selectors` under tests/ (all .py): zero imports — adapter rot is CI-invisible exactly as claimed. The pytest addopts ignore `benchmarks` (pyproject.toml:326) and src/_benchmarks is outside tests/ collection, so "bench only, not CI" holds.

### [bizvalue_value_proofs-15] The flagship real-data showcase (layer83 70-cell matrix) has no fast mode and a LogReg/LinearRegression-only downstream — the showcase silently weakens on every laptop run
Severity: LOW
Kind: bizvalue
Files: tests/feature_selection/test_biz_value_mrmr_layer83.py (grep `slow_only|is_fast_mode|FAST`: zero hits; downstream scoring built on LogisticRegression/LinearRegression imports at :70-71); cross-ref param_axes-12 (loop-vs-parametrize in the same file — different axis).
Evidence: the session-scoped fixture materialises all 70 (dataset x mechanism) cells unconditionally; under `pytest --fast` (the repo-wide convention, tests/conftest.py:147-170) the file runs in full or must be deselected by hand — both outcomes erode the "every code path has a fast representative" rule for the single most showcase-shaped suite in tests/feature_selection.
Proposal: add module-level fast gating: under `is_fast_mode()` shrink the roster to 2 datasets (breast_cancer, diabetes) x 3 mechanisms (plug_in, jmim, lasso) inside the matrix fixture (keys drive the contracts, so the assertion classes need no change beyond iterating the live roster). Optionally add one LGBM-downstream column for the two fast datasets (importorskip lightgbm) so the real-data matrix also certifies the no-harm-for-boosting leg from finding 09/12.
Effort: S
VERDICT: CONFIRMED — verifier's independent grep of layer83 for `slow_only|is_fast_mode|FAST`: zero hits; the linear-models-only downstream confirmed (single import line `from sklearn.linear_model import LinearRegression, LogisticRegression` at :70 — finder cited :70-71, one line off; scoring uses LogisticRegression at :264 / LinearRegression at :267 and nothing else); header confirms "The full 7x10=70 matrix is materialised in a session-scoped fixture" (:21-22); fast-mode convention block verified at tests/conftest.py:~145-175 (`is_fast_mode()`, `MLFRAME_FAST`, `fast_subset`). Mechanism names in the proposal (plug_in, jmim, lasso) match the layer's documented roster.

---

## Summary

| Severity | Count |
|---|---|
| P0 | 0 |
| P1 | 6 (01, 03, 04, 06, 08, 09) |
| P2 | 7 (02, 05, 07, 10, 11, 12, 13) |
| LOW | 2 (14, 15) |
| **Total** | **15** |

Verdict: the suite is exceptionally deep on INTERNAL value proofs (signal-vs-noise floors, redundancy traps, engineered-vs-raw linear lift, GPU-kernel speedups) but almost empty on the showcase dimension this mission targets: there is not a single CI assertion that any mlframe selector beats — or even ties at lower cost — an external baseline a practitioner would reach for first (SelectKBest-MI, plain RFE, pip mrmr), and the marquee claims (K-budget efficiency, bootstrap stability via DCD, hybrid compute-once economics, suite-level FS speedup, budget respect) are either bench-only or asserted in no-harm form. The proposed tests are deliberately honest: hard wins only where the mechanism structurally guarantees one (redundancy/K-efficiency/stability/noise-injection), documented within-epsilon frontiers everywhere else (linear no-redundancy data, GBDT downstream, smooth real datasets).

[verifier note on the mission verdict: holds after verification with one trim — "budget respect" should be struck from the marquee-claims list as stated; budget-respect-with-valid-partial-output IS CI-pinned for BorutaShap/ShapProxiedFS (test_boruta_shapproxied_budget_stop.py) and smoke-pinned for RFECV/screen_predictors; what is missing is a wall-clock promptness bound and the scaling envelope (finding 11, as corrected). The external-baseline-head-to-head absence (findings 01/03) fully survived independent refutation attempts.]

## Verification summary

- CONFIRMED: 9 (03, 05, 06, 07, 08, 10, 13, 14, 15)
- CORRECTED: 6 (01, 02, 04, 09, 11, 12)
- REFUTED: 0

The 3 most load-bearing corrections:

1. **[04]** The "zero hits for nogueira|kuncheva|stability_index" grep is false — `selection_stability_(metric="kuncheva")` is exercised at tests/feature_selection/test_wrappers_phase4.py:80-84 (unit-range only), and bootstrap-resample stability IS tested at test_biz_value_mrmr_layer12.py (StabilityMRMR, 15 bootstraps, frequency contracts incl. stable_x >= 0.80). The defensible residual gap: no Nogueira index, no stability comparison vs any external baseline selector, no stability-index-over-bootstrap-masks for base selectors.
2. **[11]** "No test pins 'budget exceeded -> valid partial selection'" is refuted by tests/feature_selection/test_boruta_shapproxied_budget_stop.py (BorutaShap `max_runtime_mins=1e-6` -> early abort + usable selection; stop-file aborts for both BorutaShap and ShapProxiedFS), plus RFECV tiny-budget smoke (test_wrappers.py:551), screen_predictors budget-guard (test_screen_coverage.py:134) and the unit-level exhaustion detector (test_evaluation.py:233). What genuinely remains: no `elapsed <= k * budget` wall-clock bound for MRMR/RFECV and no scaling envelope.
3. **[09]** "RFECV ... zero real-data coverage" is literally false — test_wrappers_sklearn_coverage_gaps.py:24 uses load_iris for RFECV input-type/contract mechanics — and rc2_undersampled pins an absolute real-data floor (diabetes Ridge R2 >= 0.35) rather than an all-features tolerance. Both are non-value coverage, so the corrected (and still P1) claim is "zero real-data VALUE coverage outside MRMR-family tolerance/floor contracts".

Proposal-coherence corrections worth carrying into implementation: `make_correlated_redundant` builds a single correlated cluster (not 3x4 — findings 01/03 need a custom fixture), and `fe_max_steps=1` is documented insufficient for 3-way-XOR linearization (mrmr.py:380-386; findings 01/12 should use the default depth 2).
